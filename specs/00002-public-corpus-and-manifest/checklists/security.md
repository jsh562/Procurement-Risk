# Security: Public Corpus and Manifest
**Created**: 2026-07-26 | **Feature**: [spec.md](../spec.md)

## Path Containment for Manifest-Supplied Paths

- [X] CHK001 Is the order of operations in the containment rule fixed — the path resolved first and the containment assertion applied to the resolved result — given a comparison performed before resolution is defeated by segments the filesystem evaluates afterwards (CWE-22, CWE-23, CWE-36)? [Unambiguous, data-model §VR-009 / research §Security criteria for path fields and redirect-following retrieval] <!-- Evaluator: Resolved — VR-009 rewritten to fix the order explicitly: reject separators, `..`, and absolute forms; resolve to the real path (links resolved, `..` collapsed by the filesystem rather than by string arithmetic); only then compare against the base. The research decision's ordering is now the rule rather than an implied reading of "resolved absolute path" -->
- [X] CHK002 Does "resolved absolute path" state whether symbolic links are followed during resolution, so a `location` naming a link that points outside the corpus root is decided by the rule rather than by the resolver's default (CWE-59, CWE-61, CWE-64)? [Unambiguous, data-model §VR-009 / §Entities Document] <!-- Evaluator: Resolved — decided by prohibition rather than by resolution behaviour: VR-009 now states that links are rejected outright by the new VR-067 rather than followed to somewhere admissible, so the outcome does not depend on whether the resolver follows them -->
- [X] CHK003 Is a symlink prohibition stated for corpus files at all, given `Document.path` requires a regular file and a link to a regular file elsewhere satisfies that test under a link-following stat? [Complete, data-model §Entities Document / §VR-010] <!-- Evaluator: Resolved — added VR-067 (no symbolic link anywhere under the corpus root, tested with a non-following stat before any other test) and amended the Document entity to carry `is a regular file under a non-following stat` plus a separate `is not a symbolic link` check, with the reason the two are separate stated inline -->
- [X] CHK004 Does any rule constrain the location-discovery walk against symlinked directories and link cycles, given membership is defined as a directory test and flatness is expressed as "contains no subdirectory", which leaves a link-shaped entry undecided? [Complete, data-model §Physical Artifacts / §VR-004 / §VR-064] <!-- Evaluator: Resolved — VR-067 states that the discovery walk does not follow links and does not descend into them, so no location can be introduced from outside the root and no cycle can arise; VR-064 now says its no-subdirectory test is over entries without following links, so a link-shaped entry falls to VR-067 instead of passing as a flat entry -->
- [X] CHK005 Is the base that a resolved path must stay under named as one value, given `data/`, `data/corpus/`, and the entry's own location directory each appear as the declared base for the same field? [Consistent, Spec §FR-018 / §SC-007 / data-model §VR-009] <!-- Evaluator: Resolved — VR-009 now names one base, the entry's own corpus location directory, and states that `data/corpus/` and `data/` are consequences of it rather than alternative bases, which is what makes FR-018's and SC-007's statements true without competing with the rule -->
- [X] CHK006 Is a containment obligation stated for every path-valued field the validator resolves — the `generation_inputs` keys and the supporting-artifact paths — or only for `location`, leaving the others externally controlled paths with no stated rule (CWE-73)? [Complete (set), Spec §FR-009b / data-model §VR-061 / §VR-065] <!-- Evaluator: Resolved — VR-061 now states that `generation_inputs` keys are path-valued and externally controlled, compared as literal strings against their closed three-value set before any filesystem access, and subject to VR-009's ordering and VR-067's link prohibition for the path they denote; the validator-read supporting artifacts are named as validator-owned literals under VR-065, subject to the same rules -->
- [X] CHK007 Is a case-folding and Unicode-normalization rule stated for `location` before file lookup and the entry↔file bijection, given two codepoint-distinct entry values can name one file on a folding filesystem? [Complete, data-model §Entities CorpusManifestEntry / §VR-011 / §MS-1] <!-- Evaluator: Resolved — added VR-068: NFC normalization then exact codepoint matching for file lookup (never locale collation, which MS-1's codepoint ordering already assumes), plus a within-manifest case-folding collision check, so VR-011's bijection is decided by this model rather than by the filesystem the check happens to run on -->


## Redirect-Following Retrieval

- [X] CHK008 Is re-application of the host allow-list at every redirect hop stated as a requirement, or does it appear only as a test condition, given the requirement-level allow-list constrains the recorded `source_location` and not the client's behaviour during the fetch (CWE-918)? [Complete, Spec §FR-002a / §FR-008 / plan §Testing Strategy Unit] <!-- Evaluator: Resolved — added FR-002b, which states per-hop re-evaluation of FR-002a's allow-list, including the final hop, as an obligation on the client rather than a condition a test happens to exercise; the plan's unit-tier conditions now read as evidence for a requirement instead of standing in for one -->
- [X] CHK009 Does any requirement state which URL becomes `source_location` when retrieval crosses the documented cross-host 301 — the requested URL or the final origin — given the allow-list assertion on that field is the only committed check touching the retrieval path? [Unambiguous, data-model §VR-022 / research §Retrieving and vendoring the real layer] <!-- Evaluator: Resolved — FR-008 now fixes it as the **requested** first-hop URL, not the final redirect target, so the recorded value stays the citable published location and FR-008b re-fetches by re-traversing the chain; VR-022 restates it and requires the chain's hosts to be allow-listed, so the recorded field and the fetch-time allow-list govern one consistent set of hosts -->
- [X] CHK010 Is the storage origin the WBDG redirect lands on required to appear in the policy's `source_hosts`, or does the allow-list carry the first-hop host only, leaving the deliberate cross-host hop unrepresentable in the artifact that governs it? [Complete, Spec §FR-002a / data-model §Supporting Artifacts RetrievalPolicy] <!-- Evaluator: Resolved — FR-002a now requires `source_hosts` to enumerate every host a retrieval may land on across a redirect chain, naming the storage origin the documented 301 lands on; the RetrievalPolicy row in data-model carries the same, so the epic's one known redirect is representable in the policy that governs it -->
- [X] CHK011 Is allow-list matching defined as exact host equality rather than suffix or substring containment, so a host ending in an allow-listed name is decided by the rule rather than by the comparison a reader assumes? [Unambiguous, data-model §VR-022 / §Supporting Artifacts RetrievalPolicy] <!-- Evaluator: Resolved — FR-002b(b) and VR-022 both state exact case-insensitive host equality after lowercasing, never suffix or substring containment, with the near-miss named; the plan's unit tier adds a suffix-match near-miss as a required failing case -->
- [X] CHK012 Is a scheme allow-list stated for the schemes the client may follow into, given the recorded-field rule fixes `https` on the stored value only and clients have auto-followed into `file://` and `scp://`? [Complete, data-model §VR-022 / research §Security criteria for path fields and redirect-following retrieval] <!-- Evaluator: Resolved — FR-002b(a) restricts the client to `https` at every hop and requires a redirect into any other scheme to be rejected rather than followed, stated separately from FR-008's constraint on the stored value -->
- [X] CHK013 Is the maximum redirect hop count stated as a value, or does "the hop-count bound" name a bound that no artifact quantifies and no case can be written against? [Verifiable, plan §Testing Strategy Unit / research §Security criteria for path fields and redirect-following retrieval] <!-- Evaluator: Resolved — quantified at **5 hops**, failing on the sixth (FR-002b(c)); the plan's unit and coverage tiers now name "a sixth redirect hop" as the case rather than "the hop-count bound" -->
- [X] CHK014 Is a bound stated on the response body the retrieval and re-verification clients will read, given the epic's decision to state no size bounds is recorded over committed corpus weight and CI runtime rather than over a fetched response? [Complete, Spec §Scope Excluded / §FR-008c] <!-- Evaluator: Resolved — FR-002b(d) bounds the response body at 50 MB (52,428,800 bytes) per document, failing rather than truncating; the Excluded entry now states explicitly that its no-bounds decision covers corpus weight and validator runtime and is **not** over the retrieval client's response body, so the two do not read as contradictory -->
- [X] CHK015 Do the allow-list, scheme, and hop obligations reach the re-verification job as well as first retrieval, or are they stated for one network path and inherited by assumption for the other? [Complete (set), Spec §FR-008b / plan §Requirement Coverage Map FR-008b] <!-- Evaluator: Resolved — FR-002b binds "every retrieval this epic performs", naming both paths in its first clause, and FR-008b restates the binding so the re-verification job carries the controls explicitly; the plan's FR-002b row makes it one shared client policy rather than two implementations that can drift -->
- [X] CHK016 Is the trust-on-first-use posture stated — that a first retrieval has no pre-recorded digest to compare against, so authenticity at that moment rests on transport and the allow-list alone (CWE-345)? [Correct, Spec §FR-008a / §FR-008c / data-model §Uncovered Requirements FR-008a / FR-008c] <!-- Evaluator: Resolved — FR-002b closes with the posture stated in those terms: no reference digest exists at first retrieval, authenticity rests on TLS and the allow-list, and `upstream_digest` establishes continuity from that moment rather than authenticity at it; the data-model Uncovered Requirements row for FR-008a/FR-008c now carries it as the deeper half of the same gap -->


## Supply-Chain Posture for the Added Dependencies

- [X] CHK017 Is the exact-pin-plus-artifact-hash obligation for the four added distributions stated as a requirement or success criterion, or does it exist only as an exit criterion inside the plan's testing strategy? [Necessary, plan §Testing Strategy Security / research §Supply-chain criteria for new dependencies] <!-- Evaluator: Resolved — added SC-027, which raises the obligation to criterion level: exact version pin plus artifact hash in the committed lockfile, default public index, zero ranges, zero absences, lock-versus-manifest check passing. The plan's exit criterion now cites it rather than being the only place it exists -->
- [X] CHK018 Is artifact-hash verification at install time required, distinct from hashes being present in the committed lockfile, given lock verification compares a lockfile against its manifest rather than a downloaded artifact against a hash? [Complete, plan §Testing Strategy Security / research §Supply-chain criteria for new dependencies] <!-- Evaluator: Resolved per user — **not required**, and the distinction is now stated rather than left to be inferred. SC-027 stays scoped to what the committed lockfile contains (exact pin plus artifact hash per distribution in the closure) and closes with what it does *not* assert, stated where a reader of the criterion meets it: `uv sync --locked` and `uv lock --check` verify lock-versus-manifest agreement, never a downloaded artifact against its recorded hash, so SC-027 is a claim about lockfile contents and never an install-time guarantee. A matching row sits in data-model §Uncovered Requirements beside the epic's other disclosed gaps, owning the posture at **project level** rather than at E002's, with the production-scale alternative named. This is a recorded exposure, not a closure -->
- [X] CHK019 Is the transitive closure enumerated as a complete set, or exemplified, given four transitive distributions are named while the extraction dependency's own chain carries further ones the count does not reach? [Complete (set), plan §Testing Strategy Security / §AD-004 / §AD-005] <!-- Evaluator: Resolved — exemplified, and now said so. The plan's Security tier states that the packages AD-004 and AD-005 name are examples of the review surface rather than the closure, which reaches further through pdfminer.six, cffi, and the extra's own chain; SC-027 and SC-028 quantify over every distribution the committed lockfile gains, so the closure is enumerated from the lockfile rather than by hand and a package no artifact names is inside the obligation -->
- [X] CHK020 Is the licence posture of the closure stated as an obligation — that no added distribution carries a licence incompatible with redistributing this repository — given the non-GPL extra is chosen for exactly that reason while no requirement states the rule it satisfies? [Complete, plan §AD-005 / research §Supply-chain criteria for new dependencies] <!-- Evaluator: Resolved — added SC-028: zero distributions added by this epic, direct or transitive, carry a licence forbidding redistribution or imposing copyleft, measured over the same lockfile-derived closure; AD-005's non-GPL choice is now evidenced against a stated obligation rather than left as an unexplained preference -->
- [X] CHK021 Is commit-SHA pinning of the verification workflow's third-party actions stated as an obligation, given this epic amends that workflow and it resolves its actions by floating tag? [Complete, Spec §FR-034 / §FR-017 / research §Supply-chain criteria for new dependencies] <!-- Evaluator: Resolved per user — **no obligation is stated, deliberately**, and the item closes as a decided non-obligation rather than as an oversight. The three actions keep their floating major tags; the Scope → Excluded entry says so in those words so a later reader records it as settled instead of re-filing it. The plan's Security tier repeats it, so no task is expected to carry pinning -->
- [X] CHK022 If action pinning is deliberately out of scope, is that recorded as a scope decision carrying cause, owner, and reversal trigger in the form the branch-protection exclusion already uses, rather than left absent without a record? [Conforming, Spec §Scope Excluded / project-instructions §VII] <!-- Evaluator: Resolved per user — added a Scope → Excluded entry in the same form the branch-protection and dependency-scanner entries use. Cause: pinning a shared workflow's actions is a repository-wide posture with an ongoing renovation cost, not corpus work, while E002 touches `verify.yml` only for FR-034's trigger and FR-017's validation step. Consequence, stated honestly: a floating tag is a mutable reference the upstream owner can re-point, so an upstream release executes inside the merge-gating check, and this sits **inconsistently beside E001's TR-026**, which digest-pinned container images — the repository pins the images it pulls and not the actions it runs, and the asymmetry is accepted rather than explained away. Owner: the repository administrator, matching the branch-protection entry, because the decision spans every workflow. Reversal trigger: any workflow gaining a secret or a write-scoped token (which would void FR-034a(c)'s premise), or a project-wide pinning policy landing. Production-scale alternative named per §VII -->
- [X] CHK023 Is the reach of the existing repository-root supply-chain check stated against what this epic adds — that it asserts index configuration and image digest pinning and asserts nothing about version pins or artifact hashes for the four new distributions? [Correct, plan §Testing Strategy Security / plan §Tests to extend] <!-- Evaluator: Resolved — the plan's Security tier now states the reach precisely: index configuration across all three Python entries and image digest pinning, and **nothing** about version pins, artifact hashes, or licences for the four additions, which are carried by the lockfile, `uv lock --check`, and SC-027/SC-028. The "needs no change" claim is corrected to hold only of the index parametrization -->
- [X] CHK024 Is the absence of any dependency-vulnerability scanning recorded with its consequence, given this epic adds a parsing dependency whose input is third-party files rather than repository-authored ones? [Complete, plan §Testing Strategy Security / Spec §Scope Excluded] <!-- Evaluator: Resolved — added a Scope → Excluded entry in the form §VII requires: cause (no scanner project-wide, required categories are lint and coverage), consequence (an advisory against the extractor, renderer, or imaging library surfaces in no run this repository executes, while the parsed input is third-party files), owner (epic owner), and reversal trigger (any path feeding the parser bytes that did not arrive through a reviewed commit) -->


## Parsing Third-Party PDFs as Untrusted Input

- [X] CHK025 Does any requirement state the trust posture toward the vendored PDFs — that they are third-party input to the validator rather than trusted because their provenance is recorded? [Necessary, Spec §FR-001 / data-model §VR-013] <!-- Evaluator: Resolved — added FR-001a: the vendored layer is untrusted third-party input to every component that opens it, because a recorded provenance establishes where bytes came from and not what they contain, so neither a manifest entry nor a license basis makes a document safe to parse -->
- [X] CHK026 Is a bound stated on what a malformed or hostile corpus PDF may consume when the validator opens it — wall clock, memory, page count, or object nesting depth — given the decision to state no validation-runtime bound records CI cost as its only consequence? [Complete, Spec §Scope Excluded / data-model §VR-013 / §VR-035d] <!-- Evaluator: Resolved as a recorded exposure rather than as a bound, since the epic's no-bounds decision is explicit and reversing it is the user's call: the Excluded entry now carries the second consequence it was missing — the validator opens third-party PDFs inside the merge-gating check, so unbounded consumption is an availability exposure and not only CI cost — with why it is accepted (a committed, reviewed corpus, every document opened once at retrieval) and a reversal trigger that now includes the first document that hangs or exhausts the runner, at which point per-document wall-clock, memory, and page-count bounds are set -->
- [X] CHK027 Is a parse failure of a corpus document an enumerated error category with a stated response, given the error categories cover schema, drift, generation precondition, retrieval, renderer pin, and file↔entry mismatch but not a document the reader cannot open? [Complete (set), plan §Error Handling Strategy / data-model §VR-013] <!-- Evaluator: Resolved — added the missing row to plan §Error Handling Strategy: a truncated, malformed, or hostile PDF fails for that document with all failures collected, naming the location, the file, and the rule (VR-013 for a file that does not open, the relevant VR-035x for one whose derivation raises), and is never skipped so the run can continue — skipping would exempt it from every rule asserted over emitted documents. A second row covers FR-002b's four retrieval-policy rejections -->
- [X] CHK028 Is the availability consequence stated — that extraction runs inside the check gating the merge, so a document that never returns blocks that gate rather than merely costing runtime? [Correct, Spec §FR-017 / §Scope Excluded] <!-- Evaluator: Resolved — stated in both places it is read: the Excluded entry names it as the half that is "not merely cost", and the new Error Handling row repeats it where an implementer meets the extraction step -->
- [X] CHK029 Is the byte-for-byte vendoring prohibition stated together with its parse-surface consequence — that no sanitization or normalization pass may stand between the published bytes and the extractor? [Complete, Spec §FR-001 / plan §AD-004] <!-- Evaluator: Resolved — FR-001a states the consequence as part of the posture: FR-001's prohibition fixes it rather than softening it, no sanitization, normalization, re-encoding, or repair pass may stand between the published bytes and the extractor, and the extractor is therefore the first component to read structure this project did not author. The plan's FR-001a coverage row says the same against the two modules -->
- [X] CHK030 Is the extractor's version pin stated at requirement level the way the renderer's is, given the extractor is what reads third-party bytes while only the renderer's pin is carried by a requirement? [Consistent, Spec §FR-021a / plan §AD-004] <!-- Evaluator: Resolved — FR-031a now requires the extractor pinned exactly in the modeling boundary's lockfile the way FR-021a pins the renderer, with the word-splitting tolerances stated explicitly rather than defaulted, and names the asymmetry it removes: the extractor reads third-party bytes while the renderer only writes this project's own. AD-004's pin is now the design record of a requirement rather than the only statement of it -->


## Automatic Pull-Request Triggering as an Execution Surface

- [X] CHK031 Does any requirement state that the new trigger causes checks to execute proposed head content, so what the merge gate runs is unreviewed rather than merged content? [Complete, Spec §FR-034 / §US5] <!-- Evaluator: Resolved — added FR-034a(a): the run executes proposed head content, so a contributor's test code, check harness, and lockfile take effect inside the run that judges them -->
- [X] CHK032 Is the token permission and secret surface of the pull-request run stated at requirement level, in the form E001 stated it for the dispatched run, or is it carried only by the workflow file? [Complete, Spec §FR-034 / plan §Requirement Coverage Map FR-034] <!-- Evaluator: Resolved — FR-034a(c) states it in E001 TR-020's form: token scoped to `contents: read`, no check the run invokes requires credential access (static analysis, tests over committed fixtures and pure functions, a local image build, corpus validation over committed files), so no repository secret is exposed to head content, and a later epic adding such a check must state its own surface. The plan's FR-034a row makes the permissions scope a committed file assertion -->
- [X] CHK033 Is the trigger fixed as `pull_request` rather than `pull_request_target`, with the distinction stated, given the two differ in whose content executes and what that execution can reach? [Unambiguous, Spec §FR-034] <!-- Evaluator: Resolved — FR-034a(b) fixes `pull_request` and forbids `pull_request_target`, stating the distinction rather than assuming the reader knows it: `pull_request_target` runs the base ref's workflow with the base repository's token and secret access while checking out head content. The workflow-trigger check gains an assertion that `pull_request_target` is absent -->
- [X] CHK034 Is the exposure that a pull request may alter a lockfile the run then installs from stated, given every entry is synchronised against its own lockfile before any check executes? [Complete, Spec §FR-034 / plan §Testing Strategy Security] <!-- Evaluator: Resolved — FR-034a(d) records it rather than leaving it to be discovered: every entry is synchronised against its own lockfile from the head before any check executes, the consequence is bounded by the run holding no secret and only `contents: read` — a wrong verdict on that pull request rather than a credential or a write — and the reversal trigger is stated (a secret or write scope, at which point installs must be gated on the base ref's lockfile) -->


## Legal and Licensing Exposure

- [X] CHK035 Is `statute` constrained to a closed set the way the basis identifier is, so an entry cannot record a citation conferring no public-domain status while satisfying the non-empty rule? [Verifiable, data-model §License Basis / Spec §FR-011] <!-- Evaluator: Resolved — FR-011 now states the statute is drawn from a closed set keyed to FR-012a's basis identifier, and the License Basis table carries it as `enum{17 U.S.C. §105(a)}` with an agreement check against `basis_id`; VR-023 asserts both. The asymmetry with the basis identifier — closed on one component, non-empty on the other — is removed rather than left to the reader -->
- [X] CHK036 Is the scope of the point-of-use copyright check stated — whole document or sampled pages, by which party, and at what moment — given the recorded outcome is a human judgement nothing re-derives? [Complete, Spec §FR-005 / §FR-011 / data-model §Uncovered Requirements FR-005] <!-- Evaluator: Resolved — FR-011 fixes all three: whole document, every page, never a sample (a sampled check cannot support FR-005's exclusion of a document reproducing such text anywhere); performed by the party performing retrieval with the epic owner accountable, in FR-036's form; at retrieval, before the entry is written. The data-model Uncovered row now separates what the fixed scope removes (ambiguity about what was claimed) from what it does not (the need to trust the claim) -->
- [X] CHK037 Is the assumption that federal hosting implies federal authorship recorded with its residual, given the research position that "federal, therefore public domain" is an assumption rather than a basis? [Correct, Spec §Assumptions / research §Public-domain basis for federal guide specifications] <!-- Evaluator: Resolved — added an Assumptions entry carrying the research position verbatim in substance: §105(a) removes copyright from works of the government but permits assigned copyrights and leaves contractor-produced works to agency discretion, so a federally hosted section can still carry third-party rights. The residual is stated — no check establishes authorship; the statute component, the whole-document check, and the exclusion ledger are what stand in for it -->
- [X] CHK038 Is a remediation path stated for a copyrighted excerpt discovered after a section is committed, given the lifecycle table records a detection path and consequence for every other post-commit defect but none for this one? [Complete (set), Spec §Risks / data-model §State & Lifecycle] <!-- Evaluator: Resolved — added the missing lifecycle row: detection is by a person and never by a run (the check is a one-time human judgement), and the consequence is one change removing document and entry together (removing either alone fails VR-011), a ledger record with the reproduces-a-copyrighted-standard cause — legal only once the entry is gone, since VR-026 forbids an excluded identifier in a manifest — and a coverage shortfall refilled by vendoring another section rather than by lowering VR-025's floors. The spec's Risk entry carries the same path -->


## Credential Posture Against the New Surface

- [X] CHK039 Does any artifact state whether this epic's new committed surface — the corpus package under `/src/model` and the files under `data/` — changes the population the credential-marker scan covers, given that scan reads the serving and gateway entries only? [Complete (set), plan §Tests to extend / Spec §FR-018] <!-- Evaluator: Resolved — plan §Tests to extend now states it rather than leaving "test_supply_chain.py is unmodified" to cover a claim it does not reach: the credential scan iterates ("api", "gateway") only, so `/src/model` and `data/` are outside its population today, and this epic widens the entry tuple to include `model` and adds `data/` to the scanned roots. Two properties are stated with it — the assertion passes vacuously on landing, since this epic introduces no credential and FR-002a forbids allow-listing a source needing one, and the file is a repository-root cross-entry check, so the widening touches a shared assertion and is proposed rather than assumed -->
- [X] CHK040 Is it stated as a constraint that no allow-listed retrieval source may require authentication, so the epic's zero-credential posture is a requirement rather than an incidental property of the hosts that happened to be chosen? [Necessary, Spec §FR-002a / research §Retrieving and vendoring the real layer] <!-- Evaluator: Resolved — stated on both sides. FR-002a: every allow-listed host must serve its targets anonymously, a source requiring authentication is not allow-listable, and a candidate reachable only behind one is excluded under FR-004 rather than retrieved with a credential. FR-002b(e): the client sends no credential, cookie, or authorization header and is configured with no secret -->


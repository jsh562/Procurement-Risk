# Data Integrity: Document Ingestion and Extraction
**Created**: 2026-07-27 | **Feature**: [spec.md](../spec.md)

## Completeness — Ownership Boundary and Migration Confinement

- [ ] CHK001 Does the requirement set state the ownership boundary over all six E003 tables E006 populates, given Scope Excluded names only the chunk, extracted-value, and extraction-failure schemas while `document`, `field_vocabulary`, and `extracted_value_contributing_chunk` are equally populated and equally not owned? [Completeness, Spec §Scope Excluded, §FR-039, data-model §Scope]
- [ ] CHK002 Is "zero columns, zero constraints, zero indexes added to an E003-owned table" stated as a requirement with its own acceptance condition, or does it exist only as a data-model validation rule with no FR and no success criterion behind it? [Completeness, Spec §FR-039, §FR-040, §SC-034, data-model §VR-015]
- [ ] CHK003 Is the drop and rebuild of `ix_chunk__embedding_hnsw` — data-definition change to an index E003 owns — admitted anywhere in the requirement set as a bounded exception to the no-alteration boundary, with the executing role and the restoration obligation stated? [Completeness, Spec §Scope Excluded, plan §AD-006, data-model §Operator Procedures 1]
- [ ] CHK004 Does the specification enumerate the objects this epic creates, so that "zero schema objects are placed outside the claimed block" has a population to range over, rather than the inventory living only in a downstream artifact? [Testability, Spec §FR-040, §SC-034, data-model §Named Object Inventory]
- [ ] CHK005 Is FR-040's reservation of `0200`–`0299` for a concurrent epic stated as an obligation something outside this specification ratifies, and is the amendment to the build-gating partition check — a file this epic does not own — covered by a requirement rather than only by a plan decision? [Completeness, Spec §FR-040, §Compliance Check condition 2, plan §AD-013]

## Consistency — Generation State and the Reader's Filtering Obligation

- [ ] CHK006 Does FR-055's "mark each ingestion run active or superseded" state the granularity the rest of the epic depends on, given FR-043 has a run ingest only the documents whose input tuple changed, so one run is simultaneously live for some documents and retired for others? [Consistency, Spec §FR-055, §FR-043, §SC-043, data-model §ingestion_run_document]
- [ ] CHK007 Is the retention bound FR-055 requires actually stated in the specification as a value, together with the purge procedure, and is either covered by a success criterion — the SC set carries none for retention or purge? [Completeness, Spec §FR-055, §Success Criteria, data-model §Operator Procedures 3]
- [ ] CHK008 Does the requirement that "downstream readers filter on the active run" name the consumers it binds and state the outcome when one does not, given the obligation falls on epics outside this one and no mechanism here can compel it? [Completeness, Spec §FR-055, §SC-043, data-model §G-4]
- [ ] CHK009 Is the coexistence of a retained superseded generation with its successor addressed against E003's uniqueness of `(document_id, ordinal)` on `chunk`, given both generations hold chunks for the same document from ordinal zero and this epic may add no constraint to relax it? [Consistency, Spec §FR-015, §FR-055, §SC-003, E003 data-model §chunk]
- [ ] CHK010 Do the total-population criteria — every chunk's text found on its named page, contiguous ordinals per document, one page per chunk — state whether their population is the active generation or every generation in the database? [Testability, Spec §SC-002, §SC-003, §SC-031, §SC-043]
- [ ] CHK011 Is the retirement order stated in the requirement set rather than deferred to "an order the restricting foreign keys permit", and does that order account for the one edge E003 declares `ON DELETE CASCADE` — contributing chunks to their value — where the ordering rationale does not hold? [Clarity, Spec §FR-041, §US6 scenario 2, E003 data-model §Referential Actions]
- [ ] CHK012 Do FR-043's "remove-and-reload only the documents whose tuple differs" and the generation mechanism describe one behaviour or two, given a re-ingest supersedes and inserts while removal is an operator procedure the job cannot perform? [Consistency, Spec §FR-043, §FR-041, §SC-025, data-model §Write Order]
- [ ] CHK013 Does the input tuple's first member mean the corpus-wide manifest digests it names, or the single document's own content hash the comparison requires, and is the difference resolved in the requirement rather than in the design that reinterprets it? [Consistency, Spec §FR-043, §Glossary "Input tuple", data-model §input_tuple_digest]

## Completeness — Atomicity, Write Order, and Recovery

- [ ] CHK014 Does FR-054's enumerated write order cover every row the document's transaction contains, given the generation record and the line-item association are absent from the list yet both are written inside it? [Completeness, Spec §FR-054, §FR-059, data-model §Write Order and Transaction Boundary]
- [ ] CHK015 Is it stated that the record explaining an aborted document is written after the rollback and outside the transaction that discarded it, and that it cannot be a per-field failure row because that row's source chunk no longer exists? [Completeness, Spec §FR-056, §FR-042, §SC-044, data-model §VR-008]
- [ ] CHK016 Do FR-038's "start and finish" and SC-022's "zero fields are absent" admit the aborted run SC-044 requires, in which no finish exists and recording one would assert a completion that did not happen? [Consistency, Spec §FR-038, §SC-022, §SC-044]
- [ ] CHK017 Is "complete row set" defined for a document that legitimately produces no extracted values, no contributing chunks, no failures, and no line items — the 26 specifications — so the criterion distinguishes a correct empty set from a truncated one? [Testability, Spec §SC-042, §SC-012, §FR-022]
- [ ] CHK018 When a hash mismatch or an identifier collision aborts a run part-way, is the standing of the documents already committed stated — whether their generations remain active and whether the corpus is consumable in that state — or does the requirement address only the document that triggered the abort? [Completeness, Spec §FR-005, §FR-052, §SC-030, §SC-042]
- [ ] CHK019 Is a recovery path stated for a run that aborts while the similarity index is absent, given nothing in the database restores it, no migration recreates it on an already-migrated database, and the resulting state is correct but silently degraded? [Completeness, Spec §FR-042, data-model §G-7, §Operator Procedures 1]

## Completeness — Append-Only Semantics and the Role Split

- [ ] CHK020 Are the two roles the append-only rule turns on identified in the specification — the role the ingestion job connects as and the schema-owning role a correction runs under — or are they referred to only by description? [Clarity, Spec §FR-041, §SC-024, data-model §Privileges]
- [ ] CHK021 Do the four association tables this epic adds carry the append-only rule as a requirement, given FR-041 names only the three tables E003 revoked and these rows are themselves provenance? [Completeness, Spec §FR-041, §FR-039, data-model §Privileges]
- [ ] CHK022 Is the set of updates that remain permitted stated — the finish timestamp, the run-failure columns, and the active-to-superseded flip — so that FR-041's prohibition and SC-024's "extraction tables" are bounded by enumeration rather than by an undefined table group? [Clarity, Spec §FR-041, §SC-024, data-model §Privileges]
- [ ] CHK023 Does SC-024 state the configuration under which it is measured, given the deployed connection uses a role that bypasses the revoke and the limitation table records that the criterion must not be reported as fully enforced in deployment? [Testability, Spec §SC-024, §Disclosed Limitations, data-model §G-6]

## Consistency — Citation and Provenance Integrity

- [ ] CHK024 Does SC-019's "one contributing chunk per page it drew on" agree with the normative model in which the anchor citation is contributor one and only contributors two through N occupy the contributing-chunk table, so a two-page value carries one contributor row and not two? [Consistency, Spec §SC-019, §US4, E003 data-model §extracted_value_contributing_chunk]
- [ ] CHK025 For a field whose label ends one page and whose value begins the next, is it stated which contributing chunk supplies the anchor citation — the page carrying the label or the page carrying the printed value — given the cited page is a single scalar and downstream traceability resolves through it? [Clarity, Spec §FR-029, §US4, §SC-009]
- [ ] CHK026 Is it stated where the parse signals a confidence is computed from are recorded, given SC-026 requires recomputation "from the signals recorded with it" while alternate-label and repaired-invocation status appear in no column this epic may add and no column it owns? [Testability, Spec §SC-026, §FR-031, §FR-046, §Scope Excluded]
- [ ] CHK027 Do FR-031's four confidence signals and FR-057's three deductions describe the same computation, given "whether the value was printed or absent" survives in one and was withdrawn in the other? [Consistency, Spec §FR-031, §FR-057, §Clarifications 2026-07-27 (Clarify)]
- [ ] CHK028 Does FR-059's line-item key state that membership is scoped to a generation, given the key it names — value, document, item ordinal — omits the run and two generations of one document would otherwise be readable as one item three? [Completeness, Spec §FR-059, §SC-046, data-model §extracted_value_line_item]
- [ ] CHK029 Is the unenforceable half of SC-021 disclosed in the specification — that an association row *existing* for every chunk, value, and failure is cross-table absence, and that an association's document may disagree with its target row's document — or do both appear only as gaps in a downstream artifact? [Completeness, Spec §SC-021, §FR-039, §Disclosed Limitations, data-model §G-1, §G-2]
- [ ] CHK030 Is "contributor order" defined for SC-027's comparison of a page-split value against the concatenation of its contributing chunks, given the ordinal sequence carries no declared precedence in the normative model? [Clarity, Spec §SC-027, §FR-062, E003 data-model §TR-060]

## Testability — Identity, Cardinality, and Population

- [ ] CHK031 Does FR-002's derivation rule state the whole transformation from a file stem to an identifier — case, separator handling, and the length bounds the declared format imposes — or only the lower-casing step, leaving the normalization that FR-052's collision case presupposes unstated? [Clarity, Spec §FR-002, §FR-052, §SC-036, E003 data-model §document]
- [ ] CHK032 Is the identifier-collision check required to complete over the whole corpus before the first document commits, given per-document transactions mean a collision found at document forty leaves thirty-nine documents durable? [Completeness, Spec §FR-052, §FR-054, §SC-036]
- [ ] CHK033 Is `PRJ-000` stated as reserved — barred from being minted as an ordinary project identifier by any other producer — given the project identifier is a format-checked text column with no referent table, so nothing structurally prevents the sentinel from acquiring a second meaning? [Completeness, Spec §FR-003, §Assumptions, §Disclosed Limitations]
- [ ] CHK034 For a field absent from an entire document and recorded once per document, is it stated which chunk and which page the single failure record names, given both are required on every failure and neither has an obvious referent for a document-level absence? [Completeness, Spec §FR-058, §FR-035, §SC-039]
- [ ] CHK035 Does SC-046's "100% of extracted values belong to exactly one line item" account for values a transmittal prints once for the whole document rather than per item, and is per-field cardinality within an item — one manufacturer, one part number — stated as a rule or left unasserted? [Consistency, Spec §SC-046, §FR-058, §FR-059, data-model §G-5]
- [ ] CHK036 Is the ordinal assignment rule stated — the order chunks are numbered in and whether a page that yields no storable text consumes an ordinal — so that contiguity from zero and re-run determinism are both decidable? [Testability, Spec §FR-015, §SC-003, §SC-007, §FR-012]
- [ ] CHK037 Is the obligation to draw field names only from unretired vocabulary terms stated as a requirement, given retirement is advisory in the normative schema and the filter is assigned to this epic by that document rather than claimed by this one? [Completeness, Spec §FR-024, §SC-010, E003 data-model §G-7, data-model §VR-020]

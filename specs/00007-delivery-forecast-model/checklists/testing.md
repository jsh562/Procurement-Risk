# Testing: Delivery Forecast Model
**Created**: 2026-07-27 | **Feature**: [spec.md](../spec.md)

## Singularity — Can One Verification Pass or Fail Against It

- [X] CHK001 Does FR-014's roughly eighteen recording obligations decompose into separately failing checks, so a manifest missing one field fails a named obligation rather than one compound requirement whose result says only that something is absent? [Singular, Spec §FR-014 / §SC-020] <!-- Evaluator: Covered by data-model §Additions to `forecast_run`, where each of FR-014's new obligations is a NOT NULL column with its own named CHECK (`ck_forecast_run__fixture_digest_format`, `__input_layer`, `__datasheet_ref_present`, `__canonical_serialization`, `__split_seed_format`, `__split_hash_format`, `__covariates_non_empty`) and its own Requirement cell; the delivered half rides on E003's NOT NULL manifest columns. A manifest missing one field violates a constraint named for that field, not FR-014 as a whole -->
- [X] CHK002 Are FR-017's two gates stated as separate obligations — a post-sampling blocking-diagnostic refusal and a pre-sampling blocking-precondition refusal — given their observable evidence differs (nothing sampled versus nothing written) and a run can breach one while satisfying the other? [Singular, Spec §FR-017 / §Published Constants] <!-- Evaluator: Resolved — split into two requirements. FR-017 now states the post-sampling blocking-diagnostic refusal only, and FR-035 (placed in context, numbered by append) states the pre-sampling blocking-precondition refusal with its own observable and its own exit message, which names a precondition rather than a breached diagnostic. plan §Requirement Coverage Map, §Error Handling Strategy and data-model §Requirement Traceability carry FR-035; DV-035 and NC-14 are its assertions -->
- [X] CHK003 Does SC-014's own wording name the row class its quantifier ranges over, or does it inherit "blocking rows only" from the Published Constants preamble, leaving a tolerance row readable as a gate by anyone reading the criterion alone? [Unambiguous, Spec §SC-014 / §Published Constants] <!-- Evaluator: Resolved — SC-014 now quantifies over "any **blocking-diagnostic** threshold in Published Constants" in its own wording, and states why: a tolerance row bounds an agreement check and never gates a run, and a reported row never gates one either, so the unqualified form left a tolerance readable as a gate -->
- [X] CHK004 Are FR-023's two outcomes — refusal on a moved row or split hash, provenance warning on a moved fixture digest — each independently falsifiable, with the exit status and the emitted artifact that distinguish them stated per outcome? [Singular, Spec §FR-023 / plan §Error Handling Strategy] <!-- Evaluator: Covered by plan §Error Handling Strategy, whose two rows give the response per outcome (non-zero exit naming which hash moved and both values; provenance warning naming the break with the run proceeding), by NC-3 and NC-4 which evidence the two directions separately and say why one cannot stand for the other, by DV-015/DV-016/DV-017, and by G-16 which records where the warning survives. FR-023 now also states the warning outcome's zero exit -->
- [X] CHK005 Do FR-016's three obligations (run at least the minimum chain count, monitor five metrics against thresholds, name the monitored parameter set) each resolve to their own pass or fail, given only the second is carried by `forecast_diagnostic` rows? [Singular, Spec §FR-016 / data-model §`forecast_diagnostic`] <!-- Evaluator: Resolved — the chain minimum had no assertion of its own, only a precondition in the job. Added DV-035: every emitted run records a `chain_count` equal to the published minimum, scoped like DV-014 to runs the assertion itself emitted, with NC-14 the failing direction. data-model §Requirement Traceability FR-016 now names all three separately: DV-035, DV-011, and the enumerated `parameter_name` values (AD-006) -->
- [X] CHK006 Does each of FR-028's four obligations — record the realized count, state whether it supports the band, pre-register a change to a published band, pre-register a change to the held-out fraction — carry its own verification, given SC-025 reaches only the first two? [Singular, Spec §FR-028 / §SC-025] <!-- Evaluator: Resolved — added SC-032, which covers both pre-registration obligations (a published band and the held-out fraction) and names its verification: review of the commit history of `model.forecast.config`, where `HELD_OUT_FRACTION` and `SPLIT_SEED` are committed constants (AD-005), and no check. Closes the LOW the spec's own §Compliance Check left open -->
- [X] CHK007 Can SC-002 fail on one array property in one store, or does asserting four properties across two stores in one criterion make a partial breach unreportable — and is the enforcing mechanism named per property per store rather than per criterion? [Singular, Spec §SC-002 / data-model §`held_out_prediction`] <!-- Evaluator: Covered by data-model §`held_out_prediction` and §Named Object Inventory/Constraints, which name a separate check per property per store (`ck_held_out_prediction__survival_monotone`, `__survival_unit_interval`, `__survival_length`, `__residual_matches_grid_tail`, with the delivered counterparts on `line_posterior`), and by DV-027, which pairs the seven shared properties one for one across the two tables. A partial breach fails a constraint named for that property in that store -->
- [X] CHK008 Are FR-012's placement decision and its anchor-recording obligation separately falsifiable, given G-5 discloses that the two populations are structurally disjoint on only one side? [Singular, Spec §FR-012 / data-model §G-5] <!-- Evaluator: Covered separately — placement by DV-001, DV-002 and DV-030 (cross-store anti-join; the three populations exhaustive and pairwise disjoint), anchor recording by DV-023 plus `ck_held_out_prediction__anchor_convention`, `ck_forecast_run__open_line_semantic` and §Array and Anchor Semantics' "Anchor recorded as" row. G-5 is the disclosed one-sided disjointness and names DV-001/DV-030 as its cover, which is the direction this item asks about -->


## Verification Method — Test, Inspection, or Absence

- [X] CHK009 Does SC-021 declare that its three conjuncts are discharged by static inspection of the import graph rather than by executing a path, and state what completeness that inspection buys — since "no provider call occurs on any path" and "no path imports the provider" are different claims with different residual risk? [Verifiable, Spec §SC-021 / data-model §DV-022] <!-- Evaluator: Resolved — SC-021 now names static inspection of the import graph (DV-022's import-linter contract, indirect detection on) as the verification method for all three conjuncts, states what it buys (every path reachable through an import edge, transitively) and what it does not reach (a provider called without one), and points at the new gap G-17, which records the residual with its evidence bound, its reversal trigger and a production-scale alternative -->
- [X] CHK010 Is the "emitted set" SC-026 quantifies its absence over defined by enumeration, and is "a coverage threshold, calibration verdict, or pass/fail judgement" reduced to a predicate a checker evaluates rather than a category a reviewer recognises? [Verifiable, Spec §SC-026 / data-model §DV-021] <!-- Evaluator: ASK — the gap is real (DV-021 says "row, report or file" and NC-7 plants "an artifact containing a coverage threshold", but neither enumerates the emitted set nor reduces the three categories to a predicate). The enumeration half is derivable; the predicate half is a genuine design choice — a lexical deny-list over the emitted files against a whitelisted-section structure — with materially different false-positive profiles and materially different NC-7 plants. Left unchecked pending the batched question -->
  <!-- Evaluator: FR-040 enumerates three emitted files and makes the run report a closed schema; DV-021 restated as a structural predicate and DV-041 asserts the set as an equality. A lexical deny-list was rejected because SC-025 and L-3 require the report to state the registered coverage band, so it would need a permanent carve-out. -->
- [X] CHK011 Does SC-011 state that its verification inspects the fit's own design matrix rather than the fitted parameters, so the criterion records that the evidence is a proxy for the claim it makes? [Verifiable, Spec §SC-011 / data-model §DV-008] <!-- Evaluator: Resolved — SC-011 now states that it is verified over the fit's own input frame and not the fitted parameters (DV-008), and records explicitly that this is a proxy: nothing reads influence back out of a posterior, so what is checked is the input and the inference is that the parameters could not have seen what the fit was never given -->
- [X] CHK012 Is FR-028's prohibition half recorded as verified by review of a commit diff rather than by any check, where a reader of the criteria set will see it — given G-11 records that gap only in the data model and a green suite would otherwise be read as covering it? [Verifiable, Spec §FR-028 / plan §AD-005 / data-model §G-11] <!-- Evaluator: Resolved — the same SC-032 added for CHK006 states the verification method in the criteria set: review of the commit history of `model.forecast.config`, and no check, with G-11 named as why and with the explicit note that a green suite discharges only SC-025's publish-and-state half -->
- [X] CHK013 Does SC-013 distinguish recording a semantic label from measuring the property the label names, given `open_line_draw_semantic` and `held_out_prediction.duration_semantic` are single-value CHECKs that the re-based implementation FR-029 forbids would satisfy identically? [Verifiable, Spec §SC-013 / §FR-029 / data-model §Array and Anchor Semantics] <!-- Evaluator: Resolved — SC-013 gains a sub-bullet stating that recording a semantic is not measuring it and that the criterion covers the label only, that a label agrees with itself and the re-based implementation would satisfy both CHECKs identically, that the measured counterpart for the open population is SC-027/DV-005, and that for the held-out population the label plus the anchor FK — which fixes the anchor and does not reach the duration — are the whole of the evidence today. Whether that population gains a measurement is CHK024 -->
- [X] CHK014 Does any obligation require SC-006's recorded covariate list to equal the covariate set the fit actually used, or is the criterion discharged by a non-emptiness constraint that three plausible strings satisfy? [Verifiable, Spec §SC-006 / data-model §`ck_forecast_run__covariates_non_empty`] <!-- Evaluator: Resolved — no obligation existed. Added DV-036: `forecast_run.covariate_names` equals the covariate set the fit's design matrix actually carries, asserted over the fit's own input frame — the artifact DV-008 already asserts over — so the recorded list is a measurement rather than a label beside the fit. data-model §Requirement Traceability FR-002 and plan §Success Criterion Coverage Map SC-006 now name it -->
- [X] CHK015 Does SC-022 state which half of it is checked and which is argued, given G-13 discharges "deterministic tie-breaking" by showing ties are byte-indistinguishable rather than by any assertion, leaving "the serialization is recorded" as the only mechanically checkable clause? [Verifiable, Spec §SC-022 / §FR-009 / data-model §G-13] <!-- Evaluator: Resolved — SC-022 now names the split: "the serialization is recorded" is carried by the delivered `ck_forecast_run__draw_serialization`, while "deterministic tie-breaking" is discharged by argument and by no assertion (equal float64 draws are byte-identical, so there is nothing to break a tie on), citing G-13. G-13 itself carries no reversal trigger and does not say which half of SC-022 is checked, so this needed the criterion to say it -->
- [X] CHK016 Is SC-023's universal quantification over "any reader, at any point during or after a run" reduced to a condition some tier can discharge, and is the structural argument that carries it — both arrays as NOT NULL columns of one row inside transaction 1 — named as the verification method rather than left as design commentary? [Verifiable, Spec §SC-023 / data-model §Write Order] <!-- Evaluator: Resolved — SC-023 now states that no tier can range over "any reader at any time", names the structural argument as the verification method (both arrays NOT NULL columns of one row, written by a single statement inside transaction 1, AD-010 and data-model §Write Order steps 3–4, so no window exists), and names what the Integration tier actually discharges: the write order and its atomicity, the condition beneath the argument rather than the quantifier -->


## Oracle Declaration and Relation Class

- [X] CHK017 Does each success criterion declare itself oracle-based or relational, given the relation class is stated per module property in the plan and nowhere per criterion — so SC-003, SC-018, SC-023 and SC-027 are relational only by inference and a property test could be accepted as an example test with better coverage? [Verifiable, Spec §Success Criteria / plan §Mandated properties: relation class and input domain] <!-- Evaluator: Resolved — added a second standing obligation to spec §Measurable Outcomes, in the form of the STF-005 one already there: every criterion is oracle-based unless it declares itself relational and names the relation and its operands. The relational set is enumerated (SC-003 alternate implementation, SC-005, SC-008, SC-018, SC-027) with SC-023 recorded as neither — discharged structurally — and each of those criteria now carries the tag inline -->
- [X] CHK018 Does SC-003 require its recomputation to run by a different code path from the one that wrote the stored value, so agreement is evidence rather than a restatement of the writer? [Verifiable, Spec §SC-003 / data-model §DV-003] <!-- Evaluator: Covered by SC-003's "recomputed independently from the draw array", DV-003's explicit "recomputed by a different path from the one that wrote it, so the check is an agreement test rather than a restatement", FR-011's requirement to compute the residual from the draws rather than copy the grid tail, and plan §Mandated properties, which classes the `posterior.py` residual property as Alternate implementation by a different code path -->
- [X] CHK019 Does SC-018 state that no independent expected value exists and that its reference is the prior run's artifact, so a back-to-back comparison is not read as an accuracy claim about the forecast? [Verifiable, Spec §SC-018 / plan §AD-004] <!-- Evaluator: Resolved — SC-018 now carries a sub-bullet stating that the reference is the original run's stored artifact rather than a known-correct forecast, that no independent expected value exists, and that agreement is therefore evidence of reproducibility and never an accuracy claim, with the calibration verdict left to E014 per FR-026/SC-026 -->
- [X] CHK020 Is AD-004's third outcome — a comparison reported as outside the tolerance's stated basis when realized predictive ESS falls below half the draw count, which is neither pass nor fail — carried in SC-018 itself, or reachable only by reading the decision record? [Complete, Spec §SC-018 / plan §AD-004] <!-- Evaluator: Resolved — SC-018 now carries the third outcome in its own text: where any line's realized predictive effective sample size falls below half the draw count the comparison is reported as outside the tolerance's stated basis, neither pass nor failure, because AD-004 derives the tolerance at a predictive ESS the harness measures rather than assumes -->
- [X] CHK021 Does SC-027 name the relation it rests on — a monotone comparison between the longest- and shortest-elapsed deciles — and name where the survival floor's derivation from the fitted one-day hazard is published, so the floor is not chosen after the deciles are seen? [Verifiable, Spec §SC-027 / data-model §DV-005] <!-- Evaluator: Resolved — the relation was already named; the floor's home was not. SC-027 now tags itself relational and states that the floor's *derivation rule* is a committed constant in `model.forecast.config` under AD-005's mechanism (a change is a diff with an author and a date) while the *realized* floor is published in the run report beside the comparison it judges — so the derivation cannot follow the result, which is the move FR-028 prohibits for bands -->
- [X] CHK022 Does SC-008 state that its independently derived floor is the oracle substitute and that the derivation is published before the realized delta is observed, so a demonstrated ablation cannot become a floor fitted to its own result? [Verifiable, Spec §SC-008 / §FR-033 / plan §AD-008] <!-- Evaluator: Resolved — SC-008 now says both. The floor is named as the criterion's oracle substitute (no independently known expected delta exists) and the temporal ordering AD-008 carried is stated in the criterion itself: computed before and independently of the fit, published with its derivation -->
- [X] CHK023 Do SC-005 and SC-030 name their comparisons' operands precisely enough that no alternative pairing satisfies them — the vendor with fewest against the vendor with most training lines, and an observed library version against the recorded pin? [Unambiguous, Spec §SC-005 / §SC-030 / data-model §DV-010] <!-- Evaluator: Resolved — SC-005 now counts its two vendors from the run's own `forecast_split_assignment` rows with `split_side = 'train'`, so the operands are fixed by stored data rather than chosen after the intervals are seen, and closes the tie case (every tied pairing must satisfy the comparison). SC-030 now names the pin as `forecast_run.library_versions`, the whole recorded set rather than a chosen subset, which is the reading FR-032 and DV-019 already carry -->
- [X] CHK024 Is the held-out population's total-duration-from-order-date semantic evidenced by a measurement, or only by a constant CHECK and a foreign key that fix the anchor without reaching the duration, given DV-005 provides the measured counterpart for open lines and has none here? [Complete, Spec §SC-013 / data-model §DV-005 / §`held_out_prediction`] <!-- Evaluator: ASK — the gap is real and is now stated plainly in SC-013 (the label plus the anchor FK, which does not reach the duration, are the whole of the evidence), but closing it is a judgement call: disclose it as a gap with a reversal trigger, or add a measured counterpart to DV-005, and if a measurement then which one — a distributional comparison against the training split's own total durations, or a per-line comparison against the observed outcome, which edges into the calibration verdict FR-026 reserves for E014. Left unchecked pending the batched question -->
  <!-- Evaluator: DV-040 added as DV-005's counterpart: the aggregate median of stored held-out draws must land inside a band around the training-split Kaplan-Meier median, published before any draw is seen. Reuses AD-008's estimator. Not a per-line comparison against observed outcomes — that is grading, reserved to E014 by FR-026. -->


## Failure-Path Evidence for Every Refusal

- [X] CHK025 Is a failing direction stated for the pre-sampling precondition refusal — a run configured below the four-chain minimum — given NC-1 exercises only the post-sampling gate and the two refusals leave different evidence behind? [Complete, plan §Negative Controls NC-1 / Spec §FR-017 / §Published Constants] <!-- Evaluator: Resolved — added NC-14: a run configured below the four-chain minimum exits non-zero naming the precondition and its realized value with **nothing sampled**, so no sampler output exists to inspect. Stated against NC-1 explicitly, since nothing written after a sample and nothing sampled at all are different evidence and one cannot stand for the other. Tier Integration -->
- [X] CHK026 Do the refusal requirements name the stream the reason is written to and the exit-status class observed, given G-8 makes the exit output and the report file the only surviving record of why a run refused? [Verifiable, Spec §FR-017 / data-model §G-8 / research §Observability for a batch job] <!-- Evaluator: Resolved — neither was named anywhere. FR-017 gains a standing refusal-path obligation reaching FR-017, FR-035, FR-021 and FR-023: the reason goes to **stderr**, never stdout (research §Observability for a batch job, which fixes the stream); the exit-status class is a **single non-zero status** rather than per-category codes, with the category carried by the reason text — the reading every existing statement already took. plan §Error Handling Strategy carries the same paragraph once for all its rows -->
- [X] CHK027 Do SC-015's three named stores and DV-013's five snapshotted tables agree, and does SC-015 state whether the report file a refused run emits under G-8 sits inside or outside its quantifier? [Complete, Spec §SC-015 / data-model §DV-013 / §G-8] <!-- Evaluator: Resolved — they did not agree: SC-015 named three groups and omitted `forecast_diagnostic`, the one store a reader expects a refusal to fill. SC-015 now enumerates all five DV-013 snapshots by name and adds the exclusion: the job's stderr output and the emitted report file sit **outside** the quantifier, because a refused run does write them and G-8 makes them the only surviving record — a refusal that left nothing at all would satisfy the criterion and defeat it -->
- [X] CHK028 Is a failing direction required for DV-022 — a planted import from `model.forecast` reaching the gateway — given an import contract naming a module that does not exist passes vacuously and every other build-gating claim in the set carries one? [Verifiable, data-model §DV-022 / plan §Negative Controls] <!-- Evaluator: Resolved — added NC-15: a planted import from a `model.forecast` module to `gateway`, and a second indirect one through an intermediate module, must make the contract fail. Without it a contract naming a module that does not exist is green and empty, which is the one failure mode a build-gating claim has; G-17 records what the contract still does not reach -->
- [X] CHK029 Do the three cross-row rules the schema cannot enforce each carry a demonstrated failing case — a missing split assignment, a partially covered monitored parameter, an omitted vendor — given a completeness check over an empty enumeration passes silently? [Verifiable, data-model §DV-006 / §DV-011 / §DV-009 / §G-6 / §G-7 / §G-9] <!-- Evaluator: Resolved — added NC-16 with all three planted cases: one `forecast_split_assignment` row removed fails DV-006; one monitored parameter's `ess_tail` row omitted fails DV-011's partial-coverage clause; a `vendor_shrinkage` object missing one of the twelve vendors fails DV-009's membership half. These are exactly the directions G-6, G-7 and G-9 disclose as unreachable by any constraint -->
- [X] CHK030 Is the reproduction harness itself demonstrated failing on a perturbed artifact, or is the failing direction evidenced only at `compare.py`'s property tier while DV-018 asserts agreement alone and the harness wiring goes unexercised? [Verifiable, data-model §DV-018 / plan §Error Handling Strategy] <!-- Evaluator: Resolved — added NC-17: a stored artifact perturbed beyond the tolerance on one line's P80 must make `forecast-reproduce` exit non-zero naming that line and its realized delta. `compare.py`'s property proves the comparison function; the wiring that reads both stores, pairs lines and applies the tolerance was exercised in the passing direction only -->
- [X] CHK031 Is migration `0300`'s refusal against a populated `forecast_run` backed by a requirement and a demonstrated failing case, or does it exist only as an error-handling row and a disclosed gap? [Complete, plan §Error Handling Strategy / data-model §G-2] <!-- Evaluator: Resolved — it existed only as an error-handling row, HINT-001 and G-2. Added **FR-036** (the migration must verify its own precondition and refuse with a named error rather than let the database report a not-null violation) and **NC-18** (applying `0300` against a populated `forecast_run` raises the migration's own named error), with rows in plan §Requirement Coverage Map and data-model §Requirement Traceability. The guard's whole value is its message, so a guard never run against a populated table is a message nobody has read -->
- [X] CHK032 Is the negative-control set derived by applying its own admission rule to every claim rather than enumerated, so a claim whose passing direction is satisfiable without its mechanism working but which carries no entry is detectable as a miss? [Complete, plan §Negative Controls] <!-- Evaluator: Resolved — added a derivation paragraph to plan §Negative Controls: the admission rule is applied to every SC-### and every DV-###, and the three exclusion classes are named (declarative-constraint enforcement, property-tier algebraic identity or alternate implementation where Hypothesis's falsification is the failing direction, and delivered checks this epic inherits). A claim outside those classes with no entry is now a detectable miss — which is how NC-14 through NC-18 were found -->
- [X] CHK033 Do FR-021 and FR-023 name what must be observed unchanged after their refusals, or is the store-and-pointer enumeration inherited by assumption from FR-017 — which is the only refusal requirement that states it? [Complete, Spec §FR-021 / §FR-023 / §FR-017] <!-- Evaluator: Resolved — both now state it. FR-021 records that its refusal is pre-sampling and leaves unchanged the same five stores and pointer FR-017 enumerates; FR-023 records the same for its refusal outcome and adds that the warning outcome refuses nothing and completes with a zero exit. FR-017's new standing refusal-path clause names all four requirements, so the enumeration is inherited by statement rather than by assumption -->


## Coverage and Consistency of the Check Set

- [X] CHK034 Is there a stated mapping from each `SC-###` to the validation rule or negative control that discharges it, given the coverage map traces functional requirements to components and records only that criteria "map through" the validation rules? [Traceability, plan §Requirement Coverage Map / data-model §Validation Rules] <!-- Evaluator: Resolved — added plan §Success Criterion Coverage Map, one row per SC-001…SC-032 naming the validation rule, constraint or structural argument that discharges it and the negative control that demonstrates its failing direction, with an em dash and the exclusion class where none applies. The collective "criteria map through the validation rules" sentence is retained beneath it but is no longer the mapping; building the table is what surfaced SC-006, SC-029 and FR-028's prohibition half as criteria with a requirement and no mechanism -->
- [X] CHK035 Does any obligation assert the presence of the horizon-extrapolation limitation SC-029 requires, given DV-024 checks only that whatever limitations are present carry four parts — so a limitation set omitting L-2 entirely would pass? [Complete, Spec §SC-029 / data-model §DV-024 / §L-2] <!-- Evaluator: Resolved — none did. Added DV-037: the emitted limitation set contains each of L-1–L-4 **by identity**, L-2 in particular with the computed maximum observed duration published beside the horizon (G-10). It is the presence half SC-029 needs, against DV-024's form half — the difference between "the limitations that were written are well formed" and "the limitations that were owed were written" -->
- [X] CHK036 Does each validation rule's tier name the level that actually proves it, given DV-004, DV-005, DV-007, DV-010 and DV-020 are assigned the Property tier while their enforcement points are stored rows, the fitted posterior, or a job report — all outside that tier's stated mock boundary? [Correct, data-model §Validation Rules / plan §Testing Strategy] <!-- Evaluator: Resolved — plan's mock-boundary cell reconciled the tier, but the five rules still read "asserted over the stored rows" against a Property tier that never reads a database, which is the contradiction this item names, and the database-backed half was assigned to no tier at all. All five rows corrected in place: DV-004 and DV-005 are now Property + Integration with the in-memory half and the stored-row half stated separately; DV-020 is Property + Unit (values from `ablation.py`, then the emitted report); DV-007 and DV-010 keep Property with the artifact named as in-memory and the reason no second half exists -->
- [X] CHK037 Does the red-green pairs obligation quantify over the admitted qualifying set rather than a fixed count, given the property tier row and the test-first observable still count seven modules where § What qualifies admits nine, and tasks generated from the smaller count would leave two mandated modules with no evidenced failing test? [Correct, plan §Testing Strategy / §The test-first observable] <!-- Evaluator: Covered — verified against the three places. plan §Testing Strategy's Property row names nine modules and says "**nine**"; §The test-first observable binds "all **nine** qualifying modules — the seven admitted by clause 3 plus `likelihood.py` and `compare.py`"; §What qualifies carries exactly nine yes-rows. The pairs list is stated over the named set rather than a bare count, so tasks generated from it reach every mandated module -->
- [X] CHK038 Does SC-030 state that FR-032's not-published branch is a pass, so an optional digest claim's absence is not read as a failure and its presence is not left with no covering obligation? [Verifiable, Spec §FR-032 / §SC-030] <!-- Evaluator: Resolved — SC-030 now states it: FR-032's claim is optional, so where no draw-digest equality claim is published the criterion is satisfied vacuously and the absence is not a failure, and where one is published this criterion is the obligation covering it — so being optional does not leave it uncovered -->
- [X] CHK039 Do SC-005 and SC-008 state the disposition when their comparisons do not hold, given both assert outcomes of a fit rather than properties of code — a defect to fix, or a shortfall published in the four-part form? [Feasible, Spec §SC-005 / §SC-008 / §FR-027] <!-- Evaluator: ASK — neither states a disposition, and the choice is material: treating a breach as a defect makes the criterion release-blocking against an outcome that partly belongs to the data, while treating it as a four-part shortfall under FR-027 makes a green suite compatible with a model that does not pool or a censoring contribution that does not bite. The two criteria may also warrant different answers. Left unchecked pending the batched question -->
  <!-- Evaluator: Split, because the two criteria fail for different reasons. SC-005 now states its disposition as a defect — a sparse vendor whose interval does not widen means the hierarchy is not pooling, a property of the estimator E007 builds. SC-008 states its disposition as a published shortfall in FR-027's four-part form, following L-3, because the delta's magnitude is a property of the dataset's censoring rather than of the estimator. -->


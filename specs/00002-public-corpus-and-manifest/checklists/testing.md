# Testing: Public Corpus and Manifest
**Created**: 2026-07-26 | **Feature**: [spec.md](../spec.md)

## Test Basis and Test Conditions

- [X] CHK001 Does each tier of the testing strategy identify its test basis — the numbered VR-### rules or requirements it discharges — or does it state a prose scope that nothing maps back to a numbered rule? [Traceability, plan §Testing Strategy / data-model §Validation Rules] <!-- Evaluator: Resolved — plan §Testing Strategy gains a **Test Basis** column; each of the four tiers now names the numbered rules or the policy clause it discharges instead of prose scope alone -->
- [X] CHK002 Is a tier assigned to every rule the data model hands the modeling test suite, given VR-046, VR-047, and VR-049 assert over regenerated document models and appear in neither the unit tier's nor the integration tier's stated scope? [Complete (set), plan §Testing Strategy / data-model §Validation Rules runner split] <!-- Evaluator: Resolved — VR-046, VR-047, VR-048, and VR-049 assigned to the integration tier; VR-050 to unit; VR-044 to the workflow's contract step plus its negative fixture; VR-045 to the existing repository-root scan. The data model's runner-split row now points at that assignment, so VR-040…VR-050 is fully placed -->
- [X] CHK003 Are test conditions stated per structurally derived irregularity class — a document carrying the class and a document not carrying it — or does the strategy state only that the derived set equals the recorded set over the committed layer? [Complete, plan §Testing Strategy Unit / data-model §VR-035…§VR-035d / Spec §FR-031a] <!-- Evaluator: Resolved — the Unit tier states a positive **and** a negative fixture per structural class, and VR-035 carries the same condition with its reason: set equality over the committed layer alone is satisfied by a deriver that echoes the entry -->
- [X] CHK004 Does any requirement oblige each validation rule to be exercised in its failing direction, given US2's Independent Test removes exactly one field and SC-004 asserts only over the passing population? [Able to be validated (set), Spec §US2 Independent Test / §SC-004 / data-model §Validation Rules] <!-- Evaluator: Resolved — added SC-025 obliging every numbered rule to have at least one failing-direction case that names it. VR-056 already required each failure to name its rule, which nothing can observe without such a case; the unit and integration tiers carry it as their exit criterion -->
- [X] CHK005 Is the coverage item for the layer asymmetry stated as the field-by-direction matrix — each of the eight REAL-only and seven SYNTHETIC-only fields prohibited on the opposite layer — or as the single condition "layer-dependent field-set validation"? [Complete, plan §Testing Strategy Unit / data-model §VR-017 / §VR-027] <!-- Evaluator: Resolved — the Unit tier now states fifteen prohibited-field conditions (eight REAL-only on a SYNTHETIC entry, seven SYNTHETIC-only on a REAL entry) plus the two required-set conditions and VR-015's whitespace-only case, in place of one named condition -->
- [X] CHK006 Is the retrieval fixture's required scope stated — a redirect hop re-checked against the host allow-list, a non-200 status, a non-https scheme — or is "exercised against a local fixture" the whole statement of what the retrieval tests must reach? [Complete, plan §Testing Strategy Unit / research §Security criteria for path fields and redirect-following retrieval] <!-- Evaluator: Resolved — six conditions enumerated from research §Security criteria: allow-list re-check per redirect hop, a hop landing outside the allow-list, a non-https scheme, a non-200 status, the hop-count bound, and a digest diverging from the recorded upstream_digest -->
- [X] CHK007 Does the integration tier state where a re-run writes — a temporary tree or the repository working copy — given VR-042 compares each manifest byte-for-byte against its committed state and an in-place rewrite would compare a file against itself? [Unambiguous, plan §Testing Strategy Integration / data-model §VR-042] <!-- Evaluator: Resolved — the re-run writes into a temporary tree and never the working copy, stated in both the integration tier and VR-042, which now names the in-place rewrite as the one way that rule can be made vacuous -->

## Test Oracle for Determinism and Reproducibility

- [X] CHK008 Does the determinism strategy name which environment dimensions differ between the two compared runs — wall clock, working-directory path, filesystem enumeration order, locale, timezone, process identity — or is SC-011 satisfiable by running one command twice from one directory? [Verifiable, Spec §SC-011 / plan §Testing Strategy Integration] <!-- Evaluator: Resolved — six dimensions named and made normative in three places a reader meets them: SC-011, VR-040a, and the integration tier — a different absolute checkout path, a different process, a distinct PYTHONHASHSEED, a non-UTC TZ, a non-C LC_ALL, and a shuffled directory-enumeration order. Wall clock is excluded by construction rather than varied (DM-3, FR-009a) -->
- [X] CHK009 Is the reference artifact of the reproducibility comparison fixed, given US3 AS1 compares two fresh runs against each other while VR-040 compares a fresh run against the value recorded in the committed manifest — two oracles that fail on different defects? [Unambiguous, Spec §US3 AS1 / §SC-011 / data-model §VR-040] <!-- Evaluator: Resolved — the **committed manifest's recorded value** is SC-011's reference artifact (VR-040a). US3 AS1's run-against-run comparison is kept, not deleted, and relabelled as the companion stability assertion: run-against-run catches a nondeterministic generator whose commit is current, run-against-committed additionally catches a corpus committed from a generator that no longer produces it -->
- [X] CHK010 Does SC-011 state its two claims separately enough to be judged separately, given the model-hash claim holds unconditionally while the byte-identity claim holds only under the pinned renderer and FR-021a declares a pin change a regeneration event rather than a failure? [Singular, Spec §SC-011 / §FR-021a / data-model §VR-041] <!-- Evaluator: Resolved — split. SC-011 keeps the unconditional document-model-hash claim; new SC-024 carries the byte-identity claim with FR-021a's carve-out written into it, so a pin change is excluded from its population rather than counted as a failure -->
- [X] CHK011 Is the platform under which the byte-identity claims are evaluated stated, given the only automatically triggered runner is Linux while the development machine rewrites line endings and HINT-004 records that as a byte-comparison hazard? [Complete, Spec §SC-012 / plan §HINT-004 / data-model §MS-4] <!-- Evaluator: Resolved — the Linux verification runner is named as the platform of record for every byte-identity claim, in MS-4, VR-041, VR-042, SC-012, SC-024, and the integration tier's exit criterion -->
- [X] CHK012 Is a sensitivity oracle stated for the document-model hash — that a change to any one hashed component, a per-page render directive included, moves the digest — given DM-2's reason for hashing the directives rests on it while VR-040 asserts stability only? [Complete, Spec §FR-021 / data-model §DM-2 / §VR-040] <!-- Evaluator: Resolved — VR-040 split into VR-040a (stability) and VR-040b (sensitivity) on E001 VR-008's precedent, failing independently and named separately in failure output; FR-021 states the sensitivity obligation, and PB-3 is its property. A hash that silently dropped the directives would otherwise pass VR-040 forever -->
- [X] CHK013 Is the attribution rule for a byte-identity mismatch stated as a pass/fail condition, given VR-041 requires the observed and pinned renderer versions in the report but no criterion says which combination is a pass? [Unambiguous, data-model §VR-041 / Spec §FR-021a] <!-- Evaluator: Resolved — VR-041 and SC-024 state three outcomes: pin equal and bytes equal is a pass; pin equal and bytes differ is a failure naming the document; pin differing is neither, a regeneration event reported with both versions -->
- [X] CHK014 Is the observation window for the zero-network and zero-model claims stated — whether the socket guard and the import contract cover module import as well as the generator's run — or is "zero network requests" asserted over an unnamed span? [Verifiable, Spec §SC-014 / data-model §VR-043 / §VR-044] <!-- Evaluator: Resolved — window stated in SC-014 and VR-043: from before the generator package is imported until the process exits, so an import-time fetch is inside it. VR-044 notes the import contract is static over the module graph and covers import edges by construction -->
- [X] CHK015 Is an evidence path stated for the new forbidden import contract failing when violated, given the verification workflow's violation-injection input offers only the provider-import and computation-boundary payloads? [Able to be validated (set), plan §Requirement Coverage Map FR-022 / data-model §VR-044] <!-- Evaluator: Resolved — a committed negative fixture at `tests/fixtures/corpus_offline/` exercised by `tests/checks/test_contract_fixtures.py`, which runs automatically, following E001's TR-007/SC-004 mechanism rather than adding a dispatch payload that evidences the harness and must be remembered -->

## Property-Based Test Adequacy

- [X] CHK016 Does the property-based scope enumerate the same digest set the design declares, given the unit tier names "the three digest functions" while the plan's own summary and the data model name five digest kinds? [Consistent (set), plan §Testing Strategy Unit / §Data Model Summary / data-model §The Digest Kinds, Kept Distinct] <!-- Evaluator: Resolved — a stale count from before `generation_inputs` was added and then split from `roster_hash`. The Unit tier now names the five and enumerates them, and the Instructions Check Principle I row is corrected from three to five, so every count agrees with the current design rather than with an intermediate one -->
- [X] CHK017 Does each property over the canonical serialization declare its relation class — round-trip, invariant, metamorphic, or alternate implementation — or is "property-based tests" the entire specification of what those tests assert? [Verifiable, plan §Testing Strategy Unit / research §Test-strategy completeness criteria] <!-- Evaluator: Resolved — added plan §Property-Based Test Specification with PB-1…PB-6, each declaring its relation class and the serialization step or rule it is asserted over -->
- [X] CHK018 Is the generator domain stated for the document-model properties — which models may be constructed, and whether non-NFC strings, empty per-page text, and an empty irregularity-class list lie inside it? [Complete, plan §Testing Strategy Unit / data-model §DM-1 / §DM-5] <!-- Evaluator: Resolved — the domain is stated field by field, with the three cases named explicitly as inside it, and wall-clock values, floats, booleans, and nulls named as outside it per DM-3 -->
- [X] CHK019 Is an example count or any bound on generated examples stated, and is the property run made repeatable across invocations so a determinism gate is not itself nondeterministic between two CI runs of the same commit? [Verifiable, plan §Testing Strategy Unit / Spec §SC-011] <!-- Evaluator: Resolved — `max_examples=200`, `derandomize=True`, `deadline=None`, registered as a profile in `src/model/tests/conftest.py` and selected in CI, so two runs of the same commit generate the same examples -->
- [X] CHK020 Are boundary cases named separately from the generated population — NFC-versus-NFD equivalents, a duplicate manifest key, a codepoint-ordering collision in the entries sort, the empty class list — given generated coverage is not systematic? [Complete, plan §Testing Strategy Unit / data-model §MS-1 / §DM-5 / §VR-001] <!-- Evaluator: Resolved — seven boundary cases named separately from the generated population, including all four the item lists plus a single-page document, an empty per-page text string, and a pair of models differing only in one degradation parameter -->
- [X] CHK021 Is the invariant MS-1…MS-6 exists to hold stated as a property — equal manifest content serializes to equal bytes — or only as a sequence of serialization steps a reader is asked to trust? [Verifiable, data-model §Canonical Serializations MS-1…MS-6 / Spec §FR-006b] <!-- Evaluator: Resolved — the invariant is stated under the MS table in both directions (equal content to equal bytes, differing content to differing bytes) and carried as property PB-5; VR-042 and VR-063 assert the first direction over the committed set, the property tests both over generated manifests -->


## Coverage Items and the Gate's Denominator

- [X] CHK022 Does the coverage requirement state the resulting source scope after widening — which packages enter the denominator — or only that `--source=src/model/roster` must widen to include the new corpus package? [Unambiguous, plan §Testing Strategy Coverage / §HINT-002] <!-- Evaluator: Resolved — the widened scope is stated literally as `--source=src/model/roster,src/model/corpus`, and the denominator after widening is stated as exactly those two packages from the model entry's run combined with the repository-root check harness's data file, with nothing leaving it. HINT-002 carries the same string -->
- [X] CHK023 Is a per-package or per-entry coverage floor stated, given the gate is a single 80% figure combined at the repository root where an already-covered package can carry an uncovered new one across the threshold? [Verifiable, plan §Testing Strategy Coverage / project-instructions §Testing & Quality Policy] <!-- Evaluator: Resolved — `model.corpus` measured alone carries the same 80% floor as the aggregate, stated as the coverage tier's exit criterion and as SC-026. The project figure is reused rather than a second number invented, so this narrows where the gate applies without changing what it demands -->
- [X] CHK024 Do the fixture-backed tests standing in for the network paths of `retrieve.py` and `reverify.py` state which paths they must reach, given the plan commits to them in place of a coverage-scope exclusion but names no condition they must exercise? [Complete, plan §Testing Strategy Coverage / §Requirement Coverage Map FR-008b] <!-- Evaluator: Resolved — the same six conditions CHK006 names are bound to these tests in the coverage tier and in the FR-008b row, with the digest divergence called out as the one outcome the re-verification job exists to report, so the widened denominator is covered by assertions rather than by import -->
- [X] CHK025 Is the coverage criterion behind SC-021's "cover 100% of the five irregularity classes" defined — one passing test per class, or an assertion per injected effect — so "cover" is measurable rather than counted by test name? [Unambiguous, Spec §SC-021 / data-model §VR-050] <!-- Evaluator: Resolved — covering a class is defined as an assertion over the injector's effect on the emitted artifact: for the four structural classes, the deriver recovers exactly that class and does not recover it from an undegraded control; for scan degradation, the FR-031b oracle. Stated in both SC-021 and VR-050 -->
- [X] CHK026 Is a coverage criterion stated over the committed corpus itself — that every validation rule has a non-empty population to assert against — given a rule over an empty set passes silently, which is the defect STF-001 already found once? [Able to be validated (set), Spec §Stress-Test Findings STF-001 / data-model §VR-032 / §VR-038] <!-- Evaluator: Resolved — added VR-066 generalizing VR-038's non-vacuity clause to every population rule: each reports its observed population size and fails on an empty one, naming the rule and the count -->
- [X] CHK027 Is the coverage-gate outcome expressed as a measurable criterion anywhere in the success criteria, or does SC-021's injector coverage remain the only test-related criterion the epic states? [Complete (set), Spec §Success Criteria / plan §Testing Strategy Coverage] <!-- Evaluator: Resolved — added SC-026 for the coverage-gate outcome (root aggregate and corpus package both at or above 80%) and SC-025 for failing-direction coverage of the validation rules, so the epic states test-related criteria beyond SC-021 -->


## Entry and Exit Criteria

- [X] CHK028 Is the strict test-first obligation given an observable exit condition — what artifact shows the property tests existed and failed before the serialization did — or is the ordering an assertion no reviewer can check after the fact? [Verifiable, plan §Instructions Check Testing & Quality Policy / §HINT-001] <!-- Evaluator: Resolved — the observable artifact is the branch history: a commit adding `test_corpus_model_hash.py` with no `corpus/model.py` beside it, whose workflow run fails, preceding the commit that adds the module, with the two not squashed together on the branch. Stated in the Instructions Check row that makes the claim -->
- [X] CHK029 Is HINT-001's ordering expressed as an entry criterion for the rendering work — no PDF committed until the serialization properties pass — or as advice carrying no gate? [Appropriate, plan §HINT-001 / §Testing Strategy Unit] <!-- Evaluator: Resolved — HINT-001 is restated as an entry criterion ("no rendered PDF and no manifest is committed until the canonical serialization and PB-1…PB-6 pass") and the Unit tier carries it in its Entry column, so it gates rather than recommends -->
- [X] CHK030 Does each tier state an exit criterion, given the four tiers carry tool, scope, mock boundary, and install columns and only the coverage tier names a threshold? [Complete (set), plan §Testing Strategy] <!-- Evaluator: Resolved — an Entry / Exit Criterion column added; all four tiers now carry an exit criterion, and the unit and integration tiers additionally carry entry criteria (HINT-001's ordering, and a complete corpus in the checkout) -->
- [X] CHK031 Is an entry criterion stated for the corpus-validation step — that the checkout holds a complete corpus — so an empty or partially fetched corpus is distinguishable from a passing one? [Complete, plan §Requirement Coverage Map FR-017 / data-model §VR-005 / §VR-025 / §VR-034] <!-- Evaluator: Resolved — the integration tier's entry criterion names six locations, the five-way bijection, and the document floors (VR-004, VR-005, VR-025, VR-034); VR-066 makes an empty or partially fetched checkout fail as a zero population rather than pass over nothing -->
- [X] CHK032 Is the security tier's exit criterion stated over the right population, given it claims existing checks cover "the three dependencies this epic adds" while the Technical Context declares four direct additions and the research names a transitive closure the direct count understates? [Consistent, plan §Testing Strategy Security / §Technical Context / research §Supply-chain criteria for new dependencies] <!-- Evaluator: Resolved — another stale count. The tier now reads four direct additions (ReportLab, Pillow, pdfplumber, jsonschema[format-nongpl]) plus the reviewed transitive closure — charset-normalizer and cryptography via pdfplumber, rfc3339-validator and the non-GPL URI validator via the extra — and gains an exit criterion over exact pins with artifact hashes in the committed lockfile -->


## Evidence Paths for Claims No Structural Check Confirms

- [X] CHK033 Does the FR-031b evidence path state its oracle — what an injector unit test compares the degraded output against in order to decide pass or fail — given the substitute is assigned but its pass rule is not? [Verifiable, Spec §FR-031b / data-model §VR-050 / §Uncovered Requirements] <!-- Evaluator: Resolved — the oracle is the same page rendered with the degradation profile disabled. Pass is: body raster differs from that control, extracted text layer identical to the control's, citation anchor outside every raster rectangle. Stated in FR-031b, VR-050, and the plan's FR-031b row -->
- [X] CHK034 Is the bound of the injector-test substitute stated where a reader of SC-021 meets it — that passing injector tests establish nothing about whether any committed document carries degradation? [Complete, Spec §SC-021 / data-model §Uncovered Requirements FR-031b / §VR-036] <!-- Evaluator: Resolved — the bound is now written into SC-021 itself: the tests establish that the injector degrades correctly and nothing about whether any committed document carries degradation, which stays generator-asserted and narrowed only by VR-036's necessary condition -->
- [X] CHK035 Is a pass/fail condition stated for degradation being "bounded", or is boundedness evidenced only by the citation-anchor property of already-emitted pages rather than by any constraint on the injector's parameters? [Verifiable, Spec §FR-032 / §Edge Cases / data-model §VR-037 / §DM-1] <!-- Evaluator: Resolved — FR-032 defines "bounded" as exactly two conditions (text layer equals the undegraded control, anchor disjoint from every raster rectangle) and requires them over the injector's declared parameter domain rather than over emitted pages only; VR-050 asserts them across that domain, so boundedness is a property of the injector -->
- [X] CHK036 Does the strategy state what the re-derivation's independence requires beyond not reading generator state, given the injector and the deriver both read `field-label-vocabulary.json` and a shared misreading of it would make FR-031a's comparison agree for the wrong reason? [Correct, Spec §FR-031a / data-model §VR-035b / §Supporting Artifacts FieldLabelVocabulary] <!-- Evaluator: Resolved — VR-035 now states the reach of independence (generator state and computed values, not the shared artifact) and adds the test condition that excludes the shared misreading: VR-035a…VR-035d are additionally exercised against hand-authored fixtures whose expected class sets are written in the test and whose vocabulary is a fixture. A new Uncovered Requirements row records the residual — a change to the vocabulary is detectable via `generation_inputs`, a mistake in it is not -->
- [X] CHK037 Are the word-extraction tolerances the structural re-derivation depends on stated as pinned, given the derived set is the oracle for FR-031a and the extraction library's splitting defaults are not a stable contract across versions? [Verifiable, data-model §VR-035d / plan §AD-004 / research §Extraction for independent validation] <!-- Evaluator: Resolved — AD-004's chosen option now reads "pinned exactly, with word-splitting tolerances pinned explicitly", and VR-035d requires `x_tolerance`, `y_tolerance`, `keep_blank_chars`, and `use_text_flow` to be passed from one module-level constant rather than defaulted, with `extract_text()`'s `layout` fixed. A tolerance change is treated as a change to the oracle -->

- [X] CHK038 Does the FR-001 disclosure name when and by whom its substitute runs, given the substitute is an opt-in job excluded from every automatic run and no cadence or owner is recorded for invoking it? [Complete, data-model §Uncovered Requirements FR-001 / Spec §FR-008b] <!-- Evaluator: Resolved per user — cadence and owner stated in the branch-protection exclusion's form (cause, owner, discharging condition): the job runs before each release tag, invoked by the repository administrator, with its outcome in the release record, matching the boundary project-instructions.md already sets for the evaluation reproduction job. Written where each reader meets it — FR-008b, SC-009, US2 AS11, Implementation Signals, the FR-001 and FR-008b Uncovered rows, the lifecycle row, and plan's FR-008b row -->
- [X] CHK039 Where a disclosed gap names diff review as its only control — the three validator-read supporting artifacts, and FR-009b's enumeration currentness — is that review named as a gate with an owner, or left as an unassigned expectation? [Verifiable, data-model §Uncovered Requirements Supporting-artifact loosening / §FR-009b] <!-- Evaluator: Resolved per user — added FR-036, a pull-request review gate requiring explicit sign-off on the loosening direction of any change to manifest.schema.json, retrieval-policy.json, exclusions.json, or FR-009b's enumeration, with the epic owner accountable. No CODEOWNERS: the platform-enforced variant is recorded in Scope → Excluded as the outside-the-repository deviation it would be. Both Uncovered rows and the drift story now point at the named gate and label it an assigned control, not a mechanical one; FR-036 carried into plan's coverage map and the data model's traceability table -->

- [X] CHK040 Is the evidence path for SC-022 stated, given no committed artifact can assert that a pull-request run occurred and the workflow's violation injection is reachable only on manual dispatch? [Able to be validated (set), Spec §SC-022 / §US5 Independent Test / plan §Requirement Coverage Map FR-034] <!-- Evaluator: Resolved — SC-022's evidence is split three ways, following E001 SC-013's move from manual dispatch to automatic evidence: the declared `pull_request` trigger asserted by a committed workflow-file check (needs no run); the failing-check half carried by the committed negative fixtures, which execute inside the pull-request run; and the "a run occurred" half recorded by URL in the QC report, which is the only half no committed artifact can hold -->


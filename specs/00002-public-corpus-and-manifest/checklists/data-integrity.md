# Data Integrity: Public Corpus and Manifest
**Created**: 2026-07-25 | **Feature**: [spec.md](../spec.md)

## Provenance Completeness and Credibility

- [X] CHK001 Is the REAL applicable field set enumerated identically in FR-008, the CorpusManifestEntry entity, and the evaluator-facing statements, given US2 AS9 and SC-006 name seven fields where FR-008 requires eight? [Consistent (set), Spec §FR-008 / §US2 AS9 / §SC-006] <!-- Evaluator: Resolved — US2 AS9 and SC-006 now enumerate all eight FR-008 fields and bind the set to FR-008 by name rather than restating it -->
- [X] CHK002 Is the omission of the retrieval response status from the evaluator-determinable set stated as a deliberate narrowing, or does it read as a drafting slip a reader must resolve by inference? [Complete, Spec §SC-006 / data-model §Entities] <!-- Evaluator: Resolved — the omission was a drafting slip, not a narrowing; the response status is now in the evaluator-determinable set in both US2 AS9 and SC-006 -->
- [X] CHK003 Does any requirement oblige an entry to carry the basis identifier that FR-013 compares, given FR-011 enumerates statute, document identifier, and check outcome but no identifier field? [Complete, Spec §FR-011 / §FR-013] <!-- Evaluator: Resolved — added FR-012a mandating a closed two-value basis identifier in both layers; FR-011 now lists it as a fourth component and FR-013 cites it -->
- [X] CHK004 Does FR-011 state the admissible outcomes of the point-of-use copyright check, or does it require only that some outcome be recorded, leaving the single-value enum unsupported by any requirement? [Complete, Spec §FR-011 / data-model §VR-024] <!-- Evaluator: Resolved — FR-011 now fixes the single admissible outcome and routes any other outcome to the exclusion ledger, which is what VR-024 was already enforcing -->
- [X] CHK005 Is FR-012's license-basis statement expressed with a machine-determinable component, given the requirement asks for a statement while the design carries the checkable half in two `const` fields no requirement names? [Verifiable, Spec §FR-012 / data-model §License Basis] <!-- Evaluator: Resolved — FR-012 now requires the generated-by-this-project assertion and the NONE third-party-rights value as machine-checkable constants alongside the prose statement -->
- [X] CHK006 Is the issuing body constrained to agree with the entry's agency variant, or may a well-formed entry name an issuing body that contradicts the variant describing the same document? [Accuracy — semantic, Spec §FR-008 / data-model §Entities] <!-- Evaluator: Resolved — added FR-008d and VR-062; RetrievalPolicy.agency_variants now maps each variant token to both its section suffix and its issuing body, the same pattern document_identifier already used -->
- [X] CHK007 Is the retrieval policy — host allow-list, closed agency-variant set, and target sections — required by any requirement, given four validation rules and FR-002's weighting judgement rest on it as a normative artifact? [Necessary, data-model §Supporting Artifacts / Spec §FR-002 / §FR-008] <!-- Evaluator: Resolved — added FR-002a mandating the retrieval policy as a committed normative artifact with all three contents -->
- [X] CHK008 Is the manual-retrieval provenance path reconciled with the entry field set, given the plan's fallback records "the same provenance fields plus retrieved-by" while the REAL field set admits no more and no fewer fields? [Consistent, plan §AD-007 / data-model §VR-017] <!-- Evaluator: Resolved — plan AD-007 corrected; the manual record carries exactly FR-008's eight fields, since `retrieved-by` would fail VR-017 and additionalProperties:false. Who fetched manually is recorded in the commit, not the manifest -->


## Digest Definition and Non-Conflation

- [X] CHK009 Is the digest algorithm, prefix, and character case fixed at requirement level for the file content hash, the upstream digest, and the document-model hash, or only for the roster hash? [Precision, Spec §FR-007 / §FR-008 / §FR-020] <!-- Evaluator: Resolved — FR-007 now fixes SHA-256, the `sha256:` prefix, and lowercase hexadecimal for every digest an entry records, matching what VR-016 already enforced -->
- [X] CHK010 Is the enumerated set of digest-shaped values in agreement between plan and data model, and does either enumeration include the per-input digests FR-009b requires? [Complete (set), plan §Data Model Summary / data-model §The Three Digests, Plus the Fourth] <!-- Evaluator: Resolved — both enumerations now carry five kinds including `generation_inputs.*`; data-model section retitled "The Digest Kinds, Kept Distinct" and plan's summary restated as five -->
- [X] CHK011 Is each digest field paired with a stated independent recomputation source, and is the one field whose recorded value nothing re-derives identified as such rather than presented alongside the others? [Verifiable, data-model §VR-012 / §VR-018 / §VR-029 / §VR-040 / §VR-061] <!-- Evaluator: Covered by data-model §The Digest Kinds, Kept Distinct (now an explicit "Independently re-derived by" column naming VR-012, VR-040, VR-029, VR-061) and §Uncovered Requirements FR-008a/FR-008c, where `upstream_digest` is recorded as re-derived by nothing -->
- [X] CHK012 Does FR-008a state the precondition on which its equality has force — that the upstream digest was computed from the bytes as retrieved, at retrieval — or does the requirement read as an unconditional equality? [Correct, Spec §FR-008a / data-model §Uncovered Requirements] <!-- Evaluator: Resolved — FR-008a now names its dependence on FR-008c's procedure and states that back-filled it is a tautology no offline check distinguishes -->
- [X] CHK013 Is any requirement assigned the obligation to compute the upstream digest from the retrieved response body before the file is written, given that procedure is what distinguishes FR-008a from a self-comparison? [Complete, Spec §FR-001 / §FR-008 / plan §Requirement Coverage Map FR-001] <!-- Evaluator: Resolved — added FR-008c placing the obligation on the retrieval procedure and publishing it as uncovered by construction; plan FR-001/FR-008c rows and AD-007 bind the manual path to it too -->
- [X] CHK014 Is the difference in hashed scope between the file content hash (raw bytes) and the roster hash (canonical re-serialization) stated where a reader comparing the two would encounter it, rather than only in a design aside? [Understandability, Spec §FR-007 / §FR-020 / data-model §The Three Digests, Plus the Fourth] <!-- Evaluator: Resolved — FR-020 now states the canonical-content scope and contrasts it with FR-007's byte scope; the Glossary entries for content hash and roster hash carry the same contrast -->
- [X] CHK015 Can the two constraints on the roster's generation-input digest both hold, given it is required to equal the sha256 of the roster file's bytes and also to agree with the roster hash, which is taken over canonical content? [Correct, data-model §Entities CorpusManifestEntry / §VR-061 / §VR-029] <!-- Evaluator: Resolved — the contradictory pair is gone. The roster is no longer a `generation_inputs` key; its digest is `roster_hash` (canonical content, VR-029) and the mapping holds exactly the three remaining inputs as raw-byte digests (VR-061). The two procedures are stated, not implied -->
- [X] CHK016 Is it determined whether a formatting-only roster edit is drift, given a byte-level input digest would move while the lifecycle table records reformatting as invariant by design and therefore not drift? [Consistent, data-model §VR-061 / §State & Lifecycle] <!-- Evaluator: Resolved — determined as not drift, consistently: no byte digest covers the roster file, and the lifecycle row now states that keeping the roster out of the byte-digest mapping is what makes that hold -->
- [X] CHK017 Does FR-021's enumeration of what the document-model hash covers match the canonical serialization it governs, given that serialization additionally includes per-page render directives? [Complete, Spec §FR-021 / data-model §DM-1, §DM-2] <!-- Evaluator: Resolved — FR-021 now enumerates identity fields, ordered field values, per-page text, and per-page render directives, and carries DM-2's reason for including the directives -->


## Layer Asymmetry Enforcement

- [X] CHK018 Does any requirement prohibit a REAL entry from carrying generator identity, seed, generation date, generation inputs, document-model hash, or irregularity classes, or is the asymmetry stated on the SYNTHETIC side only? [Complete, Spec §FR-008 / §FR-009] <!-- Evaluator: Resolved — FR-008 now carries the mirror prohibition over all seven generation fields, and VR-017 asserts it twice exactly as VR-027 does on the other side -->
- [X] CHK019 Does an acceptance scenario or success criterion exercise the REAL-side prohibition, given US2 AS4 and SC-005 both assert only that a SYNTHETIC entry carries no retrieval field? [Able to be validated (set), Spec §US2 AS4 / §SC-005] <!-- Evaluator: Resolved — added US2 AS12 for a REAL entry carrying a generation field, and SC-005 now counts zero REAL entries carrying any of the seven -->
- [X] CHK020 Does FR-009's list of prohibited retrieval fields match the list the validation rule enforces, given the rule additionally prohibits the MasterFormat section and the agency variant? [Consistent, Spec §FR-009 / data-model §VR-027] <!-- Evaluator: Resolved — FR-009 now prohibits all eight REAL-only fields, field for field with VR-027, which cites FR-009 as the source of its list -->
- [X] CHK021 Is prohibiting a MasterFormat section on a SYNTHETIC entry reconciled with FR-023's requirement that each synthetic document carry the specification section it answers, so a reader can tell which surface the prohibition applies to? [Unambiguous, Spec §FR-009 / §FR-023 / data-model §VR-027] <!-- Evaluator: Resolved — FR-009 states the prohibition is over manifest-entry fields only; the section a document answers is content asserted over the regenerated model by VR-046. VR-027 and the entity's Asymmetry note repeat the scope -->
- [X] CHK022 Is "retrieval field" in SC-005 bound to an enumerated list, so the criterion is measurable without the reader re-deriving the set from FR-008? [Unambiguous, Spec §SC-005 / §FR-009] <!-- Evaluator: Resolved — SC-005 now reads "any of the eight REAL-only fields FR-009 prohibits", binding the count and the list to one enumeration -->
- [X] CHK023 Is the SYNTHETIC applicable field set enumerated consistently, given the roster hash is mandated by FR-020 but absent from FR-009's list while the exactness rule admits only "the six FR-009 fields"? [Consistent, Spec §FR-009 / §FR-020 / data-model §VR-027] <!-- Evaluator: Resolved — one representation chosen: `roster_hash` stays a first-class named field and `generation_inputs` covers only the three remaining inputs. FR-009 now enumerates seven, VR-027 admits seven, FR-020 and the entity table agree, and FR-009b states the split and its reason -->
- [X] CHK024 Is "an entry's applicable set" defined per layer by a requirement, so FR-010's fail-on-absence rule has a determinable scope on each side of the asymmetry? [Unambiguous, Spec §FR-010 / §FR-007 / §FR-008 / §FR-009] <!-- Evaluator: Resolved — FR-010 now defines the applicable set as FR-007's common fields plus the layer's own set, no more and no fewer, so a field outside it fails rather than being ignored -->
- [X] CHK025 Is the rule that a manifest holds exactly one layer stated as a requirement, or only derivable from the single-license-basis rule plus the layer-specific basis identifiers? [Complete, Spec §FR-013 / §FR-017a / data-model §VR-007] <!-- Evaluator: Resolved — added FR-014a stating that a manifest declares its location's layer and every entry must carry it; SC-005 counts zero locations with more than one layer, and VR-007 now cites FR-014a -->


## Manifest and File-Tree Consistency

- [X] CHK026 Is "corpus location" defined by a determinable condition rather than by the license-basis property the Glossary uses, which cannot be evaluated before the manifest inside it is read? [Unambiguous, Spec §Glossary / data-model §Physical Artifacts] <!-- Evaluator: Resolved — the Glossary now carries the data model's mechanical definition (a directory beneath `data/corpus/` containing a manifest.json) and demotes the one-basis, one-layer properties to what validation enforces rather than what identifies a location -->
- [X] CHK027 Is the corpus root path fixed by a requirement, given FR-018 constrains placement only to `data/` while every path rule and the file-tree reconciliation are expressed against a narrower root? [Complete, Spec §FR-018 / data-model §Physical Artifacts] <!-- Evaluator: Resolved — FR-018 now fixes `data/corpus/` as the corpus root with every location a directory beneath it -->
- [X] CHK028 Does FR-006's bidirectional rule state which files inside a location are exempt from requiring an entry, so "a corpus file with no manifest entry" is determinable rather than resolved by convention? [Unambiguous, Spec §FR-006 / data-model §VR-011] <!-- Evaluator: Resolved — FR-006 names `manifest.json` as the single exemption; VR-011 cites it, and VR-064's flatness makes the file set non-recursive and determinable -->
- [X] CHK029 Is the treatment of a non-PDF file placed under the corpus root outside any location stated, given the stray-file rule closes only over `*.pdf`? [Complete, Spec §FR-006 / data-model §VR-060] <!-- Evaluator: Resolved — added FR-018a closing the corpus root over exactly the seven supporting artifacts, enforced by new VR-065; VR-060 keeps the PDF half and now cross-references it -->
- [X] CHK030 Is the completeness limit of manifest-set discovery — that a location nothing discovers is a location nothing validates — stated alongside FR-006a's no-index decision, with the population floors named as its bound? [Complete, Spec §FR-006a / data-model §Physical Artifacts] <!-- Evaluator: Covered by data-model §Physical Artifacts ("No aggregate index (FR-006a)"), which states the exposure and names VR-005 and VR-025 as the two population rules that bound it, and by §State & Lifecycle "A whole corpus location deleted" -->
- [X] CHK031 Is FR-013's narrowing to the basis identifier stated where a reader of FR-011, SC-005, and the Glossary entries for "corpus location" and "license basis" would encounter it, or only inside FR-013 itself? [Consistent, Spec §FR-011 / §FR-013 / §SC-005 / §Glossary] <!-- Evaluator: Resolved — the narrowing now appears in FR-011, FR-012a, SC-005 ("governing basis identifier"), and both Glossary entries, not only in FR-013 -->
- [X] CHK032 Is the manifest's canonical form — entry ordering, key ordering, separators, trailing newline, line endings — required by a requirement, given SC-012's byte-identity claim is unachievable without it? [Verifiable, Spec §SC-012 / data-model §Canonical Serializations] <!-- Evaluator: Resolved — added FR-006b raising MS-1…MS-6 to requirement level, and VR-063 checking entry ordering directly so the once-written real manifest is covered as well as the five VR-042 re-runs -->


## Drift Detection Across Generation Inputs

- [X] CHK033 Does FR-009b's obligation to cover "every committed artifact the generator reads" remain satisfiable when the generator gains a further input, or is the four-item enumeration a closed list that nothing re-derives from the generator? [Complete, Spec §FR-009b / data-model §VR-061] <!-- Evaluator: Resolved — FR-009b now states the enumeration is closed by maintenance rather than derivation and obliges extension in the same change; data-model §Uncovered Requirements gains an "FR-009b — enumeration currentness" row stating the cause and the bound, per the artifact's own disclose-rather-than-claim posture -->
- [X] CHK034 Is SC-023's "committed generation inputs" bound to the same enumerated set as FR-009b, so the criterion and the requirement cannot be satisfied over different populations? [Consistent, Spec §SC-023 / §FR-009b] <!-- Evaluator: Resolved — SC-023 now names FR-009b's four inputs explicitly and cites the requirement as the source of the enumeration -->
- [X] CHK035 Do FR-016 and FR-009b state their relationship where both cover the roster, given they compare against different bases and one would otherwise read as redundant with the other? [Singular, Spec §FR-016 / §FR-009b] <!-- Evaluator: Resolved — FR-009b assigns the roster to `roster_hash` and states why; FR-016 states that the two are not redundant because its comparison is against a live reader call over canonical content. Plan's FR-009b and FR-016 rows carry the same statement -->
- [X] CHK036 Is the failure report's attribution — naming "every document generated from" a drifted input — determinable from recorded fields rather than from an assumption that every synthetic document shares every input? [Verifiable, Spec §FR-009b / data-model §VR-061] <!-- Evaluator: Resolved — FR-009b and VR-061 now state the attribution rule explicitly: a document is generated from an input exactly when its own entry records that input. A State & Lifecycle row for a non-roster input edit records the same -->


## Disclosed Gaps, Traceability, and Set-Level Consistency

- [X] CHK037 Is the supporting-artifact loosening exposure disclosed with a bound that matches the artifact table, given the disclosure names two still-uncovered artifacts while three of the six sit outside both the generator's input set and any recorded digest? [Complete (set), data-model §Supporting Artifacts / §Uncovered Requirements] <!-- Evaluator: Resolved — the undercount is corrected to three, naming `exclusions.json` alongside `manifest.schema.json` and `retrieval-policy.json`. The drift-story preamble now partitions the six into the three the generator reads and the three the validator reads; plan's supporting-artifacts note carries the same three -->
- [X] CHK038 Does the attribute-coverage claim agree with what it counts, given it excepts "the four recorded under Uncovered Requirements" where that section carries more rows and states them as requirements rather than attributes? [Correct, data-model §Validation Rules / §Uncovered Requirements] <!-- Evaluator: Resolved — the claim now excepts exactly one attribute, `Document.bytes` for a REAL document, and states that the Uncovered Requirements table is a separate list in a different unit whose row count is unrelated. Added VR-063 (entry ordering) and VR-064 (location flatness) so the two previously ruleless declared attributes are covered rather than exempted -->
- [X] CHK039 Are FR-009b and SC-023 present in the Requirement Traceability table, given both are cited by a validation rule that the table itself does not reach? [Traceability, data-model §Requirement Traceability] <!-- Evaluator: Resolved — added FR-009b and SC-023 rows, along with rows for every requirement added by this evaluation: FR-002a, FR-006b, FR-008c, FR-008d, FR-012a, FR-014a, FR-018a -->
- [X] CHK040 Does each Uncovered Requirements row state both its cause and the half that remains covered — including FR-002's unquantified weighting and the currentness posture that a re-fetch divergence means the source changed rather than the committed copy — so a reader can tell a disclosed limit from an unaddressed defect? [Traceability, data-model §Uncovered Requirements / Spec §FR-002 / §FR-008b] <!-- Evaluator: Resolved — FR-008b was pooled into a row with an empty covered half; it is now its own "FR-008b — currentness" row carrying the covered half and the posture that a divergence means the source changed, never the committed copy. FR-002's row already stated both halves and is unchanged -->


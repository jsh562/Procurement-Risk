# Testing: Document Ingestion and Extraction
**Created**: 2026-07-27 | **Feature**: [spec.md](../spec.md)

## Oracles and Ground Truth

- [X] CHK001 Is the extraction reference set named in a requirement rather than only inside two success criteria, given FR-050 fixes a baseline and FR-060 fixes a denominator while no functional requirement names the corpus generator's pre-render document model as the ground truth every accuracy figure is scored against? [Complete, Spec §FR-050 / §FR-060 / §SC-013 / §SC-027] <!-- Evaluator: Resolved — added FR-067 naming the pre-render document model as the reference set, digest-verified against the manifest's document-model hash before any figure is computed; SC-052 measures it -->
- [X] CHK002 Is the circular oracle excluded by requirement — a stored value compared against the chunk text it was read out of, which agrees whenever the parse is self-consistent and is silent on whether the parse is right — or is the pre-render reference distinguished from the parsed text only by the wording of SC-013? [Correct, Spec §SC-013 / §SC-027 / §FR-025] <!-- Evaluator: Resolved — FR-067 forbids scoring a stored value against its source chunk, against this epic's parsed page text, or against any artifact of the run being measured; SC-052 states the prohibition as a zero -->
- [X] CHK003 Does "an independent extraction of the page it names" state what the extraction is independent of — a separate parse rather than the cached page text the chunker cut the chunk from — given FR-008 pins both sides to one set of tolerances and one normalization form? [Unambiguous, Spec §FR-010 / §FR-008 / plan §Risk Mitigation] <!-- Evaluator: Resolved — FR-010 now states independence as a fresh post-run read of the document's bytes addressed by the recorded page number, never the chunker's cached text or mapping, and states that it is NOT independent of the parser, which is disclosed rather than implied -->
- [X] CHK004 Is SC-037 stated as a comparison between two separately computed derivations, or is it satisfied by construction the moment page attribution and the corpus's structural derivation call the same function, in which case the criterion asserts nothing about either? [Verifiable, Spec §SC-037 / §FR-008] <!-- Evaluator: Resolved — FR-008 requires one reader (no second tolerance map, normalization, or page-text assembly) and SC-037 is restated as the contract check that no second reader exists, with the shared-parser residual added to Disclosed Limitations -->
- [X] CHK005 Where are the parse signals a confidence is recomputed from recorded, given SC-026 names "the signals recorded with it" while E006 adds no signal column and leaves `extracted_value` unaltered, so the alternate-label and repaired-invocation signals have no stated home? [Complete, Spec §SC-026 / §FR-046 / §FR-057 / data-model §Scope] <!-- Evaluator: Covered by spec §FR-063, §FR-057 (weights and left-to-right order on the run row), §SC-049, and data-model §extracted_value_parse_signal / VR-026 -->
- [X] CHK006 Is the deterministic baseline required to be authored independently of the generator's own template definitions, given a template extractor reading the templates the corpus was generated from scores itself and its defeat carries no information about the model? [Correct, Spec §FR-050 / §SC-029 / plan §AD-012] <!-- Evaluator: Resolved — FR-050 bars the baseline from the generator's layout templates, rendering code, and pre-render model, enforced by an import contract; the committed field-label vocabulary is the one permitted shared input; AD-012 records the rejected option -->
- [X] CHK007 Is the set of claims resting on human inspection enumerated, or does FR-011 govern a population that may be empty, making SC-032's "the inspected count and error bound of any sampled claim" satisfiable with nothing inspected and nothing bounded? [Able to be validated (set), Spec §FR-011 / §SC-032] <!-- Evaluator: Resolved — FR-011 now requires the claim set to be enumerated and published even at an inspected count of zero, and names its known member: structural detection on the 26 real specifications, for which no reference exists -->
- [X] CHK008 Is SC-011's oracle stated as a reconciliation between the run's attempted extractions and the invocations actually recorded, given a static import contract establishes that no other module *can* reach the provider and produces no count of the requests that were made? [Verifiable, Spec §SC-011 / §FR-023 / §FR-048] <!-- Evaluator: Resolved — added FR-070 (one run-scoped trace identifier, recorded on the run, reconciled against the traced path), amended SC-011 to the reconciliation, added `ingestion_run.run_trace_id` with VR-027 and gap G-10 -->

<!-- Evaluator note (CHK003/CHK004): the alternative resolution — a second, independently authored page reader compared against the first over all 51 documents — was rejected as contrary to the repository's pinned single-normalization rule (`corpus/derive.py`, plan §Brownfield Notes). Overturning that choice reopens both items. -->

## Total Versus Sampled Claims

- [X] CHK009 Is the method by which FR-011's error bound is computed fixed in the requirement, or does "computed by a stated method" leave the choice to whoever writes the report, so two runs may publish bounds that are not comparable? [Unambiguous, Spec §FR-011 / research §Validating parser page attribution] <!-- Evaluator: Resolved — FR-011 fixes the method: rule-of-three 3/n at zero defects, never quoted for n <= 30, and the Wilson 95% interval otherwise, denominator printed -->
- [X] CHK010 Does every criterion phrased as "100%" or "zero" state whether it holds over the whole population or over an inspected subset, given SC-002, SC-013, and SC-027 say so explicitly while SC-003, SC-039, SC-040, SC-045, and SC-046 do not? [Complete (set), Spec §Success Criteria] <!-- Evaluator: Resolved — added a governing statement at the head of §Measurable Outcomes: every "100%"/"zero" criterion is total over its named population and publishes that population's count (FR-068); the only sampled claims are FR-011's enumerated set -->
- [X] CHK011 Is FR-061's similarity threshold required to be declared before the run, given a threshold chosen after the clusters are observed makes the published near-duplicate count a fitted figure, which is the discipline FR-032 imposes on the confidence floor for the same reason? [Correct, Spec §FR-061 / §SC-048 / §FR-032] <!-- Evaluator: Resolved — FR-061 fixes the measure (cosine over the chunk embeddings already computed) and replaces the single threshold with a grid declared before the run (0.80/0.85/0.90/0.95/0.99), so a curve is published and no threshold is privileged by having been chosen; SC-048 restated -->
- [X] CHK012 Is the observation basis stated for SC-041's "zero runs fail on an over-long leaf that a sentence-level split could have resolved", given a run that failed wrote no rows to inspect and the counterfactual is a property of no stored artifact? [Verifiable, Spec §SC-041 / §FR-014] <!-- Evaluator: Resolved — SC-041 now observes the clause over the enumerated leaf population FR-053 publishes, not over failed runs, and names the failing observation: a leaf above the window the ladder left unsplit that is not a single sentence -->
- [X] CHK013 Does any rule oblige each total check to report the population it asserted over and to fail on an empty one, given a containment check over zero chunks and a text-agreement check over zero text-kind values both report success? [Able to be validated (set), Spec §FR-010 / §SC-002 / §SC-027] <!-- Evaluator: Resolved — added FR-068 (publish the enumerated population and its count; fail rather than pass on an empty one) and SC-053 -->


## Denominators

- [X] CHK014 Is precision's denominator stated, and F1's, given FR-060 fixes recall's denominator on the generator's printed-field set and leaves the other two figures to be denominated by inference? [Complete, Spec §FR-060 / §SC-047] <!-- Evaluator: Resolved — FR-060 states precision's denominator (values stored for that field and layer), keeps recall's on the printed-field set, and states that F1 has no denominator of its own but inherits both, which must be printed beside it; SC-047 requires every published figure to print its denominator. Open and reported separately: FR-060 attaches a *Wilson* interval to F1, which is not a binomial proportion -->
- [X] CHK015 Is SC-008's "100% of extracted values carry a page citation and a per-field confidence" denominated on attempted fields rather than on stored rows, given non-nullable columns make the stored-row form true of every row that could exist and false of nothing? [Verifiable, Spec §SC-008 / §FR-029 / §FR-030] <!-- Evaluator: Resolved — SC-008 is restated over attempted field extractions: every attempt resolves to a stored value carrying citation and confidence or to a failure record carrying an outcome, zero unaccounted; FR-069 carries the ledger -->
- [X] CHK016 Is the distribution FR-033 publishes denominated on every computed score including those the floor rejected, or only on stored values, which SC-045 confines to at most three distinct scores and which therefore conceal exactly what the floor excluded? [Complete, Spec §FR-033 / §SC-017 / §SC-045] <!-- Evaluator: Resolved — FR-033 denominates the distribution on every score the run computed, rejected ones counted and labelled separately, and states that the rejected half comes from the run's tally because FR-036 gives it no stored home; SC-017 restated -->
- [X] CHK017 Is the counting unit stated for the valid, repaired, and failed figures and for any failure rate, given FR-026 budgets repair per invocation, FR-058 attempts a field subset per chunk, and FR-037 records a whole-document absence once per document — three units in one table? [Unambiguous, Spec §SC-018 / §FR-058 / §FR-037 / §SC-039] <!-- Evaluator: Resolved — FR-069 defines the three units (attempt, invocation, document) and assigns each published figure to one; SC-018 requires the unit to be named and the invocation-level and attempt-level figures to be two tables, not one -->
- [X] CHK018 Do the chunk-population criteria state which generation they are denominated on, given at most one superseded generation is retained per document and SC-002, SC-003, SC-031, and SC-038 speak of "every chunk in the corpus" while the database may legitimately hold two generations of one document? [Consistent (set), Spec §SC-002 / §SC-003 / §FR-055 / data-model §v_active_ingestion_generation] <!-- Evaluator: Covered by spec §SC-043, which names the population for every total-count criterion, and by {SAD:ADR-0020} — retention is now zero, so two generations of one document are unstorable (data-model §Invariant map 5b) -->
- [X] CHK019 Is "per layer" well defined for the real layer in FR-060 and SC-047, given SC-012 fixes its extracted-value count at zero, so every per-field proportion on that layer has a zero denominator and no interval? [Correct, Spec §FR-060 / §SC-047 / §SC-012] <!-- Evaluator: Resolved — FR-060 requires the real layer to be published as not measured with its reason (no extraction attempted, no reference exists) and forbids 0/0, 0%, or a blank cell; SC-047 states it as a zero -->
- [X] CHK020 Does FR-009's zero-recognition-error claim state the population it holds over, given the Assumptions record that the corpus datasheet governs only the synthetic layer while the requirement asserts the property of the corpus text layer as a whole? [Consistent, Spec §FR-009 / §Assumptions / §Disclosed Limitations] <!-- Evaluator: Resolved — FR-009 now states the claim per layer: zero by construction on the synthetic layer (the datasheet's scope, and the only layer extraction runs on), and the narrower no-recognition-step claim on the real layer with the embedded-text-layer residual named; the Disclosed Limitations row was widened to match -->
- [X] CHK021 Are printed fields the extractor never attempts counted in recall's denominator, given FR-058 narrows attempts to a declared transmittal subset while FR-060 denominates recall on everything the generator recorded as printed? [Unambiguous, Spec §FR-058 / §FR-060 / §SC-047] <!-- Evaluator: Resolved — FR-058 requires the declared subset to cover every term the generator can print, states that an unattempted printed field is counted in recall's denominator as a miss, and requires any such field to be published as unattempted-but-printed rather than absorbed -->


## Test-First and Property-Based Obligations

- [X] CHK022 Is the strict test-first obligation over `model/compute/confidence`, `coerce`, and `metrics` carried by a requirement or a success criterion, or does it exist only as a row in the plan's Testing Strategy that task generation is under no stated obligation to honour? [Complete, plan §Testing Strategy Property / project-instructions §Testing & Quality Policy] <!-- Evaluator: Covered by plan §Testing Strategy Property, which now states both mandates and obliges task generation to order the test task before its implementation task for every module under model/compute/ -->
- [X] CHK023 Is an observable artifact named that shows a computation module's tests existed and failed before the module existed, or is red-green-refactor an ordering claim no reviewer can judge once the branch is squash merged? [Verifiable, plan §Testing Strategy Property / §Instructions Check Testing & Quality Policy] <!-- Evaluator: Resolved — plan §The test-first boundary names two artifacts that survive a squash merge: the ordered task pair in tasks.md, and the test task's completion condition (the module's tests observed failing against the absent module, recorded on the task line) -->
- [X] CHK024 Is the boundary of "deterministic computation module" stated for this epic, given the chunker ladder, the tokenizer budget, and the input-tuple digest are all deterministic pure computation while the plan assigns them to the test-after unit tier and the three `compute` modules to the mandate? [Unambiguous, plan §Testing Strategy Unit / §Testing Strategy Property / project-instructions §Testing & Quality Policy] <!-- Evaluator: Resolved — plan §The test-first boundary states the classifying rule (a module producing a number that is stored or published is a scoring function and lives under model/compute/) and says why the chunker, tokenizer budget, and digest are outside it and what carries them instead -->
- [X] CHK025 Does each property over the computation modules declare its relation class — round-trip, invariant, metamorphic, or alternate implementation — or is "property-based tests over pure functions" the whole statement of what those tests assert? [Verifiable, plan §Testing Strategy Property / §Requirement Coverage Map FR-031] <!-- Evaluator: Resolved — plan §The test-first boundary adds a per-module relation-class table: alternate implementation and invariant for confidence, round-trip and metamorphic for coerce, invariant and metamorphic for metrics, each with what is asserted -->
- [X] CHK026 Is the generator domain stated for the confidence properties, and is exhaustive enumeration required rather than sampling, given FR-057's three binary signals admit exactly eight scores and a sampled property over an eight-point domain is strictly weaker than covering it? [Appropriate, Spec §FR-057 / §SC-045 / plan §AD-008] <!-- Evaluator: Resolved — the confidence domain is enumerated exhaustively over all eight signal combinations, with Hypothesis reserved for the weights and floor (run-row inputs), and the deduction order asserted as bit equality -->


## Offline Reproducibility and Fixture Discipline

- [X] CHK027 Are the fixture-regeneration triggers stated as a requirement, given the Assumptions record that any change to prompt text or to a schema constraint invalidates every existing fixture while no requirement obliges the re-record or names who observes that the trigger fired? [Complete, Spec §Assumptions / §FR-045 / §FR-056] <!-- Evaluator: Resolved — FR-045 now requires the re-record and makes the trigger structural rather than remembered: the change moves the fixture key, so it fires as a replay miss and FR-056's run-level failure, and the re-record is a record-mode run committed with the change -->
- [X] CHK028 Is the fixture resolution key stated, so a changed prompt or output schema resolves to a miss and the named run-level failure of FR-056 rather than to a stale response that replays silently under the new prompt? [Correct, Spec §FR-045 / §FR-056 / §SC-044] <!-- Evaluator: Resolved — FR-045 states the key and its hashed inputs (request including provider model and prompt text, output schema digest, prompt template digest), recorded as inherited from E004 rather than declared a second time -->
- [X] CHK029 Does FR-043's input tuple account for the provider model, given the tuple's five members omit it, so a run under a different model skips every document whose tuple is unchanged and replays fixtures recorded against the previous model while `ingestion_run.provider_model` records a change nothing acts on? [Consistent, Spec §FR-043 / §FR-038 / data-model §ingestion_run] <!-- Evaluator: Covered by spec §FR-043 (provider model is a tuple member, per document) and data-model §ingestion_run_document.input_tuple_digest / VR-004 -->
- [X] CHK030 Is the observation window for SC-023's zero provider calls and zero network access stated — whether it opens before the ingestion package is imported or when the run begins — given an artifact fetched at import time falls outside the narrower window? [Verifiable, Spec §SC-023 / §FR-045 / §FR-019] <!-- Evaluator: Resolved — SC-023 now opens the window at process start, before the ingestion package is imported, and closes it at process exit, naming the encoder artifact as the thing that would otherwise escape it -->
- [X] CHK031 Does FR-019's "pinned model identity and revision" state where the encoder artifact and its tokenizer resolve from, given a pinned upstream revision is a network resolution unless the artifact is committed and integrity-checked, and given chunk boundaries depend on that tokenizer? [Complete, Spec §FR-019 / §FR-014 / plan §Complexity Tracking] <!-- Evaluator: Resolved — FR-019 resolves both from a repository-committed artifact path, digest-verified before the session is created, failing rather than falling back to a fetch; committing is forced by SC-023's window, and the tokenizer is pinned to the encoder's revision -->


## Coverage Denominator

- [X] CHK032 Is the coverage-denominator obligation carried by a requirement or criterion and its resulting source set stated literally, given the spec states no coverage criterion and the plan records only that an enumeration which overrides rather than merges "must be appended" to? [Complete, plan §Testing Strategy Coverage / project-instructions §Testing & Quality Policy] <!-- Evaluator: Covered by plan §Testing Strategy Coverage, now marked **not configured** with the literal source set (roster, schema, corpus + ingest, llm, compute) and `.github/workflows/verify.yml` in §Project Structure's change list -->
- [X] CHK033 Is a per-package floor stated for the packages this epic adds, given a single combined figure lets already-covered packages carry a newly added one across the threshold without any of its lines being exercised? [Verifiable, plan §Testing Strategy Coverage / project-instructions §Testing & Quality Policy] <!-- Evaluator: Resolved — the Coverage row now states an 80% floor per package on each of ingest, llm, and compute alongside the combined floor, asserted per package, with the carrying arithmetic named as the reason -->
- [X] CHK034 Is FR-010's total page-attribution check owned by the ingestion job or by the test suite, given the requirement states a system obligation while the coverage map discharges it in a test module whose path disagrees with the one the project structure declares? [Consistent, Spec §FR-010 / plan §Requirement Coverage Map FR-010 / §Project Structure] <!-- Evaluator: Resolved — FR-010 names both actors and what each carries (job: write-time guard inside the document transaction; suite: the independent corpus-wide check), and the path is corrected to the entry-local `src/model/tests/ingest/test_page_attribution.py` in both the coverage map and §Risk Mitigation -->


## Determinism and Reproducibility Conditions

- [X] CHK035 Is what constitutes a chunker-version bump stated in a requirement, given the sentence segmenter's version, the ladder's structural detection, and the boundary-class rules all move boundaries while only research and a data-model column note record the segmenter case? [Complete, Spec §FR-017 / research §Deterministic sentence segmentation / data-model §ingestion_run.chunker_version] <!-- Evaluator: Resolved — FR-017 enumerates what obliges a bump (boundary-class rules, structural detection, ordinal assignment, segmenter identity or pinned version) and states that the encoder identity and revision are deliberately excluded, being their own tuple members -->
- [X] CHK036 Does FR-017's guarantee survive a tokenizer revision change, given chunk length is measured in the encoder's own tokenizer and the encoder revision is an input-tuple member distinct from the chunker version, so boundaries can move with the chunker version unchanged? [Correct, Spec §FR-017 / §FR-014 / §FR-043] <!-- Evaluator: Resolved — FR-017's guarantee is restated over the full input tuple rather than over the chunker version alone, matching SC-007, with attribution assigned to whichever tuple member moved -->
- [X] CHK037 Under what condition is SC-007 observable, given FR-043 obliges a re-run over an unchanged input tuple to skip every document and create no rows, so the second run of the comparison produces no boundaries to compare against the first? [Verifiable, Spec §SC-007 / §FR-043 / §SC-025] <!-- Evaluator: Covered by spec §SC-007, restated as chunking the same document twice in isolation and comparing, with the reason stated -->
- [X] CHK038 Which environment dimensions must differ between the two runs SC-007 compares — checkout path, process identity, hash seed, locale, timezone, filesystem enumeration order — or is the criterion satisfied by one command issued twice from one directory? [Complete, Spec §SC-007 / §FR-017] <!-- Evaluator: Resolved — SC-007 now names the dimensions that must differ (process, hash seed, working directory and checkout path, process identity, filesystem enumeration order), what must agree (the tuple members and the pinned segmenter and tokenizer), and why locale and timezone are excluded -->
- [X] CHK039 Is the arithmetic form of the confidence computation fixed — the order the deductions are applied and the representation they are held in — given SC-026 demands exact reproduction of a stored `double precision` value while 0.15, 0.10, and 0.25 have no exact binary form and two subtraction orders need not agree in the last bit? [Unambiguous, Spec §SC-026 / §FR-057 / data-model §ingestion_run.confidence_floor] <!-- Evaluator: Covered by spec §FR-057 (deduction order declared left to right) and data-model §ingestion_run, which fixes the form ((1.0 - alternate) - page_split) - repaired and makes "exactly" bit equality; VR-026 asserts it against the run's own weight columns -->
- [X] CHK040 Is chunk identity required to be a function of the chunk's content and position rather than of the run that wrote it, given FR-018 publishes "a chunk identifier is a function of the chunker" as a contract E008 is told to key its evaluation set around? [Correct, Spec §FR-018 / §SC-007 / §Disclosed Limitations] <!-- Evaluator: Resolved — FR-018 is restated over the run rather than the chunker, since FR-043's tuple re-mints identifiers on a provider-model or prompt change even at identical boundaries; content-derived identity is explicitly not required, being a change to an E003-owned column, and the Disclosed Limitations row was widened to match -->


# API Quality Requirements: Hybrid Retrieval and Reranking
**Created**: 2026-07-29 | **Feature**: [spec.md](../spec.md)

## Error Taxonomy and the Refuse-Versus-Degrade Distinction

- [X] CHK001 Does the contract state which problem-details members are required *on this boundary*, given RFC 9457 makes every member optional and the plan's "errors use one `Problem` shape throughout" is therefore a shape claim rather than a completeness claim? [Completeness, Plan §API Surface Summary; contracts/openapi.yaml §Problem] <!-- Evaluator: Covered by contracts/openapi.yaml §Problem — `required: [type, title, status, correlation_id]`, with `correlation_id` explicitly required on every problem response including 422. The boundary states its own completeness rather than inheriting RFC 9457's all-optional default. -->
- [X] CHK002 Are the members that carry FR-007's two encoder identities declared as schema properties, or do they exist only inside the `InternalError` example, so the one machine-readable fact distinguishing which side to change is outside the contract a consumer validates against? [Completeness, Spec §FR-007; contracts/openapi.yaml §responses.InternalError, §Problem] <!-- Evaluator: Resolved — the four identity members existed only in the InternalError example under `additionalProperties: true`. Declared `encoder_model_id`, `encoder_model_revision`, `corpus_model_id`, `corpus_model_revision` as properties of §Problem and required them by an if/then on `type` = encoder-identity-mismatch. The refuse/degrade distinction itself already held via the closed `type` enum and was not the gap. -->
- [X] CHK003 Is the set of problem `type` identifiers a consumer may encounter declared closed, with a stated stability rule — never reused for a different condition, never renamed in place, adding one the only compatible change? [Completeness, contracts/openapi.yaml §Problem.type] <!-- Evaluator: Covered by contracts/openapi.yaml §Problem.type — closed at four with an `enum`, the stability rule stated verbatim, and §info "Compatibility and change" naming a new type as one of only two compatible changes. -->
- [X] CHK004 Are FR-007's refusal and FR-021's degradation distinguishable **by a machine** from the contract alone — a closed problem `type` on one side, required state members on a `200` on the other — rather than only by the prose that contrasts them? [Completeness, Spec §FR-007, §FR-021; contracts/openapi.yaml §info "Refuse and degrade", §Problem.type, §Mode] <!-- Evaluator: Covered by contracts/openapi.yaml §Problem.type (`encoder-identity-mismatch` in the closed enum, 500, no results) against §Mode (required `degraded`, `reranked`, `unreranked_reason`, `statement` on a 200, with the allOf conditional forcing the statement when `reranked` is false). The info table is the prose; the enum and the required members are the machine-readable half. -->
- [X] CHK005 Is the deliberate **absence** of a problem type for reranker unavailability recorded as a rule with its reason, so a later revision cannot add one without contradicting a stated decision rather than merely filling a gap? [Clarity, Spec §FR-021; contracts/openapi.yaml §Problem.type] <!-- Evaluator: Covered by contracts/openapi.yaml §Problem.type — "No problem type exists for reranker unavailability... A `type` for it would let a caller branch on it as a failure, and the whole point of FR-021 is that it is not one." Stated as a rule with its reason, not as an omission. -->
- [X] CHK006 Is the machine-readable representation of the ready-degraded state **defined and versioned inside this contract** — a status value plus a required boolean plus a stated reason — rather than deferred to an external convention, given no standard representation for it exists? [Completeness, Spec §FR-021, §FR-022; contracts/openapi.yaml §ReadinessResponse.status, §Mode.degraded] <!-- Evaluator: Covered by contracts/openapi.yaml §ReadinessResponse (required `status` enum including `ready_degraded`, required `degraded` boolean, required `statement` with minLength 1) and §Mode (required `degraded` plus `unreranked_reason`). §info records that no standard exists and that the expired Internet-Draft precedent must not be cited as one, so the representation is defined here and versioned with `info.version`. -->
- [X] CHK007 Does FR-022's obligation on "every response produced without reranking" hold for a `lexical` or `dense` response, which is unreranked but is **not** fusion-only — or does the requirement need narrowing to the degraded case the contract's `unreranked_reason` vocabulary already separates? [Consistency, Spec §FR-022; contracts/openapi.yaml §Mode.unreranked_reason, §Mode.degraded] <!-- Evaluator: Resolved — a genuine requirement defect, not a contract gap. FR-022 amended 2026-07-29: the fusion-only claim is scoped to responses fusion-only *because the reranker was unavailable*, while every unreranked response must still carry a machine-readable unreranked reason. The contract's `unreranked_reason` vocabulary already separated the two and said so at §Mode.degraded. -->
- [X] CHK008 Is SC-010's "100% of responses produced without reranking carry the fusion-only flag" satisfiable for the empty-result case, where the reranker had nothing to score and the response is neither degraded nor fusion-only by choice? [Consistency, Spec §SC-010, §FR-009; contracts/openapi.yaml §Mode.unreranked_reason `no_candidates_to_score`] <!-- Evaluator: Resolved — SC-010 amended 2026-07-29 alongside FR-022. The fusion-only population is now the degraded one; the empty result carries `no_candidates_to_score` and satisfies the unreranked-reason half without making a false fusion-only claim. -->
- [X] CHK009 Is a reranker session lost **mid-request** owned by a requirement, given FR-021 speaks of the reranker being "unavailable" while the spec's Edge Cases names a session killed under memory pressure, and is the resulting response required to be a degraded success rather than a fault? [Completeness, Spec §FR-021, §Edge Cases; contracts/openapi.yaml §Mode.unreranked_reason, §RerankerFailure.reason] <!-- Evaluator: Resolved — the contract decided it (`reranker_failed_during_request`, `RerankerFailure.reason: session_lost`) and no requirement owned it. FR-021 amended 2026-07-29 to state that "unavailable" includes a session lost mid-request, that the in-flight request completes as a degraded success rather than a fault, and that the two causes stay distinguishable so a run does not read a mid-request loss as a startup failure. -->
- [X] CHK010 Is the choice to **degrade** rather than refuse on a reranker digest mismatch traceable to a requirement, given FR-016 requires the digest verified before session creation but states no consequence for a mismatch? [Traceability, Spec §FR-016, §FR-021; contracts/openapi.yaml §RerankerFailure.reason] <!-- Evaluator: Covered by spec FR-024 (amended 2026-07-29), which forces the degraded path with "an absent, unreadable or digest-mismatched reranker artifact" and asserts ready-degraded as the required outcome — so the mismatch-to-degrade consequence FR-016 omits is carried by a requirement, not only by contracts/openapi.yaml §RerankerFailure.reason. -->


## Observability of Declared Behaviour

- [X] CHK011 Is a deterministic identifier hit distinguishable from a ranked relevance hit by a required closed-vocabulary member of the result schema, and is the precedence rule stated for a chunk the route matched *and* fusion returned? [Completeness, Spec §FR-013; contracts/openapi.yaml §MatchKind, §RetrievalResult.match_kind] <!-- Evaluator: Covered by contracts/openapi.yaml §RetrievalResult (`match_kind` in the required list) and §MatchKind — a two-value closed enum whose description states the precedence: a chunk the route matched and fusion returned carries `deterministic_identifier` with a non-null `fused_rank`. -->
- [X] CHK012 Is the per-result primitive FR-005's per-layer proportion is computed from obliged on every result, and is the "all three fields empty" predicate defined in one place so two consumers cannot compute it differently? [Completeness, Spec §FR-005; contracts/openapi.yaml §WeightedFields] <!-- Evaluator: Covered by contracts/openapi.yaml §RetrievalResult (`weighted_fields` required) and §WeightedFields, which requires `all_empty` alongside the three booleans and states it is carried explicitly "rather than left to a consumer to recompute... so two consumers cannot disagree about what 'all empty' means". -->
- [X] CHK013 Does FR-005's *per-layer* obligation have a named owner for the aggregation step, given the interface emits the per-result primitive and the layer but never the proportion itself? [Traceability, Spec §FR-005; Plan §Requirement Coverage Map; contracts/openapi.yaml §RetrievalResult.source_layer] <!-- Evaluator: Covered by plan.md §Requirement Coverage Map FR-005 — `retrieval/arms.py`, `retrieval/report.py`, verified by test_report.py, noted as "Publishes the empty-weighted-field proportion per layer" — with `source_layer` required on every result so the per-layer split is computable from what the surface emits. -->
- [X] CHK014 Are both halves of FR-019 reachable through the interface — the candidate-length distribution and the truncated fraction — and is it stated how a run assembles SC-009's census from per-query responses without a second call or a sample? [Completeness, Spec §FR-019, §SC-009; contracts/openapi.yaml §RerankingReport] <!-- Evaluator: Covered by contracts/openapi.yaml §RerankingReport — `candidate_token_lengths` (the distribution half, length equal to `candidates_scored`), `candidates_truncated` and `sequence_limit_tokens` all required — and by §info "Which figures live where", which states the assembly rule: E014 builds the census by summing what it already receives, with no second call per query and no sampling. -->
- [X] CHK015 Does the contract oblige **every** ranking parameter FR-029 enumerates as a required member of every result set, rather than leaving any of them — fusion constant, tie-break key, missing-arm convention, fetch depth, index settings — to convention or to a separate call? [Completeness, Spec §FR-029, §FR-004; contracts/openapi.yaml §RankingParameters] <!-- Evaluator: Covered by contracts/openapi.yaml §RankingParameters, whose required list carries all five plus the lexical, encoder and reranker identities, and by §RetrievalResponse, which requires `ranking_parameters` on every response — stated as "returned with every result set rather than only with results an evaluation consumes", because whether a result is consumed by an evaluation is not knowable when it is produced. -->
- [X] CHK016 Are `tie_break_key` and `missing_arm_convention` constrained to a stated vocabulary, or are they free-form strings whose agreement between two runs a machine cannot check — which is what FR-004's "record all three alongside every result" is for? [Testability, Spec §FR-004; contracts/openapi.yaml §RankingParameters.tie_break_key, §RankingParameters.missing_arm_convention] <!-- Evaluator: Resolved — both were free-form `type: string`. Constrained to `pattern: '^[a-z][a-z0-9_]*$'` with the reason stated (`chunk_id ascending` and `ascending by chunk_id` name one rule and compare unequal, so a re-wording is indistinguishable from a re-tuning); the example moved to `chunk_id_ascending` in the schema and all four response examples. FR-004 amended 2026-07-29 to require stable identifier tokens. The enum is deliberately left open because FR-004 keeps the choice of value outside the contract — what is fixed is the form. -->
- [X] CHK017 Does the response carry both the arm requested and the arm actually served, so FR-023's "no figure produced in degraded mode can be read as reranked" is checkable by comparing two recorded fields rather than by remembering to consult a flag? [Testability, Spec §FR-023; contracts/openapi.yaml §Mode.arm_requested, §Mode.arm_served] <!-- Evaluator: Covered by contracts/openapi.yaml §Mode — both required, with the description stating that the pair is what makes FR-023 structural: an output claiming `fused_reranked` while `arm_served` was `fused` is caught by comparing two fields in the record. -->
- [X] CHK018 Is FR-006's machine-readable half — that this arm uses no corpus-wide term statistics — obliged on every response, and does the half binding *emitted figures and labels*, which the contract records it cannot enforce, have a named owner? [Traceability, Spec §FR-006; Plan §Requirement Coverage Map; contracts/openapi.yaml §LexicalArmSettings] <!-- Evaluator: Covered by contracts/openapi.yaml §LexicalArmSettings (`uses_corpus_wide_term_statistics`, `const: false`, required, reached on every response through the required `ranking_parameters.lexical`) and by plan.md §Requirement Coverage Map FR-006 → `retrieval/report.py`, "Assertion over emitted artifacts, not over intent". Now also enumerated as item 5 of the contract's new § Requirements this contract cannot express. -->
- [X] CHK019 Does FR-036's obligation to report against the strongest single arm and to label fusion-only as the weak comparator have a named owning component, given the contract states plainly that it cannot enforce an obligation on emitted figures? [Traceability, Spec §FR-036; Plan §Requirement Coverage Map; contracts/openapi.yaml §Arm] <!-- Evaluator: Covered by plan.md §Requirement Coverage Map FR-036 → `retrieval/metrics.py` with `report.py`, verified by test_metrics.py including the overlapping-interval case, and explicitly "**Not `arms.py`** — that makes arms runnable and computes nothing". Now also item 6 of the contract's new § Requirements this contract cannot express. -->
- [X] CHK020 Do the requirements or the contract fix the **measurement occasion and counter** for the reranker's resident memory — before or after warm-up, process resident set or container working set, one session or the two AD-011 commits — so `resident_memory_bytes` and `over_budget` name one measurement rather than a family of them? [Clarity, Spec §FR-033; Plan §Technical Context, §AD-011; contracts/openapi.yaml §DiagnosticsResponse.reranker_session] <!-- Evaluator: Covered by spec FR-033 (amended 2026-07-29) — counter: resident set size, itemized by session; occasion: after readiness and again after the run's queries, peak beside it; sessions: the query encoder and both graphs AD-011/AD-013 keep resident; plus the corpus-size qualifier — restated in plan.md §Technical Context. The contract was aligned to it in the same pass: the per-session `memory_budget_bytes` that implied an apportioned budget is replaced by §ProcessMemory, which names the counter, the occasion, the container total and the qualifier. -->
- [X] CHK021 Is FR-008's prohibition on synthesising a page number made structural by the response schema — one member the page can arrive through and no open extension point — rather than resting on a prohibition a later revision could widen? [Testability, Spec §FR-008, §SC-003; contracts/openapi.yaml §RetrievalResult] <!-- Evaluator: Covered by contracts/openapi.yaml §RetrievalResult — `additionalProperties: false` with the reason stated ("there is no member here a page number could arrive through except `page_number`"), `page_number` required, and its description binding the value to the stored chunk row for SC-003's identity assertion. Reinforced outside the contract by plan.md AD-004's private factory plus construction-site scan. -->


## Contract Completeness and Traceability

- [X] CHK022 Does the section "§ Requirements this contract cannot express", cited four times as the home of deferred obligations — FR-002's non-establishment, FR-020's scoping, the encoder's word-piece truncation, and FR-010's pattern-coverage figure — actually exist in the document, or do four requirements defer to a section with no text? [Completeness, contracts/openapi.yaml §info, §RetrievalResult, §parameters.QueryText, §DeterministicRoute.pattern] <!-- Evaluator: Resolved — it did not exist; four citations pointed at empty text. Written into §info as a six-item section: each deferral states what is deferred, why a schema cannot carry it, and the owner that carries it instead (test_single_statement.py plus the import-linter contract for FR-002; plan §Testing Strategy's limitation record for FR-020's scoping; HINT-004 and gateway.inference.encoder for the encoder's word-piece truncation, disclosed as a gap since no requirement obliges reporting it; test_part_number_coverage.py with AD-009's enumeration for FR-010's coverage), plus FR-006's and FR-036's emitted-artifact halves. -->
- [X] CHK023 Does the diagnostics report define a representation for **both** reranker artifacts and both sessions AD-011 commits, given `reranker_artifact`, `reranker_session` and readiness's `precision` are each singular while FR-016 demands identity, revision, licence basis, source and digest of every vendored artifact and FR-033 demands resident memory against the budget? [Completeness, Spec §FR-016, §FR-025, §FR-033; Plan §AD-011; contracts/openapi.yaml §DiagnosticsResponse, §ReadinessResponse.reranker] <!-- Evaluator: Resolved in the contract rather than recorded again — `reranker_artifacts` and `reranker_sessions` are now collections of at most two (new §RerankerArtifactRecord, §RerankerSession), `encoder_session` itemizes the third session FR-033's counter names, `process_memory` (new §ProcessMemory) carries the container total the per-session figures attribute, and §ReadinessResponse.reranker became per-session so FR-017's warm-up of both graphs is reportable. plan.md §API Surface Summary updated from "must become a collection before implementation" to performed. One consequence needs a decision and is recorded, not invented: which `status` a partially loaded reranker reports. -->
- [X] CHK024 Is FR-010's declared pattern published as a readable value rather than a claim in prose, and does the **measured coverage** of that pattern against the enumerated corpus part-number set — which the contract explicitly declines to serve — have a named surface or artifact that carries it? [Completeness, Spec §FR-010, §SC-005; Plan §AD-009; contracts/openapi.yaml §DeterministicRoute.pattern, §DiagnosticsResponse.deterministic_route_pattern] <!-- Evaluator: Resolved — the pattern was already published twice as a value (per response and once authoritatively on diagnostics), but the coverage figure was deferred to a section that did not exist. Item 4 of the new § Requirements this contract cannot express now names its carrier: test_part_number_coverage.py reading AD-009's pre-render document model, with SC-005 as the gate the figure is read against. -->
- [X] CHK025 Are the licence basis, source and digest FR-016 requires of a vendored artifact reachable through the interface rather than only from the repository tree, so a published figure resolves to the bytes that produced it without reading the checkout? [Completeness, Spec §FR-016; contracts/openapi.yaml §VendoredArtifact] <!-- Evaluator: Covered by contracts/openapi.yaml §VendoredArtifact — `license_basis`, `source`, `digest` and repository-relative `path` all required — surfaced on diagnostics as `encoder_artifact` and, after this pass, as `reranker_artifacts` for both graphs rather than for one. -->


## Parameters, Bounds and Response Invariants

- [X] CHK026 Is the invariant that `limit` bounds the **ranked-relevance portion only**, with deterministic matches additional and never displaced, stated in the contract itself rather than only in the plan's prose — and is the counting relation it implies stated as an assertable expression? [Completeness, Spec §FR-012; Plan §Contract Decisions Confirmed at Plan; contracts/openapi.yaml §parameters.Limit, §RetrievalResponse] <!-- Evaluator: Covered by contracts/openapi.yaml §info "Recorded interface decisions" item 2, §parameters.Limit, and §RetrievalResponse's counting invariants, which state it assertably: `result_count == min(limit, ranked_relevance_count) + deterministic_route.added_count`. -->
- [X] CHK027 Is it stated that `limit` cannot change the ranking — that the reranked count stays equal to the fused candidate set whatever the bound — so FR-003 and FR-018 are not silently parameterised by a request field? [Testability, Spec §FR-003, §FR-018; contracts/openapi.yaml §parameters.Limit, §RerankingReport.candidates_scored] <!-- Evaluator: Covered by contracts/openapi.yaml §parameters.Limit ("It cannot change the ranking... `reranking.candidates_scored` therefore does not move with `limit`, and that is assertable from the response") and §RerankingReport.candidates_scored, which binds the value to the fused candidate set under FR-018. -->
- [X] CHK028 Is the declared upper bound on the `results` array derived from a stated requirement, given `limit` caps at 50 while the deterministic route's match count carries no stated ceiling? [Completeness, Spec §FR-003, §FR-012; contracts/openapi.yaml §RetrievalResponse.results] <!-- Evaluator: NOT RESOLVED — needs a decision. FR-037 supplies a bound of 100 on the *fused candidate set* (two arms of 50 fuse to at most 100 distinct candidates) but not on this array: `limit` cuts the ranked portion to at most 50 first, and route additions are counted outside `limit` by design, so `maxItems: 100` silently assumes the route contributes at most 50 and no requirement says so. Either the route's contribution gets a stated ceiling and a stated behaviour when it binds — which risks FR-012's "MUST NOT remove" — or the bound is dropped. Disclosed on the member and recorded in plan.md §"Two contract points still open". -->
      > **Closed at checklist:** Resolved by rule rather than number: bounded by `limit` plus one result per recognised part-number token, with the query capped at 1,000 characters. Recorded in plan §Checklist Outcome.
- [X] CHK029 Is SC-006's "the route never removes" stated as checkable from a single response, and is the member that makes it checkable — null fused rank exactly on route additions — required rather than optional? [Testability, Spec §SC-006, §FR-012; contracts/openapi.yaml §RetrievalResult.fused_rank] <!-- Evaluator: Covered by contracts/openapi.yaml §RetrievalResult — `fused_rank` is in the required list, typed `[integer, 'null']` so the null is a value rather than an absence, with its description stating it is what makes SC-006 checkable from one response without a route-disable switch; §info records why no such switch exists. -->
- [X] CHK030 The contract narrows FR-010 and FR-014 by not firing the route on the `lexical` and `dense` arms: is that narrowing recorded in the project's mandated limitation format — scope decision, supporting evidence, reversal trigger, production-scale alternative — or only as an interface decision with a rationale? [Consistency, Spec §FR-010, §FR-014; Plan §Contract Decisions Confirmed at Plan; contracts/openapi.yaml §DeterministicRoute.skipped_reason] <!-- Evaluator: Resolved — it was a rationale in two places and a limitation in neither, the same asymmetry the Testing evaluation found inside §Testing Strategy. plan.md §Contract Decisions Confirmed at Plan now carries all four parts (reversal trigger: the route's contribution is already subtractable via `added_count` and null `fused_rank`, so it can fire everywhere and be netted out; production-scale alternative: publish it as a separate reported component of each arm's figure), and spec FR-010 carries the narrowing on its own side so the requirement no longer reads as unconditional. -->
- [X] CHK031 Is the ordering digest's domain defined — including the response that returns nothing — with the rule that equality of that value alone must not be read as sameness of response? [Clarity, Spec §FR-020, §SC-012; contracts/openapi.yaml §RetrievalResponse.ordering_digest] <!-- Evaluator: Covered by contracts/openapi.yaml §RetrievalResponse.ordering_digest — "Defined on every response, including one whose `results` is empty, where it is the digest of the empty sequence... equality of this value alone MUST NOT be read as sameness of response. It is not a cache validator and this surface defines none." -->
- [X] CHK032 Is FR-020's repeatable-ordering guarantee kept distinct from HTTP idempotency, so no artifact discharges it by appealing to the method being a safe read — a request can be idempotent and still return a different order? [Clarity, Spec §FR-020; contracts/openapi.yaml §info "GET rather than POST", §paths./retrieval/search] <!-- Evaluator: Resolved — nothing conflated the two, but nothing stated the distinction either, leaving a later revision free to discharge FR-020 by the method. A paragraph added under §info "GET rather than POST": RFC 9110 defines safe and idempotent over what the client requests and neither says anything about what a second call returns; FR-020 is witnessed by `ordering_digest` and the determinism assertions, and would be owed in the same terms on a POST. -->
- [X] CHK033 FR-020 is written unqualified while the plan scopes the rebuild guarantee to the exact path: is that narrowing recorded in the mandated limitation format and reflected where a consumer of the approximate path would read it, rather than only in the testing strategy? [Consistency, Spec §FR-020; Plan §Testing Strategy; contracts/openapi.yaml §RetrievalResponse.ordering_digest] <!-- Evaluator: Covered by plan.md §Testing Strategy (all four parts, declared normative over FR-020 under {SAD:ADR-0017}), spec FR-020 (amended, carrying the narrowing on its own side), and contracts/openapi.yaml §RetrievalResponse.ordering_digest plus the search operation description, where an approximate-path consumer reads it. Item 2 of the new § Requirements this contract cannot express now also records the deferral and the still-open half: the substitute assertion names candidate overlap and a recall delta with no threshold for either. -->
- [X] CHK034 Are the three "nothing came back" conditions separated by stated rules — an honest empty result, an inadmissible query, and an unreachable datastore — so FR-009's empty is not reachable by an outage and not confused with a rejected request? [Consistency, Spec §FR-009; contracts/openapi.yaml §parameters.QueryText, §responses.ValidationFailed, §responses.DatastoreUnavailable] <!-- Evaluator: Covered by contracts/openapi.yaml §parameters.QueryText ("a query with nothing in it and a query that matched nothing are different statements and FR-009 governs only the second"), §responses.ValidationFailed (422 with a closed reason vocabulary), §responses.DatastoreUnavailable ("one means the system could not look, the other means it looked and found nothing, and FR-009's honest empty must not be reachable by an outage"), and the 200 description for the empty case. §parameters.ProjectId additionally rules out a fourth reading, a well-formed project with no chunks. -->
- [X] CHK035 Is the query length cap identified as a constraint this contract originates rather than one derived from a requirement, with the condition under which it would be revisited stated? [Traceability, contracts/openapi.yaml §parameters.QueryText] <!-- Evaluator: Covered by contracts/openapi.yaml §parameters.QueryText — "The 1000-character cap is set by this contract, not derived from a requirement... the boundary at which the GET-versus-POST decision would be revisited" — matching the reversal condition stated in §info for the same decision. -->


## Arm Selection and Configuration

- [X] CHK036 Is the consequence of AD-006 — that E014 obtains SC-012's sixth arm by running **two differently configured processes**, not two requests — stated in the contract E014 binds to, or only in the plan a consumer has no reason to read? [Completeness, Spec §SC-012, §FR-026; Plan §AD-006, §Contract Decisions Confirmed at Plan; contracts/openapi.yaml §info "The exact-versus-approximate flag", §parameters.Arm] <!-- Evaluator: Covered by contracts/openapi.yaml §info "The exact-versus-approximate flag" — "The consequence, stated rather than hidden: E014 constructs SC-012's sixth arm... by running the same query set against two differently configured processes, not by sending two requests" — repeated at §parameters.Arm where a consumer choosing an arm reads it, and matched by spec SC-013 and plan AD-006. -->
- [X] CHK037 Do the spec, the plan and the contract agree on how many arms exist and which are request-selectable, given SC-012 counts six, FR-025 enumerates five, the contract's arm enumeration is closed at five, and the coverage map describes the selection module as carrying six? [Consistency, Spec §FR-025, §SC-012; Plan §Requirement Coverage Map; contracts/openapi.yaml §Arm, §DiagnosticsResponse.arms] <!-- Evaluator: Resolved — the coverage map's FR-025 row read "Six arms", agreeing with SC-012 and contradicting FR-025 and the contract's closed five-value enum, so a reader could not tell which was the miscount. The row now reads five request-selectable arms and names the sixth as the FR-026 configuration flag; FR-025 amended 2026-07-29 to state that five request-selectable arms plus one configuration is what SC-012's six counts. No count was changed, only reconciled. -->
- [X] CHK038 Is the vector-search mode required to be **reported** on every response rather than requested, so a served result and a measured result each name the configuration that produced them and the index settings FR-029 requires are never absent? [Completeness, Spec §FR-026, §FR-029; contracts/openapi.yaml §VectorSearchSettings, §x-configuration] <!-- Evaluator: Covered by contracts/openapi.yaml §RankingParameters (`vector_search` required on every response), §VectorSearchSettings ("Reported, never requested", `mode` and `index` both required, `index` null exactly when the mode is exact), and §x-configuration, which declares `RETRIEVAL_VECTOR_SEARCH_MODE` read at process start and never from a request. -->


## Versioning, Compatibility and Cross-Artifact Consistency

- [X] CHK039 Is the versioning rule stated — which of the three counters a consumer binds to, and why the readiness probe sits outside the versioned prefix — so that placement is a recorded decision rather than an unexplained exception? [Clarity, contracts/openapi.yaml §info "Versioning", §paths./readyz.servers] <!-- Evaluator: Covered by contracts/openapi.yaml §info "Versioning" — three counters distinguished, the `/api/v1` segment named as the one a consumer binds to, `info.version` as the document's own revision, and model revisions as artifact properties whose mismatch is FR-007's refusal rather than an interface version mismatch — with §paths./readyz.servers carrying the override and its reason: version-binding a probe would let an interface revision take a healthy instance out of service. -->
- [X] CHK040 Is what counts as a breaking change defined against the recorded consumers E011 and E014, given every response object is closed and almost any payload addition is therefore breaking for a strict consumer — and does the plan's API surface table name the same response schemas the contract defines for readiness and diagnostics? [Consistency, Plan §API Surface Summary; contracts/openapi.yaml §info "Compatibility and change", §ReadinessResponse, §DiagnosticsResponse] <!-- Evaluator: Resolved on the second half — §info "Compatibility and change" already enumerated the breaking set, acknowledged that closed response objects make almost any addition breaking, and tied the change window to both consumers shipping from this commit. plan.md §API Surface Summary named `Mode` for /readyz and four component schemas for diagnostics, none of which the contract returns; corrected to `ReadinessResponse` and `DiagnosticsResponse` with a note recording what was wrong, since `Mode` is a member of the *search* response and `RerankingReport` is a per-query object diagnostics does not carry. -->


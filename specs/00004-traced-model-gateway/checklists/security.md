# Security: Traced Model Gateway
**Created**: 2026-07-25 | **Feature**: [spec.md](../spec.md)

## Egress Sink Inventory and Its Completeness

- [X] CHK001 Is the set of egress sinks TR-024 redacts stated as a closed and enumerable list, so its completeness is decidable rather than a matter of reviewer judgement? [Complete (set), Spec §TR-024, §OBJ5] <!-- Evaluator: Resolved — added TR-059 closing the inventory at five enumerated sinks; OBJ5 opening line and Glossary §Egress sink restated to match -->
- [X] CHK002 Does any requirement account for check output — assertion messages interpolating the client handle or the raw request — as an egress sink, or does it sit outside the stated inventory with no recorded decision? [Complete (set), Spec §TR-024, Research §Credential redaction inside the boundary] <!-- Evaluator: Resolved — check output is sink (5) of TR-059, naming assertion messages that interpolate the client handle or a raw request; OBJ5 rationale updated -->
- [X] CHK003 Is the local spool named as an egress sink in the spec's own inventory, given `data-model.md` VR-020 extends the fixture-scan credential pattern over the spool file? [Consistent (set), Spec §TR-024, §TR-030, Data Model §VR-020] <!-- Evaluator: Resolved — spool file is sink (4) of TR-059 and is inside TR-060's scan denominator; VR-020 and the data-model traceability table now cite both -->
- [X] CHK004 Does the inventory separate the provider-echoed error body from the gateway's own exception payload, or does one sink stand for both despite different origins and different controls? [Complete (set), Spec §TR-024, §TR-025] <!-- Evaluator: Resolved — TR-059 states the provider-echoed body is deliberately not a sink: different origin, and suppression under TR-025/TR-064 rather than redaction -->
- [X] CHK005 Is "exception payload" defined to include the local frames a variable-capturing traceback renders, rather than only the exception's message and attributes? [Unambiguous, Spec §TR-024, §OBJ5 Rationale] <!-- Evaluator: Resolved — TR-059 sink (2) defines the term as message, attributes, and every local frame a variable-capturing traceback renders, the client-handle frame included -->
- [X] CHK006 Are the secret-handling requirements anchored to an external criterion at a pinned standard version — OWASP ASVS 5.0.0 chapters V16, V14, V13 — rather than to a bare chapter number that reads differently under 4.0.3? [Verifiable, Spec §OBJ5, Research §Secret-handling security requirement quality] <!-- Evaluator: Covered by research.md §Secret-handling security requirement quality (decision + v5.0.0 Sources Index); anchor now also carried at requirement level in spec.md §TR-059 -->


## Detectors, Negative Tests, and Scan Coverage

- [X] CHK007 Is "credential-shaped material" defined by a stated pattern derived from the provider credential's actual shape, rather than left as an undefined term the scan requirement rests on? [Unambiguous, Spec §TR-030, §Glossary] <!-- Evaluator: Resolved — added TR-060 defining two detectors (exact configured-value match; `sk-ant-` prefix-anchored pattern) plus a Glossary entry; the implementing task must correct the prefix against provider documentation -->
- [X] CHK008 Does TR-030's scan obligation cover the same sinks SC-010 claims zero occurrences across, given the requirement names the committed fixture store while the criterion reaches captured log output and normalized error payloads? [Consistent (set), Spec §TR-030, §SC-010] <!-- Evaluator: Resolved — TR-060 puts the scan over every sink in TR-059's inventory and SC-010 restated to that same five-sink denominator -->
- [X] CHK009 Is each scanned sink required to carry a negative case — seeded credential-shaped material that the scan must report — so a zero-finding result is distinguishable from a detector that matches nothing? [Verifiable, Spec §TR-030, §SC-010] <!-- Evaluator: Resolved — TR-060 requires a per-sink seeded positive case using a synthetic never-live value in a temporary instance of the sink; SC-010 and plan.md Testing Strategy carry it -->
- [X] CHK010 Is the per-fixture provenance record stated to be inside the scanned fixture store, or does "committed fixture store" leave the sidecar's coverage to interpretation? [Unambiguous, Spec §TR-030, §TR-033] <!-- Evaluator: Resolved — TR-059 sink (3) and TR-060's scan scope both name the provenance sidecars explicitly, rather than leaving it to data-model.md §Storage Boundaries to imply -->
- [X] CHK011 Is redaction stated as fail-closed — material that cannot be redacted is not written — rather than only as a transformation applied before writing? [Complete (individual), Spec §TR-024, §OBJ5 Deliverables] <!-- Evaluator: Resolved — TR-059 states redaction is fail-closed: unredactable material produces a refused write; OBJ5 deliverable restated -->


## Credential Handling Inside the Boundary

- [X] CHK012 Is "credential read once at client construction and held off any repr'd or serialized object" carried at requirement level, or only as an OBJ5 deliverable and its validation criterion? [Complete (individual), Spec §OBJ5 Deliverables, §TR-024] <!-- Evaluator: Resolved — added TR-061 carrying it as a requirement, extended to spooled objects and to the client handle exposing no credential-bearing attribute -->
- [X] CHK013 Does any requirement name the configuration key the credential is read from, so TR-023's presence check and the configuration-failure path have a stated subject? [Unambiguous, Spec §TR-023, §Implementation Signals] <!-- Evaluator: Resolved — added TR-062 naming `ANTHROPIC_API_KEY` as the single key, chosen so the guard and the SDK's own resolution cannot disagree; TR-065 names it on the configuration-failure path -->
- [X] CHK014 Is the credential environment variable treated as a transport rather than a control, so no requirement rests a confidentiality property on the variable itself? [Correct, Spec §TR-023, Research §Credential redaction inside the boundary] <!-- Evaluator: Covered by research.md §Credential redaction inside the boundary (variable rejected as a control) and OBJ5, where confidentiality rests on redaction; restated at requirement level in spec.md §TR-061 -->
- [X] CHK015 Does a requirement state that credential material must not enter the committed repository, the fixture store included, or is that obligation inherited from E001 with no stated link? [Complete (set), Spec §TR-024, §Data Provenance] <!-- Evaluator: Resolved — TR-061 states the obligation directly for the repository, the fixture store, and its sidecars, rather than leaving it inherited from E001 -->


## Replay-Mode Guard and the Record-Mode Opt-In

- [X] CHK016 Does TR-023 state how credential presence is determined — which names are inspected and under what match rule — so the guard has a checkable subject rather than an intent? [Unambiguous, Spec §TR-023] <!-- Evaluator: Resolved — TR-062 fixes the rule: exact-name lookup of one key in the gateway's own process environment, empty/whitespace treated as absent, no prefix or case-insensitive match -->
- [X] CHK017 Is the guard's blind spot recorded — a credential reaching the provider SDK through a path other than the gateway's own process environment — rather than absorbed into an unqualified offline claim? [Correct, Spec §TR-023, §OBJ4 VC4] <!-- Evaluator: Resolved — TR-062 carries a Disclosed limit for SDK config files, explicit arguments, and platform keychains, and requires the limit to be disclosed wherever the offline claim is made -->
- [X] CHK018 Is the credential-free child environment stated as an obligation on the check harness rather than on the developer's shell, so SC-022 rests on a mechanism rather than on discipline? [Verifiable, Spec §TR-023, §SC-022] <!-- Evaluator: Covered by spec.md §TR-023 sentence 2 ("The replay check harness MUST execute in a child environment..."), §OBJ4 VC4, §STF-003, and plan.md coverage map (`src/gateway/tests/conftest.py`) -->
- [X] CHK019 Is the `record`-mode opt-in specified as a distinct named control, or only as "separate from mode selection", leaving its form unfixed? [Unambiguous, Spec §TR-027, §OBJ6 Deliverables] <!-- Evaluator: Resolved — added TR-063 fixing the control as `GATEWAY_ALLOW_PROVIDER_CALLS`, permitting `record` only at value `1` -->
- [X] CHK020 Does a requirement state that the opt-in control is absent in continuous integration, or is credential-free CI the only stated half of the posture? [Complete (set), Spec §TR-027, §Technical Constraints] <!-- Evaluator: Resolved — TR-063 requires the control absent from every CI environment and a check asserting that absence; plan.md adds `tests/checks/test_ci_provider_gate_absent.py` -->
- [X] CHK021 Are the smoke check's skip condition and its hard-failure condition stated over disjoint states, so no configuration requires both outcomes at once? [Consistent (set), Spec §OBJ6 VC1, §OBJ6 VC3, §STF-005] <!-- Evaluator: Covered by spec.md §OBJ6 Deliverables (skip keys on the gate; hard failure on gate-present-without-credential) and §STF-005's recorded resolution -->


## Provider-Error Normalization and Configuration Failures

- [X] CHK022 Is the normalized gateway error's field set stated as closed — status, error type, request identifier and nothing further — rather than as a minimum a later field could be added to? [Complete (individual), Spec §TR-025] <!-- Evaluator: Covered by spec.md §TR-025 ("exposing only..."), §OBJ5 VC3 ("carrying only..."), and §OBJ5 Deliverables ("bounded field set") -->
- [X] CHK023 Does any requirement address retention of the original provider exception as a chained cause, given a chained exception re-renders the provider body in the traceback the normalization exists to suppress? [Complete (individual), Spec §TR-025, §OBJ5 VC3] <!-- Evaluator: Resolved — added TR-064 forbidding retention as either explicit cause or implicit context; status/type/request id are extracted before the chain is broken, so TR-025's "only" holds of the rendering and not just the fields -->
- [X] CHK024 Is the request identifier constrained to a provider-issued correlation value, so the one field carried through cannot itself convey echoed request material? [Unambiguous, Spec §TR-025, §OBJ5 VC3] <!-- Evaluator: Resolved — TR-064 constrains it to a provider-issued correlation value from response metadata, never an echo of request material, and requires omission rather than substitution when none is issued -->
- [X] CHK025 Is the recorded `error_type` value domain fixed to normalized gateway classes at requirement level, or only in the data model's column detail, leaving TR-012's field list open to a provider exception name? [Consistent (set), Spec §TR-012, §TR-025, Data Model §Column Detail] <!-- Evaluator: Resolved — TR-064 fixes the domain at requirement level (`validation_failed`, `transport_failed`, `deadline_exceeded`); data-model.md traceability row updated to TR-025, TR-064 -->
- [X] CHK026 Is the obligation that a configuration failure names no secret material stated as a requirement, or carried only by an edge case, one validation criterion, and the plan's error table? [Complete (individual), Spec §Edge Cases, §OBJ6 VC3, Plan §Error Handling Strategy] <!-- Evaluator: Resolved — added TR-065 carrying the obligation at requirement level across all five configuration-failure causes -->
- [X] CHK027 Does "names no secret material" state what it excludes — the credential value, the configuration key's name, or a truncated or hashed form — so a reviewer can return a verdict on a message? [Unambiguous, Spec §OBJ6 VC3, §Edge Cases] <!-- Evaluator: Resolved — TR-065 states both lists: forbidden (credential value, any substring/truncation/prefix/suffix/hash/length, any value read from the key) and permitted (the key's name and the remedy), matching plan.md's "naming the missing setting" -->


## Content Capture and Committed Content

- [X] CHK028 Is the set of fields written to log output stated as a closed list, so "carries no prompt or completion content" has an enumerable denominator rather than a prohibition over an open set? [Complete (set), Spec §TR-026, §OBJ5 VC4] <!-- Evaluator: Resolved — added TR-066 closing the log field list at TR-012's record field set plus TR-053 spool depth plus TR-058's absent-cost warning fields, and nothing else -->
- [X] CHK029 Does TR-026's default-off obligation reach exception payloads and the invocation record, or is it scoped to log output alone — and is that scoping stated rather than inferred? [Complete (set), Spec §TR-026, §TR-012] <!-- Evaluator: Resolved — TR-066 states the scoping: content capture governs log output alone, and the record (TR-012) and normalized error (TR-025, TR-064) carry no content at any toggle setting because both field sets are closed -->
- [X] CHK030 Is the deliberate retention of prompt and completion content inside committed fixtures distinguished from the content-capture prohibition, so the two are not read as contradictory obligations? [Consistent (set), Spec §TR-024, §TR-026, §Key Entities] <!-- Evaluator: Resolved — TR-066 states fixtures deliberately retain content as a different sink under different controls (credential redaction plus TR-067's corpus bound), not an exception to TR-026 -->
- [X] CHK031 Does the spec state that TR-024's fixture redaction removes credential material only, given the Compliance Check cites it as what keeps proprietary content out of the fixture store? [Correct, Spec §TR-024, §Compliance Check] <!-- Evaluator: Resolved — TR-059 states redaction is credential-only and carries no content-exclusion property; the Compliance Check Data Provenance row now rests content exclusion on E002's corpus constraint restated in TR-067 -->
- [X] CHK032 Where content capture is explicitly enabled, is any obligation stated on the resulting output, or does the requirement end at the default and leave the enabled state unconstrained? [Complete (individual), Spec §TR-026] <!-- Evaluator: Resolved — TR-066 constrains the enabled state: redaction and fail-closed still apply, never enabled in CI or in a run writing to the committed repository, and the output is not committed -->


## The Provider as Sole Egress Boundary

- [X] CHK033 Is the sole-egress claim stated at requirement level, or carried only by an integration point and an import contract that constrains import edges rather than network reach? [Complete (set), Spec §IP-007, §TR-001] <!-- Evaluator: Resolved — added TR-067 carrying IP-007's sole-egress claim as a requirement: one external destination, reached only from the provider-facing module in opted-in `record` mode -->
- [X] CHK034 Is the reach of the egress claim stated — import edges, with no network-level restriction asserted — so a module reaching the provider without importing the client is a disclosed limit rather than an assumed impossibility? [Correct, Spec §TR-001, §IP-007] <!-- Evaluator: Resolved — TR-067 states TR-001 constrains import edges rather than sockets and discloses the residual as a code-review-only guard -->
- [X] CHK035 Does SC-008's "0 outbound provider requests" name the observation point that establishes it, rather than asserting an absence nothing observes? [Able to be validated, Spec §SC-008, §TR-022] <!-- Evaluator: Resolved — TR-067 requires a suite-wide network guard failing any outbound connection attempt, and SC-008 amended to name it as the observation point -->
- [X] CHK036 Is the egress of document text to the provider bounded by a requirement naming which corpus material may be sent, or does IP-007 state only where egress happens? [Complete (individual), Spec §IP-007, §Data Provenance] <!-- Evaluator: Resolved — TR-067 bounds egress to corpus material E002 labels public-domain or synthetic and forbids proprietary or customer content crossing the boundary -->


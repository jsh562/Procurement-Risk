# Testing Requirements Quality: Risk-Ranked Coordinator Worklist
**Created**: 2026-07-28 | **Feature**: [spec.md](../spec.md)

## Verifiability of Criteria

- [X] CHK001 Do the criteria phrased as coordinator capabilities name an observable that stands in for the capability — "can identify the highest-harm line", "can see which inputs placed it at its rank", "is visible to the coordinator" — given the spec records that no user is available to validate this surface? [Verifiable, Spec §SC-001, §SC-004, §SC-008, §Assumptions] <!-- Evaluator: Resolved — added FR-034 to spec.md §Boundaries, naming the observable that discharges each of SC-001, SC-004 and SC-008 -->
- [X] CHK002 Is SC-003 capable of failing, given the read path issues no provider call, so an unreachable provider is indistinguishable from any other request and every implementation satisfies the criterion — and is a means stated that would detect a provider call being introduced later? [Verifiable, Spec §SC-003, §FR-023, §FR-002] <!-- Evaluator: Resolved — SC-003 restated over the import contract and the invocation-record delta, with the rendering named as a structural consequence; FR-035 added to spec.md §Boundaries -->
- [X] CHK003 Does SC-009's second clause state which of the eight states "compromise trust in a figure", given FR-018a orders the five row states along that axis without partitioning them, so the clause has no decidable subject? [Verifiable, Spec §SC-009, §FR-018a, §FR-018] <!-- Evaluator: Resolved — FR-018a now partitions the eight states into trust-compromising, partially-available, and annotating -->
- [X] CHK004 Does any success criterion cover FR-018a's resolution itself — that the winning state governs the row in full and losing states contribute no behaviour — given SC-009 asserts only that the eight states are individually distinguishable, and STF-005 was a CRITICAL finding closed by that rule? [Completeness, Spec §SC-009, §FR-018a, §STF-005] <!-- Evaluator: Resolved — added SC-019 [US3] asserting label, ranking membership and figure suppression all follow the winning state, over every pair FR-033 records -->
- [X] CHK005 Is there a success criterion that fails when the adopted worklist p95 of 1.5 s is missed, given the target is recorded only in Assumptions and no criterion mentions latency? [Verifiable, Spec §Assumptions, §Success Criteria; Plan §Performance Goals; specs/sad.md §Compute envelope] <!-- Evaluator: Resolved — added SC-017 [US1]; spec.md §Assumptions now points at it as the gating clause -->
- [X] CHK006 Are the measurement conditions for that p95 stated — the hardware, the line population, warm or cold cache, sample count, and whether the measurement is taken at the endpoint or includes the interface tier — so two runs of the same check are comparable? [Measurable, Spec §Assumptions; Plan §Testing Strategy; specs/sad.md §Compute envelope] <!-- Evaluator: Resolved — added plan.md §Performance measurement conditions: CPU limit 1.0, the frozen fixture's 16 lines (corrected from "E005 seeded set" by T072 — the benchmark never used that population), warm with 20 discarded warm-ups, 200 samples, measured server-side at the endpoint with the interface tier excluded -->
- [X] CHK007 Is US2's "fast enough to feel like direct manipulation" reduced to a stated figure for the override re-query, given AD-004 makes it a server round trip and no criterion measures it apart from the initial load? [Measurable, Spec §US2, §FR-011; Plan §AD-004] <!-- Evaluator: Resolved — added SC-018 [US2] binding the override re-query to the same 1.5 s p95, which is the budget the contract's NeedByOverride note already claims it stays inside; measured as a second benchmark variant -->
- [X] CHK008 Do the criteria expressed as absences state an observation procedure — SC-005's "creates no row in the model-invocation record" needs a stated window and a before-and-after comparison, and FR-024's "the interface tier opens no datastore connection" needs a stated observation point? [Verifiable, Spec §SC-005, §FR-011, §FR-024] <!-- Evaluator: Resolved — added plan.md §Observation procedures for the absence criteria, giving each of the three a window and an observation point -->
- [X] CHK009 Does FR-032's "automated tests assert" attach to a stated gate — that the assertions execute and pass before merge — or does it oblige only that such tests exist? [Verifiable, Spec §FR-032; Plan §Instructions Check] <!-- Evaluator: Resolved — FR-032 now obliges execution in the merge gate under FR-040 and splits the contract across the endpoint and rendered-page tiers -->
- [X] CHK010 Is a verification means stated for FR-020a, given "a row's figures MUST NOT change other than by" three admitted causes is a claim over unobserved time that no single response can falsify? [Verifiable, Spec §FR-020a] <!-- Evaluator: Resolved — FR-020a now states a two-sided validator check over exactly the admitted inputs, matching the ETag the contract already defines -->

## Fixture Determinism

- [X] CHK011 Is it stated what a frozen posterior fixture must contain to reach the boundary cases the spec itself names — a median equal to the eightieth percentile, a tie in expected harm, a line with no posterior, a residual tail mass that rounds to an extreme — given research records "one named test per boundary case" as an implication no requirement carries? [Completeness, Research §Acceptance criteria and boundary cases; Spec §Edge Cases & Boundaries; Plan §Testing Strategy] <!-- Evaluator: Resolved — added FR-036 enumerating the required fixture lines with one named test per boundary case, plus plan.md §The frozen fixture -->
- [X] CHK012 Is the rounding-boundary case constructible as stated, given FR-008 fixes half-up rounding over a value stored as double precision, where most half-percent boundaries are not exactly representable — so does anything state which stored value the fixture must carry to sit on the boundary rather than near it? [Verifiable, Spec §FR-008; Plan §AD-001; specs/00003-core-data-schema/data-model.md §line_posterior.survival] <!-- Evaluator: Resolved — plan.md §The frozen fixture derives the only four exactly representable half-percent values (0.125, 0.375, 0.625, 0.875), fixes 0.125 and 0.875 as the fixture's half-up cases, and rules out Python's half-to-even `round` -->
- [X] CHK013 Is a fixture required to satisfy the stored artifact's own invariants — draws ascending, a survival array of exactly `horizon_days` entries, non-increasing and within [0,1], residual tail mass equal to the last entry — so no test exercises a state the schema forbids? [Verifiable, Spec §Key Entities; specs/00003-core-data-schema/data-model.md §line_posterior] <!-- Evaluator: Resolved — added FR-037 requiring the fixture to be loadable only through the migrated schema, with the invariants named -->
- [X] CHK014 Is "today" stated as an injectable input rather than a request-time clock reading, given FR-029's age, FR-030's calendar-passed flag and the stale banner are all functions of it — so a fixture with absolute dates renders different states as wall-clock time advances? [Verifiable, Spec §Glossary "Today", §FR-029, §FR-030; Plan §Testing Strategy] <!-- Evaluator: Resolved — added FR-038 making `today` a supplied input echoed as the reported value, and plan.md HINT-006 -->
- [X] CHK015 Is the percentile convention traceable to a requirement here, given FR-003 names a median and an eightieth percentile without stating rank rule or interpolation, so a frozen draw array admits more than one expected pair until the upstream normative rule is cited? [Clarity, Spec §FR-003, §SC-002; specs/00003-core-data-schema/data-model.md §Array semantics] <!-- Evaluator: Resolved — FR-003 now cites `schema_constants.percentile_convention` and E003's `draws[ceil(p x draw_count)]`, and requires the convention to travel with the figures -->
- [X] CHK016 Is the provenance of a hand-built posterior fixture stated — generator, seed, and regeneration procedure — given it is synthetic data committed to the repository and the project's provenance rules require generated data to record how it was produced? [Traceability, project-instructions.md §Data Provenance; Plan §Testing Strategy] <!-- Evaluator: Resolved — FR-037 requires generator identity, seed, generation date and a byte-identical regeneration command, and states the fixture takes generator provenance rather than the corpus-document form -->
- [X] CHK017 Is the roster-mismatch fixture's construction stated, given the schema obliges both a line and a run to carry a well-formed hash and constrains neither to differ, so the state exists only where a fixture deliberately perturbs one? [Verifiable, Spec §FR-021, §SC-009; specs/00003-core-data-schema/data-model.md §purchase_order_line.roster_hash, §forecast_run.roster_hash] <!-- Evaluator: Resolved — named in FR-036's inventory as a well-formed digest differing from the run's, with the construction stated in plan.md §The frozen fixture -->

## Coverage of Degraded States

- [X] CHK018 Does the already-late state have an acceptance scenario reachable from stored data, given US2's scenario reaches it only through a session what-if and none of US3's eight scenarios covers a line whose stored need-by predates the as-of date? [Completeness, Spec §FR-030, §US2, §US3] <!-- Evaluator: Resolved — added US3 acceptance scenario 9, a stored need-by predating the as-of date with no override applied -->
- [X] CHK019 Is the empty-filter state demonstrable at P1, given SC-009 and US3's Independent Test both require all eight states while the filtering that produces this one is delivered by P2 US4? [Consistency, Spec §SC-009, §FR-018, §US3, §US4]
- [X] CHK020 Does the beyond-horizon state carry a criterion that fails when its bound is computed as a complement, given STF-001 records exactly that inversion reaching a normative upstream artifact and FR-017's own wording is the only place the direction is fixed? [Verifiable, Spec §FR-017, §STF-001; Plan §HINT-002] <!-- Evaluator: Resolved — added SC-020 [US3] fixing both directions: the bound is the residual tail mass and not its complement, the in-horizon figure is the survival value and not its complement -->
- [X] CHK021 Is a criterion stated for FR-017's degradation at the extremes — a residual mass that would render "at most 0% late" instead falling back to the bounded wording — given SC-010 asserts only that no displayed probability asserts certainty? [Completeness, Spec §FR-017, §FR-008, §SC-010, §STF-007] <!-- Evaluator: Resolved — added SC-021 [US3] naming the degradation and forbidding the literal strings "at most 0%" and "at least 100%" -->
- [X] CHK022 Is the reachable set of co-occurring states enumerated, given FR-018a names one combination while a check over the precedence needs to know which pairs are jointly constructible — not-covered with beyond-horizon requires a posterior the first state denies? [Testability, Spec §FR-018a, §FR-016, §FR-017] <!-- Evaluator: Resolved — added FR-033 enumerating the constructible pairs, including not-covered with beyond-horizon (a date comparison needing no posterior) and the beyond-horizon/calendar-passed pair at today > as_of + horizon_days -->
- [X] CHK023 Does any criterion or scenario cover a page-scope banner composing with a row label, given FR-018a states the three page-scope states compose rather than compete and every scenario shows one state alone? [Completeness, Spec §FR-018a, §SC-007, §SC-016] <!-- Evaluator: Resolved — added SC-022 [US3] and US3 acceptance scenario 10, a stale banner over a beyond-horizon row label -->
- [X] CHK024 Is the staleness boundary fixed at the value a check would assert on — a run whose age equals the threshold exactly — given FR-029 says "once its age exceeds seven days" and the response field is defined as a strict comparison? [Verifiable, Spec §FR-029; contracts/openapi.yaml §ForecastRunMeta.stale] <!-- Evaluator: Covered by spec.md §FR-029 ("once its age exceeds seven days") and contracts/openapi.yaml §ForecastRunMeta.stale ("True when age_days > staleness_threshold_days") — both strict and in agreement, so age == threshold is not stale; both sides of the boundary are now named in FR-036's fixture inventory -->
- [X] CHK025 Does SC-008 state evidence that distinguishes a line correctly excluded for having no posterior from one dropped by a defective query, given both render identically and no criterion asserts that the active run was consulted for that line? [Verifiable, Spec §SC-008, §FR-016] <!-- Evaluator: Resolved — added SC-023 [US3] requiring every open line in scope to appear exactly once and the counts to reconcile; contracts/openapi.yaml §Counts now states ranked + unranked == total as the invariant behind it -->

## Property Invariants

- [X] CHK026 Does FR-013 state its interval's endpoints precisely enough to generate cases, given it opens at the as-of date while the survival grid starts at k=1 and a need-by equal to the as-of date resolves to already-late with no probability to compare, and closes at "the end of its horizon" without saying whether the last in-grid day is included? [Clarity, Spec §FR-013, §FR-030, §SC-006; Plan §HINT-002] <!-- Evaluator: Resolved — FR-013 now fixes the interval as `as_of_date < d <= as_of_date + horizon_days`, open below because d == as_of_date is already-late with no subject, closed above because k = horizon_days is in grid -->
- [X] CHK027 Is the ordering that "does not decrease" refers to defined over displayed values, given FR-008 admits `<1%` and `>99%` as displayed forms and no comparison is stated between a bound and an integer? [Clarity, Spec §FR-013, §SC-006, §FR-008] <!-- Evaluator: Resolved — FR-013 now states the comparison over displayed forms, with `<1%` ranking below every integer and `>99%` above every integer -->
- [X] CHK028 Is the "a displayed pair always sums to 100" invariant stated with its exception, given FR-008's bounded forms and FR-017's "at most N% / at least (100 − N)%" are not two integers, so the invariant as written in the plan is false at exactly the boundaries FR-008 exists to govern? [Consistency, Spec §FR-008, §FR-006, §FR-017; Plan §Testing Strategy] <!-- Evaluator: Resolved — plan.md §Testing Strategy now scopes the invariant to pairs where both directions render as integers, records that the unconditional form was false, and filters the generator to bounded: false / measure: point with the bounded cases carrying named tests -->
- [X] CHK029 Is FR-013a's subject the computed ranking function or the rendered row position, given FR-026 admits three sort keys under which criticality does not determine position and the property would hold vacuously? [Clarity, Spec §FR-013a, §FR-026] <!-- Evaluator: Resolved — FR-013a now names the harm-ordered sequence under the default key and a line's position within it as the subject, explicitly not the rendered order under another key -->
- [X] CHK030 Does FR-013a state what "holding all else equal" ranges over — whether the tiebreak inputs are held fixed, and whether the property must still hold across the zero-harm block where criticality multiplies a score of zero and only the tiebreak orders the lines? [Clarity, Spec §FR-013a, §FR-010, §FR-001] <!-- Evaluator: Resolved — FR-013a now ranges "all else equal" over the line's draws, need-by date, identifier and every other line, holds FR-010's tiebreak inputs fixed, and states the property as weak and holding across the zero-harm block via FR-010's criticality-descending key -->
- [X] CHK031 Are the input domains for the invariants stated — generated survival arrays non-increasing and unit-valued, generated draws ascending — so a property is not reported as falsified by an input the storage layer cannot produce? [Testability, Spec §FR-013, §FR-013a; specs/00003-core-data-schema/data-model.md §line_posterior] <!-- Evaluator: Resolved — FR-039 states the generated domains, and plan.md §Testing Strategy repeats them with the residual-tail-mass tie and the need-by regions -->
- [X] CHK032 Is the project's obligation to carry property-based tests over the pure functions of deterministic computation modules traceable to a requirement for this feature, or does it exist only as a plan tier and a research implication? [Traceability, project-instructions.md §Testing & Quality Policy; Plan §Testing Strategy; Research §Acceptance criteria] <!-- Evaluator: Resolved — added FR-039 carrying both property-based tests and test-first sequencing as a requirement of this feature -->

## CI Reachability and Demonstration

- [X] CHK033 Is strict test-first for `compute/ranking.py` and `compute/probability.py` traceable to a requirement or a success criterion, given the project mandates red-green-refactor for deterministic computation modules and the obligation currently appears only as a plan hint no artifact gates? [Traceability, project-instructions.md §Testing & Quality Policy; Plan §HINT-005] <!-- Evaluator: Resolved — FR-039 carries the test-first obligation; plan.md §Instructions Check now cites FR-039 and corrects its dangling "sequenced in HINT-006" reference to HINT-005, where the sequencing actually lives -->
- [X] CHK034 Do the requirements state that a criterion counts as met only when its check executes in the merge gate, given `verify.yml` carries no `Unit tests (api)` step and none of this feature's api unit, property, or integration tests would run as it stands? [Verifiable, Plan §Instructions Check "Development Workflow / CI"; .github/workflows/verify.yml] <!-- Evaluator: Resolved — added FR-040; plan.md §Check inventory names which five of nine obligations need a new workflow step -->
- [X] CHK035 Is the presentation contract's verification reachable, given FR-032 requires automated assertion, its only stated tier is Playwright, and no workflow step collects Playwright specs — so the three criteria resting on those words would be demonstrated by nothing? [Verifiable, Spec §FR-032, §SC-014, §SC-016; Plan §Testing Strategy] <!-- Evaluator: Resolved — FR-032 now splits the contract across the endpoint tier (sort keys, four comparison quantities, primary/secondary partition) and the rendered-page tier, and FR-040 plus the Check inventory make the E2E step and script scoped work -->
- [X] CHK036 Is the 80% coverage mandate stated to apply to this feature's code, given the gate's source list names neither `/src/api` nor `/src/web` and no requirement or criterion records the figure this feature must reach? [Completeness, project-instructions.md §Coverage Target; pyproject.toml §tool.coverage.run; Plan §Instructions Check] <!-- Evaluator: Resolved — FR-040 states the 80% target applies to this feature's code on both sides of the boundary; plan.md §Coverage — two floors names the source-list and paths change and the web threshold -->
- [X] CHK037 Is the aggregation the plan claims defined, given "aggregated ≥ 80%" spans two tools that write separate data files and the web step runs its suite without coverage at all? [Consistency, Plan §Testing Strategy; .github/workflows/verify.yml §Unit tests (web)] <!-- Evaluator: Resolved — replaced the undefined aggregate with two independently failing floors (coverage.py combined over src/api/src/api; Vitest v8 over src/web/app/worklist), and recorded why /src/web is not added to coverage.py's source list -->
- [X] CHK038 Does the adopted performance target have a stated execution site, given the plan adds a benchmark tier and its dependency while listing no workflow step for it — so a missed target would be published by nothing, against the obligation to publish the miss? [Verifiable, project-instructions.md §VII; Plan §Testing Strategy, §Project Structure] <!-- Evaluator: Resolved — plan.md §Performance measurement conditions names a `Performance benchmark (api)` step publishing both variants' p95, listed in Project Structure and the Check inventory; SC-017 carries the publish-the-miss obligation -->
- [X] CHK039 Is it stated which of this feature's obligations are enforced by a check that already runs and which need a new one, given the architecture-contract step runs per entry today while the new `api.risk_read` package reaches the forbidden-module list only through a plan hint? [Traceability, Plan §HINT-005; .github/workflows/verify.yml §Architecture contracts] <!-- Evaluator: Resolved — added plan.md §Check inventory mapping each obligation to its step and marking it "runs today" or "new step"; the architecture-contract row records that the step runs but the contract itself must be extended -->
- [X] CHK040 Is the testability consequence of E007 not having landed recorded as an assumption or a requirement, given the absent-forecast path is the only state with real data behind it, so every populated state is demonstrable only against a hand-built fixture until forecasts exist? [Completeness, Spec §Assumptions, §Key Entities; Plan §Integration Points, §HINT-001, §Risk Mitigation] <!-- Evaluator: Resolved — added the assumption to spec.md §Assumptions and extended plan.md §Risk Mitigation, both tying it to FR-036 and FR-037 -->

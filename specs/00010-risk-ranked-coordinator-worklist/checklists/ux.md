# UX Requirements Quality: Risk-Ranked Coordinator Worklist
**Created**: 2026-07-28 | **Feature**: [spec.md](../spec.md)

## Completeness

- [X] CHK001 Is there a requirement that owns the empty-filter state and states what the page displays in it, given FR-018a delegates behaviour to "the winning state's owning requirement" and only a US4 acceptance scenario describes this state? [Completeness, Spec §FR-018, §FR-018a] <!-- Evaluator: Resolved — added FR-042 to spec.md §Degraded states as the state's owning requirement: distinct wording from FR-015, both groups empty, the scoping control and its full project set retained, and an unfiltered empty worklist explicitly excluded from borrowing it. Contract's `empty_filter` row and `x-degraded-states` entry now cite FR-042 -->
- [X] CHK002 Is it stated whether a stale run's populated rows continue to carry their figures or suppress them, given FR-029 states only that staleness and age are reported while FR-018a's ordering principle ranks trust-compromising states above annotating ones? [Completeness, Spec §FR-029, §FR-018a] <!-- Evaluator: Resolved — FR-029 now states that staleness annotates and suppresses nothing: figures, ranking membership and row labels are all unchanged, matching the contract's `stale_run` row ("unchanged, with age reported") and FR-018a's placement of stale among the annotating states -->
- [X] CHK003 Do FR-015, FR-016 and FR-021 state what a row still shows — identity and need-by date — rather than only what is withheld, given that obligation appears in US3's narrative but in no requirement? [Completeness, Spec §FR-015, §FR-016, §FR-021] <!-- Evaluator: Resolved — FR-015 now requires every open line to remain listed with its identity and need-by date; FR-016 states the retained content and that nothing further may be carried; FR-021 routes to FR-016 for row content as well as for ranking -->
- [X] CHK004 Is there a requirement forbidding the display of any two figures whose arithmetic combination yields a point delivery estimate, given FR-007 enumerates surfaces and FR-009 states the reconstruction rule for calendar margin alone? [Completeness, Spec §FR-007, §FR-009] <!-- Evaluator: Resolved — added FR-041 to spec.md §Presentation honesty, stating the prohibition over the whole displayed set rather than per field, and made finite by FR-027's three closed content classes. FR-007 now points at it; the contract's `ExpectedHarm` removal note rests on it -->
- [X] CHK005 Is it stated why the mandated combination of an on-row as-of date and durations counted from it is not the reconstruction route FR-009 forbids for calendar margin? [Completeness, Spec §FR-003, §FR-019, §FR-007] <!-- Evaluator: Resolved — FR-041's second paragraph states the distinguishing test: a forecast-derived figure may sit beside a date it can be added to only as a member of the FR-004 pair carrying its FR-005 labelling, so the arithmetic yields two labelled quantiles rather than one bare date; a mean overrun yields a date with no proportion attached -->
- [X] CHK006 Does FR-027 state a ceiling on comparison quantities once opt-in quantities are admitted, or does "at most four" stand alongside an unbounded opt-in path? [Completeness, Spec §FR-027, §SC-014] <!-- Evaluator: Resolved — FR-027 now records that this feature offers no opt-in quantity at all (the response's primary object is closed over exactly four), so the cap is unconditional here, and caps a later opt-in at one active at a time — a row never carries more than five -->
- [X] CHK007 Is the ordering within the unranked group specified in the requirements? [Completeness, Spec §FR-016, §US3] <!-- Evaluator: Resolved — added FR-045: need-by date ascending, then line identifier ascending, lifting the rule out of the contract's `unranked` description and into a requirement -->
- [X] CHK008 Is it stated whether FR-025's project scope applies to the unranked group, and whether that group's order responds to a non-default sort key? [Completeness, Spec §FR-025, §FR-026, §FR-016] <!-- Evaluator: Resolved — FR-045 states both: the group is scoped by FR-025 exactly as the ranking is (which is what makes SC-023's reconciliation hold), and its order is fixed under every offered key because expected harm needs draws an excluded line has none of -->
- [X] CHK009 Is there a requirement stating how the ordering and a line's position within it reach a coordinator using assistive technology, given SC-001 is phrased visually and the ranking quantity is absent from the row? [Completeness, Spec §SC-001, §FR-001] <!-- Evaluator: Resolved — added FR-048 (one-based position as row text; ranked group exposed with an ordering-and-size semantic; position always the position under the active key) and SC-025 -->
- [X] CHK010 Is FR-004's "single labelled pair" required to be one labelled unit in the accessibility tree, or only visually, given a linearised reading of two adjacent numbers reintroduces the independent-quantile reading FR-004 removes? [Completeness, Spec §FR-004] <!-- Evaluator: Resolved — added FR-049: the pair, its anchor and its FR-005 labelling reachable under one accessible name, never exposed as two adjacent unrelated numbers -->
- [X] CHK011 Is there a requirement that a degraded state and FR-031's unsaved mark are carried by text rather than by colour, position, or icon alone? [Completeness, Spec §FR-018, §FR-031] <!-- Evaluator: Resolved — added FR-050: both carried by text in the accessibility tree; colour, position, an unnamed icon, or the absence of a figure may not be the sole carrier of either -->
- [X] CHK012 Does any requirement or success criterion state an accessibility obligation beyond FR-012's live region — keyboard operability of the need-by adjustment and the sort control, and announcement of the bounded forms `<1%` and `>99%`? [Completeness, Spec §FR-012, §FR-032] <!-- Evaluator: Resolved — added FR-051 (keyboard operation of the adjustment, sort and scoping controls with their current values exposed; `<1%` and `>99%` announced as words) and SC-025 -->
- [X] CHK013 Is the feedback for an adjustment that does change the ordering specified, given FR-012 and SC-015 cover only the unchanged case and a reorder carries no announcement obligation? [Completeness, Spec §FR-012, §SC-015] <!-- Evaluator: Resolved — added FR-046 (same live region, wording distinguishable from FR-012's, the new position named, focus retained on the adjusted row) and SC-026 -->
- [X] CHK014 Is there a requirement that the tiebreak rule is stated to the coordinator, given FR-010 records it as the sole ordering within the zero-harm block and FR-026 mandates on-screen enumeration only for the sort key and direction? [Completeness, Spec §FR-010, §FR-026] <!-- Evaluator: Resolved — added FR-047 requiring the rule on screen beside the active key and direction; the contract's `SortState.tiebreak` already returns it and now cites FR-047 as the reason -->
- [X] CHK015 Is a state for "the stored artifacts could not be read" specified, given FR-018 declares its enumeration of eight canonical while the plan defines a page-level failure treatment outside it? [Completeness, Spec §FR-018] <!-- Evaluator: Resolved — added FR-043: no rows, no figures, a correlation identifier, wording distinct from FR-015's, and stated as deliberately outside FR-018's eight rather than a ninth — those eight are states of what the system knows, each reached by a successful read. FR-018, the plan's error table and the contract's `PageState` now say so -->
- [X] CHK016 Is an export within this feature's scope, so that FR-007's prohibition on a single delivery date "in an export" binds a defined surface? [Completeness, Spec §FR-007, §Scope] <!-- Evaluator: Resolved — Scope § Excluded now records that this feature emits no export, download or printable report, and FR-007 states that its enumeration binds every surface later added rather than describing surfaces that exist, so the clause is a standing prohibition rather than a reference to nothing -->

## Clarity

- [X] CHK017 Is "comparison quantity" defined by an observable property of the row rather than by what a coordinator is presumed to scan, so two reviewers counting the same row reach the same total? [Clarity, Spec §Glossary, §FR-027] <!-- Evaluator: Resolved — FR-027 and the Glossary now define it by region membership: a comparison quantity is a member of the primary region FR-032 names, not a figure a coordinator is presumed to scan -->
- [X] CHK018 Are the row's degraded-state label, its rank position, and FR-031's unsaved mark classified as comparison quantities or as context, given FR-027 exempts only FR-009 and FR-019 context? [Clarity, Spec §FR-027, §FR-018] <!-- Evaluator: Resolved — FR-027 now classifies the row's content in three closed classes and names the third: **row status** is exactly the rank position, the resolved state label and the unsaved mark, none a forecast figure, none counting against the cap. Added a Glossary entry for it -->
- [X] CHK019 Is it stated whether FR-006's two directions count as one comparison quantity, given the quantile pair carries an explicit "counts as one" and the probability pair does not? [Clarity, Spec §FR-027, §FR-006] <!-- Evaluator: Resolved — FR-027 now reads "the probability of missing it -- whose two FR-006 directions count as one, exactly as the pair does", and FR-006 states the same from its side -->
- [X] CHK020 Does FR-032's "distinct type scale" state a direction of distinction, given a secondary region set larger than the primary would satisfy the words while inverting FR-009's subordination? [Clarity, Spec §FR-032, §FR-009] <!-- Evaluator: Resolved — FR-032 now requires the secondary region at a *smaller* type scale than the primary's and never a heavier weight, recording that "distinct" without a direction is satisfied by the inversion. The contract's `SecondaryContext` description matches -->
- [X] CHK021 Is the "reading order" FR-032 asserts identified — which of the four quantities comes first, and whether the order is the visual one or the assistive-technology one? [Clarity, Spec §FR-032, §FR-027] <!-- Evaluator: Resolved — FR-032 now names the order (identity, need-by date, miss probability, quantile pair) and binds it to both the rendered document and the accessibility tree's traversal, matching the contract's declared `PrimaryFigures` property order -->
- [X] CHK022 Is the "persistent" acknowledgement bounded by a stated termination condition — dismissal, the next adjustment, or reload? [Clarity, Spec §FR-012, §FR-032] <!-- Evaluator: Resolved — FR-012 now bounds it: the message remains until the next adjustment replaces it, the coordinator dismisses it, or the page is reloaded, and it must not expire on a timer -->
- [X] CHK023 Is FR-005's "reference class" defined as a population a coordinator would recognise, and is the term in the Glossary, given research records that an unstated reference class is read in mutually contradictory ways? [Clarity, Spec §FR-005, §Glossary; Research §Communicating an interval and a lateness probability] <!-- Evaluator: Resolved — FR-005 now requires the class to name a recognisable population (comparable orders) rather than the artifact it was computed from, states that the payload's `posterior_predictive_draws` basis is provenance and may not stand in for the rendered statement, and defers covariate naming to E012. Added a Glossary entry -->
- [X] CHK024 Does FR-005 specify the framing of the complementary frequency — a natural frequency such as "four in five" versus a percentage — given the research attributes measurably better decisions to frequency-framed displays? [Clarity, Spec §FR-005; Research §Communicating an interval and a lateness probability] <!-- Evaluator: Resolved — FR-005 now requires a natural frequency over a stated denominator ("four in five land by this day, one in five later") rather than a bare percentage, citing the research finding it rests on -->
- [X] CHK025 Does FR-006 state anything about the relative prominence of the two directions, given the research finding that positive and negative framings are not behaviourally equivalent? [Clarity, Spec §FR-006; Research §Communicating an interval and a lateness probability] <!-- Evaluator: Resolved — FR-006 now requires equal prominence (same type scale and weight, adjacent within one statement) and a fixed order on every row, miss first, so the pair is a dual framing rather than one framing with a footnote -->
- [X] CHK026 Is "the coordinator's calendar" in FR-018 and FR-030 reconciled with the Glossary's Today, which is fixed by configuration and explicitly not taken from the viewer's device? [Clarity, Spec §FR-018, §FR-030, §Glossary] <!-- Evaluator: Resolved — FR-018 and FR-030 now say "relative to Today as the Glossary defines it — the request-time date in the single configured time zone, never the viewer's device", and FR-018 records that the phrase means exactly that wherever the spec uses it -->
- [X] CHK027 Is "line identifier" in FR-010's tiebreak defined — the generated key or the human-readable identity — given the two produce different orders and the Glossary defines neither? [Clarity, Spec §FR-010, §Glossary] <!-- Evaluator: Resolved — FR-010 now defines it as the stable generated key rather than the human-readable identity, which is the only one unique by construction and therefore the only one that can terminate; the contract already used `po_line_id`. Added a Glossary entry -->
- [X] CHK028 Are FR-015's "plainly" and FR-031's "visibly marked as unsaved" reduced to stated properties, given FR-032's presentation contract covers FR-009, FR-019, FR-026, FR-027 and FR-012 but not these two? [Clarity, Spec §FR-032, §FR-015, §FR-031] <!-- Evaluator: Resolved — FR-032 now carries both: "plainly" is a page-scope banner with FR-044's committed copy, before any row content in the reading order and never discharged by absent figures; "visibly marked as unsaved" is a text mark on the adjusted need-by quantity itself, in the accessibility tree under FR-050. FR-015 restates its half -->


## Consistency

- [X] CHK029 Is FR-030's "earlier than the active run's as-of date" reconciled with the clamp at `need_by == as_of_date`, which the plan records as resolving to already-late? [Consistency, Spec §FR-030; Plan §HINT-002] <!-- Evaluator: Resolved — FR-030 now states `need_by <= as_of_date` and why the equality case belongs to it (the survival grid is one-based with no `k = 0`, so there is no offset to read), matching FR-033's arithmetic and the storage clamp. plan.md HINT-002 updated: the divergence is closed in the spec rather than only in the hint -->
- [X] CHK030 Is FR-018a's cited reachable combination — a stale run making a line both beyond-horizon and calendar-passed — consistent with a seven-day staleness threshold, given the combination requires today to fall later than the as-of date plus the whole horizon? [Consistency, Spec §FR-018a, §FR-029] <!-- Evaluator: Resolved — FR-018a now states that staleness is necessary but nowhere near sufficient: the pair needs `age_days > horizon_days`, more than fifty times the threshold at a 365-day horizon. FR-033 (which already carried the arithmetic) adds that a fixture reaching it sets FR-038's injected today past the whole horizon, not merely past the threshold -->
- [X] CHK031 Does FR-018a's stated derivation principle account for "not covered", where no figure exists rather than one being untrustworthy, and would it locate a newly added state without a second rule? [Consistency, Spec §FR-018a, §FR-016] <!-- Evaluator: Resolved — FR-018a's first class is restated as *no figure may be shown* rather than *the figure cannot be trusted*, so not covered belongs to it without strain, and a placement rule for a new state is now stated: which of the three classes, then whether it names a more specific cause than its neighbours — which is also why roster mismatch precedes not covered -->
- [X] CHK032 Is SC-001 scoped to the default ordering, given FR-026 admits three other active keys under which row position no longer indicates expected harm? [Consistency, Spec §SC-001, §FR-026] <!-- Evaluator: Resolved — SC-001 restated: the harm claim is scoped to the default ordering, and under any other key what is asserted is that the active key and direction are stated and each row's position is its position under that key -->
- [X] CHK033 Is FR-026's stated reason for excluding the miss probability — that ordering the whole list by one probability discards criticality — reconciled with admitting criticality and calendar margin, each of which orders the list by one figure and discards the others? [Consistency, Spec §FR-026, §SC-012] <!-- Evaluator: Resolved — FR-026 now states the rule the set actually satisfies: no offered key may order the list by a forecast-derived figure other than the composite harm score. Criticality and calendar margin take no forecast input at all (FR-009), so ordering by them cannot be mistaken for the model's ranking; the objection is answered rather than left standing -->
- [X] CHK034 Are FR-019's "visible without further interaction" and FR-032's "reachable without hover or expansion" the same obligation, given text can be present and reachable while not visible? [Consistency, Spec §FR-019, §FR-032] <!-- Evaluator: Resolved — FR-019 now states they are one obligation and reduces it to one property (text inside the row's own element on the initial render, in the visual output and the accessibility tree alike, no hover, focus, expansion, click or panel), and FR-032 says the same from its side -->
- [X] CHK035 Do the requirements carry a row-level signal for the page-scope states that leave figures on screen, given the research finding that a page-level banner alone is insufficient once rows are sorted or filtered? [Consistency, Spec §FR-029, §FR-018a; Research §Absent, stale, and failed model artifacts] <!-- Evaluator: Resolved — stale is the only page-scope state that leaves figures on screen, and FR-029 now requires each populated row's as-of date to be marked stale in the row itself, so a row read in isolation cannot be taken as current. No new field: the response already carries `stale` and each row's `as_of_date`, now noted at the contract's `ForecastRunMeta.stale` -->


## Testability

- [X] CHK036 Is any wording specified, or any criterion of distinctness stated, that would let SC-009's "distinguishable by wording alone" be evaluated without the implementer first inventing the copy — and is the research obligation to name the cause and the path forward carried into a requirement? [Testability, Spec §SC-009, §FR-018; Research §Absent, stale, and failed model artifacts] <!-- Evaluator: Resolved — added FR-044: the copy lives in one committed table keyed by state, each entry names the cause and what would change it (the research obligation), and distinctness is a stated property of that table — no entry a substring of another, every entry holding a phrase occurring in no other — so SC-009 is a comparison over the table rather than a reading of a screen already built -->
- [X] CHK037 Is a counting procedure stated for SC-014, given FR-027 defines the cap over a category and FR-032 asserts it by region membership, so a fifth scannable figure placed in the secondary region satisfies both as written? [Testability, Spec §SC-014, §FR-027, §FR-032] <!-- Evaluator: Resolved — FR-027 now closes all three classes (four comparison quantities, exactly three context items, exactly three status marks) and states that anything outside them is a fifth comparison quantity whatever region it is rendered in, which is the counting procedure SC-014 is evaluated by. The contract's `SecondaryContext` records that its `additionalProperties: false` over three is what closes the escape -->
- [X] CHK038 Does FR-032 state where the named primary and secondary regions are observable — the response payload, the rendered document, or the accessibility tree — so the assertion has a stated subject? [Testability, Spec §FR-032] <!-- Evaluator: Covered by spec.md §FR-032 — the tier split already names the endpoint's response as the subject for the primary/secondary partition, the sort keys and the four quantities, and a rendered page for the type scale, reading order, no-hover as-of date and live region. This pass additionally named the rendered tier's subject as the document plus the accessibility tree computed from it -->
- [X] CHK039 Does FR-008 state the scale its half-up rounding applies to and the treatment of a probability the artifact supports as exactly zero, so the bounded form is a stated rule rather than an inference? [Testability, Spec §FR-008] <!-- Evaluator: Resolved — FR-008 now fixes the scale (the stored probability times one hundred, rounded half-up to an integer, so `0.125` renders `13%`) and states that an exact zero or one takes the same bounded form as a value that merely rounds there, since several thousand draws cannot evidence a certainty and the contract makes `0%` and `100%` unrepresentable -->
- [X] CHK040 Is SC-004 evaluable from criticality and calendar margin alone, given the ranking quantity is absent from the row's required content and FR-009 names only two of its inputs? [Testability, Spec §SC-004, §FR-009, §FR-001] <!-- Evaluator: Resolved — FR-034 already named SC-004's observable; FR-009 now names all three inputs the score actually has, the third being the delivery-duration distribution the FR-004 quantile pair already summarises on the row, so the decomposition is complete rather than partial and the score itself still stays off the row under FR-041 -->


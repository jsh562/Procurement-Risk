phase: Phase 4 in progress — OBJ3 (Invocation Record with Recomputable Cost)
completed: 37
remaining: 38
blocked: none
next: T040-T050 — the record writer with its own connection and transaction
      (T040, T041), the absent-cost warning and completion log line (T043),
      the SQLite spool (T044) and its exactly-once drain (T045), then the
      tests T046-T049 and the README procedures (T050). T042 verifies the
      price-pin resolves before request construction.
notes: T026-T039 delivered. Four migrations live in the 0100-0199 block and
       apply from empty; the compute package is the first real arithmetic the
       computation-boundary contract has had to protect, and a seeded direct
       edge from provider.py to compute.pricing was confirmed to break it.
       TR-070's pin was wrong and T026's mandated verification caught it:
       semconv v1.36.0 defines gen_ai.system, not gen_ai.provider.name, the
       attribute the pin was selected for. Corrected to 1.37.0 across all five
       recording sites (OI-12).
       E003's schema suite went red on 11 checks the moment these revisions
       landed — all encoding "E003 is the sole author of this chain", which
       ADR-0013 has made false. Rescoped to authorship on the user's decision
       (OI-13), plus one genuine over-strictness in its constraint audit
       (OI-14). E003 is green at 1072 passed, and an E003 revision creating
       llm_invocation still fails as it should.
       Two environment hazards hit this phase, both cross-checkout: the project
       DB container and volume were removed by an external `down -v` mid-run,
       and `procurement-api:e001` is an unqualified tag a sibling checkout had
       rebuilt with an older gateway manifest, which failed the image check
       until rebuilt here. Neither is a defect in this epic.
timestamp: 2026-07-26

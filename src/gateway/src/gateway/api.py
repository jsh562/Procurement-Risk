"""The gateway's public surface — the entry point both Python boundaries call.

Skeleton placed by T002 so the public-surface import contract has a module to
bind to; see `errors.py` for why the contract must precede the module. The
invocation entry point arrives with T010, and replaces E001's `client_type`
placeholder (TR-004).
"""

from __future__ import annotations

__all__: list[str] = []

# UX Requirements Quality: Line Detail and Traceability
**Created**: 2026-07-30 | **Feature**: [spec.md](../spec.md)

## Completeness

- [X] CHK001 Is there a requirement obliging the textual equivalent to be composed from the same published figures the plot renders from, given FR-014 enumerates its five contents but no requirement forbids a second, separately composed copy — the path research.md names as where a point estimate re-enters unnoticed? [Completeness, Spec §FR-014, §FR-017; Research §Accessible equivalents] <!-- Evaluator: Resolved — added FR-048 to spec.md: composed from the same published figures, no separately composed copy -->
- [X] CHK002 Does any requirement oblige the equivalent to convey the spread and tail length the fifty marks convey, given FR-014's list reaches only the eightieth percentile and the residual mass while US1 states the reader can see how long the tail runs? [Completeness, Spec §FR-014, §US1, §SC-005] <!-- Evaluator: Resolved — FR-083: the equivalent states the span the plotted marks cover -->
- [X] CHK003 Is the obligation Scope states — the equivalent available to every reader rather than to assistive technology alone — carried by a requirement, given FR-014 rules out alt text and FR-015 fixes placement and disclosure without naming who the equivalent is available to? [Completeness, Spec §Scope, §FR-014, §FR-015] <!-- Evaluator: Resolved — added FR-049 to spec.md, carrying Scope's obligation and adopting E010 FR-019's "visual output and accessibility tree alike" form that FR-015 references but restates only in part -->
- [X] CHK004 Is there a requirement fixing how a reader is told a displayed source is synthesized at the moment the page is shown, given FR-022 binds "wherever such a record is listed or its page displayed" and a byte stream can carry the distinction only outside the reader's view? [Completeness, Spec §FR-022, §US2] <!-- Evaluator: Resolved — FR-080: provenance reaches the reader before the source opens -->
- [X] CHK005 Does any requirement state the reader-facing consequence FR-020a records — that the reader is handed a whole document positioned at a page rather than the page — including what the view states when the position is not honoured? [Completeness, Spec §FR-020a, §FR-020] <!-- Evaluator: Resolved — added FR-050 to spec.md: the offer states that the whole document is served positioned at the cited page and names that page, so a viewer ignoring the fragment leaves the reader able to reach it -->
- [X] CHK006 Is there a requirement giving a reader who cannot read the streamed source an equivalent route to the cited value, given FR-018 lists the extracted span in the record list but no requirement makes that span the accessible equivalent of the page FR-020 requires displayed? [Completeness, Spec §FR-018, §FR-020, §FR-020a] <!-- Evaluator: Resolved — FR-050 makes the listed title, page, span and confidence sufficient to read the cited value without opening the source at all -->
- [X] CHK007 Is the reader's return path from a displayed source specified — whether the source replaces or accompanies the view, and how a reader using a keyboard gets back to the line — given Scope excludes navigation chrome and FR-020 requires the page displayed? [Completeness, Spec §FR-020, §Scope] <!-- Evaluator: Resolved — FR-079: separate browsing context, so the view is never left -->
- [X] CHK008 Is an accessibility standard and conformance level named anywhere in the requirements, or are FR-015, FR-040 and FR-043 the whole of the obligation this view is judged against? [Completeness, Spec §FR-015, §FR-040, §FR-043] <!-- Evaluator: Resolved — FR-081: WCAG 2.2 AA; product-wide adoption recorded as an amendment -->
- [X] CHK009 Does any requirement fix how the bounded forms FR-041 mandates are rendered as words, given a rendering that drops the inequality states the flat certainty the bounded form exists to prevent, and FR-041 adopts the delivered form without the announcement obligation that accompanies it? [Completeness, Spec §FR-041, §FR-040] <!-- Evaluator: Resolved — added FR-052 to spec.md, adopting E010 FR-051's announcement obligation ("less than one percent" / "greater than ninety-nine percent") alongside the numeral form FR-041 already adopted -->
- [X] CHK010 Is there a requirement that the census FR-024 publishes names its population and its no-interval declaration in reader-facing words, given the figure counts every linked record the active resolution run carries and therefore reads, on a line carrying two records, as a statement about that line? [Completeness, Spec §FR-024, §SC-010] <!-- Evaluator: Resolved — added FR-051 to spec.md: population, no-interval declaration and licensed reason rendered as words rather than carried as payload members alone -->
- [X] CHK011 Is the per-field confidence FR-018 requires distinguished in reader-facing terms from the frequencies published beside it, given it is a self-reported score rather than a probability and renders as one more number about the same line? [Completeness, Spec §FR-018, §FR-041] <!-- Evaluator: Resolved — FR-051 requires it named as a self-reported extraction score, carrying what it is a score of, and never rendered as a percentage; the contract's confidence.basis and non-percentage display pattern already encode it -->
- [X] CHK012 Is there a requirement stating what the reader is told when a session need-by adjustment is in force — that the figures answer a what-if rather than the record — given Scope excludes the control and FR-005 names only the effective date? [Completeness, Spec §FR-005, §Scope; Plan §AD-008] <!-- Evaluator: Resolved — added FR-054 to spec.md, adopting E010 FR-031/FR-032's unsaved-mark form and showing the recorded date beside the adjusted one -->
- [X] CHK013 Is the wording of the states FR-042 enumerates committed as data beyond the identity states, given FR-026 commits copy for the unresolved case alone while FR-043 rests its decidability on distinctness being asserted over committed copy? [Completeness, Spec §FR-026, §FR-042, §FR-043] <!-- Evaluator: Resolved — added FR-053 to spec.md committing the wording of every state at all three scopes; tasks.md T010 now exports ANNOTATION_COPY beside RESOLUTION_COPY and SECTION_COPY -->
- [X] CHK014 Do the annotation states carry the obligation FR-026 places on the identity state — naming what the state withholds — given a reader meeting the at-or-before-anchor state finds the miss mass absent with no stated reason? [Completeness, Spec §FR-042, §FR-026, §FR-006] <!-- Evaluator: Resolved — FR-053 requires each entry to name what its state withholds or bounds, naming the at-or-before-anchor, beyond-horizon and stale-run cases explicitly -->
- [X] CHK015 Is there a requirement governing the vocabulary of reader-facing copy, given the reader is stated not to be a statistician while the terms the spec uses for its own figures — quantile, percentile, posterior, census, residual mass — have no stated lay rendering? [Completeness, Spec §Assumptions, §Glossary] <!-- Evaluator: Resolved — added FR-047 to spec.md: reader-facing copy may not require the reader to know those terms, and where one is rendered it carries its lay meaning at the point of use -->
- [X] CHK016 Is the navigation target FR-035 fixes required to carry an accessible name identifying the line, given the target is an existing worklist field and the worklist's row content is closed against adding an element? [Completeness, Spec §FR-035, §SC-016] <!-- Evaluator: Resolved — added FR-056 to spec.md: an accessible name identifying the line, composed from the identity the row already renders; tasks.md T024 carries it -->

## Clarity

- [X] CHK017 Is FR-015's "within the same region as the figures it describes" reducible to an observable property — a shared container, reading-order adjacency, or a visual area — given neither the requirements nor the Glossary define "region" while FR-038's whole domain is now fixed by one? [Clarity, Spec §FR-015, §FR-038, §Glossary] <!-- Evaluator: Resolved — Glossary gains a Region row fixing the property as containment in a labelled container (nesting allowed), never proximity, reading order or visual grouping. Which regions the view has is a separate question, carried at CHK024 -->
- [X] CHK018 Does FR-015 close the set of mechanisms it forbids, or does "hover, focus, expansion, click or panel" leave placement below the fold, an off-screen node, or a block hidden by a media query admissible? [Clarity, Spec §FR-015] <!-- Evaluator: Resolved — FR-049 states the property positively rather than closing an enumeration: off-screen, clipped to no size, hidden by a media query, or otherwise removed from the visual rendering all fail it -->
- [X] CHK019 Does FR-040 require one carrier serving both readings, or is it satisfied by text present only in the accessibility tree, leaving a sighted reader with colour or position as their sole carrier? [Clarity, Spec §FR-040, §FR-015] <!-- Evaluator: Resolved — FR-049 requires one carrier reaching both readings, and names colour, position or an unnamed icon serving a sighted reader while text serves the other as the failure -->
- [X] CHK020 Is "a phrase" in FR-043 defined, and is its no-substring rule applied within each of FR-042's three scopes or across all of them, given SC-018 scopes indistinguishability to "any scope" while FR-043 binds every state against every other? [Clarity, Spec §FR-043, §SC-018, §FR-042] <!-- Evaluator: Resolved — FR-053 defines a phrase as two or more contiguous words compared after case and whitespace normalisation, and applies both rules across all three scopes together, since states from different scopes render on one screen at one time -->
- [X] CHK021 Is FR-004's "adjacent to each frequency" scoped to the two labelled quantiles alone or to every frequency the view publishes — the per-mark share, the miss mass in both directions, the residual mass, and each cumulative band? [Clarity, Spec §FR-004, §FR-001, §FR-005, §FR-008] <!-- Evaluator: Resolved — FR-004 now cites FR-047, which binds every frequency -->
- [X] CHK022 Does FR-001's "that share stated in words beside the figure" fix a form — a natural frequency naming both the count and the population it counts, or a percentage — given the misreading research.md measured is a denominator that lost its noun? [Clarity, Spec §FR-001; Research §Discrete outcomes versus continuous density] <!-- Evaluator: Resolved — FR-047 fixes the form: the statement names both the denominator the share is taken over and the population that denominator counts, which the delivered percentage numeral plus the committed reference-class wording satisfies and a bare numeral does not -->
- [X] CHK023 Is the reference class required to name a population a coordinator would recognise rather than the artifact it was computed from, given the Glossary says "comparable orders" while the committed constant the plan reconciles to says "out of 100 lines like this one"? [Clarity, Spec §FR-004, §Glossary; Plan §AD-005] <!-- Evaluator: Resolved — FR-047 requires the population rather than the artifact, and the already-committed worklist wording rather than a second phrasing (AD-005). The Glossary's Reference class row now records that "comparable orders" is this spec's name for the population, not the rendered copy; the contract's ReferenceClass object carries the reading convention, not the noun -->
- [X] CHK024 Do any requirements establish the distribution, mass, evidence and covariate regions FR-038's domain is defined by, so that a reviewer can locate a region's boundary without reading the implementation? [Clarity, Spec §FR-038, §FR-042] <!-- Evaluator: Resolved — FR-082 establishes the four regions as named containers -->
- [X] CHK025 Is FR-038's locus decidable for a value rendered in more than one place — the as-of date, which its *date* class admits and its artifact-identification carve-out excludes? [Clarity, Spec §FR-038, §FR-013] <!-- Evaluator: Covered by spec.md FR-038 ("the domain is a locus, not an adjective") — the same value is a stated figure where it is rendered as a figure about this line and artifact identification where it is not, which is precisely the two-place case. The carve-out list does not name the as-of date, and the contract encodes both loci structurally: figures.date.as_of_date under x-figure-classes, meta.forecast_run.as_of_date outside it -->
- [X] CHK026 Is FR-017's "any single value as the expected outcome" reducible to a decidable property in the way FR-037 reduces a forbidden date to one carrying no stated proportion? [Clarity, Spec §FR-017, §FR-037] <!-- Evaluator: Resolved — FR-048 applies FR-037's device to FR-017: no value the equivalent states may carry no stated proportion -->
- [X] CHK027 Is FR-045's "mark its own figures as such" decidable between one mark carried once for the view and a mark carried by each figure? [Clarity, Spec §FR-045] <!-- Evaluator: Resolved — added FR-055 to spec.md: once, inside the region holding the figures it qualifies, naming the as-of date and the threshold. That is what FR-045's own rationale asks (the mark must not sit on a surface the reader has left); per-figure repetition adds no information -->
- [X] CHK028 Does FR-042's section-scoped composition rule fix what the reader is told, given a covariate panel with one withheld value among three carries the same section state as one in which every value is withheld? [Clarity, Spec §FR-042, §FR-031] <!-- Evaluator: Covered by spec.md FR-031, which names the state per withheld value, together with FR-042's rule for the section; the contract carries state on every Covariate and on every LinkedRecord as well as on the section, so the two panels are distinguishable item by item -->

## Consistency

- [X] CHK029 Is SC-031's "reachable without assistive technology" reconciled with FR-015, which records that "reachable" alone is satisfied by a disclosure widget hiding the equivalent by default? [Consistency, Spec §SC-031, §FR-015] <!-- Evaluator: Covered by spec.md SC-031, which now carries FR-015's decidable form verbatim — "present without any hover, focus, expansion, click or panel" — rather than "reachable without assistive technology" -->
- [X] CHK030 Is FR-042's four-member annotation set reconciled with the downstream artifacts carrying it, which close at three with no member for the calendar-passed state, leaving FR-040's text obligation nothing to bind to for that state? [Consistency, Spec §FR-042, §FR-040; Plan §Requirement Coverage Map] <!-- Evaluator: Resolved — the enum, admissible sets, excluded pairs and plan coverage row already read four; four places of residual three-member prose in contracts/openapi.yaml corrected (x-states description, AnnotationState description, the adopted-state-name list, the annotations array note), and AnnotationState now records that calendar_passed is carried by committed copy under FR-040/FR-053 like the rest. tasks.md T010 gains ANNOTATION_COPY, which is what FR-040 binds to -->
- [X] CHK031 Is the co-occurrence of the calendar-passed state with the other three annotations decided, given FR-042 states the four are determined independently while the only excluded pair on record is beyond-horizon with at-or-before-anchor? [Consistency, Spec §FR-042, §FR-006] <!-- Evaluator: Covered by contracts/openapi.yaml x-states — annotation_excluded now records all three date-derived pairs with their arithmetic, and annotation_admissible_sets enumerates the eight admissible sets, so every co-occurrence is decided. The stale triple sentence in the third exclusion was corrected while checking this -->
- [X] CHK032 Is SC-020's five-figure list reconciled with FR-041, which obliges the bounded form for three of them and leaves the labelled quantiles' shares and the miss mass to a form adopted from another feature rather than required here? [Consistency, Spec §SC-020, §FR-041] <!-- Evaluator: Resolved — added FR-052 to spec.md binding the two shared figures to E010 FR-008's form here as well, so all five figures SC-020 names carry a requirement; tasks.md T044 now asserts it over the whole set -->
- [X] CHK033 Is FR-009's permission for a calendar date attached to a labelled quantile reconciled with SC-030, which enumerates such labels among the dates present, so a reviewer can tell whether the criterion permits them or asserts them? [Consistency, Spec §FR-009, §SC-030] <!-- Evaluator: Resolved — SC-030 now reads as a permission and records that the response declines FR-009's quantile-date permission (LabelledQuantile.days is a duration by design), so a view carrying only the need-by mark's label satisfies it. ID unchanged -->

## Testability

- [X] CHK034 Does any success criterion measure that the denominator is read as intended by a reader who is not a statistician, or do SC-005 and SC-020 measure only that the figures are present and bounded? [Testability, Spec §SC-005, §SC-020; Research §Discrete outcomes versus continuous density] <!-- Evaluator: Resolved — recorded as a limitation: measured as present, not as read -->
- [X] CHK035 Is SC-031 evaluable against a stated property of the equivalent — a container relation or an initial-render condition — rather than against a screen already built? [Testability, Spec §SC-031, §FR-015] <!-- Evaluator: Resolved — SC-031's second clause is already an initial-render condition; the Glossary's Region row now gives its first clause a container relation (containment in a labelled container), so both halves are evaluable against a stated property -->
- [X] CHK036 Is SC-018's "no two states in any scope share wording that leaves them indistinguishable" evaluable over a committed artifact, given FR-026 commits the copy for one state and the wording of the rest is unowned? [Testability, Spec §SC-018, §FR-043, §FR-026] <!-- Evaluator: Resolved — FR-053 puts every state's wording, at all three scopes, in the committed table the criterion is evaluated over, and fixes the comparison's terms; tasks.md T010 carries the distinctness test -->
- [X] CHK037 Is SC-004 well-formed enough for two reviewers to agree what it asserts, given its condition clause and its subject clause each introduce a different line? [Testability, Spec §SC-004] <!-- Evaluator: Covered by spec.md SC-004, now one sentence with one subject: a line that exists, is open, and has no posterior in the active run -->
- [X] CHK038 Is FR-036's "a correlation identifier they can quote" reduced to an observable property — text present in the accessibility tree rather than an image or a truncated string? [Testability, Spec §FR-036, §SC-024] <!-- Evaluator: Resolved — added FR-057 to spec.md: complete text in the accessibility tree, never an image, never truncated, never abbreviated in the rendering; tasks.md T033 carries it -->
- [X] CHK039 Does any requirement place the equivalent's own rendering under a stated check, given FR-046's property-based obligation covers the server-side derivations while FR-016's guarantee that both readings yield the same figures is a property of what the two renderings show? [Testability, Spec §FR-046, §FR-016, §SC-005] <!-- Evaluator: Resolved — FR-048's third clause puts the equivalent/plot agreement under a check that runs in the merge gate; tasks.md T023 adds the assertion and T043 asserts that it runs -->

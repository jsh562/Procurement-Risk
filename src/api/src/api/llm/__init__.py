"""Reserved package: llm.

Deliberately empty in E001. It exists so the boundary contract has something
to enforce the moment a later epic puts code here — an empty package makes the
rule real in advance rather than retroactively.
"""

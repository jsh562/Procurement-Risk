# Data Integrity: Core Data Schema
**Created**: 2026-07-25 | **Feature**: [spec.md](../spec.md)

## Completeness — Data-Quality Characteristic Coverage (ISO/IEC 25012)

- [X] CHK001 Is a currentness requirement stated for the active forecast run — a maximum permitted age of `forecast_run.as_of_date` relative to read time, beyond which a reader should treat the artifact as stale? [Completeness, Spec §TR-027, §TR-049] <!-- Evaluator: Resolved — added TR-080 (as-of date exposed, no maximum age imposed here, threshold owned by E010) plus a Clarifications bullet with the reversal trigger -->
- [X] CHK002 Does any requirement state the currentness consequence of the fixed 365-day horizon — what a reader is expected to do when `need_by_date - as_of_date` exceeds `horizon_days` — rather than leaving it to the data-model array-semantics note? [Completeness, Spec §TR-029, §TR-030, data-model §line_posterior array semantics] <!-- Evaluator: Resolved — added TR-053 elevating the data-model beyond-horizon rule (1 - residual_tail_mass) to a requirement -->
- [X] CHK003 Is a credibility requirement stated for `extracted_value.confidence` — which agent asserts the number, on what basis, and what makes it believable — or is only its presence and range required? [Completeness, Spec §TR-015, §TR-016] <!-- Evaluator: Resolved — added TR-081 (self-reported score asserted by the extracting agent at extracted_at, not a calibrated probability) plus a Clarifications bullet -->
- [X] CHK004 Is the precision of the confidence value declared — its scale, meaningful discrimination, and whether the endpoints 0 and 1 are admissible as literal certainty — or only its inclusive range? [Completeness, Spec §TR-016, §OBJ3 VC2] <!-- Evaluator: Resolved — added TR-054 declaring the double-precision continuous scale, admissible endpoints, and absence of any coarser scale -->
- [X] CHK005 Is the required precision of `residual_tail_mass` and the meaning of the `1e-9` probability-sum tolerance stated as a requirement, rather than only as the data-model note that realised error is ~1e-16 and the tolerance is deliberately slack? [Completeness, Spec §TR-030, §SC-015, data-model §Declared Constants] <!-- Evaluator: Resolved — added TR-055 (agreement within the tolerance at double precision) and TR-056 (tolerance fixed at 1e-9) -->
- [X] CHK006 Is an understandability requirement stated — that every stored column carry a reader-facing semantic definition, as `field_vocabulary.label` and `.description` require for vocabulary terms — for tables such as `line_posterior` and `forecast_run` that carry no such column? [Completeness, Spec §TR-044, data-model §line_posterior array semantics] <!-- Evaluator: Resolved — added TR-083 making data-model.md normative for per-column semantics and barring any object absent from it; Clarifications bullet records COMMENT ON as the reversal -->
- [X] CHK007 Is a recoverability requirement stated for a partially applied migration beyond OBJ1 VC7's "advances from that revision" — specifically what the operator recovers from, and what happens if the seeded `schema_constants` row or a seeded vocabulary row is later deleted, given backup and retention are excluded from scope? [Completeness, Spec §OBJ1 VC7, §TR-043, §Scope Excluded] <!-- Evaluator: Resolved — added TR-079 (recovery is re-apply-from-empty; constants loss detected by the agreement check, referenced vocabulary loss blocked by RESTRICT) plus a Clarifications bullet -->

## Completeness — Provenance Requirements (W3C PROV)

- [X] CHK008 Does any requirement state that a page citation identifies a document *version or revision*, given `document` carries `document_id`, `retrieval_date`, and `loaded_at` but no version column and no immutability rule? [Completeness, Spec §TR-041, §TR-046, data-model §document] <!-- Evaluator: Resolved — added TR-057 (the manifest key identifies one fixed revision; a superseding revision loads under a distinct key) -->
- [X] CHK009 Is provenance-of-provenance required — the agent that recorded a citation and when — or is `extracted_value.extracted_at` the only temporal fact, with no responsible agent named on the record? [Completeness, Spec §TR-015, data-model §extracted_value] <!-- Evaluator: Resolved — added TR-082 (agent identity carried at ingestion-run granularity by E006, not per row) plus a Clarifications bullet with the reversal trigger -->
- [X] CHK010 Is the granularity of attribution stated for a value that spans several pages within one chunk, or that appears on the same page in several chunks, and how each case resolves against the `(chunk_id, page_number)` tie? [Completeness, Spec §TR-017, §TR-018] <!-- Evaluator: Resolved — added TR-058 (one page per chunk, so multi-page spans are multi-source with one contributor per page; one page across chunks yields one contributor per chunk) -->
- [X] CHK011 Is TR-018's multi-source rule stated in terms of its interaction with TR-017's composite-FK page tie — that the anchor citation remains a single chunk and page and contributors occupy ordinals 2..N — or is that reconciliation only in the data model? [Consistency, Spec §TR-017, §TR-018, data-model §extracted_value_contributing_chunk] <!-- Evaluator: Resolved — added TR-059 carrying the anchor/contributor reconciliation into the requirement set -->
- [X] CHK012 Is the degenerate case stated — a value asserted about a whole document with no identifiable source page — with its outcome named as reject, quarantine as an extraction failure, or structurally unrepresentable? [Completeness, Spec §TR-015, §TR-019] <!-- Evaluator: Resolved — added TR-061 (unstorable as a value; recorded as a missing-citation extraction failure against the attempting chunk) -->
- [X] CHK013 Is immutability and retention of provenance stated — whether an `extracted_value` or `extraction_failure` row may be updated or deleted once written, and for how long the record is kept — given retention policy is excluded from scope? [Completeness, Spec §Scope Excluded, §TR-015, §TR-019] <!-- Evaluator: Resolved — added TR-084 (append-only; correction is remove-and-reload) and TR-085 (retained for the life of the database) plus a Clarifications bullet -->
- [X] CHK014 Is the ordering semantics of `contributor_ordinal` stated — whether the sequence 2..N is meaningful order or an arbitrary key — so a downstream reader can interpret multi-source provenance without inspecting the writer? [Clarity, Spec §TR-018, data-model §extracted_value_contributing_chunk] <!-- Evaluator: Resolved — added TR-060 (ordinal 1 is the anchor; 2..N is a stable but unordered enumeration carrying no precedence) -->
- [X] CHK015 Is provenance required for the derived forecast artifact — a stated link from a `line_posterior` row back to the extracted values and lifecycle events it was computed from — or is `forecast_run.input_data_hash` the only asserted link? [Completeness, Spec §TR-026, project-instructions §I] <!-- Evaluator: Resolved — added TR-062 (run-granularity provenance, no per-line lineage, reproduction by re-running the recorded run) plus a Clarifications bullet with the reversal trigger -->

## Completeness — Outcome on Violation

- [X] CHK016 Is the outcome on violation — reject, default, or quarantine — stated for every integrity rule in the TR set, including those where the data model silently chooses a default such as `loaded_at DEFAULT now()` or `is_active DEFAULT false`? [Completeness, Spec §Requirements, data-model §Entities] <!-- Evaluator: Resolved — added TR-063 making rejection the uniform outcome and enumerating the declared-default columns as the only exception -->
- [X] CHK017 For each of the ten disclosed gaps G-1 through G-10, is the outcome when the rule is violated stated — build-time test failure only, or a runtime consequence for a reader — rather than the gap merely being named with a test owner? [Completeness, data-model §Disclosed Gaps] <!-- Evaluator: Resolved — added the Gap disclosure record table to data-model.md with an outcome-on-violation column for G-1..G-10 -->
- [X] CHK018 Is the reader-side obligation on an unrecognised `artifact_schema_version` stated as a requirement with an outcome, or does it exist only as OBJ5 VC6's "fail loudly" phrasing and gap G-10's deferral to E010? [Completeness, Spec §TR-032, §OBJ5 VC6, data-model gap G-10] <!-- Evaluator: Resolved — added TR-064 (refuse to read the arrays and report no usable forecast) and cross-referenced it from gap G-10 -->
- [X] CHK019 Is the outcome stated when the deferrable closing-event foreign key proves unworkable and a fallback is taken — which of the two named fallbacks applies, who decides, and what records the decision? [Completeness, Spec §TR-021, §Risks, data-model §purchase_order_line] <!-- Evaluator: Resolved — added TR-065 fixing the ordered fallback ladder, the deciding migration, and the recording obligation in the invariant-to-mechanism map -->

## Clarity — Singularity and Unambiguity (ISO/IEC/IEEE 29148)

- [X] CHK020 Does TR-021 state a single obligation, or does it bundle right-censoring representation, closed-line rejection, the deferrable foreign key, the terminal-flag carry, and a fallback trigger into one sentence such that none is independently verifiable? [Clarity, Spec §TR-021] <!-- Evaluator: Resolved — TR-021 reduced to the single commit-boundary rejection outcome; the bundle split into TR-066 (right-censoring), TR-067 (deferrable FK with terminal-flag carry), TR-065 (fallback ladder) -->
- [X] CHK021 Does TR-028 state a single obligation, or does it bundle array storage, sortedness via an `IMMUTABLE` helper, length enforcement by composite foreign key, and a rationale about declared array sizes? [Clarity, Spec §TR-028] <!-- Evaluator: Resolved — TR-028 reduced to the sortedness outcome; split into TR-068 (canonical artifact), TR-069 (length outcome), TR-070 (IMMUTABLE helper mechanism), TR-073 (composite-FK length mechanism) -->
- [X] CHK022 Does TR-029 state a single obligation, or does it bundle horizon length, whole-day units, anchor measurement, shared day-zero across lines, recording the horizon on the run row, and the enforcement mechanism? [Clarity, Spec §TR-029] <!-- Evaluator: Resolved — TR-029 reduced to one array per line-run over the horizon in whole days; shared day-zero stays with TR-049, and TR-071 (horizon on the run row), TR-072 (length outcome), TR-073 (mechanism) carry the rest -->
- [X] CHK023 Do the requirements that name a mechanism — composite foreign key, partial unique index, `IMMUTABLE` helper — also state the outcome independently of that mechanism, so verification does not reduce to checking that the mechanism was used? [Clarity, Spec §TR-017, §TR-027, §TR-028] <!-- Evaluator: Resolved — every mechanism is now a separate requirement (TR-067, TR-070, TR-073) paired with a mechanism-free outcome requirement (TR-021, TR-028, TR-069, TR-072); TR-017 and TR-027 already state the outcome first -->
- [X] CHK024 Is "approximately 4,000 posterior draws per line" restated as an exact required value anywhere, given `line_posterior.draws` length is enforced against a per-run `draw_count` and planning fixed the value at 4000? [Clarity, Spec §Technical Constraints, plan §AD-005] <!-- Evaluator: Resolved — added TR-056 fixing the seeded draw count at exactly 4000 alongside the horizon and tolerance -->

## Consistency — Requirement Set and Downstream Artifacts

- [X] CHK025 Is the consequence of `document.project_id` being NOT NULL — that a shared reference standard is loaded once per project, producing duplicate document rows for one source — stated in the requirements, or only in the data-model note under `document`? [Consistency, Spec §SC-006, §SC-018, data-model §document] <!-- Evaluator: Resolved — added TR-074 (every document row scoped to one project; one row per source-and-project pair under a distinct key) -->
- [X] CHK026 Is the interaction between per-project duplication of reference-standard documents and the Data Provenance rule — cite never include, and do not mix licenses within a corpus location — stated anywhere in the requirement set? [Consistency, data-model §document, project-instructions §Data Provenance] <!-- Evaluator: Resolved — added TR-075 (five provenance columns per row, license basis replicated with each duplicate, copyrighted standards represented by the citation row alone with body-text loading owned by E002/E006) -->
- [X] CHK027 Do TR-043's enumerated constants and the `schema_constants` columns agree exactly in name and count, and is the direction of authority stated — which of the DDL literal and the published row governs when they disagree? [Consistency, Spec §TR-043, §TR-048, data-model §Declared Constants] <!-- Evaluator: Resolved — names and count already agree six-for-six; added TR-076 and the Direction of authority note in data-model.md fixing the DDL literal as authoritative over the published row -->
- [X] CHK028 Are the three values AD-005 fixed during planning — horizon 365 days, draw count 4000, tolerance 1e-9 — carried into a requirement, each with a recorded reversal trigger and production-scale alternative as the Publish the Miss principle requires of a limitation? [Consistency, plan §AD-005, project-instructions §VII] <!-- Evaluator: Resolved — added TR-056 and the Scope-decision record table in data-model.md giving each value its evidence, reversal trigger, and production-scale alternative; AD-005 now points at both -->

## Testability — Verifiability and Acceptance Conditions

- [X] CHK029 Does TR-052 state an acceptance condition precise enough for a reviewer to decide pass or fail without judgement — the exact post-correction text of the two `specs/project-plan.md` Shared Data Entities rows, and who confirms it? [Testability, Spec §TR-052, §SC-017] <!-- Evaluator: Resolved — added SC-027 giving the exact Introduced-by cell text for both rows, requiring the Consumed-by cells to stay unchanged, and naming QC as the confirming phase -->
- [X] CHK030 Can a failing case be written for TR-013's "no schema change between them" — is there a stated observable that distinguishes a compliant table from a non-compliant one? [Testability, Spec §TR-013, §SC-005] <!-- Evaluator: Resolved — added SC-026 naming the observable (same relation, same vector column, session-level planner setting the only difference, object set unchanged between the arms) and the failing cases -->
- [X] CHK031 Is the floating-point tolerance SC-015 refers to as "a stated tolerance" actually stated in the specification, or only as a data-model constant and a DDL literal? [Testability, Spec §SC-015, §TR-030, data-model §PROB_SUM_TOLERANCE] <!-- Evaluator: Resolved — TR-056 now states the 1e-9 value in the specification and TR-055 states the comparison it governs -->
- [X] CHK032 Is the population under measurement defined for the "100% of stored chunks" criteria, given this epic stores no chunks and E006 populates them — measured against test fixtures, against loaded corpus data, or unmeasurable within this epic? [Testability, Spec §SC-006, §SC-018] <!-- Evaluator: Resolved — SC-006 and SC-018 amended to name the population: this epic's schema-test fixtures plus every row the schema structurally admits; corpus-data measurement assigned to E006 -->
- [X] CHK033 Is SC-009's "zero partial value rows" measurable as stated, given gap G-5 records that the cross-table absence of a value row for a failed extraction is carried by a test rather than by the schema? [Testability, Spec §SC-009, data-model gap G-5] <!-- Evaluator: Resolved — SC-009 amended to split the criterion into its structural half (non-empty canonical text column) and its tested half (the G-5 test), measured over the epic's fixtures -->

## Traceability — Scope Decisions and Cross-Epic Obligations

- [X] CHK034 Is each of the ten disclosed gaps recorded as a scope decision with supporting evidence, a reversal trigger, and a production-scale alternative, or are they listed as a gap plus a covering test with no reversal condition? [Traceability, data-model §Disclosed Gaps, project-instructions §VII] <!-- Evaluator: Resolved — added the Gap disclosure record table to data-model.md giving every gap G-1..G-10 an outcome on violation, a reversal trigger, and a production-scale alternative -->
- [X] CHK035 Is the obligation created by gap G-9 — that E002 and E006 adopt the `document_id` format E003 declared because E002 never froze its manifest key space — stated as a requirement with a named owner and acceptance condition, or only as a note on IP-012 and a gap-table entry? [Traceability, Spec §TR-041, §IP-012, data-model gap G-9] <!-- Evaluator: Resolved — added TR-077 naming E002 and E006 as owners with the acceptance condition (every manifest key matches the declared format, asserted by E006's loader test before any document row is written) -->
- [X] CHK036 Is the reversal condition stated for E003's declaration of the `document_id` format — what becomes of already-loaded document rows and their chunk citations if E002 later freezes a different key space? [Completeness, Spec §TR-041, §IP-012, data-model gap G-9] <!-- Evaluator: Resolved — added TR-078 (forward migration updating the key in place, cascading to chunks, citations untouched because they reference the chunk) and the matching G-9 reversal trigger in data-model.md -->

# Data Integrity: Traced Model Gateway
**Created**: 2026-07-25 | **Feature**: [spec.md](../spec.md)

## Constraint Completeness — Nullability, Uniqueness, Referential Action

- [X] CHK001 Is the nullability of each field in the E013 read contract fixed by a requirement, or does TR-012 enumerate the field list while every NOT NULL and every permitted-NULL decision lives only in the design document? [Complete, Spec §TR-012 / data-model §Column Detail — `llm_invocation`] <!-- Evaluator: Resolved — added TR-044 (nullability contract, four conditionally-absent fields with stated conditions, carried by column constraints) to spec.md; VR-021 to data-model.md -->
- [X] CHK002 Does a requirement state the uniqueness key that makes "exactly one row per invocation" determinable, given that a primary key on a gateway-generated identifier excludes duplicate identifiers rather than a second row describing one invocation? [Verifiable, Spec §TR-011 / data-model §VR-001] <!-- Evaluator: Resolved — added TR-045 (one identifier minted per invocation before the first write, reused by spool and reconcile, no database default) to spec.md; VR-022 and an `invocation_id` Column Detail rewrite to data-model.md -->
- [X] CHK003 Is the referential action on the invocation-to-price-version foreign key stated at requirement level for update as well as delete, given that `version_id` is a mutable natural key rather than a surrogate? [Complete (set), Spec §TR-014 / data-model §Entities, §Column Detail `price_table_version_id`] <!-- Evaluator: Resolved — added TR-046 to spec.md; both FKs changed to ON DELETE RESTRICT ON UPDATE RESTRICT in data-model.md §Entities and §Column Detail; VR-023 added -->
- [X] CHK004 Does a requirement fix the value domain of the trace identifier — its format and the rejected all-zero value — and name where a non-conforming caller-supplied identifier is refused, given that refusal at the database converts a caller input error into a fail-closed invocation? [Complete, Spec §TR-031 / data-model §VR-004] <!-- Evaluator: Resolved — added TR-047 (32 lowercase hex, all-zero excluded, refused at the gateway boundary before request construction) to spec.md; `trace_id` Column Detail, VR-024, and the Row-existence precondition updated in data-model.md -->
- [X] CHK005 Are the three columns the design document records as unnamed by TR-012 reconciled with TR-012's own claim that its field list is a superset of every field this spec mandates? [Consistent, Spec §TR-012 / data-model §Disclosed Divergences D-1…D-3] <!-- Evaluator: Resolved — TR-012 already names all three; data-model.md §Disclosed Divergences restated as closed (IDs D-1…D-3 preserved) and §E013 Read Contract gained rows for `invocation_id`, `pricing_timestamp`, `cost_absent_reason` -->
- [X] CHK006 Is the enumerated value domain of the cost-absent reason closed over every path on which cost can be absent, including a pinned price-table version that resolves to no row at all? [Complete (set), Spec §TR-016 / data-model §CD-1, §VR-005] <!-- Evaluator: Resolved — added TR-048 (pin verified before request construction; reason set closed at three) and TR-049 (out-of-range) to spec.md; `cost_absent_reason` CHECK extended with `cost_out_of_range`, CD-1 and CD-5 updated, VR-025 added -->

## Exactly-Once Semantics of the Spool Reconcile

- [X] CHK007 Is the exactly-once reconcile claim decomposed at requirement level into a named idempotency key and a stated conflict action, or does TR-041 assert the property while only the design document names the mechanism that produces it? [Verifiable, Spec §TR-041, §OBJ3 VC14 / data-model §VR-018] <!-- Evaluator: Resolved — added TR-052 (idempotent on the TR-045 identifier; conflict action ignores primary-key conflicts and no other; delete only after commit) to spec.md; VR-018 restated -->
- [X] CHK008 Do the requirements distinguish exactly-once *effect* from exactly-once *delivery*, so a spool row re-inserted after a crash inside the reconcile window is not readable as a violation of the criterion? [Unambiguous, Spec §OBJ3 VC14, §SC-021 / data-model §State & Lifecycle — `invocation_spool`] <!-- Evaluator: Resolved — TR-052 states the property is of the effect, not delivery, and names the re-insert-after-crash path as conforming; VR-018 now asserts it as a passing test rather than leaving it inferable -->
- [X] CHK009 Is the drain trigger stated as a determinable condition, including the disposition of spooled records when no further invocation occurs after the outage ends? [Complete, Spec §TR-041, §OBJ3 VC14] <!-- Evaluator: Resolved — added TR-053 (drain at the start of every invocation that opens a connection; no timer, thread, or background process; records stay durably spooled until one occurs; depth logged) to spec.md; data-model.md §State & Lifecycle gained a Drain trigger subsection -->
- [X] CHK010 Is concurrent access to the spool by more than one gateway process addressed by a requirement, given that two drains over one local file could each attempt the same reconcile and the same local delete? [Complete, Spec §TR-041, §Edge Cases / data-model §invocation_spool] <!-- Evaluator: Resolved — TR-053 requires concurrent-drain tolerance and names why it holds (local transactions serialize claims, conflict action absorbs the duplicate, delete of a deleted row is a no-op); data-model.md §Concurrency added -->
- [X] CHK011 Does a requirement state the consequence of a reconcile that fails a referential check — whether the failure surfaces to the unrelated invocation whose connection triggered the drain, and whether the spool row is retained? [Complete, Spec §TR-041 / data-model §VR-019] <!-- Evaluator: Resolved — added TR-054 (row retained, logged error naming constraint and invocation id, does NOT fail the triggering invocation, drain continues); VR-019 restated with the isolation test -->
- [X] CHK012 Is a compatibility rule stated for the spool payload's schema version, so a spool written before a gateway upgrade has a defined outcome rather than an undefined read? [Complete, Spec §TR-041 / data-model §Entities `invocation_spool`] <!-- Evaluator: Resolved — TR-054 requires a drain to reconcile only understood payload versions and to retain an unrecognised one as a loud error; `payload_schema_version` column note updated in data-model.md -->

## Migration Idempotency and the Claimed `0100`–`0199` Range

- [X] CHK013 Is "idempotent on re-run" defined by an observable postcondition rather than by the runner completing without error, and is the second mechanism — each file individually re-runnable — carried by a requirement rather than only by the design document? [Able to be validated, Spec §TR-017, §OBJ3 VC1 / data-model §Migrations, §VR-014] <!-- Evaluator: Resolved — added TR-050 (postcondition: ledger and information schema identical after a second run; every file individually re-runnable) to spec.md; data-model.md §Migrations Idempotency bullet restated; VR-027 added -->
- [X] CHK014 Is the runner-owned migration ledger, created outside both claimed ranges, excluded from SC-015's "100% of applied migration numbers fall within `0100`–`0199`" by a stated denominator? [Correct, Spec §SC-015, §TR-018 / data-model §Migrations] <!-- Evaluator: Resolved — SC-015 restated with an explicit denominator (numbered files in this epic's source directory), excluding the runner-owned ledger and E003's `0001`–`0099`; data-model.md §Migrations gained an "SC-015's denominator" bullet -->
- [X] CHK015 Does a requirement name the enforcement point for the `0001`–`0099` half of the range split, and state the behaviour of the range check when the other epic's migration directory is not yet present? [Feasible, Spec §TR-018, §Assumptions / plan §IP-008 / data-model §VR-013] <!-- Evaluator: Resolved — added TR-051 (check enumerates every configured source directory; an absent directory is reported not-present and skipped, never a silent pass or a failure) to spec.md; §Migrations Range bullet and VR-028 updated -->
- [X] CHK016 Is "forward-only" given a detection mechanism at requirement level — a checksum over an already-applied file — or asserted as a property that nothing observes? [Verifiable, Spec §TR-017 / data-model §Migrations] <!-- Evaluator: Resolved — TR-050 requires a per-file checksum in the ledger and a failing run when an already-applied file's checksum changes; §Migrations Forward-only bullet restated as detected rather than asserted -->
- [X] CHK017 Are the re-run semantics of the seed migration stated, so that a corrected rate in a seed file not being applied on re-run is a decided outcome rather than a side effect of conflict-ignoring inserts? [Complete, Spec §TR-017 / data-model §Migrations `0103`] <!-- Evaluator: Resolved — TR-050 makes a rate correction a new higher-numbered migration and an edited applied seed file a checksum failure; data-model.md §Migrations gained a "Seed re-run semantics" bullet -->

## Monetary Precision and Rounding Order for Cost

- [X] CHK018 Is the decimal scale for stored cost and for each rate column fixed by a requirement, or does TR-014 demand exact reproduction while the scale and the rounding mode appear only in the design document? [Precision, Spec §TR-014, §TR-015 / data-model §CD-2, §Column Detail — price tables] <!-- Evaluator: Resolved — added TR-049 fixing cost at scale 10, rates at scale 6, and ROUND_HALF_EVEN as requirement-level rules; data-model.md rate-column note now cites TR-049 rather than owning the decision -->
- [X] CHK019 Is the sum-then-quantize ordering stated as a normative rule at requirement level, given that per-term rounding produces a different figure that satisfies TR-014 read literally? [Unambiguous, Spec §TR-014, §SC-006 / data-model §CD-2 / plan §HINT-004] <!-- Evaluator: Resolved — TR-049 makes sum-at-full-precision-then-quantize-once normative and per-term quantization a defect; CD-2 extended to the multi-attempt case; VR-026 requires a property-based test that fails a per-term implementation -->
- [X] CHK020 Is SC-017's "cost equals the sum of its attempts' spend" expressed at a precision consistent with the single-quantization rule, given that quantizing each attempt separately and summing can differ from quantizing the total once? [Consistent, Spec §SC-017 / data-model §CD-2] <!-- Evaluator: Resolved — SC-017 restated as the invocation's total spend: token counts summed across attempts first, then priced and quantized once per TR-049, never per-attempt costs quantized and added -->
- [X] CHK021 Is the currency of every monetary value stated as a requirement, rather than carried implicitly by column naming and a single-currency assumption recorded in the design document? [Precision, Spec §TR-014, §TR-015 / data-model §Column Detail — price tables] <!-- Evaluator: Resolved — TR-049 states United States dollars as the single currency of this epic and requires a visible schema change for a second; `cost_usd` Column Detail updated -->
- [X] CHK022 Is the representable range of a stored cost stated together with the behaviour when a computation exceeds the declared precision, given that token counts carry a lower bound and no upper bound? [Complete, Spec §TR-014 / data-model §Entities `cost_usd`] <!-- Evaluator: Resolved — TR-049 states the range and requires an out-of-range value to be recorded absent with reason rather than truncated; data-model.md adds CD-5, the `cost_usd` range note, and a third `cost_absent_reason` value `cost_out_of_range` -->
- [X] CHK023 Is "reproduces the stored cost exactly" defined as decimal equality at a stated scale with a stated prohibition on any binary-float intermediate on either the read or the recompute path, so the criterion is decidable? [Able to be validated, Spec §SC-006, §OBJ3 VC2 / data-model §CD-3] <!-- Evaluator: Resolved — TR-049 carries the definition and SC-006 was restated to name decimal equality at scale 10 with no binary-float intermediate on either path; CD-3 updated to match -->

## Append-Only and Immutability Claims

- [X] CHK024 Is the immutability of the invocation record — never updated, never deleted — carried by a requirement, or asserted only in the spec's Key Entities prose and the design document's lifecycle table? [Traceability, Spec §Key Entities ModelInvocation / data-model §State & Lifecycle] <!-- Evaluator: Resolved — added TR-055 (no UPDATE or DELETE against the invocation table or either price table; enforcement disclosed as convention, code review, and TR-046's referential restriction, not a trigger or revoked grant) -->
- [X] CHK025 Is the append-only claim on the price tables stated with a named enforcement point or explicitly labelled as held by convention, and does that disclosure appear where the claim is made rather than only in the lifecycle section? [Verifiable, Spec §Key Entities PriceTableVersion, PriceTableEntry / data-model §State & Lifecycle] <!-- Evaluator: Resolved — TR-055 requires the disclosure to appear wherever the claim is made; the label was added inline to spec.md §Key Entities (all three entities) and to both price-table rows and the `llm_invocation` row of data-model.md §Entities -->
- [X] CHK026 Is "append-only" defined for the spool, given that TR-041 names it append-only while the reconcile lifecycle deletes from it after a successful commit? [Unambiguous, Spec §TR-041, §Glossary Spool / data-model §State & Lifecycle — `invocation_spool`] <!-- Evaluator: Resolved — TR-052 defines it as "no spooled row is ever updated in place"; the Glossary Spool entry now carries the same definition and states that the reconcile's delete is the terminal step of the property, not an exception to it -->
- [X] CHK027 Is the rule that a rate change is a new version rather than an edit stated as a requirement, given that every historical cost recomputation depends on it holding? [Necessary, Spec §TR-014, §TR-039 / data-model §price_table_version — append-only] <!-- Evaluator: Resolved — TR-055 states it as a requirement with the recomputation dependency as its reason; TR-050 extends the same rule to the seed migration and TR-046 blocks the identifier-edit route -->
- [X] CHK028 Is unbounded retention stated together with the condition that would reverse it, so the absence of any deletion or aggregation path is a recorded scope decision rather than an omission? [Complete, Spec §Technical Constraints / data-model §State & Lifecycle] <!-- Evaluator: Resolved — spec.md §Technical Constraints retention bullet now states the reversal condition (invocation table above one million rows, or E013's panel ordering query missing its latency budget) and that neither is reachable at the stated scale -->

## Aggregation Across Attempts — Token Counts and Latency

- [X] CHK029 Does the aggregation rule state how an attempt reporting no usage — a transport failure that returns no response body — contributes to the summed token counts, or does "sum across every attempt" leave that term undefined? [Complete, Spec §TR-040 / data-model §Column Detail — token columns] <!-- Evaluator: Resolved — added TR-056: an attempt returning no response body contributes zero to each billing class, so the sum is defined over every attempt; token-column note and VR-029 updated -->
- [X] CHK030 Are the start and stop points of the aggregated latency interval fixed by a requirement, so it is determined whether validation, retry backoff, repair-prompt construction, and the record write fall inside it? [Precision, Spec §TR-040, §TR-012 / data-model §Column Detail `duration_ms`] <!-- Evaluator: Resolved — TR-056 fixes the interval from the start of the first attempt to the terminal outcome, placing validation, backoff, and repair-prompt construction inside it and the record write outside it -->
- [X] CHK031 Is the unit and numeric type of the recorded latency stated at requirement level, given that TR-012 names "latency" with no unit while the design document fixes integer milliseconds for an equality-assertion reason? [Precision, Spec §TR-012, §TR-040 / data-model §Column Detail `duration_ms`] <!-- Evaluator: Resolved — TR-056 states an integer count of milliseconds taken from a monotonic clock -->
- [X] CHK032 Is the aggregation rule stated for `replay` mode, so it is determined whether recorded token counts come from the fixture's provenance record and whether latency measures the replay or the original recording? [Complete, Spec §TR-033, §TR-037, §TR-040 / data-model §ER Diagram RESPONSE_FIXTURE] <!-- Evaluator: Resolved — TR-056 takes replay token counts from the fixture's provenance record (so a replayed cost is reproducible) and measures latency on the replay execution itself; `duration_ms` and token Column Detail rows updated -->
- [X] CHK033 Is the name of the aggregated latency field consistent across spec, plan, and data model, given that the plan's data-model summary and the data model itself use different column names for it? [Consistent, plan §Data Model Summary / data-model §Field Naming Alignment] <!-- Evaluator: Resolved — plan.md §Data Model Summary corrected from `latency_ms`/`cost` to `duration_ms`/`cost_usd`; a one-name-only note added to data-model.md §Field Naming Alignment and HINT-007 added to plan.md -->
- [X] CHK034 Does a requirement state that a fixture lookup counts as a transport attempt, so the attempt-count lower bound holds on replay rows without inference from the glossary? [Traceability, Spec §Glossary Attempt, §TR-010 / data-model §Column Detail `transport_attempt_count`] <!-- Evaluator: Resolved — TR-056 states it as a requirement; the `transport_attempt_count` Column Detail row now cites TR-056 instead of the Glossary -->

## Price-Lookup Determinism

- [X] CHK035 Is the comparison between a timestamp-with-zone pricing timestamp and a date-typed effective-from stated together with the time zone that governs it, given that the zone decides which side of a boundary an invocation falls on? [Precision, Spec §TR-039, §TR-043 / data-model §CD-1] <!-- Evaluator: Resolved — added TR-057 requiring the comparison as UTC calendar dates; CD-1 changed from `pricing_timestamp::date` (which resolves against the session TimeZone) to `(pricing_timestamp AT TIME ZONE 'UTC')::date`; `effective_from` Column Detail, VR-030, and plan HINT-006 added -->
- [X] CHK036 Is the conversion from a fixture's recording date to the stored pricing timestamp specified, so a replayed invocation resolves one entry rather than depending on how a date is widened to an instant? [Complete, Spec §TR-043, §OBJ3 VC15 / data-model §CD-4, §Disclosed Divergences D-1] <!-- Evaluator: Resolved — TR-057 requires the recording date to be widened to midnight UTC of that date when stored; CD-4 and the `pricing_timestamp` Column Detail row state the widening explicitly -->
- [X] CHK037 Is model matching in the price lookup stated as exact equality on the resolved model identifier, including case sensitivity, rather than only as a prohibition on nearest-match fallback? [Unambiguous, Spec §TR-039, §TR-016 / data-model §CD-1] <!-- Evaluator: Resolved — TR-057 states exact, case-sensitive equality with no normalization, prefix match, or fallback; CD-1 and a new `price_table_entry.model_id` Column Detail row carry the positive half alongside TR-016's prohibition -->
- [X] CHK038 Does a requirement cover the case where the pinned price-table version identifier resolves to no row at all, given that the enumerated absent-cost reasons cover only a missing entry and an unresolved model, and that a non-null foreign key makes this a write failure rather than a recorded absence? [Complete (set), Spec §TR-016, §TR-039 / data-model §CD-1, §Column Detail `price_table_version_id`] <!-- Evaluator: Resolved — TR-048 requires the pin to be verified before any request is constructed, making it a configuration error on an invocation that never billed rather than a non-null-FK write failure after one did; Row-existence precondition, CD-1, and VR-025 updated -->
- [X] CHK039 Is the determinism of the lookup traced to the uniqueness key that makes a tie unrepresentable, rather than resting on an ordering that a duplicate model-and-date row inside one version would make arbitrary? [Verifiable, Spec §TR-039, §TR-015 / data-model §VR-010, §Column Detail — price tables] <!-- Evaluator: Resolved — TR-057 requires (model identifier, effective-from) to be unique within a version and states that determinism rests on uniqueness rather than on the ORDER BY … LIMIT 1; CD-1 restated to say the same; spec §Key Entities PriceTableEntry updated -->
- [X] CHK040 Is the currentness of the pinned price table addressed — how a pin predating a rate change or a newly resolved model is surfaced — given that its symptom is an absent cost rather than an error? [Complete, Spec §TR-016, §TR-039 / data-model §price_table_version] <!-- Evaluator: Resolved — added TR-058 requiring every absent-cost row to emit a warning naming the pinned version, the resolved model, and the reason, so a stale pin is visible when it first bites; VR-031 added -->

# Testing: Document Ingestion and Extraction
**Created**: 2026-07-27 | **Feature**: [spec.md](../spec.md)

## Oracles and Ground Truth

- [ ] CHK001 Is the extraction reference set named in a requirement rather than only inside two success criteria, given FR-050 fixes a baseline and FR-060 fixes a denominator while no functional requirement names the corpus generator's pre-render document model as the ground truth every accuracy figure is scored against? [Complete, Spec §FR-050 / §FR-060 / §SC-013 / §SC-027]
- [ ] CHK002 Is the circular oracle excluded by requirement — a stored value compared against the chunk text it was read out of, which agrees whenever the parse is self-consistent and is silent on whether the parse is right — or is the pre-render reference distinguished from the parsed text only by the wording of SC-013? [Correct, Spec §SC-013 / §SC-027 / §FR-025]
- [ ] CHK003 Does "an independent extraction of the page it names" state what the extraction is independent of — a separate parse rather than the cached page text the chunker cut the chunk from — given FR-008 pins both sides to one set of tolerances and one normalization form? [Unambiguous, Spec §FR-010 / §FR-008 / plan §Risk Mitigation]
- [ ] CHK004 Is SC-037 stated as a comparison between two separately computed derivations, or is it satisfied by construction the moment page attribution and the corpus's structural derivation call the same function, in which case the criterion asserts nothing about either? [Verifiable, Spec §SC-037 / §FR-008]
- [ ] CHK005 Where are the parse signals a confidence is recomputed from recorded, given SC-026 names "the signals recorded with it" while E006 adds no signal column and leaves `extracted_value` unaltered, so the alternate-label and repaired-invocation signals have no stated home? [Complete, Spec §SC-026 / §FR-046 / §FR-057 / data-model §Scope]
- [ ] CHK006 Is the deterministic baseline required to be authored independently of the generator's own template definitions, given a template extractor reading the templates the corpus was generated from scores itself and its defeat carries no information about the model? [Correct, Spec §FR-050 / §SC-029 / plan §AD-012]
- [ ] CHK007 Is the set of claims resting on human inspection enumerated, or does FR-011 govern a population that may be empty, making SC-032's "the inspected count and error bound of any sampled claim" satisfiable with nothing inspected and nothing bounded? [Able to be validated (set), Spec §FR-011 / §SC-032]
- [ ] CHK008 Is SC-011's oracle stated as a reconciliation between the run's attempted extractions and the invocations actually recorded, given a static import contract establishes that no other module *can* reach the provider and produces no count of the requests that were made? [Verifiable, Spec §SC-011 / §FR-023 / §FR-048]

## Total Versus Sampled Claims

- [ ] CHK009 Is the method by which FR-011's error bound is computed fixed in the requirement, or does "computed by a stated method" leave the choice to whoever writes the report, so two runs may publish bounds that are not comparable? [Unambiguous, Spec §FR-011 / research §Validating parser page attribution]
- [ ] CHK010 Does every criterion phrased as "100%" or "zero" state whether it holds over the whole population or over an inspected subset, given SC-002, SC-013, and SC-027 say so explicitly while SC-003, SC-039, SC-040, SC-045, and SC-046 do not? [Complete (set), Spec §Success Criteria]
- [ ] CHK011 Is FR-061's similarity threshold required to be declared before the run, given a threshold chosen after the clusters are observed makes the published near-duplicate count a fitted figure, which is the discipline FR-032 imposes on the confidence floor for the same reason? [Correct, Spec §FR-061 / §SC-048 / §FR-032]
- [ ] CHK012 Is the observation basis stated for SC-041's "zero runs fail on an over-long leaf that a sentence-level split could have resolved", given a run that failed wrote no rows to inspect and the counterfactual is a property of no stored artifact? [Verifiable, Spec §SC-041 / §FR-014]
- [ ] CHK013 Does any rule oblige each total check to report the population it asserted over and to fail on an empty one, given a containment check over zero chunks and a text-agreement check over zero text-kind values both report success? [Able to be validated (set), Spec §FR-010 / §SC-002 / §SC-027]

## Denominators

- [ ] CHK014 Is precision's denominator stated, and F1's, given FR-060 fixes recall's denominator on the generator's printed-field set and leaves the other two figures to be denominated by inference? [Complete, Spec §FR-060 / §SC-047]
- [ ] CHK015 Is SC-008's "100% of extracted values carry a page citation and a per-field confidence" denominated on attempted fields rather than on stored rows, given non-nullable columns make the stored-row form true of every row that could exist and false of nothing? [Verifiable, Spec §SC-008 / §FR-029 / §FR-030]
- [ ] CHK016 Is the distribution FR-033 publishes denominated on every computed score including those the floor rejected, or only on stored values, which SC-045 confines to at most three distinct scores and which therefore conceal exactly what the floor excluded? [Complete, Spec §FR-033 / §SC-017 / §SC-045]
- [ ] CHK017 Is the counting unit stated for the valid, repaired, and failed figures and for any failure rate, given FR-026 budgets repair per invocation, FR-058 attempts a field subset per chunk, and FR-037 records a whole-document absence once per document — three units in one table? [Unambiguous, Spec §SC-018 / §FR-058 / §FR-037 / §SC-039]
- [ ] CHK018 Do the chunk-population criteria state which generation they are denominated on, given at most one superseded generation is retained per document and SC-002, SC-003, SC-031, and SC-038 speak of "every chunk in the corpus" while the database may legitimately hold two generations of one document? [Consistent (set), Spec §SC-002 / §SC-003 / §FR-055 / data-model §v_active_ingestion_generation]
- [ ] CHK019 Is "per layer" well defined for the real layer in FR-060 and SC-047, given SC-012 fixes its extracted-value count at zero, so every per-field proportion on that layer has a zero denominator and no interval? [Correct, Spec §FR-060 / §SC-047 / §SC-012]
- [ ] CHK020 Does FR-009's zero-recognition-error claim state the population it holds over, given the Assumptions record that the corpus datasheet governs only the synthetic layer while the requirement asserts the property of the corpus text layer as a whole? [Consistent, Spec §FR-009 / §Assumptions / §Disclosed Limitations]
- [ ] CHK021 Are printed fields the extractor never attempts counted in recall's denominator, given FR-058 narrows attempts to a declared transmittal subset while FR-060 denominates recall on everything the generator recorded as printed? [Unambiguous, Spec §FR-058 / §FR-060 / §SC-047]

## Test-First and Property-Based Obligations

- [ ] CHK022 Is the strict test-first obligation over `model/compute/confidence`, `coerce`, and `metrics` carried by a requirement or a success criterion, or does it exist only as a row in the plan's Testing Strategy that task generation is under no stated obligation to honour? [Complete, plan §Testing Strategy Property / project-instructions §Testing & Quality Policy]
- [ ] CHK023 Is an observable artifact named that shows a computation module's tests existed and failed before the module existed, or is red-green-refactor an ordering claim no reviewer can judge once the branch is squash merged? [Verifiable, plan §Testing Strategy Property / §Instructions Check Testing & Quality Policy]
- [ ] CHK024 Is the boundary of "deterministic computation module" stated for this epic, given the chunker ladder, the tokenizer budget, and the input-tuple digest are all deterministic pure computation while the plan assigns them to the test-after unit tier and the three `compute` modules to the mandate? [Unambiguous, plan §Testing Strategy Unit / §Testing Strategy Property / project-instructions §Testing & Quality Policy]
- [ ] CHK025 Does each property over the computation modules declare its relation class — round-trip, invariant, metamorphic, or alternate implementation — or is "property-based tests over pure functions" the whole statement of what those tests assert? [Verifiable, plan §Testing Strategy Property / §Requirement Coverage Map FR-031]
- [ ] CHK026 Is the generator domain stated for the confidence properties, and is exhaustive enumeration required rather than sampling, given FR-057's three binary signals admit exactly eight scores and a sampled property over an eight-point domain is strictly weaker than covering it? [Appropriate, Spec §FR-057 / §SC-045 / plan §AD-008]

## Offline Reproducibility and Fixture Discipline

- [ ] CHK027 Are the fixture-regeneration triggers stated as a requirement, given the Assumptions record that any change to prompt text or to a schema constraint invalidates every existing fixture while no requirement obliges the re-record or names who observes that the trigger fired? [Complete, Spec §Assumptions / §FR-045 / §FR-056]
- [ ] CHK028 Is the fixture resolution key stated, so a changed prompt or output schema resolves to a miss and the named run-level failure of FR-056 rather than to a stale response that replays silently under the new prompt? [Correct, Spec §FR-045 / §FR-056 / §SC-044]
- [ ] CHK029 Does FR-043's input tuple account for the provider model, given the tuple's five members omit it, so a run under a different model skips every document whose tuple is unchanged and replays fixtures recorded against the previous model while `ingestion_run.provider_model` records a change nothing acts on? [Consistent, Spec §FR-043 / §FR-038 / data-model §ingestion_run]
- [ ] CHK030 Is the observation window for SC-023's zero provider calls and zero network access stated — whether it opens before the ingestion package is imported or when the run begins — given an artifact fetched at import time falls outside the narrower window? [Verifiable, Spec §SC-023 / §FR-045 / §FR-019]
- [ ] CHK031 Does FR-019's "pinned model identity and revision" state where the encoder artifact and its tokenizer resolve from, given a pinned upstream revision is a network resolution unless the artifact is committed and integrity-checked, and given chunk boundaries depend on that tokenizer? [Complete, Spec §FR-019 / §FR-014 / plan §Complexity Tracking]

## Coverage Denominator

- [ ] CHK032 Is the coverage-denominator obligation carried by a requirement or criterion and its resulting source set stated literally, given the spec states no coverage criterion and the plan records only that an enumeration which overrides rather than merges "must be appended" to? [Complete, plan §Testing Strategy Coverage / project-instructions §Testing & Quality Policy]
- [ ] CHK033 Is a per-package floor stated for the packages this epic adds, given a single combined figure lets already-covered packages carry a newly added one across the threshold without any of its lines being exercised? [Verifiable, plan §Testing Strategy Coverage / project-instructions §Testing & Quality Policy]
- [ ] CHK034 Is FR-010's total page-attribution check owned by the ingestion job or by the test suite, given the requirement states a system obligation while the coverage map discharges it in a test module whose path disagrees with the one the project structure declares? [Consistent, Spec §FR-010 / plan §Requirement Coverage Map FR-010 / §Project Structure]

## Determinism and Reproducibility Conditions

- [ ] CHK035 Is what constitutes a chunker-version bump stated in a requirement, given the sentence segmenter's version, the ladder's structural detection, and the boundary-class rules all move boundaries while only research and a data-model column note record the segmenter case? [Complete, Spec §FR-017 / research §Deterministic sentence segmentation / data-model §ingestion_run.chunker_version]
- [ ] CHK036 Does FR-017's guarantee survive a tokenizer revision change, given chunk length is measured in the encoder's own tokenizer and the encoder revision is an input-tuple member distinct from the chunker version, so boundaries can move with the chunker version unchanged? [Correct, Spec §FR-017 / §FR-014 / §FR-043]
- [ ] CHK037 Under what condition is SC-007 observable, given FR-043 obliges a re-run over an unchanged input tuple to skip every document and create no rows, so the second run of the comparison produces no boundaries to compare against the first? [Verifiable, Spec §SC-007 / §FR-043 / §SC-025]
- [ ] CHK038 Which environment dimensions must differ between the two runs SC-007 compares — checkout path, process identity, hash seed, locale, timezone, filesystem enumeration order — or is the criterion satisfied by one command issued twice from one directory? [Complete, Spec §SC-007 / §FR-017]
- [ ] CHK039 Is the arithmetic form of the confidence computation fixed — the order the deductions are applied and the representation they are held in — given SC-026 demands exact reproduction of a stored `double precision` value while 0.15, 0.10, and 0.25 have no exact binary form and two subtraction orders need not agree in the last bit? [Unambiguous, Spec §SC-026 / §FR-057 / data-model §ingestion_run.confidence_floor]
- [ ] CHK040 Is chunk identity required to be a function of the chunk's content and position rather than of the run that wrote it, given FR-018 publishes "a chunk identifier is a function of the chunker" as a contract E008 is told to key its evaluation set around? [Correct, Spec §FR-018 / §SC-007 / §Disclosed Limitations]

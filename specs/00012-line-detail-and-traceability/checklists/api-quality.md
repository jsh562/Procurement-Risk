# API Quality Requirements: Line Detail and Traceability
**Created**: 2026-07-30 | **Feature**: [spec.md](../spec.md)

## Contract Completeness

- [X] CHK001 Is the obligation to publish the rank the precedence rule applied stated by any requirement or criterion, given FR-042 fixes the order and SC-018 asks only that exactly one resolution state be reported? [Completeness, Spec §FR-042, §SC-018] <!-- Evaluator: Resolved — added FR-061 to spec.md requiring the applied precedence rank reported with the resolution state, on the ground E010 uses for `sort.tiebreak` and `staleness_basis`: a rule the server applied and did not state is one a reader cannot check. contracts/openapi.yaml §resolution_precedence cites it; tasks T006 and T018 carry it -->
- [X] CHK002 Is the artifact identification a response must carry closed by a requirement, given FR-013 names four members, FR-038's locus clause names seven as outside the figure domain, and the adopted run-metadata envelope requires ten? [Completeness, Spec §FR-013, §FR-038] <!-- Evaluator: Resolved — FR-067 closes it by adoption and reconciles the three counts rather than editing FR-013 or FR-038: FR-013's four are the floor, FR-038's seven are an exclusion, and E010's run-metadata envelope is the extent. The members beyond FR-013's four are there because FR-045 adopts E010's staleness rule, FR-042 compares a roster and FR-006/FR-008 read a horizon. contracts §ForecastRunMeta and §Meta cite it -->
- [X] CHK003 Is publishing the match fields identity resolution turns on — manufacturer and part number — owned by a requirement, given no FR names them and their inclusion rests on what a reader in the unresolved state is presumed to want? [Completeness, Spec §FR-026, §FR-027] <!-- Evaluator: Resolved — added FR-060: the line's own record MUST carry the manufacturer and the part number, so a coordinator meeting the unresolved state sees the values resolution had to work with. Their presence now rests on a requirement rather than on a presumption about what a reader wants. contracts §LineIdentity and task T007 carry it -->
- [X] CHK004 Is the omission of criticality from the response owned by a requirement, given Key Entities names it on the line under inspection and its exclusion rests on FR-038's five classes plus another epic's classification of it? [Consistency, Spec §Key Entities, §FR-038] <!-- Evaluator: Resolved — FR-060's second half forbids criticality here and states the ground: no requirement puts it on this view, Key Entities describes the entity read, E017 owns the override and E010 renders it as explanatory context. The contract's earlier ground — scalar, therefore a sixth class — was the *adjective* reading FR-038 explicitly disclaims, and is corrected in contracts/openapi.yaml and plan.md § API Surface Summary; a correct absence resting on a wrong reason is one argument from being reversed -->
- [X] CHK005 Is the form per-field confidence must take — carrying its basis, and distinguishable from the real frequencies rendered beside it — required anywhere, or does FR-018 require only that a confidence be carried? [Completeness, Spec §FR-018] <!-- Evaluator: Covered by spec.md §FR-051, added by the UX pass: the confidence must be distinguished in reader-facing terms from the frequencies published beside it, named a self-reported extraction score rather than a probability of anything, never rendered as a percentage, and must carry what it is a score of at the point it is shown — which contracts §LinkedRecord.confidence implements as value/display/basis -->
- [X] CHK006 Is the view's obligation to honour a session what-if made on another surface stated as a requirement, given Scope excludes editing and the pass-through rests on a plan decision's reading of FR-010? [Completeness, Spec §Scope, §FR-010; Plan §AD-008] <!-- Evaluator: Resolved — added FR-062: where an adjustment is carried across the navigation the view MUST compute its figures against it, Scope excluding the adjustment *control* and not the honouring of one already made. FR-054 already stated what the reader is then told; the honouring itself rested on AD-008 alone. contracts §NeedByOverride and task T016 carry it -->
- [X] CHK007 Does FR-014 bind the response or only the rendered view, so that assembling the structured equivalent from the same members the figures render from is a stated obligation rather than a design inference? [Clarity, Spec §FR-014, §FR-016] <!-- Evaluator: Covered by spec.md §FR-048, added by the UX pass: the equivalent MUST be composed from the same published figures the plot and the cumulative view render from and MUST NOT be a second, separately composed copy — which is the reason contracts/openapi.yaml carries no `textual_equivalent` member, now discharging a requirement rather than a design inference -->


## Structural Enforceability

- [X] CHK008 Do the requirements ask for the prohibited quantities to be *unrepresentable* in the response rather than merely absent from it, given FR-011 and FR-012 forbid what the response carries while the plan and contract both claim the prohibition is enforced structurally? [Completeness, Spec §FR-011, §FR-012; Plan §Requirement Coverage Map] <!-- Evaluator: Resolved — added FR-064: the prohibitions MUST be enforced by the *shape* of the published contract, the prohibited quantities unrepresentable rather than merely absent, with the enforcement asserted by a check that runs in the merge gate. A prohibition on what a response happens to contain is checked once; one on what it *can* contain is rechecked by every validation afterwards. tasks T017 and T044 carry the assertions -->
- [X] CHK009 Is FR-012's prohibition on a *class* of quantities reducible to a list of member names, and does any requirement state how it stays checkable when a later member could carry a central summary under a name no list anticipates? [Testability, Spec §FR-003, §FR-012] <!-- Evaluator: Resolved — FR-064 states that the closure of every object, not the name list, is the mechanism: a central summary arriving under a name no list anticipates is a schema violation, and adding a member to a closed object is a breaking change that reopens the check deliberately, the device FR-038 uses for a sixth class. contracts §x-prohibited-members is rewritten to say the published list is the assertion's aid rather than the mechanism -->
- [X] CHK010 Does FR-012's "a quantile carried without the member that pairs with it" name which member pairs — the other quantile of the labelled pair, or the complementary frequency FR-004 requires — so the requirement is not dependent on both readings being implemented to be safe? [Clarity, Spec §FR-004, §FR-012] <!-- Evaluator: Resolved — added FR-065 fixing the phrase, in FR-003 and FR-012 alike, as meaning **both** members and not either: the complementary frequency FR-004 requires *and* the other quantile of the labelled pair. The contract already enforced both (`LabelledQuantile.later_share`, `QuantilePair` requiring both members); the requirement no longer depends on both readings happening to be implemented to be safe. Fixed by adding a requirement rather than editing FR-003 and FR-012, whose enumerations are declared deliberately identical -->
- [X] CHK011 Do the requirements state how a withheld figure is encoded — a member absent rather than null, zero or a sentinel — given FR-006 and FR-031 say only "withheld" and the absence-not-null rule appears in a plan hint and the contract? [Completeness, Spec §FR-006, §FR-031; Plan §HINT-004] <!-- Evaluator: Resolved — added FR-066: withholding is encoded as the absence of the member that would have carried it, never a null, zero, empty string, dash or other sentinel, with the state naming the withholding carried as text under FR-040 so absence is never the sole carrier. HINT-004 and contracts §MissMass/§Covariate now discharge a requirement; tasks T014 and T040 carry it -->
- [X] CHK012 Is the fifty-mark count required of the response as well as of the plot, and is the agreement between a declared count and the length of the mark series owned by a requirement rather than by two independent schema constructs? [Consistency, Spec §FR-001, §FR-002, §FR-011] <!-- Evaluator: Covered by spec.md §FR-011, which binds the response in terms — "the response carries the resolution the figures need — fifty marks and a banded cumulative series" — with FR-001 binding the plot and FR-002/SC-027 fixing the count against line, run and stored draw count. Both the declared count and the series length are fifty by requirement rather than by construction, so their agreement follows from the requirement; contracts §MarkSeries records that the `const` and the array bounds state one fact to a reader and to a validator -->
- [X] CHK013 Is the bounded percentage form required for the cumulative bands' delivered-by shares, given FR-041 closes its list at three figures and SC-020 at five, and neither names them? [Completeness, Spec §FR-041, §SC-020] <!-- Evaluator: Covered by FR-041's form clause read with FR-052, added by the UX pass. FR-038's band note fixes the identity — the first bands' masses *are* the labelled quantile shares and the last is the complement of the residual mass — so each `delivered_by` is either a figure FR-052 binds or a complement, and FR-041's form requires "the complement bounded with them". The bands introduce no figure outside SC-020's five, which is the same reason they introduce no sixth class -->
- [X] CHK014 Is the obligation that the figures and the distribution encoding be *present* wherever the figures-render state is reported owned by a requirement, or do the requirements state only what is absent in the other four states? [Completeness, Spec §FR-028, §FR-042] <!-- Evaluator: Covered by FR-042's fifth resolution member, which is named for the state in which the figures render, read with FR-001's "MUST plot" and FR-011's statement of what the response carries; FR-028 adds presence across both identity states. Absence in the other four is FR-042's and SC-004's, so the contract's "present exactly when" is jointly owned rather than invented — and FR-063 now binds the same five classes to the response -->
- [X] CHK015 Does FR-038 close what the *view renders* or what the *response carries*, so that a five-class cap on the payload discharges a requirement rather than extending one? [Clarity, Spec §FR-038, §FR-039] <!-- Evaluator: Resolved — FR-038 closes the *view's* figures by locus, and a payload has no regions, so the contract's cap was extending it. Added FR-063, the payload counterpart in the same five classes with each class's members at stated response locations — the pairing FR-003 and FR-012 already use for the screen and the response, applied to FR-038. contracts §StatedFigures and §x-figure-classes cite it; T044 asserts it -->
- [X] CHK016 Is the prohibition on a figure appearing in an error body stated by a requirement, given the one object admitting unspecified members is the problem document and FR-037's enumeration is written over "the response the view renders from"? [Completeness, Spec §FR-011, §FR-037] <!-- Evaluator: Resolved — FR-064 states it: the problem document is the one object on this surface admitting members it does not declare, its openness belongs to RFC 9457 and the convention FR-036 adopts rather than being a hole in FR-011 and FR-012, and it MUST carry no figure of any of FR-038's five classes. The contract asserted "it carries no figure"; a requirement now requires it -->
- [X] CHK017 Is the rule that no stored posterior array crosses this boundary owned by a requirement binding surfaces beyond this one, or does this feature assert it only for its own response with the general rule resting on a decision record no check enforces? [Completeness, Spec §FR-011, §SC-007; Plan §Open Items] <!-- Evaluator: Resolved — deliberate; the repository-wide scan is ADR-0025's, with E019 the vehicle -->


## Failure and Degraded-State Coverage

- [X] CHK018 Is "absent data is a state carried in a successful response, not a fault" stated as a requirement, so an identifier naming no line, no active run, and a line the run does not cover are *required* to be successful outcomes rather than planned as such? [Completeness, Spec §FR-042, §FR-044; Plan §Error Handling Strategy] <!-- Evaluator: Resolved — added FR-061: every one of FR-042's five resolution states MUST be reported as the outcome of a successful read and none of them as a failure. SC-004's "never to an error page" covered three of the five and SC-012 asked only for a stated outcome; the requirement now covers all five, so the plan's error table implements an obligation rather than choosing one -->
- [X] CHK019 Do the requirements assign every condition this surface can meet to exactly one vocabulary — a resolution state inside a success, or a fault — given FR-042 closes the state set at five and names neither an unreadable datastore nor an unrecognised artifact schema version? [Completeness, Spec §FR-042; Plan §Error Handling Strategy] <!-- Evaluator: Resolved — FR-061's converse half assigns a condition in which the system *could not answer* — an unreadable datastore, an artifact schema version this build does not read — to FR-036's failure vocabulary, forbids admitting it to FR-042's set, and requires the two vocabularies between them to cover every condition this surface can meet with no condition in both. contracts §InternalError's "not a sixth resolution state" now cites it -->
- [X] CHK020 Is the annotation set's membership consistent across the artifacts, given FR-042 enumerates four annotations including the calendar-passed state carried from the worklist while the plan's coverage map and the contract's enumeration each carry three? [Consistency, Spec §FR-042; Plan §Requirement Coverage Map] <!-- Evaluator: Covered — the four artifacts now agree at four: spec §FR-042 (the stale-run, beyond-horizon, at-or-before-anchor and calendar-passed states), plan.md's coverage-map FR-042 row ("zero or more of four, incl. calendar-passed"), contracts §AnnotationState's four-member enum, and §x-states.annotation_admissible_sets' eight sets with §LineDetailResponse.annotations' prose. FR-053 puts calendar-passed's copy in the committed table and T010 exports it -->
- [X] CHK021 Do the requirements state which annotation combinations are admissible and which are contradictory, given FR-042 says "zero or more" and "determined independently" while the contract admits six sets and caps the array at two? [Completeness, Spec §FR-042] <!-- Evaluator: Covered by FR-042's own definitions, which determine admissibility without an extra rule: the three date-derived annotations are conditions on the effective need-by date relative to the as-of date, today and the horizon — beyond the horizon, at or before the anchor, and passed relative to today *while remaining inside the forecast frame* — and are pairwise unsatisfiable together. Stale-run is a property of the run's age and composes with any one of them, which is exactly the eight admissible sets and the `maxItems: 2` the contract records, each exclusion carrying its derivation rather than a stipulation -->
- [X] CHK022 Is the closed cause set for a source that cannot be produced enumerated by a requirement, or does FR-023's "naming the cause" leave both the reasons and their split across two failure statuses to the contract alone? [Completeness, Spec §FR-023, §FR-036] <!-- Evaluator: Resolved — added FR-059: the causes MUST come from **one** enumeration published by the artifact publishing these operations, in the device FR-024 already uses ("the artifact publishing it MUST enumerate the closed set those reasons come from"). The split across two statuses remains a boundary convention, which FR-067 now states is adopted whole rather than read in by the contract -->
- [X] CHK023 Does FR-036's "the same form the serving boundary already establishes" name what it binds — media type, problem document shape, the closed type vocabulary, the status convention — or does reading it as the whole boundary convention rest on an interpretation no requirement states? [Clarity, Spec §FR-036] <!-- Evaluator: Resolved — it rested on an interpretation the contract stated about itself, and that interpretation carried the ETag, the cache directive and the meta envelope with it. Added FR-067, naming what is bound: media type and document shape, the closed condition vocabulary and each condition's status, the single status for a malformed and an inadmissible parameter alike, the conditional-request mechanism FR-068 states, the run-metadata envelope, and the bounded percentage and reference-class forms. contracts' "Nothing at the boundary is re-chosen" now cites the requirement instead of its own reading -->
- [X] CHK024 Does FR-036 name which member carries the cause a reader quotes — the problem type or the per-reason enumeration — so a consumer branching on one is branching on the thing the requirement fixed? [Clarity, Spec §FR-036, §SC-024] <!-- Evaluator: Resolved — FR-067 fixes it: the cause a reader quotes is the problem's **condition**, from the closed vocabulary the contract publishes; where one condition admits several causes — a source that cannot be produced — the specific cause travels beside it in the single enumeration FR-059 fixes. A consumer branching on either branches on something a requirement closed, rather than reading prose to learn which was meant. contracts §Problem.type says so -->
- [X] CHK025 Is a rejected request inside FR-036's "every failure on this view", so a correlation identifier on a malformed-parameter refusal discharges a requirement rather than inheriting another epic's convention? [Completeness, Spec §FR-036, §SC-024] <!-- Evaluator: Resolved — FR-067 states that a request this surface refuses is a failure on this view, reaching a malformed identifier and an inadmissible parameter as well as a failure met after a request was accepted: the reader whose citation would not open and the reader whose link was malformed both have something to quote. contracts §ValidationFailed and §Problem cite it -->
- [X] CHK026 Is a non-application of the pass-through need-by value owned by a requirement that fixes what the reader is told, given the one resolution state in which it goes unapplied is also the state whose response shape admits no member that could report it? [Completeness, Spec §FR-036; Plan §Open Items] <!-- Evaluator: Resolved — FR-086: a non-applied adjustment is reported with its cause -->
- [X] CHK027 Is a per-record page state — and the marking of a citation known in advance not to open — traceable to a requirement, given FR-042 scopes its third set to sections and FR-023 states a failure at the moment a page cannot be produced? [Completeness, Spec §FR-023, §FR-042] <!-- Evaluator: Resolved — FR-059 requires a citation the view already knows will not open to be marked with its cause **before** the reader opens it, so a failure the view held is information rather than an interruption. The per-record state is FR-042's composition rule read at the item level — "the degraded member when any item carries it" presupposes items that carry one — and FR-059 says so rather than leaving it inferred. tasks T027 and T033 carry it -->
- [X] CHK028 Does FR-022's "or its page displayed" state where the synthetic marking must reach a reader viewing a streamed source, given a response header is not present in the accessibility tree FR-040 governs? [Clarity, Spec §FR-022, §FR-040] <!-- Evaluator: Resolved — FR-080: the marking arrives before the source opens -->


## Evidence and Census Declarations

- [X] CHK029 Is the declaration that the request-time span check is a proxy required by any requirement, or does it originate in a plan decision and reach the response as a member no requirement names? [Completeness, Spec §FR-024, §SC-010a; Plan §AD-010] <!-- Evaluator: Resolved — it originated in AD-010 and reached `CensusFigure.required` with no requirement behind it. Added FR-058: the basis on which FR-024's second condition was measured MUST be declared beside the figure on every instance published, and where that basis is anything other than the cited page the figure MUST be published as a **proxy** for the property it stands for. contracts §span_check_basis cites it; T029 carries it -->
- [X] CHK030 Do the requirements state which of the census's qualifying declarations are mandatory on every published instance, so a declaration missing from a response is a requirement violation rather than a permitted variation? [Testability, Spec §FR-024] <!-- Evaluator: Covered by FR-024's and FR-025's unconditional MUSTs — the exact count, its denominator, the no-interval declaration, the licensed reason, the target fixed before the first measurement, a shortfall's cause and the unjudged verdict at a zero denominator are each required of every published instance, so a declaration missing from a response violates a requirement rather than varying permissibly. FR-058 closes the one required member neither named, `span_check_basis`, and T044 asserts each of them -->
- [X] CHK031 Is the obligation to re-measure the second condition against real corpus documents and publish both measurements with any disagreement owned by a functional requirement, or does a success criterion stand alone behind a plan decision and a task? [Completeness, Spec §SC-010a; Plan §AD-010] <!-- Evaluator: Resolved — FR-058's second half owns it: the same condition MUST be re-measured against real corpus documents rather than fixtures, and both measurements published together with any disagreement, because a proxy whose agreement with the thing it proxies is never shown is a claim rather than evidence. SC-010a no longer stands alone behind AD-010 and T034; the plan's coverage map carries the row -->
- [X] CHK032 Do the artifacts agree on whether the licensed no-interval reason set is adopted from another epic by reference or stated locally, given FR-024, the spec's own compliance record, and the two halves of the census schema each answer differently? [Consistency, Spec §FR-024, §Compliance Check] <!-- Evaluator: Resolved — the story is now told one way in every place that tells it. FR-024 and contracts §CensusFigure/§no_interval_reason already said *names shared, availability stated locally*; two stale restatements still told the old one and are corrected — plan.md's coverage-map row ("licensed reason from E009's closed set") and the spec's own Compliance Check row ("adopted by reference rather than enumerated again"). While there: FR-024 says extending E009's set is "recorded here as a shared-document amendment" and the Step 6.5 list did not carry it; it is now item 2 of that list -->
- [X] CHK033 Does FR-025 fix where the unjudged verdict's cause is carried and whether it belongs to the same closed vocabulary as a shortfall's causes, given a zero denominator is not a shortfall and "nothing was measured" is not a reason any record failed? [Clarity, Spec §FR-025] <!-- Evaluator: Resolved — FR-025 amended in place (no ID change): the unjudged verdict's cause MUST travel in the same member a shortfall's causes travel in, as its own named member of that vocabulary, because a reader asking why the target is not met should read one place and not two, and a vocabulary that cannot express the state its own figure requires is one a conforming response fails. contracts §shortfall_causes and T044's assertion now cite the requirement -->
- [X] CHK034 Is the census being one run-level figure identical on every line's response — rather than a measurement about the line being read — stated by a requirement, given a reader meets it inside a single line's evidence region? [Clarity, Spec §FR-024, §FR-038] <!-- Evaluator: Covered by FR-024, which fixes the population as every linked record the **active resolution run** carries, read with FR-051 from the UX pass, which requires that population named in reader-facing words and names the misreading directly: "on a line carrying two linked records a share published without its population reads as a statement about that line, which is the opposite of what a run-level census says". Identity across responses follows from the population and is asserted by T044 across two `po_line_id`s -->


## Boundary Conventions, Caching and Read-Only Shape

- [X] CHK035 Is a conditional-request capability required by any requirement or measured by any success criterion, or does the validator-and-not-modified path exist in the contract alone? [Completeness, Spec §Requirements; contracts/openapi.yaml §parameters.IfNoneMatch] <!-- Evaluator: Resolved — it existed in the contract alone; FR-001…FR-057 and SC-001…SC-032 named neither a validator nor a conditional answer. Added FR-068, E012's counterpart to E010 FR-020a: every response MUST carry a validator computed over exactly the inputs it depends on, a request presenting a matching validator MUST be answered as unchanged with no body, the artifact publishing these operations MUST enumerate the inputs, and the check is two-sided. Added SC-033 measures it, mirroring E010 SC-027. contracts §IfNoneMatch and §ETag cite it; T011 implements and T018 exercises it -->
- [X] CHK036 Is the validator required to cover every input the response depends on, given the census is a run-level figure over every linked record while the enumerated inputs are scoped to this line's own entity and its own citations? [Completeness, Spec §FR-024; contracts/openapi.yaml §headers.ETag] <!-- Evaluator: Resolved, and the contract carried a real defect rather than only a missing requirement. FR-068 requires the validator to cover every input the response depends on and names the census as the one easily missed: `source_census` is a census over the active resolution run's whole linked-record set, so a resolution run touching some *other* line moves it while every per-line input stands still — a 304 would then withhold a response whose evidence figure had changed, which is not an incomplete validator but a false one. contracts §headers.ETag now admits the active resolution run's identifier and a digest over its whole linked-record set and states why; T028 supplies those inputs, tasks.md § Dependencies records that T028 completes the validator T011 composes, and T018's two-sided check exercises it -->
- [X] CHK037 Is the response's non-shareability required anywhere, given the cache directive rests on a session-scoped adjustment this feature does not own and on a reader identity the product records itself as not having? [Completeness, Spec §Assumptions; contracts/openapi.yaml §headers.CacheControl] <!-- Evaluator: Resolved — FR-068's third clause requires every response revalidated and never served from a cache shared between requests, resting it on the two facts this feature has rather than on an identity it does not: the request-time date staleness and the calendar-passed state are read from, and the adjustment set the request carries, the session being what E010 FR-031 defines it as. contracts §headers.CacheControl now cites the requirement instead of arguing for itself -->
- [X] CHK038 Is the read-only character of the whole surface required, or only the identity-resolution write FR-029 names, so that "no write verb anywhere" rests on Scope prose rather than on a requirement a later revision must satisfy? [Completeness, Spec §FR-029, §Scope] <!-- Evaluator: Resolved — added FR-069: the surface MUST expose no operation that writes anything, no operation may create, modify or delete stored state of any kind. Scope's exclusion states what this feature is not doing; FR-069 states what a later revision must still satisfy, so "no write verb anywhere" is a property of the operation table rather than a convention holding until somebody adds one. T038 asserts it and contracts' opening paragraph cites it -->
- [X] CHK039 Does FR-004's "reference class" name what must be carried — the population phrase a reader reads, or the convention triple the payload carries — given the Glossary defines it as the population and the delivered wording differs from the spec's? [Clarity, Spec §FR-004, §Glossary; Plan §AD-005] <!-- Evaluator: Covered by spec.md §FR-047 and the Glossary's Reference class row, both amended by the UX pass: the response carries the reading convention the figure was computed under, the population wording the reader meets is supplied by the interface from the constant already committed for the worklist, and "comparable orders" is recorded as this specification's name for the population rather than the copy rendered beside a figure (AD-005, HINT-002) -->


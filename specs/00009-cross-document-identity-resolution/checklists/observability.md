# Checklist: Observability — Requirements Quality

**Feature**: E009 Cross-Document Identity Resolution | **Domain**: Observability | **Depth**: Standard | **Audience**: Reviewer (PR) | **Date**: 2026-07-29

> These items test whether what the system must make **visible** is specified clearly and completely. They do not test implementation behavior. This feature exists because a wrong merge is silent (Principle III), so what it exposes is the product, not instrumentation around it.

## Negative evidence — gaps as data, not as absence

- [X] CHK001 For unmatched manufacturer strings, is the required content specified — the raw string, occurrence count, which run — or only that they are "recorded"? [Completeness, FR-013, SC-030] <!-- Evaluator: Resolved — it was only "recorded". FR-013 and SC-030 required the strings to exist and be retrievable and said nothing about content, so a bare list attributable to no run and no alias-table version satisfied both. FR-013 now requires the raw string as printed, its normalized form, the source record, the run, the project and the alias-table version whose gap it is. **The item's own content list was partly wrong**: `occurrence_count` does not exist and is refused by design ("two records printing the same unmatched spelling are two pieces of evidence and collapsing them would understate the gap"), so FR-013 forbids a stored count and plan.md's Data Model Summary row — which named the column — is corrected to match -->
- [X] CHK002 Is the retention scope of the unmatched-string record stated (per run, or accumulated), so a reader knows whether a gap closed or simply stopped being observed? [Clarity, FR-013, FR-039] <!-- Evaluator: Covered by data-model.md §`unmatched_manufacturer_string`: `pk_unmatched_manufacturer_string (resolution_run_id, source_record_kind, source_record_id)` is "one row per record per run", the table is append-only and never pruned under FR-039, and `alias_table_version` is carried as "the version whose gap this is — which is what makes 'adding this alias to v002 closes this row' a checkable claim". `ix_unmatched_manufacturer_string__value` is led by the version rather than the run, "because the gap is a property of the artifact and not of an execution" -->
- [X] CHK003 Is recording **every generated candidate pair** stated as a requirement in its own right, rather than as a by-product of scoring? [Completeness, FR-012] <!-- Evaluator: Covered — FR-012 is its own requirement with its own rationale ("so a true pair absent from the candidate set is attributable to blocking rather than inferred from a missing merge"), and data-model.md opens §`candidate_pair` with it: "Every candidate pair the run generated is a row (FR-012)". It is independent of the decision: a rejected pair is still a row, and both endpoint FKs resolve to records the run read -->
- [X] CHK004 Is it specified that a gap in the alias table must be visible **as data** rather than inferred from missing merges, in a form a reader can query? [Clarity, FR-013, spec Edge Cases] <!-- Evaluator: Covered by FR-013's own clause ("visible as data rather than deduced from absent merges"), the matching Edge Case, and data-model.md §`unmatched_manufacturer_string` — "answerable by query rather than by noticing that two records that should have merged did not" — with `ix_unmatched_manufacturer_string__value` declared for the driving read ("Every gap in v001, most frequent first") and VR-008 reconciling the rows in both directions -->


## Attributing a miss to the stage that caused it

- [X] CHK005 Is the rule that a true pair absent from the candidate set is a **blocking** miss and not a scoring miss stated unambiguously, and is it stated once rather than in two places that could diverge? [Consistency, FR-010, SC-024] <!-- Evaluator: Covered. It appears in four places and they agree rather than diverge, each with a distinct job: the Edge Case states the principle ("It is a blocking miss, not a scoring miss, and must be attributed to the stage that caused it"), FR-011 forbids the folding, FR-030 counts it as a recall miss, and SC-024 is the falsifiable criterion ("reported against blocking pair completeness and counted as a recall miss, and is not attributed to scoring"). data-model.md keeps the figures on separate primary keys so neither can absorb the other; task T068 asserts the attribution per stage -->
- [X] CHK006 Is blocking pair completeness required to be measured against ground truth sampled **independently of the blocking keys**, with the sampling frame published beside the figure? [Completeness, FR-010, SC-023, SC-032] <!-- Evaluator: Covered by FR-010 (both clauses in one sentence), SC-023 and SC-032, and made unfakeable by data-model.md's `ck_labeled_pair_set__frame_agrees_with_stratum`: "the estimation stratum cannot be recorded as blocking-derived … Without the second check the frame column would be a label anyone could write, and blocking pair completeness measured over a blocking-derived frame is 1.0 by construction". The frame reaches the figure through the run's calibration to `labeled_pair_set`, and task T067 renders it beside the figure -->
- [X] CHK007 Is the prohibition on folding blocking losses into a scoring metric stated as a requirement, so PC and RR cannot be presented as part of precision? [Unambiguity, FR-011, SC-023] <!-- Evaluator: Covered by FR-011 ("MUST publish blocking pair completeness and reduction ratio as figures separate from merge precision, and MUST NOT fold blocking losses into a scoring metric") and SC-023 ("Both are distinct figures from merge precision"), carried structurally by `pk_resolution_figure (resolution_run_id, figure_name)` over a closed six-value name domain, so the three figures are three rows and none can be reported as a component of another -->
- [X] CHK008 Does a true pair that is withheld, rejected, or never blocked have a specified and *distinguishable* disposition, so all three do not collapse into one undifferentiated "miss"? [Completeness, FR-030, SC-024] <!-- Evaluator: Covered — FR-030 makes all three recall misses and each remains separately recorded: never blocked is the absence of a `candidate_pair` row (FR-012) and is counted against blocking pair completeness (SC-024); withheld is `decision='withhold'` with a review item; rejected is `decision='reject'` with none. FR-011 keeps the blocking loss out of the scoring figure, and task T068 asserts all three "each counts as a recall miss **attributed to its stage**" -->


## Figure classification and interval discipline

- [X] CHK009 Is the **explicit no-interval declaration** required for a census figure specified in its content, not just its existence? Without required content, an implementation satisfies it by silently omitting the interval — the exact failure the classification exists to prevent. [Completeness, FR-025, FR-038, plan AD-008] <!-- Evaluator: Resolved — existence was enforced and content was not. `ck_resolution_figure__interval_xor_reason` refuses both-and-neither, and `…__census_declares_no_interval` requires a census to carry the reason, but `no_interval_reason` was free `text` with no domain, so `'n/a'` satisfied FR-025 and AD-008's whole point ("Attaching nothing makes a census indistinguishable from an estimate whose interval was dropped") went unenforced. FR-025 now requires the declaration to state its reason from a closed set, and data-model.md adds `ck_resolution_figure__no_interval_reason_domain` with the one licensed value, registered in the constraints table and the nullable-column-checks table -->
- [X] CHK010 Does every published figure name the stratum it draws on, and is a figure whose stratum is too small required to be reported **undefined with its denominator** rather than computed across strata? [Completeness, FR-037a, SC-037] <!-- Evaluator: Covered by FR-037a, which carries both halves in two sentences, and SC-037. Both are structural: `resolution_figure.stratum` is `NOT NULL` over a two-value domain from which `hard_negative` and any union value are absent — "the shortest way to enforce both is to make the value unwritable" — and `ck_resolution_figure__estimate_iff_denominator` makes the undefined case a stored row rather than a rendering choice. VR-013 asserts the omission by attempting a `hard_negative` insert -->
- [X] CHK011 Is the interval's **method and sidedness** required to be published alongside the figure, not merely used? [Completeness, FR-026, SC-017] <!-- Evaluator: Covered by FR-026 ("naming both the method and its sidedness"), SC-017 and US3's first acceptance scenario, with data-model.md carrying them as two columns rather than one compound value, "so a report cannot state a method and leave the sidedness to be inferred", bound by `ck_resolution_figure__sidedness_iff_method` so one without the other is unrepresentable -->
- [X] CHK012 Is the classification of each of the six published figures as estimate or census stated in the artifact, so a reader can tell which is which without re-deriving it? [Clarity, FR-038, SC-031] <!-- Evaluator: Covered as of this evaluation pass — FR-038 classified five of six until it was amended under testing.md CHK022, and merge precision (the one figure with a registered target) was the omission. All six are now named in the requirement, matching `ck_resolution_figure__kind_matches_name`, which classifies all six as a database fact so "the interval rules cannot be routed around by mislabelling a figure", and `figure_kind` is stored per row so no reader re-derives it -->
- [X] CHK013 Where merge precision is undefined (zero merges among labeled pairs), is the required published form specified — undefined **with the zero denominator shown** — and explicitly not zero and not omitted? [Unambiguity, FR-028, SC-013] <!-- Evaluator: Covered by FR-028 in exactly those terms ("MUST NOT be reported as zero or omitted"), FR-025's last clause, and SC-013/SC-017/SC-019. Structural in data-model.md: `denominator` is `NOT NULL` on every row and `ck_resolution_figure__estimate_iff_denominator` is a biconditional, so a zero denominator forces a NULL estimate **and** a real denominator forbids publishing undefined; VR-014 drives the case end to end -->
- [X] CHK014 Is coverage required to appear in the **same statement** as merge precision, so precision cannot be read without the abstention rate that produced it? [Completeness, FR-029, SC-022] <!-- Evaluator: Covered by FR-029 with its rationale, SC-022, and task T064 ("coverage beside precision"), now also required of the rendered report by FR-049(b). data-model.md is honest that the constraint cannot carry it — G-7, "a rule about a rendered artifact" — and names both the mitigation (VR-013 makes both rows mandatory) and the residual (a report-content test), with the runtime consequence stated: "precision alone is trivially gamed by withholding almost everything" -->


## Publishing the miss

- [X] CHK015 Where precision or recall falls below the registered target, is "published with its **cause**" specified to any degree, or is "cause" a word an implementation can satisfy with anything? [Clarity, FR-032, SC-026, project-instructions § Principle VII] <!-- Evaluator: Resolved — it was satisfiable with anything. FR-032 and SC-026 required a cause, `resolution_figure.shortfall_cause` is free `text`, and `ck_resolution_figure__cause_iff_shortfall` enforces only that it is non-null, so a restatement of the shortfall passed. FR-032 now requires a structured cause: the stage or stages attributed under the same vocabulary FR-011 and FR-030 already use (blocking, withheld, rejected, alias-table gap), the counts supporting that attribution, and the figure's realized denominator and stratum — with a restatement of the miss explicitly disallowed -->
- [X] CHK016 Is the prohibition on adjusting a target to match a result stated as a requirement of the run's output, not only as a project principle? [Completeness, FR-032, Principle VII] <!-- Evaluator: Covered — FR-032 carries the prohibition itself ("MUST NOT adjust the target to match the result"), the run records the target it was judged against (`resolution_figure.target_value` with `ck_resolution_figure__meets_iff_target`), and data-model.md is explicit that the run cannot self-verify it: G-6 states the exposure ("a run that believed a lowered one stores it without contradiction") and names the covering test, "comparing `target_value` against the value parsed from `specs/prd.md`, so a moved target is a build failure rather than a stored number". Task T065 reads the registered target rather than restating it -->
- [X] CHK017 Are the feature's four disclosed limitations each recorded with all four required parts — scope decision, supporting evidence, reversal trigger, production-scale alternative? [Completeness, spec Risks, Principle VII] <!-- Evaluator: Resolved — two of the four were, and plan.md's Principle VII row claimed all four were. The precision-target and E003-extension limitations carry all four parts with their labels; the **recall-target** limitation carried an amendment need and no four-part record, and the **specification-leg** limitation was not recorded in §Risks at all. Both are now recorded in the same labelled form: the recall one mirrors the precision one on the numbers already stated in its own risk, and the specification-leg one is added as a new §Risks entry (see CHK018). Principle VII violations are CRITICAL, so this was the item with the highest severity on this checklist -->
- [X] CHK018 Is the specification-leg shortfall — E006 extracts zero values from the 26 real UFGS documents — published as a run-visible disclosure rather than only as spec prose? [Completeness, spec Problem Statement, plan Summary] <!-- Evaluator: Resolved — it was prose in two places that both **asserted it was published**: the Problem Statement ("publishes the third as out of reach with its cause") and plan §Summary ("the shortfall is published rather than papered over"), while no requirement, criterion, task or table made it so. Now recorded as a four-part limitation in §Risks, restated to what survives the fourth pass — the specification *member* is deliverable through an extracted `specification_section`; what is not is a specification **document** as a candidate record, so a material no transmittal cites cannot be linked at all — and made run-visible by FR-049(g), which requires the rendered report to carry the disclosed limitations in Principle VII's four-part form -->


## The run manifest

- [X] CHK019 Does the manifest's required content cover every input a published figure depends on — alias-table version, threshold constants, **weight vector**, both stratum hashes, E002 catalogue digest, input record counts? [Completeness, FR-034, FR-044, FR-047] <!-- Evaluator: Covered across three requirements that together name all six: FR-034 (alias-table version, threshold constants, both stratum hashes — plural as amended earlier in this pass — and input record counts), FR-040 (E002 catalogue digest alongside the version), FR-047 (weight vector beside the thresholds). data-model.md §`resolution_run` carries all of them plus `alias_table_content_digest`, and every one is inside a composite foreign key, so none can disagree with the artifact it names -->
- [X] CHK020 Is it stated that **every** published figure resolves to the manifest of the run that produced it, with nothing publishable that the manifest cannot account for? [Completeness, FR-034, Principle I] <!-- Evaluator: Resolved per owner decision — option A, the figure-name domain widened from six to eight with `baseline_merge_precision` and `baseline_recall`, so FR-046's two figures are manifest rows rather than report-only and the second clause now holds. `ck_resolution_figure__name` carries eight values in `0508`; `ck_resolution_figure__kind_matches_name` needed no edit because it names the three census figures positively, which makes both baseline figures estimates by construction, and `ck_resolution_figure__stratum_matches_kind` then pins both to the estimation stratum so neither can be written as a census or decline an interval; `estimate_iff_denominator` and the no-interval-reason domain needed nothing. VR-013 asserts eight and counts from the domain rather than a literal, so count and domain cannot drift. Propagated to FR-046, SC-042, FR-038, FR-049(a), plan's Data Model Summary and Property tier, and tasks T016, T064, T066, T069 (whose edge moved T064 → T066) -->
- [X] CHK021 Is the active-run pointer specified so a consumer selects a run **explicitly** rather than by recency, and is its behavior defined when no run is active? [Completeness, FR-039, SC-033] <!-- Evaluator: Covered on both clauses — FR-039 requires explicit selection "rather than by recency" and data-model.md forecloses the alternative twice (`ix_resolution_run__started_at` is "Operational listing only. **Never the selection mechanism**", and `v_active_resolution_run` carries "No `LIMIT`, and no recency fallback"). Zero active runs is defined and meaningful ("this project has not been resolved … distinguishable from 'resolved, possibly stale'"), asserted by VR-017 at zero, one and five; the residual obligation on consumers is G-8 -->
- [X] CHK022 Are the alias-table version and the E002 catalogue digest recorded as **two separate** facts, so editing the table without reseeding is detectable? [Clarity, FR-004, FR-040, SC-036] <!-- Evaluator: Covered — FR-040 requires the catalogue digest "alongside the alias-table version", SC-036 restates it, and data-model.md keeps three separate values on `manufacturer_alias_table` (version, `content_digest`, `catalogue_digest`) with the reason stated: "a version identifier can be reused for changed contents by a careless edit, and the manifest would then record a version that no longer describes what fired". All three are in `fk_resolution_run__alias_table`, so SC-036 "is therefore a referential fact, not a convention" -->


## The review queue as an observable

- [X] CHK023 Does a review item carry enough for a person to adjudicate **without re-running anything** — both source records, the score, the band, the per-attribute agreements, the alias-table version? [Completeness, FR-022, SC-012] <!-- Evaluator: Covered by FR-022 and SC-012, which enumerate all five, and by data-model.md, which states which are columns and which are joins and why — A-8: the two records and the band are columns because the FK needs them, and the score, the agreements and the alias version are "one keyed join away through a foreign key that cannot point anywhere else", because "copying the score onto the item would create a second answer that can disagree with the pair's". `v_open_review_item` exposes the score, both thresholds and the alias version so "FR-022's list is one read from an item rather than a three-hop join". Nothing is recomputed -->
- [X] CHK024 Is the withheld set's **yield** defined for the case where no adjudication data exists yet, rather than left as an undefined figure at first run? [Completeness, FR-031] <!-- Evaluator: Resolved — "where adjudication data exists" said when the figure applies and never said what a run does when it does not, which collides with FR-028's discipline (undefined with its denominator, never zero and never omitted) and with the closed six-figure vocabulary that has no row for a yield. FR-031 now states that no adjudication data can exist within this feature's scope — E016 and learning from adjudications are both excluded — so a run publishes no yield figure at all rather than a zero or an empty value, its absence is a scope fact, and a later epic supplying adjudication data publishes it as an estimate under FR-038 -->
- [X] CHK025 Is the review item's shape specified as extensible **additively** by E016, with the fields E009 writes named as the ones that must not change? [Clarity, FR-023] <!-- Evaluator: Covered by FR-023 and by data-model.md §FR-023, which states the extension mechanism (E016 keys its own table on the stable pair identity, not on `candidate_pair_id`, with `ix_review_queue_item__pair_identity` "declared on exactly those five columns so the join exists before the consumer does") and the invariant in the strongest form available: "**Nothing E009 writes changes**: no column here is nullable-for-later, no `status` column is reserved, and no enumeration has a `pending` member waiting to be filled in". Task T050 asserts the absence -->
- [X] CHK026 Across runs, is a pair withheld twice required to be **groupable** by a stable identity, so the queue is readable rather than duplicative? [Completeness, FR-043, SC-039] <!-- Evaluator: Covered by FR-043 and SC-039, carried by the four identity columns on the item under `ck_review_queue_item__canonical_order` ("Two runs that both withhold one pair write two items carrying the same four values, so grouping them is a `GROUP BY` and an adjudication anchors to the pair rather than to the item"), `ix_review_queue_item__pair_identity` for the read, and `v_open_review_item` so an item of a superseded run is "history, not work". VR-016 asserts the same four columns and different run ids -->
- [X] CHK027 Are the withheld count and its share required as exact counts with their denominators and **no interval**, consistent with the census classification? [Consistency, FR-031, FR-038, SC-025] <!-- Evaluator: Covered by FR-038 (the withheld share is a census, "MUST NOT carry an interval, because there is no sampling uncertainty to express"), SC-025 and SC-031, enforced by `ck_resolution_figure__kind_matches_name` and `ck_resolution_figure__census_declares_no_interval` so the row cannot carry an interval and cannot omit its declaration — and, as of this pass, cannot satisfy that declaration with arbitrary text either (CHK009) -->


## The rendered report

- [X] CHK028 Is the report's required **content** specified, or only its path and existence? [Completeness, plan Project Structure, tasks T064–T067] <!-- Evaluator: Resolved — only its path, its existence and its precedent ("following E006's `ingestion-report.md` convention"). Its content lived as four task lines (T064–T067) and nowhere in the requirements, which matters more here than usual because several obligations are *rendering* rules no constraint can carry — FR-029's same-statement rule is G-7 and Principle VIII's comparison is only visible in the artifact a reader opens. FR-049 now specifies the required content in seven parts, and makes a figure present in the report but not the manifest, or the reverse, a defect -->
- [X] CHK029 Does the report specify the baseline's figures beside the resolver's, so Principle VIII's comparison is visible in the artifact a reader actually opens? [Completeness, FR-046, SC-042] <!-- Evaluator: Covered by FR-046 ("published beside"), SC-042 ("A run publishing either figure without its baseline counterpart fails"), task T066, and now FR-049(c), which makes it a required section of the rendered report rather than a property of a task. The comparability of the two figures was tightened under testing.md CHK030; that the two baseline figures have no manifest row is a separate defect, reported under CHK020 -->
- [X] CHK030 Is the report required to distinguish the estimation stratum from the hard-negative stratum wherever a figure appears, rather than leaving the reader to infer it? [Clarity, FR-037a] <!-- Evaluator: Covered — FR-037a requires each published figure to name its stratum, SC-037 restates it, and FR-049(a) now carries it into the report per figure. The distinction is absolute rather than a labelling convention: `ck_resolution_figure__stratum`'s domain holds only `estimation` and `run_candidate_set`, so **no** published figure can name the hard-negative stratum and none can name a union; the hard-negative stratum appears in the artifacts only as the collision guard's population -->


## Failure visibility

- [X] CHK031 A refused run writes no manifest — is what the **operator** sees instead specified, rather than only what is absent from the database? [Completeness, FR-035, SC-015] <!-- Evaluator: Covered by plan §Error Handling Strategy, which specifies an operator-facing outcome for every refusal path — a non-zero exit naming the offending alias, the guard breach with nothing written, the hash mismatch "and both hashes are reported", and the divergence naming both constant sets — and, as of this pass, by SC-015, which was amended under testing.md CHK006 to require the refusal to report which guard failed rather than only certifying absences -->
- [X] CHK032 Can a run that **refused** be distinguished from one that never started? Both leave no manifest, and conflating them hides a guard breach as a scheduling gap. [Unambiguity, FR-035, FR-044, FR-047] <!-- Evaluator: Resolved per owner decision — option A. The database stays empty and **no refusal relation is added**; instead FR-035 now requires a non-zero exit carrying one structured, machine-readable refusal line naming the project, the failing guard or check, and both values on a hash mismatch or constant divergence, with FR-044 and FR-047 each requiring both constant sets and which constant diverged, and plan §Error Handling Strategy stating the rule once above the table. The residual is recorded rather than closed, as new disclosed gap **G-15**: from the database a guard breach and a scheduling gap remain the same observation, because closing that would mean writing a row on the one path FR-035 says writes nothing -->
- [X] CHK033 Is the failing guard required to be **named** in the refusal output, so a collision-guard breach and a hash mismatch are not reported identically? [Clarity, FR-007, FR-017, FR-035] <!-- Evaluator: Covered — plan §Error Handling Strategy holds the two as separate rows with different outputs (the collision guard versus "the mismatch and both hashes are reported"), data-model.md §Write Order separates them as guards G1 and G2 with their own failure semantics, and SC-015 as amended requires the refusal to name which guard failed, the two hashes for a mismatch, and the colliding pair of records for a collision -->
- [X] CHK034 For a threshold or weight divergence, is the refusal required to report **both** constant sets, so post-hoc tuning is diagnosable rather than merely blocked? [Completeness, FR-044, FR-047, SC-040, SC-043] <!-- Evaluator: Resolved — plan §Error Handling Strategy required "both threshold sets" and did not mention the weights at all, which is precisely the half of the tuning surface FR-047 was added because the cutoffs alone do not cover. That row now covers thresholds **and** weights, requires both constant sets and which of the six diverged, and SC-040 and SC-043 were amended in the same pass to require the naming at criterion level -->
- [X] CHK035 Is an all-withheld run required to be reported as a **legitimate outcome** rather than a failure, with the review queue populated and precision undefined? [Unambiguity, FR-024, SC-013] <!-- Evaluator: Covered by FR-024 ("MUST complete and report zero merges with a populated review queue, and MUST NOT be reported as a failure"), SC-013 (now requiring a non-empty candidate set, per testing.md CHK002), plan §Error Handling ("complete normally … not a failure and not reported as one"), and VR-014, which asserts it end to end "because SC-013 declares this a *successful* outcome and the failure mode is a job that treats zero merges as an error" -->

---

**Traceability**: 35 of 35 items carry a reference (100%).
**Recorded gap found while writing**: several requirements say a thing is "recorded" without specifying what — CHK001, CHK015, CHK024 and CHK028 each name one. These are the items most likely to fail evaluation, and that is their purpose.

**Evaluation note** (2026-07-29): 25 passed, 10 resolved, 0 remaining — 35 of 35 checked. Eight resolved by the evaluator; **CHK020 and CHK032 resolved by project-owner decision** — the figure-name domain widened to eight so the baseline's figures are manifest rows, and a structured machine-readable refusal line on the non-zero exit with the residual recorded as disclosed gap **G-15**. All four items the authoring note predicted would fail did fail, and all four resolved by adding specificity to `spec.md` rather than by rewording the question — FR-013 gained the unmatched-string record's required content, FR-025 the no-interval declaration's, FR-032 the shortfall cause's, and FR-031 the yield's no-adjudication case, with the report's content added as FR-049. CHK001's own content list was partly wrong and is annotated in place: `occurrence_count` does not exist and is refused by design.

Evaluated third, after `data-integrity.md` and `testing.md`, so CHK012, CHK031, CHK033 and CHK035 rest partly on amendments made earlier in the same pass; each annotation says which. The highest-severity finding was CHK017 — a Principle VII violation, CRITICAL under Governance — where two of the four limitations `plan.md` claimed were recorded in four-part form were not.

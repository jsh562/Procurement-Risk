# Checklist: Data Integrity — Requirements Quality

**Feature**: E009 Cross-Document Identity Resolution | **Domain**: Data Integrity | **Depth**: Standard | **Audience**: Reviewer (PR) | **Date**: 2026-07-29

> These items test whether the requirements governing stored data are clear, complete, consistent and unambiguous. They do not test implementation behavior. The design's stated method is to make a criterion *unrepresentable to violate* rather than checked afterward, so several items ask whether that claim is actually specified or merely asserted.

## Append-only runs and run scoping

- [X] CHK001 Is "a later run MUST NOT alter or delete an earlier run's rows" specified precisely enough to exclude soft-delete, supersession, and flipping an active flag on old rows — all of which leave earlier rows technically present? [Unambiguity, FR-039, SC-033] <!-- Evaluator: Covered by data-model.md §Privileges (`0509` revokes UPDATE/DELETE on all fifteen tables), §Write Order ("Nothing is ever deleted or updated in place, other than the `is_active` flip"), §State Transitions ("Superseded is durable" — a superseded run keeps every row it wrote), §`resolution_run` ("No status column … a `status` column would invite one"), and Invariant 49. All three loopholes are closed by name: soft-delete has no column and no DELETE verb, supersession is a new run rather than an edit, and the active-flag flip is confined to `resolution_run.is_active` and is the only UPDATE granted anywhere -->
- [X] CHK002 Is the active-run pointer's state defined when **no** run is active — before the first run, and after a run refuses? [Completeness, FR-039, SC-033] <!-- Evaluator: Covered by data-model.md §`resolution_run` `ix_resolution_run__single_active` ("Zero active runs for a project is legal and meaningful — 'this project has not been resolved' — and is distinguishable from 'resolved, possibly stale'"), §The two views (same, for `v_active_resolution_run`, with no LIMIT and no recency fallback), and VR-017, asserted at zero, one and five active runs. After a refusal the pointer is unchanged, because a refused run writes nothing at all — §Write Order guards G1/G2 run before the transaction opens -->
- [X] CHK003 Is it specified what the pointer does when a run fails **midway**, given FR-035 requires an all-or-nothing write? [Completeness, FR-035, FR-039] <!-- Evaluator: Covered by data-model.md §Write Order (one transaction per run; "An abort at any point leaves nothing"; publication is a separate later transaction, and "a crash between them leaves the previous run active, which is the correct state to fail into") and §State Transitions ("A run that did not finish has no row at all — there is no *In flight* state"). The pointer keeps naming the previous run -->
- [X] CHK004 Is the prohibition on selecting a run **by recency** stated as a requirement on consumers, so a downstream epic reading `MAX(run_id)` is a violation rather than a style choice? [Clarity, FR-039] <!-- Evaluator: Covered by spec.md FR-039 ("consumers MUST select a run through an explicit active-run pointer rather than by recency") and data-model.md `ix_resolution_run__started_at` ("Operational listing only. **Never the selection mechanism** … a query reading `ORDER BY started_at DESC LIMIT 1` to decide what is current is the defect, not this index"), plus §The two views and G-8, which records the residual obligation on each consumer -->
- [X] CHK005 Is at most one active run per **project** specified, rather than one globally — given FR-020 scopes identity within a project? [Unambiguity, FR-020, FR-039] <!-- Evaluator: Covered by plan.md AD-007 and its Data Model Summary row ("`is_active` under a **per-project partial unique index**"), and data-model.md `ix_resolution_run__single_active` — `(project_id) WHERE is_active`, "five active runs across the five projects is the ordinary state" — with the one-project-per-run reading recorded as A-3 and asserted by VR-017 -->
- [X] CHK006 Does the requirement set state that `UPDATE` and `DELETE` are revoked on the entity tables, and is that stated as a *requirement* rather than only as a migration detail? An append-only guarantee enforced by privilege needs to be findable from the requirements. [Completeness, FR-039, plan P-9] <!-- Evaluator: Resolved — added FR-048 to spec.md. The revocation existed only in data-model.md §Privileges and migration `0509`; FR-048 now requires it as the enforcement mechanism for FR-039, names the single exception (the active-run pointer flip and finish stamp on the manifest), forbids reaching the same effect through a soft-delete or per-row active flag, and carries the E003 reversal (P-9 / AN-2) and the G-13 latency against the deployed SUPERUSER connection -->


## The E003 table extension

- [X] CHK007 Does FR-045 enumerate **every** constraint the extension touches? It names `uq_rem__extracted_value` and `uq_rem__po_line`; `uq_resolved_entity__normalized_identity` is re-scoped on the same reasoning but is not named. [Completeness, FR-045, data-model A-2] <!-- Evaluator: Resolved — the gap is real and is exactly as framed; verified against data-model.md §The FR-045 Extension ("**FR-045 names only the first two.** `uq_resolved_entity__normalized_identity` is equally unbuildable and is re-scoped here on the same authority") and A-2, which asked for the requirement to say so. FR-045 in spec.md now names all three constraints, declares the enumeration exhaustive, and states that no other constraint on either table is altered; A-2's "what would change" cell records the resolution -->
- [X] CHK008 Is the consequence of `0509`'s revocation of `UPDATE`/`DELETE` specified for any **existing** consumer that E003's `0010` explicitly permitted to revise a resolved entity? [Completeness, FR-039, plan P-9] <!-- Evaluator: Covered by data-model.md §Privileges (the consumer `0010`'s rationale names is E009 itself, and its replacement mechanism is stated: "Withdrawing a merge is now a *new run* that does not emit it, not an edit to the old one — which is what makes SC-033 checkable at all") and §The FR-045 Extension ("both tables are P2 objects E003 created and nothing has ever populated — E009 is their first writer"). There is no other existing consumer to affect -->
- [X] CHK009 Is the reversal of E003's recorded rationale — "a resolved entity is a revisable judgement about identity" — disclosed as a reversal of a documented decision, not merely as a privilege change? [Clarity, plan P-9, Complexity Tracking] <!-- Evaluator: Covered by plan.md P-9 ("this one reverses a documented E003 *decision* and its rationale, which is the more serious of the two") and Complexity Tracking ("the privilege revocation (P-9) reverses a *recorded E003 decision*, not just a column list, and is the part most likely to surprise a later reader"), plus data-model.md §The FR-045 Extension cost item 1, §Privileges, and AN-2, which records it as a second, distinct amendment need -->
- [X] CHK010 For the columns added to E003's two tables, is nullability, default, and backfill specified, or left for the migration to decide? *(Premise corrected at evaluation: the item asked about "already-populated" tables. Both tables are empty — `data-model.md` §The FR-045 Extension: "E009 is their first writer" — which is the precondition the chosen DDL depends on.)* [Completeness, FR-045] <!-- Evaluator: Resolved — nullability and default are specified exactly in data-model.md §The FR-045 Extension (`ADD COLUMN uuid NOT NULL`, `ADD COLUMN text NOT NULL`, `ADD COLUMN … GENERATED ALWAYS AS … STORED NOT NULL`, no defaults) with the reason no backfill is needed. plan.md's Risk Mitigation row contradicted it by describing "nullable-then-backfilled columns"; that row is amended to the actual DDL, the empty-table precondition, and the fact that the window closes the first time E009 runs -->
- [X] CHK011 Is the re-scoped uniqueness specified so the **old** guarantee still holds within a single run — a record in two entities across runs is permitted, within one run is not? [Unambiguity, FR-045, FR-039, plan HINT-003] <!-- Evaluator: Covered by data-model.md §The FR-045 Extension ("A record belongs to at most one entity **per run**, and to a different entity in a later run"), Invariant 38, VR-020 (three line members of one entity under one run coexist; two naming the same `po_line_id` under one run collide), and plan.md HINT-003, which requires an integration test asserting the old guarantee still holds within a single run -->
- [X] CHK012 Is the reliance on `NULLS DISTINCT` in the re-created unique constraints stated explicitly, given it determines whether a NULL member key is deduplicated or admitted repeatedly? [Clarity, data-model, FR-045] <!-- Evaluator: Covered — verified directly against data-model.md rather than accepted as framed. §The FR-045 Extension carries a dedicated paragraph ("The re-scoped uniqueness still depends on `NULLS DISTINCT`, and adding a NOT NULL leading column does not change that"), spelling out that every line member with a NULL `extracted_value_id` still coexists, that `UNIQUE NULLS NOT DISTINCT` would hold at most one line member per run, and that both new indexes are asserted to carry `indnullsnotdistinct = false`; VR-020 reads it from `pg_index`, and plan.md's Data Model Summary row repeats it -->
- [X] CHK013 Is it specified that E003's migrations are **not** edited — the extension arrives as a new revision in E009's own block — given Alembic is forward-only? [Completeness, plan AD-003, spec Implementation Signals] <!-- Evaluator: Covered by plan.md AD-003 ("ALTER in the `0500` block", rejecting both a parallel table and blocking on the amendment) and data-model.md §Conventions ("Forward-only … Explicit DDL only"), §Migration Sequence (`0505` chains from `0504`, the block head chains from E006's `0404`), and VR-026/VR-028, which assert every E009 filename matches `^05[0-9]{2}_` and that no E003-owned table changed across an `0404`-to-head window -->


## All-or-nothing writes

- [X] CHK014 Is the **atomic unit** defined? FR-035 requires nothing partial; is that per run, per project, or per stage? [Unambiguity, FR-035] <!-- Evaluator: Covered by data-model.md §Write Order ("**One transaction per run**, and the contrast with E006 is the design decision … A resolution run is **one** unit of publication") and plan.md §Project Structure Patterns to reuse ("E006's per-document transaction boundary, applied here as one transaction per *run* since FR-035 makes the run the atomic unit"). A run is one project, per A-3, so per-run and per-project coincide; FR-035 as amended states the unit explicitly -->
- [X] CHK015 Is the set of failure modes that must trigger the all-or-nothing outcome **enumerated**, or only exemplified? FR-035 names the collision guard and hash verification; FR-044 and FR-047 add threshold and weight divergence. [Completeness, FR-035, FR-044, FR-047] <!-- Evaluator: Resolved — FR-035 named two causes and nothing said the list was closed or open. It now names the guard, the hash verification, threshold divergence (FR-044) and weight divergence (FR-047), and states that any failure before commit produces the same outcome because the run is one atomic unit — the property data-model.md §Write Order already asserted ("FR-035 makes that explicit for two named failures; the transaction makes it true for all of them") and plan.md's Error Handling Strategy already tabulated for five -->
- [X] CHK016 Is it specified that a refused run writes **no manifest** — not a manifest marked failed — so a partial artifact cannot be mistaken for a result? [Unambiguity, FR-035, SC-015] <!-- Evaluator: Covered by spec.md FR-035 and SC-015 ("writes no resolved entities, no review items, and no manifest") and made structural in data-model.md: `resolution_run.finished_at` is **NOT NULL** so "there is no in-flight or aborted run row to represent", there is deliberately no `status` column ("a `status` column would invite one"), and §Write Order runs both guards before the transaction opens so "SC-015 holds because nothing was opened, not because something was rolled back" -->
- [X] CHK017 Are the three outputs a refused run must not write (resolved entities, review items, manifest) stated as an exhaustive list, so a fourth output added later is covered? [Completeness, FR-035] <!-- Evaluator: Resolved by the same FR-035 amendment as CHK015 — the requirement now enumerates all nine artifact classes a run writes and states that neither list is a boundary, so an output a later requirement adds is covered by the atomicity rather than by the enumeration. This matches what verification already asserted: VR-015 counts zero rows in "all thirteen E009 tables and in `resolved_entity` and `resolved_entity_member`", per table rather than in total -->


## Uniqueness and identity

- [X] CHK018 Is the unordered-pair identity specified so it is **stable across runs**, not merely unique within one — the property FR-043's grouping depends on? [Completeness, FR-009, FR-043, SC-039] <!-- Evaluator: Covered by spec.md FR-043 ("the **stable** unordered-pair identity from FR-009") and data-model.md §`candidate_pair` ("because the ordering is a total order on `(kind, id)` and nothing else, the tuple a pair canonicalizes to is **the same in every run**"), Invariant 21 ("`ck_candidate_pair__canonical_order` … mentions no run"), `ix_candidate_pair__pair_identity` (deliberately not led by the run), and VR-016's cross-run assertion -->
- [X] CHK019 Is a pair matching on **more than one** blocking key specified as exactly one candidate pair, and is the deduplication required at generation rather than at reporting? Deferring it double-counts every denominator. [Unambiguity, FR-009, SC-011, plan HINT-004] <!-- Evaluator: Covered on both halves — spec.md FR-009 ("is one candidate pair, and MUST be counted once in every published denominator") and SC-011 for the identity; plan.md HINT-004 for the placement ("Deduplicate at the point of generation, on the unordered identity, before anything counts a denominator"), carried into tasks T031/T032. data-model.md makes it structural: `uq_candidate_pair__run_pair` plus `blocking_key_kinds` as an array so a two-key pair is one row, asserted by VR-023 -->
- [X] CHK020 Is a self-pair (a record paired with itself) excluded by specification rather than by implementation accident? [Completeness, FR-009] <!-- Evaluator: Covered by data-model.md §`candidate_pair` ("The same comparison forbids a self-pair: a row value is not strictly less than itself"), Invariant 22, and the identical `ck_labeled_pair__canonical_order` on the frozen set ("One constraint doing two jobs"). Excluded by a named CHECK constraint on three tables, not by implementation -->
- [X] CHK021 For "at most one specification section per cluster", is the disposition of the **rejected** record specified — rejected and named, but is it dropped, queued, or left unclustered? [Completeness, FR-041, SC-041] <!-- Evaluator: Resolved per owner decision — option A, **left unclustered**. FR-041 now states the disposition (member of no entity in that run; the cluster keeps its first section and its other members; the record's run record, candidate pairs and any review item stand; not substituted, not queued, not discarded), SC-041 makes all four outcomes observable, plan §Error Handling records it as the one anticipated constraint violation the job recovers from, under a `SAVEPOINT` so the run still commits as one unit, data-model.md §FR-041 gives the mechanism and VR-019 gains the four assertions. The cost of the option is recorded honestly as new disclosed gap **G-14**: the rejection is visible only in the run report, so "rejected" and "nothing merged with it" are the same absence to a query — and FR-049(h) requires the report to name every rejected admission -->
- [X] CHK022 Is the cross-project prohibition specified at **both** the blocking and clustering stages, so a single-stage bug cannot produce a cross-project entity? [Completeness, FR-020, SC-006] <!-- Evaluator: Covered by plan.md's Requirement Coverage Map FR-020 row ("Blocking, Clusterer | `identity/block.py`, `identity/cluster.py` | Project scope enforced twice") and data-model.md Invariant 24, which carries it referentially at four separate edges — `fk_candidate_pair__run_policy`, `fk_resolution_run_record__run_project`, `fk_resolved_entity__run_project`, `fk_rem__run_record` — so "every project flows from the run's, and there is no second source"; VR-021 asserts it end to end -->
- [X] CHK023 Is "purchase-order lines are unbounded within a cluster" stated positively, so an implementation capping them is a violation? [Clarity, FR-041, SC-034] <!-- Evaluator: Covered by spec.md FR-041 ("Purchase-order lines are unbounded within a cluster, because partial shipments and change orders legitimately produce several against one specified material") and SC-034 ("A cluster containing several purchase-order lines is emitted without error"), with data-model.md Invariant 37, the deliberate choice of a partial index on `resolved_entity_id` alone rather than `(resolved_entity_id, member_role)`, and VR-019, which asserts a three-line cluster is **accepted** because "a test of only the first would pass an implementation that bounded everything" -->


## Digests, hashes and versioning

- [X] CHK024 Is the **canonical serialization** each digest is computed over declared — for the alias artifact and for the labeled set both? A digest over an unspecified serialization is not reproducible and FR-004's version identifier is not verifiable. [Completeness, FR-004, FR-017, FR-040] <!-- Evaluator: Resolved — verified directly against data-model.md, and the two halves differed. The alias artifact's serialization **is** declared (plan.md §Project Structure: sorted by `(canonical_key, alias_class, normalized_alias)`, LF endings, no BOM). The labeled set's was **named and never defined** — VR-005 said "a declared canonical serialization whose column order and encoding are fixed in the test", tasks T023 said "per-stratum canonical serialization", and plan.md asserted "the labeled set declares its canonical form already", which was false. Now declared in data-model.md §`labeled_pair_set` (one committed file per stratum, UTF-8/LF/no BOM, fixed column order, rows sorted by the canonical pair identity, surrogate `labeled_pair_id` excluded because it is minted at load); VR-005 now asserts that declaration instead of defining one, and plan.md §Project Structure lists both committed stratum files and drops the false claim -->
- [X] CHK025 Are the two strata's **separate hashes** distinguishable everywhere a hash is referenced, so verifying one is not mistaken for verifying both? [Unambiguity, FR-017, FR-037, SC-037] <!-- Evaluator: Resolved — the schema was unambiguous (data-model.md: two `labeled_pair_set` rows, `estimation_stratum_hash` and `hard_negative_stratum_hash` with pinned kind columns on both `threshold_calibration` and `resolution_run`, `ck_threshold_calibration__strata_distinct`, and guard G1 recomputing "each stratum's hash"), but spec.md referred to "the labeled-set hash" in the singular at FR-017, FR-034, FR-044, FR-047, SC-027, SC-040 and SC-043, under which verifying one stratum satisfies the wording. FR-017 now hashes each stratum separately, requires **both** verified on every reporting run, and fixes the singular phrase as meaning both; FR-034 records both stratum hashes on the manifest -->
- [X] CHK026 Is the alias-table version required on **every merge**, not only on the run, so a merge remains interpretable after the table is edited? [Completeness, FR-004, SC-029] <!-- Evaluator: Covered by spec.md FR-004 ("every merge MUST reference the version in force when it was decided") and SC-029, carried in data-model.md by `resolution_run_record.alias_table_version` + `alias_rule_id` under `ck_resolution_run_record__alias_rule_iff_canonical` ("the rule is stored once per record per run rather than once per merge, so two merges involving one record cannot disagree about which rule fired") and pinned by `fk_resolution_run_record__run_alias_version`. An edit is detectable independently, because `content_digest` pins the version to its contents -->
- [X] CHK027 Is the E002 catalogue digest recorded **separately** from the alias-table version, so editing the table without reseeding is detectable? [Clarity, FR-040, SC-036] <!-- Evaluator: Covered by spec.md FR-040/SC-036 and data-model.md §`manufacturer_alias_table`, which carries three distinct values — `alias_table_version`, `content_digest` ("**Not the same as the version identifier, and the difference is the point**: a version identifier can be reused for changed contents by a careless edit") and `catalogue_digest` — with all three in `uq_manufacturer_alias_table__version_content_catalogue`, the target of `fk_resolution_run__alias_table`, so a run naming a catalogue digest the artifact does not carry has no referent -->
- [X] CHK028 Is the weight vector bound to the **same** labeled-set hash as the thresholds, with divergence in either causing refusal? [Consistency, FR-047, FR-044, SC-043] <!-- Evaluator: Covered by spec.md FR-047 ("bound to the same labeled-set hash … MUST refuse rather than publish") and SC-043, and made structural in data-model.md by the eight-column `uq_threshold_calibration__strata_thresholds` and `fk_resolution_run__calibration` — both stratum hashes, both cutoffs, all four weights in one referencing tuple, so a divergent run "has no referent and cannot be inserted", failing at statement 1 of the write order. Invariant 12; VR-007 and VR-030 perturb each cutoff and each weight **alone**, since a wholesale perturbation would pass an implementation comparing only a sum -->
- [X] CHK029 Is the widening of the referenced unique key from four columns to eight specified as preceding the referencing foreign key, given forward-only migration makes the ordering irreversible? [Completeness, FR-047, plan AD-005] <!-- Evaluator: Resolved — verified directly against data-model.md. The **ordering** is specified and correct in three places (§Migration Sequence `0501`: "The referenced unique key is authored here, and a four-column key here would make the eight-column foreign key in `0502` undeclarable"; `0502`'s row; plan.md's FR-047 coverage row and tasks T009/T010). What was wrong is the **width**, in the two places a migration author would copy from: the §Constraints catalogue still declared `uq_threshold_calibration__strata_thresholds` and `fk_resolution_run__calibration` with four columns, contradicting §`threshold_calibration`, the object inventory, Invariant 12 and the traceability table. Both rows corrected to the eight-column form -->


## Alias table integrity

- [X] CHK030 Is alias-to-canonical stated as a **function**, with a duplicate alias failing **table load** — explicitly not resolved by a runtime tie-break, first-match rule, or score? [Unambiguity, FR-002, SC-028] <!-- Evaluator: Covered verbatim by spec.md FR-002 ("MUST be a function … MUST fail table loading with the alias named, and MUST NOT be resolved by a runtime tie-break, a first-match rule, or a score") and SC-028, carried by `uq_manufacturer_alias__version_alias` — data-model.md: "There is no runtime tie-break to write, no first-match rule to specify, and no score to compute — the ambiguity cannot reach match time because the table cannot be loaded"; VR-001 asserts the constraint name, never the message text -->
- [X] CHK031 Is "exactly one preferred label per canonical manufacturer" specified structurally rather than as a validation rule that could be skipped? [Completeness, FR-001, plan AD-004] <!-- Evaluator: Covered by data-model.md §`canonical_manufacturer` ("**SKOS's 'exactly one preferred label per concept', as a column rather than as a row class.** A second preferred label is not forbidden by a rule — it is unrepresentable, because there is one column to hold it"), Invariant 2 ("column, not a rule"), `uq_canonical_manufacturer__version_label`, the partial `ix_manufacturer_alias__single_preferred` for the alias row, and VR-002 asserted in both directions -->
- [X] CHK032 Are the three alias classes specified as **disjoint**, so a string cannot be both display-worthy and match-only? [Unambiguity, FR-001, plan AD-004] <!-- Evaluator: Covered by data-model.md §`manufacturer_alias` `label_class` ("**SKOS's three pairwise-disjoint label properties.** Disjointness is structural: a row has one class because the column holds one value"), with `hidden` named as the match-only class that "must match and must never be displayed"; a single string cannot hold two classes because `uq_manufacturer_alias__version_alias` admits one row per normalized alias per version -->
- [X] CHK033 Is additive normalization specified as retaining the raw string **in the same record** as the normalized form, so no stage can discard it? [Completeness, FR-003, SC-010] <!-- Evaluator: Covered by spec.md FR-003 ("retained alongside the normalized form, and no stage may discard the raw value") and SC-010, made a row-level fact in data-model.md by `ck_resolution_run_record__manufacturer_pair`, `…__part_number_pair` and `…__unit_pair` ("a normalized manufacturer without its raw string is unrepresentable"), plus `manufacturer_alias.raw_alias`, `unit_alias.raw_unit`, `unmatched_manufacturer_string.raw_value` and the four value columns on `candidate_pair_attribute_score`; Invariant 19 -->
- [X] CHK034 Is the collision guard's test population specified — the labeled **negative** pairs, from the hard-negative stratum — rather than left as "known distinct records"? [Unambiguity, FR-007, FR-037] <!-- Evaluator: Covered by spec.md FR-007 ("tested over the labeled negative pairs") and FR-037(b) (the hard-negative stratum is "used only by FR-007's collision guard and SC-003, and excluded from every published denominator"), with data-model.md guard G2 naming the population exactly — "the **hard-negative stratum's** labeled negative pairs … The stratum FR-037 built for exactly this and for nothing else" — and VR-015 planting a collision over that stratum -->


## Units

- [X] CHK035 Are non-dimensional units specified as carrying **no** base and **no** factor, so "never converted" has nothing to multiply by rather than relying on a code path? [Completeness, FR-005, SC-009] <!-- Evaluator: Covered by data-model.md §`unit_alias` ("`EA`, `LOT` and `LS` have `unit_class = 'arbitrary'`, `base_unit IS NULL` and `factor_to_base IS NULL`. There is no factor to apply … the failure is a null, not a wrong number") under `ck_unit_alias__base_iff_dimensional` and `ck_unit_alias__factor_iff_base`, with the other end closed by `ck_resolution_run_record__base_quantity_iff_dimensional`; Invariants 6 and 7, VR-003 attempting all four violating combinations -->
- [X] CHK036 Is unit agreement specified as a scored attribute that can never act as a hard filter, stated as a prohibition rather than a preference? [Unambiguity, FR-006, SC-008] <!-- Evaluator: Covered by spec.md FR-006 ("MUST contribute … as a scored attribute and MUST NOT act as a hard filter that excludes a pair from consideration") and SC-008, with data-model.md Invariant 8 recording that no object could make it one: `ck_candidate_pair__blocking_keys`'s domain has exactly two members and neither is a unit, so "a pair whose units disagree is a pair with a lower total, not an absent pair" -->


## Referential integrity against upstream

- [X] CHK037 Are the assumptions about E006's extraction output — which columns are read and their guarantees — **stated** rather than assumed, so an upstream change breaks visibly? [Completeness, spec Assumptions, plan Integration Points] <!-- Evaluator: Resolved — the guarantees were stated (spec §Assumptions: "Verified against delivered E006, not assumed", and E006's FR-028 asserting no identity between spellings) but the **read set was not**: §Assumptions named three fields, while the design also reads `unit_of_measure`, `product_description` and `specification_section`, each introduced separately elsewhere. plan.md's Integration Points E006 row now enumerates all six as `field_vocabulary` terms with the role of each, notes purchase-order lines come from `purchase_order_line`'s own columns, and states that each lands in a named `resolution_run_record` column or attribute-score row so a term E006 stops populating surfaces as a NULL and an `absent` comparison rather than a silently shifted total -->
- [X] CHK038 Is the per-attribute score's field reference required to resolve against E003's `field_vocabulary` rather than a free-text field name? [Completeness, FR-014] <!-- Evaluator: Resolved — the foreign key was already specified (`fk_candidate_pair_attribute_score__field` → `field_vocabulary (field_name)`, "the one place in the project where an agreement attribute has one", Invariant 28), but the four values E009 writes were never named against the seeded vocabulary, and both §Scale Assumptions and the spec's Glossary call the fourth attribute "description" — which is **not** a seeded term (E003 seeds `product_description`), so a row written that way fails the FK at write time. data-model.md §`candidate_pair_attribute_score` now names the four terms exactly as `0005_field_vocabulary.py` seeds them and maps `weight_description` onto `product_description` -->


---

**Traceability**: 38 of 38 items carry a reference (100%).
**Authoring note**: written from the plan and the data model's structural summary rather than a line-by-line read of `data-model.md`, after five consecutive API failures took out the delegated planner. Items CHK007, CHK012, CHK024 and CHK029 are the ones most dependent on constraint-level detail; evaluation should verify them against `data-model.md` directly rather than accepting them as framed.

**Evaluation note** (2026-07-29): 27 passed, 11 resolved, 0 remaining — 38 of 38 checked. Ten resolved by the evaluator; **CHK021 resolved by project-owner decision** (option A, the rejected specification-section record is left unclustered), which added disclosed gap **G-14** for the cost of that option. The four flagged items were checked against `data-model.md` line by line and three of the four were mis-framed to some degree:

- **CHK007 was right as written.** §The FR-045 Extension says so itself, and A-2 asked for the requirement to be corrected. Resolved in `spec.md`.
- **CHK012 was wrong.** `NULLS DISTINCT` has a dedicated paragraph, a `pg_index` assertion (VR-020) and a line in the plan's summary table. Passed on that evidence; no rewording needed, because the item asks a question rather than asserting a gap.
- **CHK024 was half right.** The alias artifact's serialization is declared in `plan.md`; the labeled set's was named in three places and defined in none. Resolved for the labeled set only.
- **CHK029 was right about the risk and wrong about the location.** The `0501`-before-`0502` ordering is specified three times; the **width** was still four columns in the §Constraints catalogue, which is what a migration author copies from. Resolved there.
- **CHK010's premise was corrected in place** — it asked about "already-populated" E003 tables; both are empty, which is the precondition the chosen DDL depends on and the reason no backfill exists.

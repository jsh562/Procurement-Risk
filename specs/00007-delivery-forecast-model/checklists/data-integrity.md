# Data Integrity: Delivery Forecast Model

**Created**: 2026-07-27 | **Feature**: [spec.md](../spec.md) | **Domain**: Data Integrity | **Depth**: Standard | **Audience**: Reviewer (PR)

## Invariant Enforcement and Its Locus

- [X] CHK001 Is every stated row and array invariant carried by a declared enforcement class — declarative constraint, application code, or test-only — recorded in one place, rather than recoverable only by reading the constraint inventory against the validation rules and the gap list? [Completeness, data-model §Named Object Inventory / §Validation Rules] <!-- Evaluator: Covered by data-model §Validation Rules — the Enforcement point and Tier columns declare the class of every rule (constraint / job / test), and §Named Object Inventory carries every declarative constraint by name -->
- [X] CHK002 For each invariant whose enforcement is application-side, are the write paths it must hold on enumerated, instead of resting on an unstated assumption that the fit job is the only writer? [Completeness, DV-001, DV-004, DV-005, DV-007] <!-- Evaluator: Resolved — added the writer enumeration table to data-model §Validation Rules, naming `model.forecast.write` as the sole writer of all five stores, why no second path exists (FR-034, FR-025/DV-022, one-writer concurrency), and the condition under which the Job-enforced rules would have to become constraints -->
- [X] CHK003 Is the population DV-014 quantifies over — "every emitted run" — defined precisely enough to separate runs this epic emits from the fixture rows E003's delivered suite inserts at an unpinned shape, so the pin's scope is stated rather than inferred? [Precision, DV-014, AD-009] <!-- Evaluator: Resolved — DV-014 now defines "emitted run" as a run row this epic's fit job wrote, identified by the run_id the assertion's own invocation returned, and states why a whole-table assertion would fail E003's legal 5-draw/3-day fixtures while a run-scoped one fails only NC-10's planted shape -->
- [X] CHK004 Is the obligation to reuse E003's helper functions rather than re-declare them stated as a requirement with a detection mechanism, rather than as a convention note, so a second definition of one shared invariant is a failure and not a silence? [Consistency, ADR-0018 §Consequences/Neutral, data-model §Conventions] <!-- Evaluator: Resolved — added DV-026: exactly one E007-declared function, asserted over the revision sources and `pg_proc`, with the note that the delivered `test_every_function_is_named_in_the_data_model` audit does not catch a re-declared helper because documenting it satisfies that audit -->
- [X] CHK005 Is an obligation stated that any future strengthening of a shared array invariant must land on both stores, with a mechanism that fails when only one is changed? [Consistency, ADR-0018 §Consequences/Negative] <!-- Evaluator: Resolved — added DV-027, a one-for-one pairing of the seven shared array-invariant checks across `line_posterior` and `held_out_prediction` over `pg_constraint` definitions; ADR-0018 names this obligation and supplies no mechanism, and DV-027 is that mechanism -->
- [X] CHK006 Is the required behaviour defined for a write that a declarative constraint rejects mid-transaction — its error category, its exit class, and whether it is reported as a refusal or as a defect in the writer? [Completeness, plan §Error Handling Strategy, FR-017] <!-- Evaluator: Resolved — added a row to plan §Error Handling Strategy: transaction 1 rolls back in full, non-zero exit naming the constraint, and the outcome is reported as a defect in the writer rather than a refusal, because the gate has already passed (§The Refusal Guarantee mechanism 3). The data outcome is identical to a refusal, which is why the report must distinguish them -->
- [X] CHK007 Is a requirement stated that every scalar on the run row derivable from its own child rows — `open_line_count`, `training_line_count`, `held_out_fraction_realized`, `held_out_uncensored_event_count` — must agree with those rows, with the agreement obligated per scalar rather than for the one that has a rule? [Consistency, FR-006, DV-001] <!-- Evaluator: Resolved — added DV-028, obligating agreement per scalar: `training_line_count`, `held_out_fraction_realized` and `held_out_uncensored_event_count` each get a named comparison against `forecast_split_assignment` (and `held_out_prediction`), joining `open_line_count`, which DV-001 already covered -->
- [X] CHK008 Is a consistency obligation stated between the stored censoring indicator and the lifecycle events it was derived from at the run's as-of date, given FR-004 requires the value be stored and never re-inferred and no stored-row rule compares the two? [Consistency, FR-004, data-model §`forecast_split_assignment`] <!-- Evaluator: Resolved — added DV-029: every `forecast_split_assignment.is_censored` agrees with the absence of a terminal `lifecycle_event` at or before `forecast_run.as_of_date`, asserted over the stored rows by a path independent of the one that wrote them. The existing `censoring.py` property proves the pure function, not the stored value -->
- [X] CHK009 Is the required behaviour stated when a monitored quantity cannot be computed — recorded as absent, omitted, or refusing the run — given every E007 column but one is NOT NULL and therefore admits no representation of absence? [Completeness, FR-016, data-model §Nullable-column checks] <!-- Evaluator: Resolved — added a row to plan §Error Handling Strategy: an uncomputable monitored diagnostic is treated as a breach and refuses. Both alternatives are closed rather than chosen — "recorded as absent" is unrepresentable under NOT NULL plus `ck_forecast_diagnostic__observed_finite`, and "omitted" fails DV-011 -->
- [X] CHK010 Is artifact immutability — an artifact row written once and never updated — stated as a requirement, and is that claim reconciled with a deployed superuser connection for which the withheld `UPDATE` grant is a latent fact? [Completeness, data-model §Migration Sequence, E003 G-11] <!-- Evaluator: Resolved — immutability is now FR-034 with covering criterion SC-031 and validation rule DV-032. The reconciliation was already stated in data-model §Migration Sequence (E003 G-11, superuser connection, latent grant) and is carried into the requirement itself: the grant records intent, the writer carries the guarantee, and DV-032 asserts both halves -->
- [X] CHK011 Does each application-side rule name both the tier that proves it and the artifact it is proved against, so a rule exercised only over a pure function is distinguishable from one asserted over stored rows? [Testability, data-model §Validation Rules, plan §Testing Strategy] <!-- Evaluator: Covered by data-model §Validation Rules (Enforcement point names the artifact — stored rows, the fit's input frame, emitted runs, the emitted report; Tier names the level) and plan §Testing Strategy, whose Property row settles the exact ambiguity by naming DV-004, DV-005, DV-007, DV-010 and DV-020 as asserted over an in-memory artifact, never a database read, with the database-backed half owned by the Integration tier -->

## The Two-Store Split

- [X] CHK012 Is a standing obligation stated that any criterion quantifying over stored forecasts must name its store and its enforcing mechanism, rather than three criteria having been corrected individually after the split was introduced? [Consistency, SC-002, SC-003, SC-013, STF-005] <!-- Evaluator: Resolved — added a standing obligation to spec §Measurable Outcomes generalising STF-005: any criterion quantifying over stored forecasts must name its store or stores and the enforcing mechanism in each, so a fourth such criterion is caught by reading it rather than by another stress-test pass -->
- [X] CHK013 Is FR-022's per-line reproduction comparison scoped to a population — open lines, held-out predictions, or both — given AD-004's tolerance derivation counts lines across both? [Precision, FR-022, SC-018, AD-004] <!-- Evaluator: Resolved — FR-022 and SC-018 now name both stores explicitly, with the reason: AD-004's derivation counts ≈68 lines across both populations, so scoping the comparison to one store would leave the other unclaimed while borrowing a tolerance derived from both -->
- [X] CHK014 Is the write-atomicity requirement quantified per store, with the mechanism carrying it named on each side rather than argued once for the delivered table? [Completeness, FR-013, SC-023, data-model §Write Order] <!-- Evaluator: Covered by data-model §Write Order steps 3 and 4 — "both arrays are columns of the same row, so FR-013 holds by table design" for `line_posterior` and "same one-row property" for `held_out_prediction` — restated per store in §Requirement Traceability FR-013 ("in both stores") and in the §Delivered Audits trigger row -->
- [X] CHK015 Is a uniqueness requirement stated that no line may hold artifact rows in both stores under one run, given disjointness is structural on the held-out side only? [Uniqueness, G-5, ADR-0018 §Consequences/Negative] <!-- Evaluator: Resolved — added DV-030, stating the cross-store uniqueness directly: no `po_line_id` holds artifact rows in both stores under one run, with the three populations asserted exhaustive and pairwise disjoint. This is G-5's other direction stated as a cross-store property rather than left implied by DV-001 and DV-002 -->
- [X] CHK016 Is the set of lines that receive no stored forecast under a run stated explicitly, rather than left as the complement of the two artifact populations? [Completeness, FR-008, DV-001, DV-002] <!-- Evaluator: Resolved — added "The third population: lines that receive no stored forecast" to data-model §Array and Anchor Semantics: `split_side = 'train'` and `is_closed = true`, ~131 of 199 lines per run, carrying a split-assignment row and nothing else, with why that is correct on both counts -->
- [X] CHK017 Does a requirement fix the artifact hash's ordering across the two populations and oblige it to be recomputable from the stored rows, as the split assignment hash is? [Traceability, FR-014, SC-022, DV-017, data-model §Hashes and What Each Covers] <!-- Evaluator: Resolved — the ordering was already fixed by §Hashes (`population_rank`, then `canonical_ordinal`); the recomputability half was missing and is now DV-031, with §Hashes stating how the ordinal is reached from an artifact row (join to `forecast_split_assignment`), so the artifact hash carries the same obligation DV-017 places on the split assignment hash -->
- [X] CHK018 Is the rule that the worklist reader must never read the held-out store recorded as a requirement with a named owner and a detection mechanism, rather than only as an ADR consequence the schema cannot enforce? [Completeness, FR-012, ADR-0018 §Consequences/Negative] <!-- Evaluator: ASK — the gap is real (data-model §Array and Anchor Semantics states "E010 must never read this table" and ADR-0018 records it as a documented contract with no schema mechanism), but the two resolutions differ materially: disclose it as a gap plus a propagation obligation on E010, or have E007 own a build-gating repository check now. Left unchecked pending the batched question -->
  <!-- Evaluator: Disclosed as G-19 with a reversal trigger, plus propagation obligation P-8 onto E010's plan. An E007-owned build-gating check was rejected: it would assert over a module no epic has authored, which passes vacuously — the detector-that-has-never-detected case the Negative Controls preamble refuses. -->
- [X] CHK019 Is there a stated obligation to disclose, to consumers of the published `anchor_date_convention` constant the serving boundary reads, that it now describes one of two forecast populations? [Completeness, FR-012, ADR-0018 §Context] <!-- Evaluator: Resolved — recorded as propagation obligation P-7 in plan §Propagation Obligations. The constant sits on E003's singleton row and is served by /src/api, so the disclosure has to reach consumers of the published constant rather than readers of E007's report; a feature branch may record the need and not perform it -->
- [X] CHK020 Are the delivered guarantees that do not extend across the split enumerated as one set — constraint coverage, the tolerance drift test, the published anchor convention, the read contract — rather than each being recorded where it happened to bite? [Completeness, G-3, ADR-0018 §Considered Options (Option A, Cons)] <!-- Evaluator: Resolved — added "What the split does not carry across — the complete set" to data-model §Array and Anchor Semantics: seven rows covering delivered constraint coverage (DV-027), the tolerance drift test (G-3), the published anchor convention (P-7), E010's read contract, structural disjointness (G-5/DV-030), the artifact hash's ordering (DV-031), and the refusal guarantee's store enumeration -->
- [X] CHK021 Is the unit and permitted representation of a stored draw stated — whole or fractional days — given one array feeds a whole-day survival grid and a day-denominated reproduction tolerance? [Precision, FR-010, AD-004, data-model §Array and Anchor Semantics] <!-- Evaluator: Resolved — added a "Unit and permitted representation of a draw" row to data-model §Array and Anchor Semantics: days, `double precision`, fractional permitted and expected, no rounding at storage, with the reason (AD-002's inverse-CDF returns a continuous value and AD-004's tolerance is measured against unrounded values) and the two places whole days do appear -->
- [X] CHK022 Is the rounding convention applied when a draw becomes a day-grid offset published for both populations, rather than left to the implementation on the store no delivered convention reaches? [Precision, FR-010, DV-004] <!-- Evaluator: Covered by data-model §Validation Rules DV-004 and DV-003, which publish the mapping for "every stored artifact row in either store" as a strict threshold count — `survival[k] = count(draws > k) / draw_count` and `residual = count(draws > horizon_days) / draw_count` — so there is no rounding step to leave to an implementation. Restated per population in §Array and Anchor Semantics -->
- [X] CHK023 Is the required behaviour stated for a run whose held-out delivered population is empty, given the zero-open-line case is refused and the empty-gradeable-set case is not addressed? [Completeness, FR-021, SC-017, DV-002] <!-- Evaluator: Resolved — added a "Zero held-out delivered lines" edge case to spec §Edge Cases & Boundaries: the run refuses. It is not made structural like FR-021, because SC-010 already requires delivered and censored lines on both sides and DV-007 evaluates that before any write, so the refusal reuses the existing refuse-before-write path; `held_out_uncensored_event_count` keeps `>= 0` because it records a realized count -->
- [X] CHK024 Is the requirement that every artifact row joins to exactly one run stated with the join key for each store, given the four child tables reach the run through two different key shapes? [Traceability, SC-001, SC-013, data-model §`held_out_prediction`] <!-- Evaluator: Covered by data-model §Entities (the Relationships column gives the join key per store), §Named Object Inventory/Constraints (`fk_held_out_prediction__run_shape` on the shape triple MATCH FULL, `fk_forecast_split_assignment__run` and `fk_forecast_diagnostic__run` on `run_id`), and §Delivered Schema — Fixed Input, which states why the artifact stores use the shape key: both arrays' lengths are proved against the run's own values with no cross-row read -->

## Migration Requirements

- [X] CHK025 Does each of the four revisions state a down-path or explicitly declare that none exists, together with the recovery procedure an operator follows in its absence? [Completeness, data-model §Migration Sequence] <!-- Evaluator: Resolved — data-model §Migration Sequence now declares "no down-path exists on any of the four revisions, by policy" and states the recovery procedure as two distinct procedures: a revision that failed while applying (nothing to recover — one transaction per run on transactional DDL, re-run `migrate`, proved by E003's delivered failure/resume tests) and a revision that applied and is unwanted (a new forward revision that drops the object, with the data-loss disclosure for `0300`) -->
- [X] CHK026 Is the declaration of preconditions stated as an obligation on every revision, rather than only on the one revision that happened to need an empty table? [Completeness, data-model §Migration Sequence, G-2] <!-- Evaluator: Covered by data-model §Migration Sequence — the table carries a Gate column populated for all four revisions, not only `0300`: the empty-table precondition on `0300`, the chain dependency on `0301` and `0303`, and the unique-key-before-FK ordering that forces `0302` to be one revision. The honest content for the three that create new tables is "no data precondition beyond chain order", and the column says so rather than omitting them -->
- [X] CHK027 Does each revision state whether any existing row is rewritten, re-validated, or rejected — including the unique key added to another epic's delivered table, whose "rejects no previously legal row" claim is asserted in prose? [Completeness, G-14, data-model §Added Object on a Delivered Table] <!-- Evaluator: Covered by data-model §Added Object on a Delivered Table, where the claim is a proof and not an assertion — the unique key's leading column is already `purchase_order_line`'s primary key, so the triple is unique on every existing and future row — plus §Migration Sequence, which states the effect on existing rows for each of the four (`0300` empty table, `0301`/`0303` new tables, `0302` the proved unique key). A standing statement of the same property across the chain was added while resolving CHK032 -->
- [X] CHK028 Is the state left by a partially applied chain declared at each revision boundary, and is the fit job required to refuse against a schema sitting at an intermediate revision? [Completeness, data-model §Migration Sequence, plan §Error Handling Strategy] <!-- Evaluator: Resolved — data-model §Migration Sequence now carries a boundary table declaring the schema state and what is missing at `0103`, `0300`, `0301`, `0302` and `0303`, and plan §Error Handling Strategy gains a first-evaluated precondition: refuse before reading when the head is not `0303`, naming the observed and required heads, and never migrate. The `0302` boundary is why it is a refusal — every write would succeed except the one recording why the run was allowed to write -->
- [X] CHK029 Is a requirement stated for preserving the single-headed revision chain when two sibling epics branch from the same baseline and would each set the same `down_revision`, given the claimed block protects filename prefixes and not Alembic heads? [Consistency, spec §Scope (numbers claimed at epic start), data-model §Migration Sequence] <!-- Evaluator: Resolved, after verifying against `src/model/src/model/schema/versions/` and the delivered checks. The premise holds — the block protects prefixes only — but a head-protecting mechanism does exist and was unnamed: `tests/checks/test_migration_ranges.py::test_the_revision_graph_resolves_to_a_single_head` derives the graph from `down_revision` on disk, and E003's `test_migration_chain.py` adds `test_chain_resolves_to_exactly_one_head` and `test_chain_is_linear`, the latter failing on a branch point — two revisions naming one parent — which is the E008/E009 collision one commit early. Added DV-033 naming them as E007's covering assertion; restated `0300`'s parent as the head at landing rather than the literal `0103`, with a re-parenting obligation if a sibling lands first; and constrained AD-007's remediation to leave the graph checks intact, since four of its parts edit the file they live in -->
- [X] CHK030 Is the privilege set each store requires stated as a requirement — including the deliberate withholding of `UPDATE` and what that withholding is claimed to guarantee — rather than recorded only as migration prose? [Completeness, data-model §Migration Sequence] <!-- Evaluator: Resolved — the privilege set is now FR-034 rather than migration prose: `SELECT, INSERT, DELETE` granted and `UPDATE` withheld, what the withholding is claimed to guarantee (an artifact row written once and never edited), why `DELETE` is retained, and the limit the claim has because the deployed connection is a superuser (E003 G-11). SC-031 covers it and DV-032 asserts both halves — the grant over `information_schema`, and the absence of any `UPDATE` against an artifact store in the writer -->
- [X] CHK031 Is there a stated verification obligation that the revisions are applied forward from the delivered head against a real database, rather than against a schema constructed by any other route? [Testability, plan §Testing Strategy (Integration), data-model §Migration Sequence] <!-- Evaluator: Resolved — added DV-034 and extended plan §Testing Strategy's Integration row: the four revisions are proved by forward application from empty against a real PostgreSQL through E003's delivered `test_chain_applies_to_an_empty_database`, which walks every revision on disk against a throwaway database, and no E007 test may build its schema by `create_all`, a dump, or an incrementally migrated developer database. Verifying this surfaced the second hard-coded block table — see the report -->
- [X] CHK032 Does a requirement state what becomes of rows already emitted under an earlier revision when a later revision adds or changes a constraint those rows were never validated against? [Completeness, data-model §Migration Sequence, G-3] <!-- Evaluator: Resolved — data-model §Migration Sequence now states it as a property of the whole chain: no E007 revision adds or changes a constraint that existing rows were never validated against, with each of the three ways it could arise closed by construction (`0300` empty table, `0301`/`0303` all-constraints-at-create, `0302`'s unique key proved satisfied by the PK prefix), `NOT VALID` declared nowhere, and a future strengthening routed through the two-step forward migration §Immutable Helper Functions already fixes -->

## Dimension Discipline and Traceability

- [X] CHK033 Does each data-quality criterion name a measurement rule beside the dimension it claims, given a dimension cited without one is no more checkable than citing nothing? [Testability, research §Data integrity for derived analytical artifacts] <!-- Evaluator: Covered — the defect the item guards against does not occur, and not by accident. No criterion in spec.md or data-model.md cites a data-quality dimension as a bare label; every one of DV-001–DV-034 is itself a measurement rule, and §Validation Rules' preamble makes that the admission condition ("machine-checkable properties ... a build-gating assertion, not a review note", with an enforcement point and a tier). research §Data integrity is where the 25012-is-a-model-not-a-conformance-standard caveat is recorded -->
- [X] CHK034 Where a data-quality dimension is named, is the taxonomy it is drawn from named with it, given the two taxonomies in circulation disagree on membership? [Clarity, research §Data integrity for derived analytical artifacts] <!-- Evaluator: Covered by research §Data integrity for derived analytical artifacts, which names both taxonomies explicitly (ISO/IEC 25012:2008's 15 characteristics against DAMA UK's 2013 six), states precisely where they disagree (uniqueness and validity absent from 25012; precision, traceability and recoverability absent from DAMA), and derives the five that transfer to a derived analytical artifact — completeness, consistency, uniqueness, precision, traceability. That is exactly the tag vocabulary this checklist uses -->
- [X] CHK035 Is any integrity criterion phrased against a true value of a stored draw — an accuracy claim whose only analogue here is calibration — which this epic is forbidden from judging? [Consistency, FR-026, SC-026] <!-- Evaluator: Covered — none is, checked criterion by criterion. Every integrity claim over draws is relational rather than truth-referring: recomputation identities (SC-003/DV-003, DV-004), a between-decile comparison and a published floor (SC-027/DV-005), an inter-vendor width comparison (SC-005/DV-010), an ablation delta against an independently derived floor (SC-008/DV-020), and a run-to-run agreement (SC-018/DV-018). FR-026 and SC-026 forbid a verdict and DV-021 checks its absence over the emitted set -->
- [X] CHK036 Is every stored artifact row traceable to the input rows it was computed from and to that input's layer label in a direction a reader can follow from the artifact, rather than only from the run manifest outward? [Traceability, FR-014, SC-020] <!-- Evaluator: Covered by data-model §Entities and §Additions to `forecast_run`: the direction the item asks for is structural, not documentary. Every artifact row's primary key leads with `run_id` under a foreign key to `forecast_run`, whose `input_layer`, `input_datasheet_ref`, `input_data_hash` and `input_fixture_digest` are all NOT NULL — so artifact → run → layer label is one join with no nullable link. The input row itself is reached the same way: every artifact row carries `po_line_id` under a foreign key to `purchase_order_line`, and the lifecycle events follow from the line -->
- [X] CHK037 Is the provenance-warning outcome — a moved fixture digest against unchanged rows — required to be recorded on the run it qualifies, rather than only emitted to the job's output, so a later reader of the artifact can see the break? [Traceability, FR-023, DV-016] <!-- Evaluator: Resolved — data-model §Hashes now states where the warning is visible and where it is not: `input_fixture_digest` is NOT NULL and records the digest observed at run time, so a later reader reconstructs the break by comparing it against E005's published `dataset_content_hash`; a nullable warning flag is deliberately refused (second nullable column against a design declaring one, and a fact the digest pair already carries). What is genuinely lost — that the job named it on that occasion — is disclosed as **G-16** in G-8's form, with a reversal trigger and a production-scale alternative -->

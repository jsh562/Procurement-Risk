phase: Phase 4 complete — OBJ3 (Invocation Record with Recomputable Cost)
completed: 48
remaining: 27
blocked: none
next: Phase 5 (OBJ4) T051-T059 — mode selection with no default and the
      provider opt-in, the content-hash fixture store, provenance sidecars,
      and offline replay. T051 unblocks orchestrator.invoke(), which still
      raises NotImplementedError because the composition needs the mode branch.
notes: OBJ3 delivered. The record writer holds its own connection and its own
       transaction, verified with two live connections and a caller rollback.
       The spool is a real SQLite file in every test; the database failures the
       drain exists for are injected through the writer's connect seam, since
       'refuse this one write then accept the next' is not something a live
       server does on request.
       Three self-referential test bugs found and fixed this phase, all the
       same shape: a check that scanned source text matched the prose
       explaining what it was looking for. Each is now an AST walk with a
       control that plants a real violation.
       A connect timeout was added to the writer — psycopg's default is no
       timeout, which turns an unreachable database into a hung process and
       defeats TR-034's deadline entirely. Found because the suite hung for
       ten minutes when the container went down mid-run.
       Environment: the project DB container was torn down by something outside
       this session twice, once with its volume. `docker compose up -d db` then
       `uv run --directory src/model migrate` restores it. The sibling
       checkouts share the unqualified `procurement-api:e001` image tag and the
       5434 port, so both are contended surfaces.
timestamp: 2026-07-26

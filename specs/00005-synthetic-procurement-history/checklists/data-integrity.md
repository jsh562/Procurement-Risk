# Data Integrity: Synthetic Procurement History

**Created**: 2026-07-26 | **Feature**: [spec.md](../spec.md)

## Committed-Artifact Contract and the Reproducibility Oracle

- [X] CHK001 Is the object FR-021 hashes — the parsed fixture payload, its complete field set, and its member ordering — pinned in exactly one artifact, with every other artifact deferring to it rather than restating a second shape? [Consistency, Plan §AD-001, §AD-002] <!-- Evaluator: Resolved — plan.md § Data Model Summary now names data-model.md §Envelope / §Line record / §Event record / §Determinism as the single normative enumeration of the hashed payload and its ordering, and marks its own row as a partial summary that defers on any difference -->
- [X] CHK002 Is the fixture envelope's field set enumerated as closed, so that a field named in one document but absent from another cannot be read as optionally inside the hashed payload? [Completeness, Data Model §Envelope] <!-- Evaluator: Resolved — added "The envelope's field set is closed" to data-model.md §Envelope: the thirteen keys named, all mandatory, no optional field and no extension point, closure extended to the line and event records, and a `dataset_schema_version` bump required to widen any of the three -->
- [X] CHK003 Are the canonicalization rules that make the payload byte-stable — key ordering, separators, ASCII policy, encoding, trailing-newline handling, and the prohibition on JSON floats — stated as normative requirements, together with the rule that the digest covers the re-serialized parsed content rather than the committed bytes? [Clarity, FR-013, FR-021] <!-- Evaluator: Resolved — FR-013 now states the rule set normatively (sorted keys, compact separators, literal non-ASCII, UTF-8, no indentation, no trailing newline) and prohibits JSON floats outright; FR-021 fixes the hashed object as the canonical re-serialization of the parsed payload and prohibits hashing the committed bytes. data-model.md §Conventions gains a "Canonical form vs committed file — not a contradiction" row and research.md scopes its "no trailing newline" to the canonical byte string -->
- [X] CHK004 Is `quantity`'s decimal scale fixed as a stated value in the fixture contract, given that the delivered `numeric` comparison ignores trailing zeros while the digest does not? [Measurability, Plan §AD-004, HINT-005] <!-- Evaluator: Resolved — the contract said "at most 1 decimal place", which is a bound and not the fixed scale AD-004 decided. data-model.md §Entities, §Line record, §Corpus overlap and DV-005 now pin scale exactly 1 via `^(0|[1-9][0-9]{0,2})\.[0-9]$` — `6.0`, never `6` or `6.00` -->
- [X] CHK005 Is a single representation specified for every temporal fixture field — calendar-date form, instant form, timezone, and time precision — so that field width cannot vary row to row and move the digest? [Completeness, Data Model §Conventions] <!-- Evaluator: Covered by data-model.md §Conventions (calendar anchors `YYYY-MM-DD`; event instants RFC 3339 UTC with a literal `Z`, always `T00:00:00Z`), §Event record, and plan.md HINT-002 pinning `timespec="seconds"` against `isoformat()`'s variable-width default -->
- [X] CHK006 Are deterministic output ordering and per-line draw independence stated as requirements with their named failure modes (positional seeding, set or hash-ordered iteration in the write path) rather than left as design commentary? [Completeness, FR-019, FR-020] <!-- Evaluator: Resolved — the failure modes were only in data-model.md §Determinism and research.md. FR-019 now prohibits positional derivation by name (index added to the root seed, one stream consumed in emission order); FR-020 now prohibits set, hash-randomised-mapping and parallel-queue iteration in the write path and notes a stream-identity check would not catch it. data-model.md gains DV-023 (ordering, run-to-run byte identity) and DV-024 (per-line independence) so both are asserted over the artifact -->
- [X] CHK007 Is the reproducibility claim scoped to an identified resolved library version, with the authoritative location of that pin named and a scope-limit report required instead of a reproduction claim outside it? [Clarity, FR-022, Plan §AD-006] <!-- Evaluator: Covered by plan.md §AD-006 (`uv.lock` is the pin, `pyproject.toml`'s `numpy>=2.4.6` is a floor, reproduced by `uv sync --locked`), the resolved `numpy==2.4.6` in data-model.md §Determinism and the envelope's `library_pin`, and plan.md § Reporting Obligations requiring an evidenced scope-limit report rather than a reproduction claim -->
- [X] CHK008 Is the negative control stated as an obligation the check can fail — a different root seed must produce a different committed digest — rather than inferred from the hashing scheme? [Measurability, SC-013, Data Model §DV-015] <!-- Evaluator: Covered by SC-013, DV-015 ("a different `root_seed` yields a different value") and data-model.md §Determinism, whose Negative control row states outright that the check must be able to fail -->

## Row-Level Mapping into the Delivered Schema

- [X] CHK009 Does the mapping assign every column of both delivered tables exactly one origin — fixture, derived at load, database-generated, or constant — with no column left unassigned and none carrying two? [Completeness, Data Model §Row-Level Mapping] <!-- Evaluator: Resolved — the table was checked column-by-column against `0007_procurement.py` and is in fact complete (21 `purchase_order_line`, 9 `lifecycle_event`), but completeness was never claimed and §Delivered Schema wrongly said `created_at DEFAULT now()` sits "on both tables" when it exists only on `purchase_order_line`, which would have a reader looking for an unassigned column. §Delivered Schema corrected and §Row-Level Mapping now states the counts, the disjointness of the four origins, and that none is unassigned or doubled -->
- [X] CHK010 Is the set of fields deliberately kept out of the fixture and derived at load named exhaustively — both surrogate uuid keys, `lifecycle_state`, `is_closed`, `closing_event_id`, `from_state`, `is_terminal` — so the "no fact has two homes" boundary is stated rather than implied? [Completeness, Plan §AD-003] <!-- Evaluator: Covered by plan.md § Data Model Summary, which names all six plus both uuid keys as "derived at load and deliberately absent, so no fact has two homes"; restated in data-model.md § Data Model Summary (FixtureLine) and §Line record / §Event record, and reasoned in AD-003 -->
- [X] CHK011 Is each derivation rule specified precisely enough that two independent readers would compute the same value, including the uuid namespace constant, the key components, and their separator? [Clarity, Data Model §Row-Level Mapping] <!-- Evaluator: PARTIAL — left unchecked deliberately. data-model.md §Row-Level Mapping gains a "Name construction" paragraph fixing the separator (single `|`, U+007C), the tag-first component order, UTF-8 encoding, verbatim text components, and base-10 ASCII integers with no leading zeros or sign. The remaining gap is `NS_E005` itself: it is named as "a committed UUID constant in generator source" but no literal value is recorded anywhere, so two readers still cannot compute the same uuid. Choosing the literal and choosing where it is pinned are decisions for the user, not the evaluator -->
- [X] CHK012 Are the database-generated columns identified as ones the loader must not supply, kept distinct from the derived columns it must supply? [Consistency, Data Model §Row-Level Mapping] <!-- Evaluator: Resolved — "the loader must not supply them" was attached to only one of the four **database** rows. data-model.md §Row-Level Mapping now carries a "What the loader supplies and what it must not" paragraph: fixture / derived / constant MUST be supplied, `closing_event_po_line_id`, `closing_event_terminal`, `prev_sequence_no` and `created_at` MUST NOT be named in any INSERT or COPY column list, with the two failure directions stated -->
- [X] CHK013 Is it stated where the roster hash is stamped from and why it is carried once in the envelope rather than per line, so FR-002's per-row obligation and the single-source rule do not read as contradictory? [Consistency, FR-002, Data Model §Envelope] <!-- Evaluator: Covered by data-model.md §Envelope, which stamps `roster_hash` from `generation_inputs[roster path]`, states the value is not repeated per line, and reconciles the two readings — FR-002 is an obligation at the storage boundary, and 199 copies of one constant inside a hashed artifact is a value that can disagree with itself. Restated in §Row-Level Mapping (origin: envelope) and plan.md § Compliance Result, Principle I -->
- [X] CHK014 Is the write order stated as a requirement that names the delivered constraint forcing each step, distinguishing the non-deferrable chain and line foreign keys from the one deferrable closing foreign key validated at commit? [Traceability, FR-024, FR-029, Data Model §Write Order] <!-- Evaluator: Covered by data-model.md §Write Order, which names `fk_lifecycle_event__line` at step 2, `fk_lifecycle_event__chain` ("not deferrable") at step 3 and `fk_purchase_order_line__closing_event` validated at COMMIT at step 4, with §Delivered Schema calling the last "the schema's only deferrable constraint"; FR-024 states the chain FK as the reason, and HINT-004 repeats the order -->

## Constraint Conformance and Value Domains

- [X] CHK015 Are the value domains for every NOT NULL descriptive column specified so a generated row cannot be rejected by a delivered presence, format, or positivity CHECK? [Completeness, FR-031, Data Model §Delivered Schema] <!-- Evaluator: Covered by data-model.md §Line record, which gives a value domain for all six FR-031 columns plus `po_number`, against the six presence checks and `ck_pol__quantity_positive` quoted in §Delivered Schema; DV-004 and DV-005 assert them, including the trim set -->
- [X] CHK016 Are the anchored identifier and digest formats the schema enforces — project id, vendor id, PO number, and the `sha256:` roster-hash form — restated as generation obligations rather than assumed from the schema? [Consistency, FR-002, Data Model §Delivered Schema] <!-- Evaluator: Covered by data-model.md §Entities and §Line record, which carry `^PRJ-[0-9]{3}$`, `^VND-[0-9]{3}$` and `^PO-[0-9]{3}-[0-9]{4}$` as generation domains, and §Conventions, which states the digest form as `sha256:` + 64 lowercase hex with uppercase and unprefixed both refused; FR-002 states the same form as an obligation on every generated line -->
- [X] CHK017 Is criticality specified both as an integer 1–5 and as a value whose five bands are all reachable, so the delivered band constraint and the tier-by-tercile derivation cannot disagree? [Completeness, FR-012, FR-035, SC-005] <!-- Evaluator: Covered by FR-035 (a tier-by-tercile table needs at least two tiers to reach five bands), the 3x3 table in data-model.md §Slack, schedule pressure, criticality with "Nine cells, five distinct bands, every band reachable", §Line record ("All five bands must occur"), DV-006 and SC-005 -->
- [X] CHK018 Are the closed-iff-closing-event, closed-iff-delivered, and terminal-iff-delivered biconditionals stated as immediate obligations satisfied at insert, distinct from the one condition validated at commit? [Clarity, FR-029, Data Model §Delivered Schema] <!-- Evaluator: Covered by data-model.md §Delivered Schema, which labels `ck_pol__closed_iff_closing_event` / `ck_pol__closed_iff_delivered` **Immediate** ("a closed line must be inserted already naming its closing event and already in `delivered`"), states `ck_lifecycle_event__terminal_iff_delivered` as a both-directions CHECK, and names `fk_purchase_order_line__closing_event` as the schema's *only* deferrable constraint — which is what makes the other three immediate by exclusion. §Write Order steps 2 and 4 separate insert-time from commit-time -->
- [X] CHK019 Is the legal transition set enumerated in one place, with the opening null-to-submitted transition accounted for and the six-transitions-versus-five-durations discrepancy explicitly reconciled rather than left to a reader's count? [Consistency, FR-005, FR-007, Data Model §State Machine Conformance] <!-- Evaluator: Covered by data-model.md §State Machine Conformance: one table of eight rows, the opening `(NULL, sequence 1) -> submitted` included, and an explicit paragraph reconciling "six forward transitions" against five inter-event durations — the opening transition is the clock start and carries no elapsed time, and the two counts must not be reconciled by inventing a sixth duration. §Duration model's share table lists exactly those five forward legs plus two rework legs -->
- [X] CHK020 Is the rework rule stated so that a loop repeats states at new sequence positions and never reuses a position, with the resulting event-count relation and the hard cap both given? [Clarity, FR-006, Data Model §State Machine Conformance] <!-- Evaluator: Covered by FR-006 (a loop repeats a state at a later sequence position rather than reusing a position; hard cap three) and data-model.md §State Machine Conformance, which gives the sequence, the relation `6 + 3L` events with `L <= 3`, and `uq_lifecycle_event__line_sequence` as what makes repeating a position unrepresentable; DV-009 asserts the cap -->
- [X] CHK021 Are the cross-row and whole-dataset properties the delivered schema does not enforce — contiguous sequence numbers from 1, instants strictly increasing with sequence, one project and one vendor per PO, and no instant later than the as-of date — stated as explicit obligations rather than assumed from construction? [Coverage, Data Model §Disclosed Gaps G-2, §DV-003, §DV-008] <!-- Evaluator: Covered — all four are build-gating DV rules rather than construction claims: DV-003 (`line_number` contiguous from 1; every PO's lines share one `project_id` and one `vendor_id`), DV-007 (`sequence_no` contiguous from 1), DV-008 (`occurred_at` strictly increasing with `sequence_no`; `max(occurred_at) <= as_of_date`). G-2 records the vendor-per-PO property as unenforced by the schema, and §Delivered Schema's closing note says E005 re-asserts E003's G-3/G-4 rather than assuming E003's tests cover its data -->

## Generation-Input Provenance

- [X] CHK022 Are both generation inputs named individually, each with the hashing convention applied to it and the reason that convention was chosen, so a reader can tell which digest to recompute for which input? [Completeness, FR-015, Plan §AD-010] <!-- Evaluator: Covered by data-model.md §Generation Inputs and Digest Kinds — a three-row table naming each digest, what it covers, the helper producing it and where it is recorded — plus AD-010's per-owner rationale and gap G-3. The roster is canonical content because that is what E001 publishes; the category map is raw bytes because that is what E002 records, with the measured divergence of the two values given -->
- [X] CHK023 Is `digest_kind` specified as a required per-entry field over a closed value set, with a missing entry defined as an error, and is the input set closed against generator-internal vocabularies that would otherwise count as a third input? [Completeness, Data Model §Envelope, §Line record] <!-- Evaluator: Covered by data-model.md §Entities (`digest_kind: 'raw_bytes'|'canonical_content'`, a closed value set) and §Envelope ("Exactly two entries", each `{path, digest, digest_kind}`, "A missing entry is an error, never an omission"). The input set is closed against generator-internal vocabularies in §Line record: the manufacturer vocabulary is "a closed tuple in generator source, not a data file — otherwise it would be a third generation input and FR-015's enumeration of two would be wrong" -->
- [X] CHK024 Does the drift check cover both inputs symmetrically, each recomputed under the convention it was recorded with, and is a mismatch specified as a refusal naming the offending input rather than a warning or a recorded discrepancy? [Consistency, FR-027, Data Model §DV-016] <!-- Evaluator: Resolved — FR-027 said only "detect and report", which reads as exactly the recorded-discrepancy outcome this item guards against, even though data-model.md §Load Decisions and plan.md § Error Handling both already said Refuse. FR-027 now requires refusal with a non-zero exit naming the offending input and both digests, explicitly not a warning and not a recorded-and-continue, and requires each input to be recomputed under the convention it was recorded with so a symmetric check cannot degrade into one convention for both -->
- [X] CHK025 Do the disclosure obligations enumerate the same input set the fixture envelope carries, so the datasheet cannot satisfy its criterion while naming only one input? [Consistency, FR-015, SC-018] <!-- Evaluator: Covered — FR-015 and SC-018 both say "every generation input" and then name the roster fixture and the equipment-category map individually, which is the same two-entry set §Envelope closes at "exactly two"; data-model.md § Generation Process disclosures carries the row as "Both named". This was compliance finding B-5, remediated in the spec -->
- [X] CHK026 Is the absence of row-level generation provenance recorded as a four-part limitation — a loaded row traces to the roster it was generated against but not to the run that produced it — rather than left as an unstated consequence of the delivered schema? [Traceability, FR-016, Data Model §Disclosed Gaps G-4] <!-- Evaluator: Covered — FR-016 names it in the minimum limitation set in the exact terms this item uses, the four-part format rule applies to every record and SC-017 asserts 100% coverage of all four parts, data-model.md § Limitation records carries it as L-7, and gap G-4 records why the schema cannot close it (no column for a dataset content hash or generator revision, and E005 may not add one) -->

## Loader Refusal Semantics

- [X] CHK027 Are the loader's outcomes enumerated as a closed set, each with a stated precondition, so no observable database state falls into an undefined branch? [Completeness, Data Model §Load Decisions] <!-- Evaluator: Covered by data-model.md §Load Decisions — "The loader has **three** outcomes and no fourth. There is no `UPDATE` path" — over five stated preconditions that partition the natural-key space (in DB not in fixture / in both and differing / in both and equal / absent from DB) plus the generation-input digest check. plan.md § Error Handling adds the concurrency precondition, which resolves to the same Refuse outcome rather than a new branch -->
- [X] CHK028 Are the two refusal cases jointly exhaustive over the ways the database can hold a different generation — divergent content under an existing natural key, and target rows the fixture lacks — with the disjointness of their triggers made explicit? [Coverage, FR-026, FR-030, SC-010, SC-022] <!-- Evaluator: Covered — FR-030 states the disjointness in its own text ("Distinct from FR-026, which fires only on a natural-key collision; this one fires when there is none"). Joint exhaustiveness is stated in US2 ("whether that shows up as differing content under the same natural keys, or as lines the fixture does not contain at all") and mechanised by AD-005's `EXCEPT ALL` in **both directions**, with data-model.md §Load Decisions partitioning the key space over the two -->
- [X] CHK029 Is the compared-content projection defined field by field, with `created_at` and the database-generated columns explicitly excluded and the reason stated, so a reload of identical content cannot register as divergence? [Completeness, FR-026, Data Model §Load Decisions] <!-- Evaluator: Resolved — the projection was given as "every column except ..." for lines, with a reason stated only for `created_at`. data-model.md §Load Decisions now enumerates it positively as a table (17 line fields, 6 event fields) with a reason for every exclusion: `created_at` is a load-time `DEFAULT now()`; `closing_event_po_line_id`, `closing_event_terminal` and `prev_sequence_no` are GENERATED functions of compared columns and unwritable by the loader; `po_line_id` and `event_id` are uuid5 of the natural key being joined on. `note` was moved *into* the projection rather than left silently out — see the reported observations -->
- [X] CHK030 Is idempotency defined over both row count and row content, with the comparison specified as content-based because a regeneration under the same natural keys moves no count? [Measurability, FR-025, SC-009, Spec §Edge Cases] <!-- Evaluator: Covered — FR-025 requires a no-op "in both row count and row content", SC-009 asserts both, the Edge Cases entry states the reason ("the row count would not change, so the loader must compare content rather than counts"), and data-model.md §Load Decisions closes it: "Row **counts** are never the comparison" -->
- [X] CHK031 Is the scope of the superset check stated — the whole target table versus some scoping predicate — together with the assumption about sole ownership of that table that justifies the chosen scope? [Clarity, FR-030, Data Model §Load Decisions] <!-- Evaluator: Covered by data-model.md §Load Decisions: "The superset check (FR-030) is over the **whole** `purchase_order_line` table: E005 is the only writer of it in this project, so 'lines the fixture does not contain' needs no scoping predicate" — the scope and the sole-ownership assumption that justifies it are in one sentence -->
- [X] CHK032 Is the required database state after a refusal specified as unchanged with the transaction rolled back, rather than left to the reader to infer from the refusal itself? [Completeness, Plan §Error Handling Strategy] <!-- Evaluator: Covered by plan.md § Error Handling Strategy, whose load-divergence row states the pattern as "Refuse, transaction rolled back" and the response as "Non-zero exit reporting the diverging keys; database unchanged"; AD-005 puts the whole compare-then-refuse-or-insert sequence inside one `REPEATABLE READ` transaction, which is what makes the rollback total -->
- [X] CHK033 Is the null-comparison semantics that the divergence check depends on recorded as an assumption requiring its own evidence, given the nullable columns in the compared projection? [Traceability, Research §Idempotent load, Plan §Testing Strategy] <!-- Evaluator: Resolved — research.md § Idempotent load recorded the assumption and said "the plan requires a test that proves it rather than a citation", but plan.md § Testing Strategy never carried that test, so the obligation dead-ended. The Integration tier now names it: `EXCEPT`'s not-distinct treatment of NULL, exercised over `closing_event_id`, `from_state` and `note` in both the equal-and-null and differ-on-null directions, recorded as an assumption carrying its own evidence -->

## Jointly Satisfiable Shape Bounds

- [X] CHK034 Are FR-010's floor and its ceiling — the greater of 80% of realized lines and 160 uncensored delivery events, at least 10% of lines censored, and at least one line in every non-terminal state — stated as one jointly checkable admissible band with a demonstrated non-empty solution, rather than as thresholds that could each be asserted separately? [Measurability, FR-010, SC-006] <!-- Evaluator: Covered — FR-010 states all three in one requirement, SC-006 asserts all three in one criterion, and DV-010 checks them as one rule. The band is stated jointly in data-model.md §Calendar and censoring as "delivered ∈ [80%, 90%], because ≥80% must be delivered *and* ≥10% must be censored", with a demonstrated witness: 199 lines, ≈170 delivered (85%), ≈15% censored, clearing the absolute 160 floor with ≈10 events of margin. The third condition is seed-dependent and disclosed as such at G-6 rather than claimed -->
- [X] CHK035 Are the pre-split floor and the product document's post-split figure named as distinct quantities, with the assumed held-out fraction recorded as an assumption this epic neither performs nor observes? [Clarity, FR-010, FR-033, Data Model §Disclosed Gaps G-5] <!-- Evaluator: Covered — FR-010 names them apart in terms ("The product document's recorded figure of approximately 120 is a **post-split** count and this floor is its pre-split counterpart ... must not be read as the same quantity") and shows the arithmetic 120 ÷ 0.75 = 160. FR-033 records 0.25 as *assumed*, gap G-5 records that the post-split count is never observed by this epic, and L-8 carries it to the datasheet reader -->
- [X] CHK036 Is the response to every shape breach specified as a failure before any artifact is written, reporting realized against required, covering the event floor, the censoring floor, the empty non-terminal state, and a spread ratio outside its band? [Completeness, FR-010, Data Model §DV-010, Plan §Error Handling Strategy] <!-- Evaluator: Covered by plan.md § Error Handling Strategy, whose shape-breach row enumerates all four named breaches, fires "Fail-fast after generation, before write", and responds with a non-zero exit "reporting realized versus required; no artifact emitted"; DV-010 and DV-011 are the assertions, and data-model.md §Calendar states the remedy is a new seed or a widened window, "never emitting the dataset anyway" -->
- [X] CHK037 Is the realized spread ratio required to be computed over a stated vendor / material-category / residual decomposition, so a ratio inside the band for category reasons is a detectable outcome rather than a passing one? [Measurability, FR-036, Data Model §DV-011] <!-- Evaluator: Covered by FR-036, which now goes past detectability and requires the ratio asserted against FR-008's band to be the vendor component *net of* the category component, with the unadjusted ratio recorded beside it and a fail when the two straddle the band. data-model.md §Duration model fixes σ_c 0.219 and σ_r 0.4605, DV-011 requires the decomposition reported alongside the ratio, and the ground-truth record carries `variance_decomposition` -->

## Gated Scope and Completion Reporting

- [X] CHK038 Is the exclusion of the gated requirement and its criterion specified precisely enough that a QC run cannot render it as a pass — naming which denominator they leave, what state must be printed, and what may never be claimed? [Clarity, Plan §AD-008, §Reporting Obligations] <!-- Evaluator: Resolved — evaluated on the precision of the specification, not on the gating, which is left exactly as it stands. AD-008 and the SC-026 row were precise on the criterion side and silent on the requirement side, so a report could publish `25/26 + 1 BLOCKED` on criteria and still print a clean requirement sweep by dropping FR-034. plan.md § Reporting Obligations gains a matching FR-034 row naming which denominator it leaves (completion only), which it stays in (the printed requirement fraction, whole, FR-034 present as a row), the literal state to print (`BLOCKED`, with its reason), and the five words that may never be claimed (`PASS`, `satisfied`, `covered`, `deferred`, `N/A`) — with a tool that cannot render a third state required to fail rather than round -->
- [X] CHK039 Is the printed success-criterion denominator specified to remain at its full count with an explicit blocked row, so the exclusion is published rather than presented as a clean sweep? [Completeness, SC-026, Plan §Reporting Obligations] <!-- Evaluator: Covered by plan.md § Reporting Obligations, first row: the denominator stays 26 because dropping SC-026 from the printed fraction would render `25/25`, "visually a clean sweep, which is the silent pass the exclusion exists to prevent", with a required row reading `BLOCKED — excluded from the completion denominator, FR-034 gated on the E002 amendment` plus a Traceability Gaps line. Verified the spec still declares exactly 26 criteria, so the literal 26 is current -->
- [X] CHK040 Is the withdrawal condition for the gated requirement stated — that a change to the downstream blocking key withdraws it rather than satisfies it — so a later reader cannot close it by reinterpretation? [Clarity, FR-034, Data Model §Disclosed Gaps G-1] <!-- Evaluator: Covered by FR-034's closing sentence: "If the resolution is to change E009's blocking key rather than amend the corpus, this requirement is withdrawn rather than met." The spec's amendment-need paragraph costs both resolution paths and takes no view, and gap G-1 keeps FR-034 / SC-026 pending and outside the completion denominator under either -->

# Data Integrity: Document Ingestion and Extraction
**Created**: 2026-07-27 | **Feature**: [spec.md](../spec.md)

## Completeness — Ownership Boundary and Migration Confinement

- [X] CHK001 Does the requirement set state the ownership boundary over all six E003 tables E006 populates, given Scope Excluded names only the chunk, extracted-value, and extraction-failure schemas while `document`, `field_vocabulary`, and `extracted_value_contributing_chunk` are equally populated and equally not owned? [Completeness, Spec §Scope Excluded, §FR-039, data-model §Scope] <!-- Evaluator: Resolved — Scope Excluded now names all six E003-owned tables and FR-065 states the boundary over them -->
- [X] CHK002 Is "zero columns, zero constraints, zero indexes added to an E003-owned table" stated as a requirement with its own acceptance condition, or does it exist only as a data-model validation rule with no FR and no success criterion behind it? [Completeness, Spec §FR-039, §FR-040, §SC-034, data-model §VR-015] <!-- Evaluator: Resolved — added FR-065 and SC-051 to spec.md, backed by data-model VR-015 -->
- [X] CHK003 Is the drop and rebuild of `ix_chunk__embedding_hnsw` — data-definition change to an index E003 owns — admitted anywhere in the requirement set as a bounded exception to the no-alteration boundary, with the executing role and the restoration obligation stated? [Completeness, Spec §Scope Excluded, plan §AD-006, data-model §Operator Procedures 1] <!-- Evaluator: Covered by spec.md §FR-064 and §SC-050 — operator procedure under the schema-owning role, unreachable from the job, sequential-scan window and re-run obligation stated -->
- [X] CHK004 Does the specification enumerate the objects this epic creates, so that "zero schema objects are placed outside the claimed block" has a population to range over, rather than the inventory living only in a downstream artifact? [Testability, Spec §FR-040, §SC-034, data-model §Named Object Inventory] <!-- Evaluator: Resolved — FR-040 now requires a named inventory of every created object and makes absence from it a defect; SC-051 ranges over it; Key Entities and Implementation Signals extended with the parse-signal record -->
- [X] CHK005 Is FR-040's reservation of `0200`–`0299` for a concurrent epic stated as an obligation something outside this specification ratifies, and is the amendment to the build-gating partition check — a file this epic does not own — covered by a requirement rather than only by a plan decision? [Completeness, Spec §FR-040, §Compliance Check condition 2, plan §AD-013] <!-- Evaluator: Resolved — FR-040 now requires the partition-check amendment declaring both blocks; Compliance Check condition 2 and data-model §Block-partition assertion restated as ratified -->


## Consistency — Generation State and the Reader's Filtering Obligation

- [X] CHK006 Does FR-055's "mark each ingestion run active or superseded" state the granularity the rest of the epic depends on, given FR-043 has a run ingest only the documents whose input tuple changed, so one run is simultaneously live for some documents and retired for others? [Consistency, Spec §FR-055, §FR-043, §SC-043, data-model §ingestion_run_document] <!-- Evaluator: Covered by spec.md §FR-055 — the mark is stated per document, with the run-level flag rejected for exactly this reason -->
- [X] CHK007 Is the retention bound FR-055 requires actually stated in the specification as a value, together with the purge procedure, and is either covered by a success criterion — the SC set carries none for retention or purge? [Completeness, Spec §FR-055, §Success Criteria, data-model §Operator Procedures 3] <!-- Evaluator: Covered by spec.md §FR-055 and §SC-043 — the bound is zero because promotion removes the prior generation, so no purge procedure exists to state; SC-043 carries "zero superseded generations' rows remain after a promotion completes" -->
- [X] CHK008 Does the requirement that "downstream readers filter on the active run" name the consumers it binds and state the outcome when one does not, given the obligation falls on epics outside this one and no mechanism here can compel it? [Completeness, Spec §FR-055, §SC-043, data-model §G-4] <!-- Evaluator: Resolved — FR-055 now names E008, E009, and E012 and states the two silent outcomes (no run attribution; no-live-generation indistinguishable from one) -->
- [X] CHK009 Is the coexistence of a retained superseded generation with its successor addressed against E003's uniqueness of `(document_id, ordinal)` on `chunk`, given both generations hold chunks for the same document from ordinal zero and this epic may add no constraint to relax it? [Consistency, Spec §FR-015, §FR-055, §SC-003, E003 data-model §chunk] <!-- Evaluator: Covered by spec.md §FR-055 and ADR-0020 — retention was unstorable, so promotion removes the prior generation inside the successor's transaction -->
- [X] CHK010 Do the total-population criteria — every chunk's text found on its named page, contiguous ordinals per document, one page per chunk — state whether their population is the active generation or every generation in the database? [Testability, Spec §SC-002, §SC-003, §SC-031, §SC-043] <!-- Evaluator: Resolved — SC-043 now fixes the population once for the whole set: after promotion, every resident row belongs to the one active generation, so both readings coincide -->
- [X] CHK011 Is the retirement order stated in the requirement set rather than deferred to "an order the restricting foreign keys permit", and does that order account for the one edge E003 declares `ON DELETE CASCADE` — contributing chunks to their value — where the ordering rationale does not hold? [Clarity, Spec §FR-041, §US6 scenario 2, E003 data-model §Referential Actions] <!-- Evaluator: Resolved — FR-041 now states the capture-then-leaf-up order in five numbered steps and names the CASCADE edge, deleting contributing rows explicitly rather than leaning on a cascade in a table this epic does not own -->
- [X] CHK012 Do FR-043's "remove-and-reload only the documents whose tuple differs" and the generation mechanism describe one behaviour or two, given a re-ingest supersedes and inserts while removal is an operator procedure the job cannot perform? [Consistency, Spec §FR-043, §FR-041, §SC-025, data-model §Write Order] <!-- Evaluator: Resolved — FR-043 now states they are one behaviour, and that a run replacing any generation executes under the schema-owning role while a first-ingest-or-skip run is unattended -->
- [X] CHK013 Does the input tuple's first member mean the corpus-wide manifest digests it names, or the single document's own content hash the comparison requires, and is the difference resolved in the requirement rather than in the design that reinterprets it? [Consistency, Spec §FR-043, §Glossary "Input tuple", data-model §input_tuple_digest] <!-- Evaluator: Resolved — FR-043 already carried the per-document hash; the stale Glossary entry is corrected to the per-document members including the provider model -->


## Completeness — Atomicity, Write Order, and Recovery

- [X] CHK014 Does FR-054's enumerated write order cover every row the document's transaction contains, given the generation record and the line-item association are absent from the list yet both are written inside it? [Completeness, Spec §FR-054, §FR-059, data-model §Write Order and Transaction Boundary] <!-- Evaluator: Resolved — FR-054 now enumerates the removal steps, the generation record, and the line-item and parse-signal rows alongside the chunks, values, contributing chunks, failures, and run associations -->
- [X] CHK015 Is it stated that the record explaining an aborted document is written after the rollback and outside the transaction that discarded it, and that it cannot be a per-field failure row because that row's source chunk no longer exists? [Completeness, Spec §FR-056, §FR-042, §SC-044, data-model §VR-008] <!-- Evaluator: Resolved — FR-056 now carries the post-rollback fresh-transaction rule and the unstorable-referent reason, matching data-model VR-008 -->
- [X] CHK016 Do FR-038's "start and finish" and SC-022's "zero fields are absent" admit the aborted run SC-044 requires, in which no finish exists and recording one would assert a completion that did not happen? [Consistency, Spec §FR-038, §SC-022, §SC-044] <!-- Evaluator: Resolved — FR-038 now records the finish only on completion and forbids one on a failed run; SC-022's field list already excludes the finish -->
- [X] CHK017 Is "complete row set" defined for a document that legitimately produces no extracted values, no contributing chunks, no failures, and no line items — the 26 specifications — so the criterion distinguishes a correct empty set from a truncated one? [Testability, Spec §SC-042, §SC-012, §FR-022] <!-- Evaluator: Resolved — SC-042 now defines completeness per document type, with the specifications' empty value set complete by FR-022 rather than short -->
- [X] CHK018 When a hash mismatch or an identifier collision aborts a run part-way, is the standing of the documents already committed stated — whether their generations remain active and whether the corpus is consumable in that state — or does the requirement address only the document that triggered the abort? [Completeness, Spec §FR-005, §FR-052, §SC-030, §SC-042] <!-- Evaluator: Resolved — FR-042 now states committed documents stay durable and active and the corpus is consumable and identifiable as partial; FR-052's collision check moved ahead of the first commit so that abort writes nothing at all -->
- [X] CHK019 Is a recovery path stated for a run that aborts while the similarity index is absent, given nothing in the database restores it, no migration recreates it on an already-migrated database, and the resulting state is correct but silently degraded? [Completeness, Spec §FR-042, data-model §G-7, §Operator Procedures 1] <!-- Evaluator: Covered by spec.md §FR-064 and §SC-050 — the rebuild step of the operator procedure is the recovery path, the absence is reported rather than silent, and data-model G-7/VR-021 supply the startup check -->


## Completeness — Append-Only Semantics and the Role Split

- [X] CHK020 Are the two roles the append-only rule turns on identified in the specification — the role the ingestion job connects as and the schema-owning role a correction runs under — or are they referred to only by description? [Clarity, Spec §FR-041, §SC-024, data-model §Privileges] <!-- Evaluator: Resolved — FR-041 now names the application role `procurement_app` and the schema-owning migration role (`procurement` in the delivered configuration) rather than describing them -->
- [X] CHK021 Do the four association tables this epic adds carry the append-only rule as a requirement, given FR-041 names only the three tables E003 revoked and these rows are themselves provenance? [Completeness, Spec §FR-041, §FR-039, data-model §Privileges] <!-- Evaluator: Resolved — added FR-066, carrying SELECT/INSERT-only onto all six tables this epic adds beyond the run record -->
- [X] CHK022 Is the set of updates that remain permitted stated — the finish timestamp, the run-failure columns, and the active-to-superseded flip — so that FR-041's prohibition and SC-024's "extraction tables" are bounded by enumeration rather than by an undefined table group? [Clarity, Spec §FR-041, §SC-024, data-model §Privileges] <!-- Evaluator: Resolved — FR-066 enumerates the permitted updates and SC-024 now names the nine tables it ranges over; the active-to-superseded mark is recorded as the schema-owning role's, not the job's -->
- [X] CHK023 Does SC-024 state the configuration under which it is measured, given the deployed connection uses a role that bypasses the revoke and the limitation table records that the criterion must not be reported as fully enforced in deployment? [Testability, Spec §SC-024, §Disclosed Limitations, data-model §G-6] <!-- Evaluator: Resolved — SC-024 now states it is measured under an explicit switch to the application role and cites the disclosed latency in deployment; the target is left absolute and the shortfall published beside it (Principle VII) -->


## Consistency — Citation and Provenance Integrity

- [X] CHK024 Does SC-019's "one contributing chunk per page it drew on" agree with the normative model in which the anchor citation is contributor one and only contributors two through N occupy the contributing-chunk table, so a two-page value carries one contributor row and not two? [Consistency, Spec §SC-019, §US4, E003 data-model §extracted_value_contributing_chunk] <!-- Evaluator: Covered by spec.md §SC-019 — corrected to anchor plus one row per *additional* chunk, with the anchor named contributor 1 and excluded from the contributing table -->

- [X] CHK025 For a field whose label ends one page and whose value begins the next, is it stated which contributing chunk supplies the anchor citation — the page carrying the label or the page carrying the printed value — given the cited page is a single scalar and downstream traceability resolves through it? [Clarity, Spec §FR-029, §US4, §SC-009] <!-- Evaluator: Resolved per user — FR-029 anchors on the chunk carrying the printed value, per the PRD's one-step verification job-to-be-done; US4 scenario 1 and the Glossary restated to match -->

- [X] CHK026 Is it stated where the parse signals a confidence is computed from are recorded, given SC-026 requires recomputation "from the signals recorded with it" while alternate-label and repaired-invocation status appear in no column this epic may add and no column it owns? [Testability, Spec §SC-026, §FR-031, §FR-046, §Scope Excluded] <!-- Evaluator: Covered by spec.md §FR-063 and §SC-049 — an E006-owned parse-signal record, with FR-057 putting the weights and their application order on the run row -->
- [X] CHK027 Do FR-031's four confidence signals and FR-057's three deductions describe the same computation, given "whether the value was printed or absent" survives in one and was withdrawn in the other? [Consistency, Spec §FR-031, §FR-057, §Clarifications 2026-07-27 (Clarify)] <!-- Evaluator: Resolved — FR-031 reduced to the three surviving signals, with the withdrawal of the printed-or-absent signal stated and its reason (an absent value is a failure record, so it can never fire) -->
- [X] CHK028 Does FR-059's line-item key state that membership is scoped to a generation, given the key it names — value, document, item ordinal — omits the run and two generations of one document would otherwise be readable as one item three? [Completeness, Spec §FR-059, §SC-046, data-model §extracted_value_line_item] <!-- Evaluator: Resolved — FR-059 now keys on the value alone and groups by run, document, and item ordinal, with membership stated as generation-scoped -->
- [X] CHK029 Is the unenforceable half of SC-021 disclosed in the specification — that an association row *existing* for every chunk, value, and failure is cross-table absence, and that an association's document may disagree with its target row's document — or do both appear only as gaps in a downstream artifact? [Completeness, Spec §SC-021, §FR-039, §Disclosed Limitations, data-model §G-1, §G-2] <!-- Evaluator: Resolved — both added as Disclosed Limitations rows with scope decision, evidence, reversal trigger, and production-scale alternative -->

- [X] CHK030 Is "contributor order" defined for SC-027's comparison of a page-split value against the concatenation of its contributing chunks, given the ordinal sequence carries no declared precedence in the normative model? [Clarity, Spec §SC-027, §FR-062, E003 data-model §TR-060] <!-- Evaluator: Resolved per user — SC-027 now concatenates in ascending page order, with the reason stated in-clause (the anchor is the later page, so contributor order would reverse the text); data-model VR-019 asserts it -->


## Testability — Identity, Cardinality, and Population

- [X] CHK031 Does FR-002's derivation rule state the whole transformation from a file stem to an identifier — case, separator handling, and the length bounds the declared format imposes — or only the lower-casing step, leaving the normalization that FR-052's collision case presupposes unstated? [Clarity, Spec §FR-002, §FR-052, §SC-036, E003 data-model §document] <!-- Evaluator: Resolved — FR-002 now states lower-casing, separator collapsing, trimming, E003's `^[a-z0-9]+(-[a-z0-9]+)*$` format and its 3–128 length bound, and fail-the-run rather than coerce -->
- [X] CHK032 Is the identifier-collision check required to complete over the whole corpus before the first document commits, given per-document transactions mean a collision found at document forty leaves thirty-nine documents durable? [Completeness, Spec §FR-052, §FR-054, §SC-036] <!-- Evaluator: Resolved — FR-052 now requires the check to complete over the whole enumerated corpus before the first document transaction commits, since identifiers derive from manifest entries and need no parsing -->
- [X] CHK033 Is `PRJ-000` stated as reserved — barred from being minted as an ordinary project identifier by any other producer — given the project identifier is a format-checked text column with no referent table, so nothing structurally prevents the sentinel from acquiring a second meaning? [Completeness, Spec §FR-003, §Assumptions, §Disclosed Limitations] <!-- Evaluator: Resolved — FR-003 now reserves `PRJ-000` corpus-wide against any producer and requires the report to state that nothing structural enforces it -->
- [X] CHK034 For a field absent from an entire document and recorded once per document, is it stated which chunk and which page the single failure record names, given both are required on every failure and neither has an obvious referent for a document-level absence? [Completeness, Spec §FR-058, §FR-035, §SC-039] <!-- Evaluator: Resolved — FR-058 now names the lowest-ordinal chunk the field was attempted on and that chunk's page, deterministic under FR-015's ordinal rule and stated as a convention rather than a claim about where the field would have printed -->

- [X] CHK035 Does SC-046's "100% of extracted values belong to exactly one line item" account for values a transmittal prints once for the whole document rather than per item, and is per-field cardinality within an item — one manufacturer, one part number — stated as a rule or left unasserted? [Consistency, Spec §SC-046, §FR-058, §FR-059, data-model §G-5] <!-- Evaluator: Resolved per user — item ordinal 0 is now a named group for document-scoped values (FR-059, SC-046, data-model check loosened to >= 0), keeping SC-046 absolute rather than narrowing its population; per-field cardinality stated as deliberately unasserted in FR-059 and disclosed as a limitation -->

- [X] CHK036 Is the ordinal assignment rule stated — the order chunks are numbered in and whether a page that yields no storable text consumes an ordinal — so that contiguity from zero and re-run determinism are both decidable? [Testability, Spec §FR-015, §SC-003, §SC-007, §FR-012] <!-- Evaluator: Resolved — FR-015 now fixes reading order (ascending page, then position within the page) and states that a page yielding no storable text produces no chunk and consumes no ordinal -->
- [X] CHK037 Is the obligation to draw field names only from unretired vocabulary terms stated as a requirement, given retirement is advisory in the normative schema and the filter is assigned to this epic by that document rather than claimed by this one? [Completeness, Spec §FR-024, §SC-010, E003 data-model §G-7, data-model §VR-020] <!-- Evaluator: Resolved — FR-024 now requires unretired terms only and names E003's G-7 as the reason the filter must be claimed here; SC-010 measures it -->


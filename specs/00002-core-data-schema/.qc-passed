E003 Core Data Schema — QC PASSED, run 2, 2026-07-26.
Governing document: project-instructions.md v1.2.0.

Required categories (profile `standard`):
  linting  — ruff check + format clean at 4 roots; 3 import contracts kept, 0 broken; 3 locks consistent
  coverage — 94% (535 statements, 28 missed) against threshold 80, exit 0, Combined 3 files

Tests: 455 model (0 skipped), 5 gateway, 75 root cross-entry.
Objectives 6/6; 47/47 validation criteria; 28/28 success criteria.
Bug tasks T055-T066 all closed and re-measured. tasks.md 66/66 [X].

Not measured here, recorded rather than implied:
  - tests/checks/test_orchestration.py was not run — its teardown runs
    `docker compose down -v` and destroys the database volume. OBJ1 VC6 is
    evidenced structurally instead. CI runs the whole directory.
    Update: main's 83df9b3 fixed the cross-checkout half of this (ports are now
    parameterised and resolved at runtime) but not the local half — teardown is
    still `down -v`, so it stays excluded from local runs. After merging main,
    root cross-entry is 89 passing and the gate is 93% on 607 statements.
  - 11 gaps G-1..G-11 are disclosed in data-model.md under Principle VII, not fixed.
    SC-028 holds against role `procurement_app`, not the connecting superuser (G-11).
  - Analysis findings A-007/A-008/A-011/A-012 were resolved after this QC pass,
    in a follow-up commit on the same branch. See spec.md > Compliance Check.
    A-007 closed as will-not-merge on evidence (14 of 15 clusters are genuine
    specialisations, each the sole anchor for its own named constraint; only
    TR-053 was a true duplicate). A-008 split: 16 documentation gaps left on
    record, 5 verification gaps closed by test, TR-064 unverifiable by nature.
    A-011 closed by reclassifying 11 soft MUSTs in place. A-012 (data-model.md
    normative over spec requirements) accepted with its mitigation named;
    an ADR to bind it beyond E003 is carried forward to main.

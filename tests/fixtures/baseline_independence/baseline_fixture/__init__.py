"""FR-050: the committed negative fixture for the baseline-independence contract.

A contract that has never been observed failing is an assertion about the build
rather than a property of it. This tree is the failing direction of
"The baseline extractor does not reach the corpus generator", shaped like the
real one in `src/model/pyproject.toml` and violated on purpose.
"""

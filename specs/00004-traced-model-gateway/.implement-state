phase: Phase 5 complete — OBJ4 (Content-Hash Fixtures and Offline Replay)
completed: 59
remaining: 16
blocked: none
next: Phase 6 (OBJ5) T062-T070 — credential redaction across the five egress
      sinks, provider-exception normalization, and the credential-shaped
      material scan. Then Phase 7 (OBJ6, P2) and Phase 8 polish.
notes: OBJ4 delivered. Mode selection has no default and no fallback; the
       provider opt-in is a separate control with a fixed form so a check can
       assert its absence from CI. Replay refuses to run beside a credential —
       refusing when the means of cheating is present is what makes the offline
       claim structural rather than believed.
       The network guard is the observation point SC-008's zero rests on. Its
       public names live in netguard.py rather than the conftest: pytest
       imports a conftest under its own module name, so a test catching the
       exception by path caught a different class object and the guard read as
       broken when it worked. The shape assertions are at the root; the
       behavioural control is in the tier that installs the fixture.
       Two checks from other epics caught real problems in this phase's work.
       E001's supply-chain scan flagged a realistic fake credential in a test
       literal — correctly, since the detector cannot tell a fake from a real
       one, which is what makes it useful. And the single-naming-site scan
       caught a comment in config.py that named the provider while explaining
       the rule against naming it.
       orchestrator.invoke() still raises NotImplementedError. Every piece it
       composes now exists; joining them is Phase 6/8 work once redaction
       lands, since the record path must not carry unredacted material.
       Environment: the DB container was torn down externally three times this
       session. `procurement-api:e001` is an unqualified tag the sibling
       checkouts share and must be rebuilt here before the image check runs.
timestamp: 2026-07-26

# API Quality Requirements: Risk-Ranked Coordinator Worklist
**Created**: 2026-07-28 | **Feature**: [spec.md](../spec.md)

## Contract Completeness

- [X] CHK001 Is the read-only, single-operation shape of the worklist surface required by anything, given FR-031 states that the *worklist* exposes no write path while the absence of every mutating method is asserted only by the contract? [Completeness, Spec §FR-031; contracts/openapi.yaml §info] <!-- Evaluator: Covered by spec.md §FR-031 ("The worklist therefore exposes no write path, which keeps this feature read-only across FR-024's boundary"), with FR-028 forbidding the only other write this surface could make, and plan.md §API Surface Summary ("One operation, GET only — so FR-031's 'no write path' is a property of the document rather than a convention"). The contract's absence of every mutating method discharges a stated requirement rather than asserting one -->
- [X] CHK002 Is the split of lines into a ranked collection and an excluded collection an obligation on the response, or does FR-016's "separate group" bind only the rendered screen, leaving the two-array partition a plan decision no requirement compels? [Completeness, Spec §FR-016, §FR-021; Plan §AD-002] <!-- Evaluator: Resolved — FR-016 now binds the response as well as the rendered screen: excluded lines travel as a collection disjoint from the ranked one rather than as a flag inside one list, so no consumer can sort an excluded line into the ranking and the exclusion survives every ordering (SC-008, FR-045). The two-array partition is no longer AD-002's alone; contracts/openapi.yaml §UnrankedRow cites the requirement -->
- [X] CHK003 Is there a requirement obliging the response to carry a means of deciding whether an ordering changed, and is that value's domain defined, given FR-012 places the acknowledgement on the interface and both empty-`ranked` examples carry an identical `ordering_digest` while describing different states? [Completeness, Spec §FR-012, §SC-015; contracts/openapi.yaml §WorklistResponse.ordering_digest] <!-- Evaluator: Resolved — FR-012 now requires every response to carry a single value over the ranked group's ordered identifier sequence, decides FR-046's acknowledgement by the same comparison, and states its domain: defined on every response including one whose ranked group is empty, where it takes the empty sequence's value, so two responses that rank nothing carry the same value and equality alone MUST NOT be read as sameness of state; meaningful only between responses under the same scope and sort key. contracts/openapi.yaml §ordering_digest records that the two empty examples are digest-identical by construction -->
- [X] CHK004 Is returning the full set of scopable projects inside the worklist response required by FR-025, or is it a latency-driven contract addition, and is a per-project open-line count owned by any requirement? [Completeness, Spec §FR-025; contracts/openapi.yaml §Scope] <!-- Evaluator: Resolved — FR-025 now requires the scoping control's domain to travel with the worklist in every state, not only in the empty-filter state FR-042 names: the full set of selectable projects, each carrying its identifier and its count of open lines, and the full set even while a scope is active. The count is owned as what separates a project holding no open line from one the coordinator has not scoped to, and as the unscoped total SC-023 reconciles against -->
- [X] CHK005 Is there a requirement that a response identifies the run, model version, and artifact schema version its figures were computed from, given FR-019 requires only the as-of date while Principle I requires every published figure to resolve to the artifact it came from? [Completeness, Spec §FR-019; project-instructions.md §I; contracts/openapi.yaml §ForecastRunMeta] <!-- Evaluator: Resolved — added FR-052: a response carrying figures must identify the run, its model version and the artifact schema version alongside FR-019's as-of date, because two runs can share an as-of date and neither version is derivable from one. The as-of date is the coordinator-facing half of the identification and the identifier is the auditable half; contracts/openapi.yaml §ForecastRunMeta cites it -->
- [X] CHK006 Is the server's obligation to supply `today` and the zone that fixes it stated as a requirement, and is the prohibition on the consumer deriving any state from a local clock stated anywhere but the contract's prose? [Completeness, Spec §Glossary "Today", §FR-029, §FR-030; contracts/openapi.yaml §Meta.today] <!-- Evaluator: Resolved — FR-038 already required today to be supplied to the read path and reported as supplied; it now also requires the zone that fixes it to travel with it (a bare date states the day and nothing about the rule that produced it), and states the consumer-side prohibition as an obligation rather than contract prose: no consumer may derive staleness, calendar-passed, already-late or a run's age from a clock of its own -->
- [X] CHK007 Does FR-029's "the basis MUST travel with it" state its destination — the configured value, the screen, or the API response — so a staleness-basis member is an obligation rather than one reading of the phrase? [Clarity, Spec §FR-029; contracts/openapi.yaml §ForecastRunMeta.staleness_basis] <!-- Evaluator: Resolved — FR-029 now names the destination: into the response that reports the staleness claim, beside the threshold and the run's age, rather than staying in the configuration file that holds the value. A basis not on the same surface as the claim it licenses is indistinguishable from one somebody guessed, which is the shortfall Principle VII forbids leaving unpublished -->
- [X] CHK008 Does any requirement admit more than one simultaneous what-if, given FR-011, FR-013 and FR-031 are each written about *a line's* need-by date while the contract accepts a repeatable, multi-line override set? [Completeness, Spec §FR-011, §FR-013, §FR-031] <!-- Evaluator: Resolved — FR-031 now states that more than one adjustment may be in force at once, each scoped to its own line and at most one per line, with the reason: a what-if confined to one line cannot answer what the list looks like once two competing dates have moved. FR-020a already admitted the set rather than a single date, and FR-055 bounds it -->
- [X] CHK009 Are the override admissibility rules traceable to requirements — the 25-entry cap, the ten-year date window, duplicate rejection, and the deliberate non-application of the stored `need_by_date >= order_date` constraint — and is a coordinator-facing consequence stated for each? [Completeness, Spec §FR-031; contracts/openapi.yaml §parameters.NeedByOverride] <!-- Evaluator: Resolved — added FR-055: a set past the cap is refused with the cap stated rather than silently truncated (a truncated set re-ranks against dates nobody asked for), a non-calendar date, a second adjustment on one line and a date outside the bounded window are each refused naming the parameter and the reason, and the stored need_by_date >= order_date constraint is deliberately not applied because the value is never stored and enforcing it would refuse a question the ranking answers correctly. The values stay in the contract, which FR-055 defers to by name -->
- [X] CHK010 Is there any requirement stating who may read the worklist, given the contract records that no scheme is defined and assigns the decision to the plan, the plan's Instructions Check does not answer it, and neither cites the registered no-authentication scope decision? [Completeness, Spec §Requirements; contracts/openapi.yaml §info "Authentication"; specs/sad.md §Security] <!-- Evaluator: Resolved by inheritance — specs/sad.md §Security carries a registered project-level scope decision ("There is no authentication, and that is a deliberate scope decision rather than an oversight"), recorded in the product document with its reversal condition and production-scale alternative. Added FR-056 stating what the inheritance obliges of this surface: no scheme, no coordinator identity, no per-reader visibility rule, and the reversal landing on FR-025's scope and FR-031's session. contracts/openapi.yaml §info Authentication now cites the registered decision instead of calling it open for the plan, and plan.md gained a Security / access row -->
- [X] CHK011 Is the meaning of a row's rank under a non-default sort key stated in a requirement, given FR-001 defines the ordering by expected harm and FR-026 admits three further keys under which a position no longer denotes harm? [Clarity, Spec §FR-001, §FR-026; contracts/openapi.yaml §RankedRow.rank] <!-- Evaluator: Covered by spec.md §FR-048 ("The stated position is the position under the active key, never a harm rank retained from a different ordering, so a row never asserts two orderings at once") and §SC-001's second sentence, both added by the UX pass; contracts/openapi.yaml §RankedRow.rank states the same from the response side -->
- [X] CHK012 Do the requirements state which combinations of the three page-scope states are admissible, given the contract permits any zero-to-three subset while a stale-run state presupposes the active run that a no-active-run state denies? [Consistency, Spec §FR-018, §FR-018a; contracts/openapi.yaml §WorklistResponse.page_states] <!-- Evaluator: Resolved — FR-033 now enumerates the page-scope combinations as well as the row ones: no-active-run and stale-run are mutually exclusive (a response asserting both reports a run and no run at once, and the schemas already contradict on forecast_run's nullability), no-active-run composes with empty-filter because whether a filter matched is decided over the line set, and stale-run composes with empty-filter. Exactly six admissible sets and no admissible triple; contracts/openapi.yaml §page_states carries the table and drops maxItems to 2 -->

## Payload Honesty

- [X] CHK013 Does FR-007's enumeration of forbidden surfaces — row, label, sort control, tooltip, export — name the API response, so withholding a predicted delivery date from the payload is an obligation rather than a contract courtesy? [Completeness, Spec §FR-007; contracts/openapi.yaml §info] <!-- Evaluator: Resolved — FR-007's enumeration now names "the response the interface renders from", with the reason it was the omission that mattered: it is the one surface no coordinator reads directly and therefore the one an enumeration of screens silently omits, an interface renders only what it is given, and a prohibition stopping at the rendered row is enforced by whoever writes the next client. FR-041's set is now taken over what the response admits as well as what the row renders -->
- [X] CHK014 Is there a requirement forbidding the response from returning the raw draw and survival arrays, and is that prohibition scoped, given the contract states it as rationale and the shared-artifact surface records E012 as consuming the same arrays? [Completeness, Spec §FR-007, §FR-002; specs/project-plan.md §Shared Data Entities; contracts/openapi.yaml §info] <!-- Evaluator: Resolved — added FR-053, whose first clause forbids the draw and survival arrays in this response and states the scope explicitly: the prohibition binds this surface, not the arrays, since E003 stores them, E007 writes them and the project plan records E012 as a second consumer for a detail view whose presentation obligations are its own to state. The contract's rationale now cites the requirement and repeats the scope -->
- [X] CHK015 Is there a requirement constraining what a later revision may add to the payload — that no admitted member may be combined arithmetically with another to yield a point delivery estimate — given a closed object is the only guard and it lives in the contract rather than in a requirement? [Completeness, Spec §FR-007, §FR-027; contracts/openapi.yaml §PrimaryFigures] <!-- Evaluator: Covered by spec.md §FR-041 (no set of displayed figures may combine into a delivery date or a single central summary, taken over FR-027's three closed classes rather than over whatever is rendered) and §FR-027's opt-in clause (a later quantity must itself satisfy FR-005 through FR-008 and FR-041). FR-007's amendment at CHK013 brings the payload into that set, so the closed object is no longer the only guard -->
- [X] CHK016 Is the withholding of the expected-harm score itself required anywhere, given FR-009 requires only criticality and calendar margin on the row and the plan records that field's removal as load-bearing for Principle II? [Completeness, Spec §FR-009, §FR-027; Plan §Instructions Check; contracts/openapi.yaml §ExpectedHarm] <!-- Evaluator: Covered by spec.md §FR-041's final paragraph ("publishing the score would surrender the mean overrun to a division and the delivery date to one further addition... the ordering reaches the coordinator as a position under FR-048 rather than as a number") and §FR-009 ("The score itself stays off the row under FR-041"), both added by the UX pass — so the removal rests on requirements rather than on the contract's removal note -->
- [X] CHK017 Is the wire encoding of a probability stated as a requirement — a server-rounded integer with a finished display string and no float — or does FR-008 fix only the displayed precision, leaving the encoding to AD-001? [Completeness, Spec §FR-008; Plan §AD-001; contracts/openapi.yaml §PercentFigure] <!-- Evaluator: Resolved — FR-053's second clause requires a probability to reach the interface already rounded under FR-008, as the displayed integer together with the finished display string and never as the stored value, so the encoding is a requirement rather than AD-001 alone. The defect it names is the one AD-001 names: a raw value hands the interface a second rounding, and two independent roundings of 0.4951 and 0.5049 produce an FR-006 pair summing to 101 -->
- [X] CHK018 Is the null-pairing rule a requirement — that both directions carry no numeric value exactly when the bounded form is in use, and that 0 and 100 are not representable — or does FR-008 state only that a bounded value's complement is also bounded? [Completeness, Spec §FR-008, §FR-006; contracts/openapi.yaml §PercentFigure.percent] <!-- Evaluator: Resolved — FR-008 already carried the complement-also-bounded rule and the unrepresentability of the displayed forms 0% and 100%; what was missing was the wire consequence, now stated in FR-053's third clause: in the bounded form neither direction may carry a numeric value at all, because a numeral surviving beside the bound leaves a certainty a renderer can reach for -->
- [X] CHK019 Is the response required to distinguish an upper bound from a point probability, given FR-017 obliges the *row* to state the bound and the distinction reaches a consumer only through a contract-defined measure field? [Completeness, Spec §FR-017; contracts/openapi.yaml §MissProbability.measure] <!-- Evaluator: Resolved — FR-017 now requires which of the two measures a displayed probability is (a point read at the need-by offset, or an upper bound on the residual tail) to be carried with the figure rather than inferred by the interface from the row's state, since the two are rendered in different words and inferring the measure puts two rules that must agree in different tiers. SC-020 fixes which value each measure carries; this fixes that the consumer is told which it has -->
- [X] CHK020 Do the requirements state how a suppressed figure is encoded — an absent member versus a null one — given FR-016, FR-021 and FR-030 say figures are withheld and a null is one renderer away from the dash FR-015 exists to prevent? [Clarity, Spec §FR-015, §FR-016, §FR-030; contracts/openapi.yaml §UnrankedPrimary] <!-- Evaluator: Resolved — added FR-054: two encodings and no third. Structural absence where nothing may ever be shown (FR-016's row carries no property a renderer could fill), an explicit empty where a figure exists in the general case and this row's state removed it (FR-030's miss probability), and a zero, dash, empty string or sentinel forbidden in both — with FR-050 keeping the empty member from being the sole carrier of the state. Both contract sites now name which encoding they are -->
- [X] CHK021 Is a rule stated for the reference class being published twice — once per quantile and again at page scope — naming which is authoritative and requiring the two to agree? [Consistency, Spec §FR-005; contracts/openapi.yaml §ReferenceClass, §Conventions] <!-- Evaluator: Resolved — FR-003 now states which copy governs: the one carried with the figure is authoritative because it is attached to the figure it describes, and the two MUST agree within a single response, a disagreement being a provenance defect under Principle I rather than a display choice. contracts/openapi.yaml §ReferenceClass and §Conventions both record it -->

## Error Semantics

- [X] CHK022 Is "absent data is a state, not a fault" stated as a requirement, so a response describing no active forecast run or an empty filter is *required* to be a successful one rather than merely planned as such? [Completeness, Spec §FR-015, §FR-018; Plan §Error Handling Strategy] <!-- Evaluator: Resolved — FR-018 now states that each of the eight MUST be reported as a successful outcome carrying its state, never as a fault, so the plan's two load-bearing 200 rows discharge a requirement rather than record a preference. FR-043 already carried the converse ("each carried inside a successful response") and remains the only condition this feature reports as a fault. The spec still names no status code, which belongs to the contract -->
- [X] CHK023 Is the condition "the stored artifacts cannot be read" owned by a requirement that fixes it as a fault rather than a degraded state, given FR-023 covers only the model provider and FR-018's enumeration of eight is declared canonical? [Completeness, Spec §FR-018, §FR-023; contracts/openapi.yaml §DatastoreUnavailable] <!-- Evaluator: Covered by spec.md §FR-043, added by the UX pass: the condition is owned, requires no row and no figure and a correlating identifier, is distinct in wording from FR-015's, and is deliberately not a ninth entry in FR-018 because the eight are states of what the system knows and this is its inability to look -->
- [X] CHK024 Are the problem identifiers a consumer may encounter enumerated and required to be stable, given the error shape mandates a type identifier while closing only the per-parameter reason vocabulary? [Testability, contracts/openapi.yaml §Problem] <!-- Evaluator: Resolved — contracts/openapi.yaml §Problem.type now enumerates the four identifiers a consumer of this operation may encounter (invalid-parameter, unsupported-artifact-schema, internal-error, datastore-unavailable) as a closed enum with a table mapping each to its status and condition, and states the stability rule: never reused for a different condition, never renamed in place, adding one the only compatible change. plan.md's error table carries the type per row -->
- [X] CHK025 Is the correlation identifier named in the plan's fault treatment a required member of the error body, given the problem schema defines no such property while the architecture records a trace identifier propagated from web through API? [Consistency, Plan §Error Handling Strategy; specs/sad.md §Observability; contracts/openapi.yaml §Problem] <!-- Evaluator: Resolved — contracts/openapi.yaml §Problem now defines correlation_id and requires it on every problem response including 422, identified as the trace identifier specs/sad.md §Observability propagates from web through API; the 500 and 503 examples carry it. FR-043's "identifier that correlates the failure to its record" now has a field, and the plan's promise is a property of the contract -->
- [X] CHK026 Is there a requirement forbidding a partially computed worklist from being returned on fault, given the contract asserts it as rationale and Principle III governs exactly the failure a coordinator cannot see? [Completeness, project-instructions.md §III; contracts/openapi.yaml §InternalError] <!-- Evaluator: Covered by spec.md §FR-043, whose condition includes a request that fails before a worklist can be assembled and which requires no row and no figure rather than as many as could be assembled, and by §SC-023's exactly-once reconciliation, which is what distinguishes a line correctly excluded from one a defective query dropped. contracts/openapi.yaml §InternalError now cites both -->
- [X] CHK027 Is the status chosen for a malformed or semantically inadmissible parameter fixed by a stated convention that the endpoints E012 and E013 will add are equally bound by, rather than by the serving framework's default? [Consistency, Plan §Error Handling Strategy; specs/project-plan.md §API Surfaces] <!-- Evaluator: Resolved — plan.md §Error Handling Strategy now states the convention this feature establishes for the boundary and that later endpoints on it adopt rather than re-choose: RFC 9457 for every fault and never a framework default body, 422 for malformed and semantically inadmissible alike so a consumer needs one branch rather than a 400/422 split nobody can state the boundary of, stable enumerated type identifiers, and a required correlation id. The contract's ValidationFailed records why 422 rather than 400 -->
- [X] CHK028 Is it required that an override naming a line the response does not contain is reported back rather than rejected or silently dropped, and is the reason vocabulary owned by a requirement rather than by the contract alone? [Completeness, Spec §FR-031, §FR-012; contracts/openapi.yaml §UnappliedOverride] <!-- Evaluator: Resolved — FR-055's non-application clause requires such an override to be reported back stating which of the three causes it was (absent, terminal under FR-022, outside the active scope under FR-025) with the rest of the set applied, and names the failure silence produces: an ordering computed without an adjustment the coordinator believes took effect, which is FR-012's failure by another route. contracts/openapi.yaml §UnappliedOverride and the errors[].reason enum both cite it -->
- [X] CHK029 Is behaviour specified for an active run whose artifact schema version the reader does not recognise, given no degraded state covers it, no error response names it, and the architecture requires a stale reader to fail loudly? [Completeness, Spec §FR-018; specs/sad.md §Data Management; contracts/openapi.yaml §ForecastRunMeta.artifact_schema_version] <!-- Evaluator: Resolved — FR-043's condition now includes an active run whose artifact schema version this reader does not recognise, stated as a deliberate refusal under specs/sad.md §Data Management's fail-loudly rule rather than as a ninth degraded state (FR-018 stays canonical at eight). contracts/openapi.yaml gives it its own problem type under 500 — not 503, because the datastore is reachable and retrying changes nothing — and plan.md's error table carries the row -->

## Caching and Idempotence

- [X] CHK030 Is a conditional-request capability required by any requirement or measured by any success criterion, or is the validator-and-304 mechanism a contract addition nothing obliges? [Completeness, Spec §FR-020a; contracts/openapi.yaml §parameters.IfNoneMatch] <!-- Evaluator: Resolved — FR-020a already required every response to carry the validator; it now states what a conditional answer asserts ("a response withheld as unchanged asserts that the whole response is unchanged and not only that no figure moved"), and added SC-027 measures it: a request answered as unchanged never withholds a worklist that would have differed. The mechanism now discharges a requirement and a criterion rather than standing alone in the contract -->
- [X] CHK031 Is cache correctness under FR-020a's three admitted change sources stated as a requirement — that an unchanged validator is a positive assertion no figure moved once a new run activates, a coordinator acts, or the day boundary advances? [Completeness, Spec §FR-020a; contracts/openapi.yaml §headers.ETag] <!-- Evaluator: Covered by spec.md §FR-020a's two-sided check, added by the testing pass: holding every admitted input fixed and repeating the request must yield an unchanged validator and unchanged figures, and changing each one in turn must change it — which is exactly the positive assertion across the three admitted change sources, reduced to one comparison -->
- [X] CHK032 Do the requirements oblige the validator to cover every input the *response* depends on, given FR-020a constrains only a row's figures while the enumerated validator inputs contain no line-level datum — so an open line added, made terminal, or re-prioritised between runs changes the worklist without changing the validator? [Completeness, Spec §FR-020a, §FR-022, §FR-028; specs/project-plan.md §Shared Data Entities; contracts/openapi.yaml §headers.ETag] <!-- Evaluator: Resolved — FR-020a's admitted input set now includes the state of the open-line set the response reports: which lines are in it and every stored field the response derives from them (identity, recorded need-by date, criticality, lifecycle state, roster hash). The reason is stated: this requirement's subject is a row's figures while the response's subject is a set of rows, so a line opened, made terminal under FR-022, or re-prioritised changes the worklist while every surviving row's figures stand still — and the line a coordinator most needs is the one that appeared since they last looked. E017 makes criticality mutable against this surface, so the input set observes it before that lands. Added SC-027; contracts/openapi.yaml §headers/ETag lists the inputs and says why a 304 would otherwise be a false statement -->
- [X] CHK033 Is the response's non-shareability stated as a requirement, given the mandated directive is `private` and the rationale calls the payload coordinator-scoped while no requirement establishes a coordinator identity or defines the session FR-031 scopes a what-if to? [Consistency, Spec §FR-031; contracts/openapi.yaml §headers.CacheControl, §info "Authentication"] <!-- Evaluator: Resolved — FR-031 now defines the session in the only terms this feature has, FR-056 having inherited the absence of any identity: the session is exactly the set of adjustments the interface holds and replays until reload, carried by the request rather than stored, and two coordinators are distinct because their requests carry different sets and for no other reason. Non-shareability follows and is stated as an obligation — a response computed under one adjustment set must not be served to a request carrying a different one — so `private, no-cache` implements a requirement rather than an inference from an identity that does not exist -->
- [X] CHK034 Is an equivalence rule stated for two identical requests inside one day boundary, naming which members may differ, given a generation timestamp is a required member and does not participate in the validator? [Testability, Spec §FR-020a; contracts/openapi.yaml §Meta.generated_at] <!-- Evaluator: Resolved — FR-020a now states the rule and names the single exception: two requests whose admitted inputs are all unchanged return responses equal member for member except the timestamp recording when the response was produced, which describes the act of answering rather than what was answered and takes no part in the validator, since a validator moving with the clock would report a change on every request and assert nothing. contracts/openapi.yaml §Meta.generated_at records it -->
- [X] CHK035 Is the read's freedom from side effects required of every request rather than only of an adjustment, given FR-011 and SC-005 are written about the reorder interaction while the same operation also serves the unmodified worklist? [Completeness, Spec §FR-011, §FR-002, §SC-005] <!-- Evaluator: Covered by spec.md §FR-035 ("a worklist request, including one carrying a need-by adjustment, MUST add no row to the model-invocation record") and §SC-003, both written over every request rather than over the adjustment, with FR-031 and FR-028 forbidding the two stored writes this surface could make and FR-031 stating that it exposes no write path at all -->

## Versioning and Compatibility

- [X] CHK036 Is there a stated gate preventing the endpoint from shipping while three artifacts name three addresses for it — the registered architecture's line-scoped path, the plan's summary table, and the contract's versioned server plus path? [Consistency, Plan §HINT-003, §API Surface Summary; specs/sad.md §Primary Flow; contracts/openapi.yaml §servers] <!-- Evaluator: Resolved — the conflict was recorded but gated by nothing. plan.md's Instructions Check Governance row now marks the deviation release-gating, and HINT-003 states the closing condition as a single comparison: the SAD's primary flow, the plan's summary table and the contract's servers plus path all resolving to one address, with the SAD amendment landing on the default branch rather than this one, per the governance rule the row already cited -->
- [X] CHK037 Is it stated which artifact carries the interface version a consumer binds to — the server prefix, the document version, or the artifact schema version — given three counters exist with no stated relationship and the validator's inputs cite "this contract's version" without saying which? [Clarity, contracts/openapi.yaml §servers, §info.version, §headers.ETag] <!-- Evaluator: Resolved — contracts/openapi.yaml §info gained a Versioning paragraph fixing all three: the /api/v1 path segment is the interface version a consumer binds to, info.version is this document's own revision and is what the ETag means by "this contract's version" because it is the counter that moves when the response shape moves, and artifact_schema_version is E003's forecast-artifact schema, a property of the data read whose unrecognised value is FR-043's fault rather than an interface mismatch. The ETag description now names info.version explicitly, and plan.md §API Surface Summary repeats the relationship -->
- [X] CHK038 Is a compatibility obligation stated toward the consumers the project plan records for this surface, and is what counts as a breaking change defined, given a closed response object makes any added member breaking for a strict consumer? [Completeness, specs/project-plan.md §API Surfaces; contracts/openapi.yaml §WorklistResponse] <!-- Evaluator: Resolved — contracts/openapi.yaml §info Compatibility names the recorded consumers (E012, E017, E019), states the fact the rule rests on (every one is in this repository and ships from the same commit; no external client exists), and defines breaking explicitly: removing or renaming a member, changing a type or admitted values, changing a member's meaning, adding a member to an object declared additionalProperties: false, adding a value to a closed enum, adding a required parameter, or moving a condition to another status — so almost any payload change is breaking for a strict consumer, which is the deliberate cost of FR-027's cap being structural. Compatible is limited to an optional parameter with a server default or a new problem type. FR-057 binds a later epic to it -->
- [X] CHK039 Is there a requirement or policy governing how a change to this contract is announced and withdrawn, given the contract carries no deprecation or sunset mechanism and E012 and E017 are scheduled to extend it? [Completeness, specs/project-plan.md §API Surfaces; contracts/openapi.yaml §paths] <!-- Evaluator: Resolved — the absence is now recorded rather than left implicit. contracts/openapi.yaml §info states the announcement and withdrawal mechanism (the same-commit consumer update plus an info.version bump; a member withdrawn from the contract and its consumers in one change) and why no Deprecation or Sunset header is defined (with every consumer released together there is no window in which an old and a new consumer both call it). plan.md carries it in the four-part form Principle VII requires — scope decision, supporting evidence, reversal trigger (the first consumer outside this repository, or one on its own cadence), production-scale alternative -->
- [X] CHK040 Is the project plan's "established in E010, read-only thereafter" convention carried into a requirement that binds the response contract as well as the layout, so a later epic extending the payload has a stated rule to satisfy? [Consistency, specs/project-plan.md §Interface shell contention; Spec §Scope] <!-- Evaluator: Resolved — added FR-057, carrying the convention into a requirement that binds the payload and not only the layout: an epic extending this surface satisfies FR-027's closed content classes, FR-041, FR-053's withholding rules and FR-007's enumeration rather than re-deciding them, and lands its change together with every consumer the project plan records, under the change rule the plan and contract state. The shell convention was written about routing and layout, but a member added to a closed response object changes the contract every consumer validates against just as surely -->


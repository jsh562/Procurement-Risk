# Security: Monorepo Scaffold and Contracts
**Created**: 2026-07-25 | **Feature**: [spec.md](../spec.md)

## Dependency Isolation and Provenance

- [X] CHK001 Is the package index each entry resolves from named in a requirement, rather than left to a toolchain default? [Complete (individual), Spec §TR-002] <!-- Evaluator: Resolved — added spec.md §TR-024 naming the index each entry resolves from (PyPI for the three Python entries, the public npm registry for web), with no alternate, supplemental, or private index configured, and restating that first-party packages resolve by path so no same-named public distribution can substitute; research.md adopts ASVS 15.1.2/15.2.4 as the criterion no requirement carried -->
- [X] CHK002 Does any requirement establish an enumerated third-party dependency inventory for the modeling, gateway, and web entries, or is an enumerated set required only for the serving image? [Complete (set), Spec §TR-012] <!-- Evaluator: Resolved — spec.md §TR-023 now names each entry's committed lockfile as that entry's enumerated third-party dependency inventory, and states that an asserted equality between an inventory and what is actually installed is required only for the serving image (TR-012) -->
- [X] CHK003 Is the gateway package required to resolve from its local path rather than by name from a public index, so a same-named public distribution cannot substitute for it? [Complete (individual), Spec §TR-002] <!-- Evaluator: Covered by spec.md §TR-002 ("both MUST declare the gateway package as a path dependency") and research.md §Python dependency isolation; the substitution consequence the item names is now stated outright in §TR-024 -->
- [X] CHK004 Is isolation between the serving and modeling boundaries stated as a requirement in its own right, rather than left implied by the four-entry layout? [Necessary, Spec §TR-002, §TR-004] <!-- Evaluator: Covered by spec.md §TR-004 (the serving resolved set MUST NOT contain any third-party distribution the modeling boundary declares directly) and §TR-002 (neither Python boundary may declare the other, and neither is a workspace member) — both stated as obligations rather than inferred from §TR-001's entry count -->
- [X] CHK005 Are "modeling stack" and "web framework" defined as sets derived from a manifest rather than hand-maintained name lists, so TR-003 is applicable without reviewer judgement? [Unambiguous, Spec §Glossary, §TR-003] <!-- Evaluator: Resolved — the "Modeling stack" row already stated its derivation ("Membership is read from the modeling boundary's manifest, never hand-listed"); the "Web framework" row carried no equivalent and now states the same rule against the request-serving boundary's manifest, so both operands of TR-003 are manifest-derived sets -->
- [X] CHK006 Is TR-004's comparison against *directly declared* third-party dependencies reconcilable with the glossary's "modeling stack", which also admits distributions reachable only transitively? [Consistent (set), Spec §TR-004, §Glossary] <!-- Evaluator: Resolved — the two sets were reconcilable but never stated as different; §TR-004 now states that its comparison set is the directly-declared subset of the Glossary's modeling stack and that the transitively-reachable remainder is deliberately outside it, because a shared transitive dependency is legitimate (OBJ1 VC2) and transitive arrival under an unrelated name is TR-012's case -->
- [X] CHK007 Does any requirement constrain the web boundary's dependency set, or is the isolation claim scoped to the Python entries — and is that scoping stated rather than inferred? [Complete (set), Spec §TR-004] <!-- Evaluator: Resolved — §TR-004 now states the scoping: the comparison covers the Python entries, and the web boundary resolves from a different ecosystem sharing no distribution namespace with them, so no dependency-isolation obligation under this requirement reaches it -->
- [X] CHK008 Is the vendored-or-copied-source blind spot — source carrying no distribution metadata and therefore invisible to a metadata query — recorded as a stated limit of the inventory-based checks? [Complete (individual), Spec §Edge Cases] <!-- Evaluator: Covered by spec.md §Edge Cases ("Vendored or copied source carries no distribution metadata and is invisible to a metadata query"), §TR-012 ("an absent finding is not evidence that a package is absent"), and §TR-013's blindness to any module a distribution's metadata does not record; research.md names the same limit -->
- [X] CHK009 Are the first-party path-dependency exclusions in TR-004 and TR-013 expressed as derivation rules rather than named exceptions that could drift out of step with the manifests? [Unambiguous, Spec §TR-004, §TR-013] <!-- Evaluator: Covered by spec.md §TR-004 ("First-party path dependencies are excluded from the comparison by derivation rule") and §TR-013 ("The exclusion is a derivation rule, not a hand-maintained name list") -->



## Lockfile Integrity and Lock-Derived Expected Sets

- [X] CHK010 For TR-012's equality claim, is the derivation rule stated for both sides — how the installed set is enumerated and how the expected set is derived from the serving lockfile? [Verifiable, Spec §TR-012] <!-- Evaluator: Covered by spec.md §TR-012 — the expected side is derived from the serving lockfile (groups, target-platform marker resolution, PEP 503 normalization) and the installed side is read from distribution metadata inside the image, with both sides normalized identically; plan.md's coverage map and HINT-002 fix the drivers (uv export versus importlib.metadata) -->
- [X] CHK011 Does the expected-set derivation state its resolution scope — target platform and environment markers, and whether development-only dependency groups are inside or outside the set? [Complete (individual), Spec §TR-012] <!-- Evaluator: Resolved — the Testing amendment had fixed platform and marker resolution but left group scope relative ("the dependency groups the image installs"); §TR-012 now fixes it as the serving boundary's default group only, with development-only groups outside both sides, since the image runs the service while every check exercising it runs from outside it (plan.md AD-003, HINT-004) -->
- [X] CHK012 Is the distribution-name normalization applied before set comparison stated in a requirement, given distribution names and import names occupy different namespaces? [Unambiguous, Spec §TR-012] <!-- Evaluator: Covered by spec.md §TR-012 ("both sides MUST be compared under PEP 503 normalized distribution names"); the distribution-versus-import namespace split the item alludes to is handled separately in §TR-013 -->
- [X] CHK013 Is per-entry lock verification — each lockfile consistent with its own manifest and consulting no other entry's — stated as a requirement, or does it appear only as an objective deliverable and validation criterion? [Complete (set), Spec §OBJ1, §TR-002] <!-- Evaluator: Resolved — it appeared only as an OBJ1 deliverable, OBJ1 VC1, and SC-001; added spec.md §TR-023, which states the obligation at requirement level: a committed lockfile per entry, verification runnable per entry, each checking that entry's lockfile against its own manifest and consulting no other entry's -->
- [X] CHK014 Does a requirement state that the single JavaScript lockfile is committed and checked against its manifest, or only that exactly one exists within the web boundary? [Complete (individual), Spec §Technical Constraints, §TR-005] <!-- Evaluator: Resolved — §Technical Constraints and OBJ1 VC6 said only that exactly one exists; §TR-023 now requires it committed and verified against its manifest, matching the obligation the Python entries carry and the "per entry" wording of OBJ1's deliverable, with plan.md's coverage map recording npm ci as the driver -->
- [X] CHK015 Is any use of the artifact digests recorded in the lockfiles required, or is lockfile reliance scoped to version pinning — and is that scoping stated rather than assumed? [Complete (set), Spec §TR-012] <!-- Evaluator: Resolved — spec.md §TR-023 now states the scope of lockfile reliance: version pinning, expected-set derivation, and the artifact hashes the locked install verifies as it installs, with no separate digest-verification check required here -->
- [X] CHK016 Does TR-013 state how modeling *distribution* names map to the *import* names its in-image check attempts, given the checked list is derived from a manifest? [Complete (individual), Spec §TR-013] <!-- Evaluator: Covered by spec.md §TR-013 — attempted module names MUST derive from the modeling boundary's installed distribution metadata (the top-level modules each declared distribution provides) rather than being assumed equal to distribution names or hand-mapped -->



## Build-Context Scoping

- [X] CHK017 Does the build-context requirement state the boundary of its guarantee — local source reachability rather than package-index installation — instead of implying a hermetic build? [Complete (individual), Spec §TR-011, §OBJ4] <!-- Evaluator: Covered by spec.md §TR-011 ("constrains local source reachability only: it does not prevent the image installing the same distributions from a package index"), §Objective 4 Rationale, §Edge Cases, and §SC-015; research.md records SLSA's parallel refusal to claim hermeticity -->
- [X] CHK018 Is TR-011's scoping expressed as an observable property of the image definition, so a reviewer can validate it without reproducing the build? [Able to be validated, Spec §TR-011] <!-- Evaluator: Covered by spec.md §SC-015 ("observable from the committed image definition") and §Objective 4 VC1, whose Given/When inspects the image definition rather than a completed build -->
- [X] CHK019 Is the data directory's unreachability from the serving image stated as a consequence of TR-011, so TR-016's single-reader placement rests on a requirement rather than on an assumption? [Consistent (set), Spec §TR-011, §TR-016] <!-- Evaluator: Resolved — TR-016 asserted the consequence ("because TR-011 keeps the data directory out of its build context") while TR-011 stated only its two reachable paths, and SC-015/OBJ4 VC1 narrow the exclusion to paths under /src; TR-011 now states the data directory's unreachability outright, so TR-016's placement rests on the requirement rather than on a reader's inference -->
- [X] CHK020 Is build-context scoping required for any image other than the serving image, or is the requirement deliberately scoped to that one image — and is the scoping stated? [Complete (set), Spec §TR-011, §TR-014] <!-- Evaluator: Resolved — spec.md §TR-011 now states the scoping: the serving image is the only image this epic builds, the database image is pulled rather than built (TR-015, IP-008), and no reciprocal obligation constrains a modeling job container, since the rule keeps the serving compute envelope reachable rather than isolating the offline side -->
- [X] CHK021 Does any requirement state that credential material must not enter the serving image's build context or its resulting layers? [Complete (set), Spec §TR-011] <!-- Evaluator: Resolved — added spec.md §TR-025: credential material MUST NOT enter the serving image's build context or any of its resulting layers, with the epic stating that it supplies no provider credential at build time; research.md adopts ASVS 13.3.1 as the criterion and no requirement carried it -->
- [X] CHK022 Is the stated coverage of the allowlist check and the in-image check bounded separately, so neither is presented as covering the other's case? [Consistent (set), Spec §TR-012, §TR-013] <!-- Evaluator: Covered by spec.md §Objective 4 Rationale (each mechanism covers a different case and none covers all), §TR-012's metadata blind spot, §TR-013's stated denylist blindness, §SC-007 (the re-locked case explicitly outside it, pointing at SC-008), and §Objective 4 VC4 -->



## Single Egress Point for the Model Provider

- [X] CHK023 Is the protected model-provider client identified by a specific distribution or module name in a requirement, rather than referred to generically? [Unambiguous, Spec §TR-008] <!-- Evaluator: Resolved — spec.md §TR-008 now names the protected package (the anthropic distribution and the top-level module of the same name), promoting plan.md §Technical Context ("Anthropic SDK (gateway)") and sad.md's stated provider to requirement level; TR-010's "provider client" now resolves through it -->
- [X] CHK024 Does TR-010's scanned root exist as a stated value, or does the requirement only require that some root be stated? [Verifiable, Spec §TR-010] <!-- Evaluator: ASK — the same open question already returned by testing.md CHK004 (Q1) and cross-referenced by data-integrity.md CHK034. TR-010 still obliges only that a root be stated plus two exclusions; this evaluation cross-references rather than answering it -->
- [X] CHK025 Is the dynamic-or-computed-import blind spot recorded as an explicit limit of the single-import claim rather than absorbed into it? [Correct, Spec §SC-005, §Edge Cases] <!-- Evaluator: Covered by spec.md §Edge Cases bullet 2 ("stated as a known limit rather than claimed as covered"), §SC-005 ("with dynamic imports stated as out of scope"), and §Risks "Contracts appear stronger than they are" -->
- [X] CHK026 Is the re-export laundering case assigned to a named mechanism rather than left to the import contract that structurally cannot see it? [Complete (individual), Spec §OBJ3, §Edge Cases] <!-- Evaluator: Covered by spec.md §Edge Cases bullet 1 ("the source scan is the check that catches it"), §Objective 3 VC4, §SC-014, and the §Glossary "Re-export laundering" row -->
- [X] CHK027 Is the single-egress claim scoped to import edges, with the absence of any network-level restriction on provider reachability stated rather than left for a reader to infer? [Unambiguous, Spec §TR-008] <!-- Evaluator: Resolved — §TR-008 now states that the claim is an import-edge claim and that no network-level restriction on provider reachability exists or is required by this epic, so a module reaching the provider over HTTP without importing the client is stated as unseen. The amendment discloses the absence of a control rather than adding one -->
- [X] CHK028 Does a requirement state whether the single-import-site claim reaches the TypeScript boundary, or is it scoped to the Python entries only? [Complete (set), Spec §TR-008, §TR-010] <!-- Evaluator: ASK — see returned question Q1. TR-008's contract and TR-010's scan are Python-shaped mechanisms, sad.md §Integration Strategy binds the interface tier to the API alone, and no artifact states whether the claim reaches /src/web; the enforcement option is entangled with the still-open scanned-root question (testing.md CHK004) -->
- [X] CHK029 Is the gateway module's provider-importing implementation attributed to E004, so its absence from this epic reads as a stated deferral rather than an unmet requirement? [Necessary, Spec §IP-005] <!-- Evaluator: Covered by spec.md §IP-005 (E004 implements the gateway module the allowlist contract designates as the sole permitted importer) and plan.md §Integration Points IP-005 -->



## Credential Boundaries

- [X] CHK030 Does any requirement state that model-provider credentials must be absent from committed source, or is that obligation carried only by convention? [Complete (set), Spec §Requirements] <!-- Evaluator: Resolved — the obligation rested on convention alone (neither the feature artifacts nor project-instructions.md carried it); spec.md §TR-025 now states it at requirement level: model-provider credentials MUST NOT be committed to the repository -->
- [X] CHK031 Where a credential obligation exists, does it name the supplying store and the holding principal, rather than only the places credentials must not appear? [Complete (individual), Spec §Requirements] <!-- Evaluator: ASK — see returned question Q2. TR-025 names the places credentials must not appear and defers provider credential supply to E004 (IP-005, sad.md §Security), but the one credential this epic's own artifacts consume — the local database service's — has no stated supplying store or holding principal -->
- [X] CHK032 Does the orchestration requirement state how database credentials are supplied for local development, given TR-015 fixes a host port but names no credential source? [Complete (individual), Spec §TR-015] <!-- Evaluator: ASK — see returned question Q2. TR-015 fixes the vector extension and a non-colliding host port and names no credential source; neither plan.md nor the registered project documents decide how the local Postgres credential is supplied, and the compose service cannot start without one -->
- [X] CHK033 Is credential redaction inside the traced invocation path attributed to E004, so its absence here is distinguishable from an omission? [Necessary, Spec §IP-005] <!-- Evaluator: Resolved — the deferral itself was unrecorded: IP-005 attributed only the gateway module's implementation to E004 and redaction appeared nowhere in the feature artifacts. IP-005 now attributes provider credential supply and redaction inside the traced invocation path to E004, matching sad.md §Security ("redacted by the traced invocation path itself") and E004's stated constraints -->
- [X] CHK034 Does the workflow requirement state whether any check needs credential access, so the dispatched run's secret surface is knowable from the requirement rather than from the workflow file? [Complete (individual), Spec §TR-020] <!-- Evaluator: Resolved — spec.md §TR-020 now states the dispatched run's secret surface: no check it invokes requires credential access (static analysis, tests over committed fixtures and pure functions, a local image build and run against public images), so no repository secret is needed, and a later epic adding such a check MUST state its own -->



## Provenance, Failure Signals, and Claim Strength

- [X] CHK035 Is build provenance or attestation for the produced serving image required anywhere, or is it absent without a recorded scope decision? [Complete (set), Spec §OBJ4] <!-- Evaluator: Resolved as a recorded scope decision rather than a new obligation — spec.md §Excluded now records that the serving image is built locally to run this epic's checks and is never published or deployed, so no attestation obligation arises; E018 owns hosted deployment and whatever attestation comes with it, and TR-026 plus TR-023 carry the reproducibility this epic needs -->
- [X] CHK036 Are the externally pulled base and database images required to be pinned by digest, or only by name and tag? [Complete (individual), Spec §TR-015, §IP-008] <!-- Evaluator: Resolved — nothing above plan.md's tag pin ("pgvector/pgvector:pg16") stated it; added spec.md §TR-026 requiring every externally pulled image this epic names — the serving image's base and the database image of TR-015/IP-008 — to be pinned by digest alongside name and tag, propagating sad.md's stated decision that the base image is digest-pinned so environment drift is not an admissible source of variance; plan.md's coverage map records the pinning points -->
- [X] CHK037 Does TR-027's drift requirement name the recorder, the comparison point, and the mismatch consequence, so drift is detectable rather than merely recorded? [Complete (individual), Spec §TR-027] <!-- Evaluator: Covered by spec.md §TR-027 — recorder (E002 and E005 under IP-001/IP-003, field roster_hash), comparison point (a recorded value against the hash the reader returns for the current fixture), and consequence (stale by definition; regenerate and re-record, or revert — "not a warning and not per-epic discretion") -->
- [X] CHK038 Does TR-022's violation-injection evidence path state its own boundary — that injected content reaches a scratch copy and cannot land in a production contract root? [Unambiguous, Spec §TR-022] <!-- Evaluator: Covered by spec.md §TR-022 — the requirement's stated property is an evidence path "that does not require committing a violation to a production contract root", and the injection option is bounded to "a scratch copy of a production root" -->
- [X] CHK039 Is each isolation and provenance success criterion worded to match its mechanism's actual reach — direct imports, set equality — rather than a stronger absolute? [Correct, Spec §SC-005, §SC-007] <!-- Evaluator: Covered by spec.md §SC-005 (direct imports, dynamic imports out of scope), §SC-006 (import edges, inline computation out of scope), §SC-007 (set equality, re-locked case handed to SC-008), §SC-014 (scan bounded to its scanned root), §SC-015 (build context evidences local source unreachability only), and §SC-001/§SC-002 (third-party declared dependencies) -->
- [X] CHK040 Does every isolation, lockfile, and egress requirement carry a stated failure signal, or does TR-019's non-zero-exit-and-name rule have to be read into each one? [Complete (set), Spec §TR-019] <!-- Evaluator: Covered by spec.md §TR-019, whose universal quantifier ("Every contract and assertion MUST exit non-zero on violation, naming the specific rule that failed and, where the violation is attributable to a source location, the offending module or file") reaches the isolation, lockfile, and egress mechanisms without per-requirement restatement — the single-obligation design testing.md CHK001 records, which the new TR-023/TR-024/TR-026 checks inherit -->


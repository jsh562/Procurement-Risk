# Observability: Delivery Forecast Model
**Created**: 2026-07-27 | **Feature**: [spec.md](../spec.md)

## What a Refused Run Leaves Behind

- [X] CHK001 Is the emission of a report file on a refusal path required by any requirement, or is it only recorded — in a disclosed gap — as the place the evidence happens to live once the database carries none? [Completeness, Spec §FR-017 / data-model §G-8] <!-- Evaluator: Resolved — it was only observed, never required: SC-015 said the report "is" written and G-8 relied on it, while no requirement obliged it. Added FR-037 (spec.md §Functional Requirements) requiring every refusal to emit the file, with SC-033, DV-038 and NC-19 -->
- [X] CHK002 Are the report file's location, name, format, retention and durability specified anywhere at requirement level, given a disclosed gap makes that file one of only two carriers of a refusal's evidence? [Completeness, data-model §G-8 / §Scope / plan §Architecture]
  <!-- Evaluator: FR-037 extended: one file per attempt under a resolved report directory, retained beside run reports and never overwritten. -->
- [X] CHK003 Is an identifier required for a refused *attempt* — which has no `run_id`, since no run row exists — so that two refusals of the same input are distinguishable and one's evidence cannot silently replace the other's? [Completeness, Spec §FR-014 / §FR-017 / data-model §G-8]
  <!-- Evaluator: FR-037 extended: a refused-attempt identifier formed from the as-of date, the input row hash and the attempt timestamp — a refusal has no run_id, so without it a retry loop leaves only its last iteration. -->
- [X] CHK004 Does SC-015's enumeration of "any store this run writes to" name the diagnostics store, or is `forecast_diagnostic` covered only by a data-model validation rule and the plan's error table? [Completeness, Spec §SC-015 / data-model §DV-013 / plan §Error Handling Strategy] <!-- Evaluator: Covered by spec.md §SC-015, which enumerates forecast_run, line_posterior, held_out_prediction, forecast_split_assignment and forecast_diagnostic as "the same five DV-013 snapshots" and names the diagnostic store explicitly as the one a reader expects a refusal to fill -->
- [X] CHK005 Do FR-017, SC-014 and SC-015 enumerate the same set of stores, or do three statements of one refusal obligation carry three different lists? [Consistency, Spec §FR-017 / §SC-014 / §SC-015] <!-- Evaluator: Resolved — FR-017 and SC-015 each carried five stores, SC-014 carried two ("no run record and no posterior"); STF-002 corrected the first two and did not reach SC-014. SC-014 now defers to SC-015's list rather than restating a third width -->
- [X] CHK006 Is emitting a file on the refusal path stated as permitted, so a report written by a refused run is unambiguously outside the "leaves nothing behind" claim rather than a candidate violation of it? [Clarity, Spec §SC-015 / data-model §Scope / §G-8] <!-- Evaluator: Covered by spec.md §SC-015 sub-bullet "What sits outside this quantifier" (stderr output and the emitted report file), restated in plan.md §Error Handling Strategy's closing paragraph -->
- [X] CHK007 Is the refusal-evidence obligation stated uniformly across every refusal category — unmet blocking precondition, moved input or split hash, breached blocking diagnostic — or only for the diagnostics breach? [Completeness, plan §Error Handling Strategy / Spec §FR-017] <!-- Evaluator: Covered by spec.md §FR-017's standing refusal-path obligation, stated to reach FR-017, FR-035, FR-021 and FR-023 alike, and by plan.md §Error Handling Strategy's "stated once for every row above", which spans the schema-head row too -->
- [X] CHK008 Does any requirement oblige a refusal to be distinguishable as pre-sampling or post-sampling in what it leaves behind, given the two carry materially different meanings to an operator deciding what to change? [Clarity, Spec §FR-017 / plan §Error Handling Strategy] <!-- Evaluator: Covered by spec.md §FR-017 ("nothing was written although a sample was taken" against FR-035's "nothing was sampled") and §FR-035, whose message names a precondition rather than a breached diagnostic; plan.md NC-14 evidences the distinction separately from NC-1 -->
- [X] CHK009 Is the provenance warning for a moved fixture digest required to survive in a durable artifact, or does it exist only in one invocation's output stream while the run it qualifies is published? [Completeness, Spec §FR-023 / data-model §DV-016] <!-- Evaluator: Covered by data-model.md §"Where the provenance warning is visible to a later reader, and where it is not" plus G-16: the break survives durably and reconstructibly in the stored digest pair; what does not survive is the fact the job named it, disclosed with a reversal trigger and a production-scale alternative rather than left implicit -->
- [X] CHK010 Is the wall-clock duration or realized sampling shape of a *refused* run recorded anywhere, or is the cost of an attempt knowable only for attempts that passed? [Completeness, Spec §FR-014 / data-model §G-8]
  <!-- Evaluator: FR-037 extended: the refusal report carries the attempt's wall-clock and realized sampling shape, or records that nothing was sampled on a pre-sampling refusal under FR-035. -->

## Exit Status and the Stream the Reason Travels On

- [X] CHK011 Does any requirement name the stream the refusal reason is written to, given the Open Group Base Specifications reserve standard error for diagnostic messages and no other stream contract exists for downstream tooling? [Completeness, Spec §FR-017 / research §Observability for a batch job] <!-- Evaluator: Covered by spec.md §FR-017's standing refusal-path obligation — "written to stderr, never to stdout" — restated once for every row in plan.md §Error Handling Strategy -->
- [X] CHK012 Is the exit status stated as a *class* — zero when every required action completed, above zero on refusal, with consumers obliged to compare against zero rather than a particular value — rather than as an unqualified "non-zero"? [Precision, Spec §FR-017 / §SC-014 / research §Observability for a batch job] <!-- Evaluator: Resolved — the clause stated only the non-zero side ("a single non-zero status class"). FR-017's standing clause now states the class on both sides: zero exactly when every required action completed (the FR-023 warning path included), above zero on every refusal, and consumers test against zero rather than a particular value. Mirrored in plan.md §Error Handling Strategy -->
- [X] CHK013 If any refusal category is intended to be distinguishable by its exit code, is that code published as a stated local convention, given no standard assigns meaning to any specific non-zero value? [Precision, plan §Error Handling Strategy / Spec §Published Constants] <!-- Evaluator: Covered — the antecedent is closed rather than satisfied: spec.md §FR-017 states no requirement allocates distinct codes and the category is carried by the reason text, and plan.md §Error Handling Strategy repeats that no row allocates one. No local convention is owed because none is claimed -->
- [X] CHK014 Is the exit status of the warning path stated as zero, so a provenance warning is not indistinguishable from a refusal to a caller that only inspects the code? [Completeness, Spec §FR-023 / plan §Error Handling Strategy] <!-- Evaluator: Covered by spec.md §FR-023 — "the run proceeds to completion with a zero exit" — and by plan.md §Error Handling Strategy's "Warn, do not refuse" row; FR-017's amended class clause names the warning path explicitly on the zero side -->
- [X] CHK015 Is what standard output carries stated at all — reserved for machine-readable results, or unused — so that placing a diagnostic there is a requirement violation rather than a style preference? [Completeness, Spec §FR-017 / research §Observability for a batch job]
  <!-- Evaluator: FR-039 added: stdout carries exactly one machine-readable line, the run_id written, and nothing on a refusal. FR-017 already reserved stderr for diagnostics; stating only what stdout may not carry left the stream undefined. -->
- [X] CHK016 Is the required content of the refusal message stated as a checkable field set rather than as the prose verb "naming", which admits any message containing the words? [Testability, Spec §FR-017 / §SC-014 / data-model §DV-013] <!-- Evaluator: Resolved — FR-017 gains a sub-bullet stating the content as a five-field set per breach (metric, parameter where parameter-scoped, realized value, threshold, threshold direction), with the two-field pre-sampling set for FR-035/FR-021 and the hash pair for FR-023 -->
- [X] CHK017 Does FR-017 state whether *every* breached diagnostic is named or only one, given several blocking rows can breach in a single run? [Unambiguous, Spec §FR-017 / §SC-014] <!-- Evaluator: Resolved — it did not; the singular "the breached diagnostic" ran through FR-017, SC-014 and DV-013. FR-017 and SC-014 now require every breached blocking diagnostic to carry its own complete field set, "not merely the first one found" -->
- [X] CHK018 For a breach of a parameter-scope metric, is the message required to name the parameter as well as the metric, given the stored record keys R-hat and both effective-sample-size metrics by `parameter_name`? [Completeness, Spec §FR-016 / §FR-017 / data-model §`forecast_diagnostic`] <!-- Evaluator: Resolved — the breach path named only the metric while the uncomputable-metric path in plan.md already named "the parameter if any". FR-017's field set now requires the parameter_name for parameter-scoped metrics; SC-014 and plan.md's blocking-diagnostic row follow -->

## Every Reported Measure and Its Decision Criterion

- [X] CHK019 Does FR-017's refusal triple — diagnostic, realized value, threshold — include the threshold's *direction*, without which a value and a bar do not resolve to a verdict for a reader who does not already know which metrics are floors? [Completeness, Spec §FR-017 / data-model §`forecast_diagnostic`] <!-- Evaluator: Resolved — it did not, though threshold_direction is a stored NOT NULL column fixed per metric by ck_forecast_diagnostic__direction_matches_metric. Direction is now field (v) of FR-017's field set, and appears in SC-014 and plan.md's blocking-diagnostic row -->
- [X] CHK020 Is the reportable unit stated once as (measure, realized value, decision criterion, verdict) — a transfer of ISO/IEC/IEEE 15939's measurement information model, which governs an organization's measurement process and is applied here as a transfer rather than a conformance claim — or restated ad hoc for each measure that happens to have a criterion? [Consistency, Spec §SC-016 / §SC-025 / research §Observability for a batch job] <!-- Evaluator: Resolved — it was restated ad hoc and lost a part each time. Added FR-038 stating the unit once, with the 15939 framing marked as a transfer and never a conformance claim, an enumeration of the measures it reaches, and the criterion-free measures it does not; covered by SC-034, DV-039 and NC-20 -->
- [X] CHK021 Does a published decision criterion exist for the one reported-but-not-blocking measure, given Published Constants assigns maximum-treedepth hits no number while the stored record requires a non-null `threshold_value` for every row? [Completeness, Spec §Published Constants / §FR-018 / data-model §`forecast_diagnostic`]
  <!-- Evaluator: Published Constants now gives max-treedepth a reporting threshold of 0 hits, direction max, explicitly non-blocking. -->
- [X] CHK022 Is the meaning of `passed` defined for a metric that gates nothing, or does a stored treedepth row assert an arithmetic verdict against a bar no requirement publishes? [Clarity, Spec §FR-018 / §SC-016 / data-model §`ck_forecast_diagnostic__passed_matches_threshold`]
  <!-- Evaluator: With the bar published, passed = false on the treedepth row means the sampler hit the cap and the run still ships. ck_forecast_diagnostic__blocking_rows_passed permits this by constraining only rows where is_blocking, so the verdict is honest rather than vacuous. -->
- [X] CHK023 Does SC-016's "beside the threshold it was judged against" bind the *stored* diagnostics record rather than any log line, so the pairing is a property of the durable evidence? [Precision, Spec §SC-016 / data-model §`forecast_diagnostic`] <!-- Evaluator: Resolved — the criterion said only "records", and the binding to the stored rows lived in plan.md's coverage map (DV-011, DV-012) rather than in the criterion. SC-016 now names the stored forecast_diagnostic rows, "not merely a log line or the emitted report", and adds the threshold's direction -->
- [X] CHK024 Is the emitted report required to carry the same four-part unit as the stored record — realized value, criterion, direction and verdict — given the report is the sole carrier when no diagnostic row can exist? [Completeness, Spec §FR-017 / §SC-016 / data-model §G-8] <!-- Evaluator: Resolved — FR-037 requires the refusal report to carry at least the field set FR-017 requires of the stream, on the reasoning that the stream is transient and the file is the durable half of G-8's pair; SC-033 and DV-038 assert content as well as presence -->
- [X] CHK025 Does the per-vendor shrinkage report pair each realized weight with the observation count below which no vendor-level claim is supported, together with an explicit per-vendor verdict, or is that count stated once for all twelve? [Completeness, Spec §FR-019 / §FR-020 / data-model §L-4] <!-- Evaluator: Resolved — the count was stated once for the population (FR-020, L-4, DV-024). FR-020 now requires each vendor's realized weight and training observation count to be paired with that count and an explicit per-vendor supported/not-supported verdict, as FR-038's unit applied per vendor -->
- [X] CHK026 Does the censoring-ablation report state the realized delta, the independently derived floor and an explicit met-or-missed verdict, rather than the delta and its interval alone? [Completeness, Spec §SC-008 / §FR-033 / plan §AD-008] <!-- Evaluator: Resolved — FR-033 required the delta with an interval and the floor's derivation, but no verdict in the report. FR-033 now requires the delta with its interval, the floor beside it, and an explicit met-or-missed verdict -->
- [X] CHK027 Is the reproduction outcome required to carry its decision criterion — the published day tolerance — beside the realized per-line deltas and a verdict, rather than reporting agreement as a bare outcome? [Completeness, Spec §FR-022 / §SC-018 / data-model §DV-018] <!-- Evaluator: Resolved — the failure path named lines and deltas (plan.md §Error Handling, NC-17) and the tolerance was published elsewhere. FR-022 now requires the outcome to be reported as the deltas, the published tolerance they were judged against, and one of SC-018's three outcomes as an explicit verdict -->
- [X] CHK028 Is the third reproduction outcome introduced by the tolerance decision — "outside the tolerance's stated basis" when realized predictive effective sample size falls below half the draw count — carried by any requirement or success criterion, or does it exist only inside a plan decision row? [Traceability, plan §AD-004 / Spec §FR-022 / §SC-018] <!-- Evaluator: Covered by spec.md §SC-018's "Three outcomes, not two" sub-bullet, which carries AD-004's basis condition in the criterion rather than leaving it reachable only through the decision record -->
- [X] CHK029 Is the horizon-extrapolation disclosure required to publish the computed maximum observed duration beside the 365-day horizon, so the disclosure is a measure against a criterion rather than an unsourced claim? [Completeness, Spec §FR-031 / data-model §L-2 / §G-10] <!-- Evaluator: Covered by data-model.md DV-037, which requires L-2 present by identity "with the computed maximum observed duration published beside the horizon (G-10)", by L-2 itself, and by G-10's record that the maximum is computed from lifecycle_event because the datasheet publishes none -->

## Run Identity and Provenance Resolvability

- [X] CHK030 Is the run identifier required to appear in the job's own output, so an operator holding only what the job printed can locate the run it produced? [Completeness, Spec §FR-014 / §SC-020]
  <!-- Evaluator: FR-039 puts the run_id on stdout, which is the operator-side half of identity; the artifact side was already structural through run_id in every artifact primary key. -->
- [X] CHK031 Does any requirement oblige the emitted report to name the run it belongs to, given the manifest is a database row and the report is a file with no stated join to it? [Completeness, Spec §FR-014 / data-model §Scope] <!-- Evaluator: Resolved — nothing joined the file to the row. FR-022 now requires the reproduction outcome to identify both runs by run_id, and FR-014's new sub-clause carries the manifest's reader-facing fields into the report; the run identifier is the join FR-037's report and the manifest share. The remaining half — what identifies a *refused* attempt, which has no run_id — is CHK003 and is open -->
- [X] CHK032 Is "joinable to exactly one run" stated for every store a run writes to — split assignments and diagnostic rows included — or only for the two artifact populations? [Completeness, Spec §SC-001 / §SC-013 / data-model §DV-011] <!-- Evaluator: Resolved — it was stated for line_posterior (SC-001) and held_out_prediction (SC-013) only, while the other two stores carried the property structurally and claimed it nowhere. SC-009 and SC-016 now state it, each naming the key and foreign key that carry it -->
- [X] CHK033 Is a reader-facing artifact required to carry the input layer label and datasheet reference, given FR-014's stated purpose is that a reader of a forecast knows every number descending from it is synthetic-derived? [Completeness, Spec §FR-014 / §SC-020] <!-- Evaluator: Resolved — both were required in the manifest row only, which the reader of a forecast does not read. FR-014 gains a sub-clause requiring the layer label and datasheet reference in the reader-facing artifact FR-020 and FR-027 already oblige -->
- [X] CHK034 Is the movement of the active-run pointer resolvable after the fact — which run it moved from, and when — or is only its current value recorded? [Completeness, Spec §FR-015 / plan §AD-010 / data-model §Write Order] <!-- Evaluator: Resolved by disclosure — only the current value is recorded, and the Entities table's three run states collapse onto one boolean. Added G-18 in the form of G-8 and G-16: what is recoverable, why neither a nullable superseded_at nor a pointer-move table is taken now, the reversal trigger, and the production-scale alternative -->
- [X] CHK035 Is a complete-but-unpublished run — the state between the artifact transaction and the pointer transaction — distinguishable by an operator from a run that was never written? [Clarity, plan §AD-010 / Spec §FR-015] <!-- Evaluator: Covered by plan.md AD-010 and data-model.md §Write Order: transaction 1 is all-or-nothing, so a complete-but-unpublished run is a full row set with is_active = false while a never-written run leaves no forecast_run row at all — the state DV-013 and SC-015 snapshot for. What it is *not* distinguishable from is a superseded run, which is G-18 -->
- [X] CHK036 Does the reproduction job's output identify both runs it compared, so its verdict resolves to the artifacts it was computed from? [Traceability, Spec §FR-022 / §SC-018 / data-model §DV-018] <!-- Evaluator: Resolved — neither FR-022, SC-018 nor DV-018 named the operands. FR-022 now requires the outcome to identify the run whose manifest was read by run_id and the re-run by its own run_id where one was written, by its manifest provenance fields otherwise -->
- [X] CHK037 Is the set of emitted artifacts enumerable at requirement level, given the no-verdict absence rule is quantified over that set and it is unstated whether a refusal report belongs to it? [Testability, Spec §SC-026 / data-model §DV-021]
  <!-- Evaluator: FR-040 enumerates the set as exactly three files — run report, reproduction report, refusal report — with the ablation record and limitation set as sections of the run report. DV-041 asserts it as an equality so a fourth file fails rather than escaping DV-021. -->
- [X] CHK038 Are the several emitted files — run report, ablation report, reproduction report, reader-facing limitation set — each named with their required content, or referred to collectively as "the emitted report" by the rules that assert over them? [Clarity, data-model §DV-024 / §DV-025 / §Scope]
  <!-- Evaluator: FR-040 defines the run report as a closed schema of typed sections and named fields; SC-035 and DV-041 validate every field against it. -->
- [X] CHK039 Is the absence of any progress or liveness signal from an unbounded offline fit recorded as a scope decision, given exit status is the only output the requirements oblige the job to produce? [Completeness, plan §Technical Context / Spec §FR-017] <!-- Evaluator: Resolved — plan.md §Technical Context recorded only that wall-clock is "recorded, not bounded", which is a performance note rather than a scope decision about the silence. spec.md §Scope/Excluded now carries the exclusion with its reversal trigger and production-scale alternative -->


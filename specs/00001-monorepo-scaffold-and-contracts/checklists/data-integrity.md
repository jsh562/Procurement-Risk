# Data Integrity: Monorepo Scaffold and Contracts
**Created**: 2026-07-25 | **Feature**: [spec.md](../spec.md)

## Fixture Declaration and Scope

- [X] CHK001 Does a requirement fix the roster's committed path, serialization format, and character encoding, or do these exist only in the design's artifact table? [Complete, Spec §TR-016 / data-model §Physical Artifacts] <!-- Evaluator: Resolved — all three sat only in data-model.md §Physical Artifacts and §CS-1; spec.md §TR-016 now fixes the committed path `data/roster/project-vendor-roster.json`, JSON serialization, and UTF-8 without a byte-order mark at requirement level -->
- [X] CHK002 Is the set of artifacts constituting the roster contract — fixture, datasheet, naming convention, exclusion list — enumerated in one place, so a reviewer can determine whether all four must land in the same commit? [Complete (set), data-model §Physical Artifacts] <!-- Evaluator: Covered by data-model.md §Physical Artifacts, whose table enumerates all four with their paths, and §State & Lifecycle, whose `Absent` exit condition is "Fixture, datasheet, convention, and exclusion list land in one commit"; the RosterDatasheet entity repeats "same directory and in the same commit" -->
- [X] CHK003 Is "readable without an added dependency" stated as a determinable condition naming the permitted module set, rather than as an intent? [Verifiable, Spec §TR-016 / data-model §VR-009] <!-- Evaluator: Covered by data-model.md §VR-009, which names the permitted set (`json`, `hashlib`, `unicodedata`, `pathlib`, `re`) and the assertion point (`/src/model`'s manifest, unchanged by this feature's roster work); plan.md AD-001 records the format choice that makes it satisfiable -->
- [X] CHK004 Are the fixed population sizes traced to a source of authority that makes a change to them a specification amendment rather than a data edit? [Traceability, Spec §Assumptions / data-model §State & Lifecycle] <!-- Evaluator: Covered by spec.md §Assumptions ("taken from the product document"), data-model.md §State & Lifecycle entity-added row ("Population size is fixed at 5/12 by spec and PRD; changing it is a spec amendment, not a data edit"), and the RosterDatasheet `population_sizes` attribute, which requires the sizes **and their source** (`specs/sad.md` Scale/Scope, `specs/prd.md`) -->
- [X] CHK005 Is the obligation that no consumer redefines projects or vendors accompanied by a stated detection mechanism, or is it a constraint nothing observes? [Verifiable, Spec §Technical Constraints / data-model §Access Boundary] <!-- Evaluator: Resolved — data-model.md §Access Boundary now names VR-013 as the mechanism for the half inside this repository and discloses the other half as uncovered: a later consumer that hardcodes its own populations opens no roster path and produces no import edge, so no E001 check sees it; that half is carried by IP-001 and IP-003. VR-013's own scanned root and detection means remain the open question in CHK034 -->
- [X] CHK006 Is the roster reader's return contract — the structure yielded and the hash value's shape — specified at requirement level, so both consuming epics integrate against the same interface? [Unambiguous, Spec §TR-027 / §IP-001, §IP-003] <!-- Evaluator: Resolved — spec.md §TR-027 now states the return contract: the validated roster (five projects, twelve vendors, each with stable identifier and display name) together with the hash as `sha256:` plus 64 lowercase hexadecimal characters, promoting data-model.md §Entities and §CS-6 to requirement level -->

## Canonical Serialization Specification

- [X] CHK007 Does the canonical-serialization requirement pin object member ordering and array element ordering as separate rules, rather than letting one be read as implying the other? [Unambiguous, data-model §CS-4, §CS-5] <!-- Evaluator: Covered by data-model.md §CS-4 (array element ordering — `projects` by `project_id`, `vendors` by `vendor_id`, ascending codepoint) and §CS-5 (object member ordering — `sort_keys=True`), stated as two separate steps so neither is inferred from the other -->
- [X] CHK008 Is the handling of duplicate keys in the source file specified as rejection, last-wins, or explicitly out of scope, rather than inherited silently from the parser? [Complete, data-model §CS-2] <!-- Evaluator: Resolved — CS-2 said only "Parse with `json.loads`", inheriting last-wins silently; it now rejects duplicate keys via an `object_pairs_hook`, classifies one as a read failure (VR-014), and records why: last-wins discards content that would then not move the digest, falsifying VR-008c's sensitivity claim. Consistent with VR-007's existing strictness on unexpected keys -->
- [X] CHK009 Is the Unicode normalization form named together with the elements it applies to and the elements it does not? [Precision, data-model §CS-3] <!-- Evaluator: Covered by data-model.md §CS-3 — the form is NFC and its scope is "every key and string value", which VR-007 ("all values are non-empty strings") and the determinism property "the payload holds strings only" make exhaustive of the payload, so no element is left silently unaddressed. The separate exclusion-matching normalization is named as such under Committed convention content -->
- [X] CHK010 Are number, boolean, and null serialization rules either specified or rendered unnecessary by a stated constraint that the payload holds strings only? [Complete, data-model §CS-5 / §VR-007] <!-- Evaluator: Covered by data-model.md §VR-007 ("all values are non-empty strings"), §CS-2 (parse into `dict`/`list`/`str` only), and the determinism property "the payload holds strings only — no numbers, booleans, or nulls", which is stated with its consequence (independence from platform float repr) -->
- [X] CHK011 Is the input encoding requirement stated with its failure condition, so a byte-order mark is unambiguously a parse failure rather than a tolerated variant? [Unambiguous, data-model §CS-1] <!-- Evaluator: Covered by data-model.md §CS-1 ("Read the file as UTF-8 text with `encoding=\"utf-8\"`. A BOM is a parse failure, not a tolerated variant"), now also carried by VR-014 as a read failure and by spec.md §TR-016's "UTF-8 without a byte-order mark" -->
- [X] CHK012 Are whitespace and separator rules — indentation, key and item separators, trailing newline — specified precisely enough for an independent implementation to reproduce the same byte sequence? [Precision, data-model §CS-5] <!-- Evaluator: Covered by data-model.md §CS-5 — `separators=(",", ":")` fixes both the item and key separators with no surrounding space, and "no indentation, no trailing newline" fixes the remaining whitespace, with §CS-6 fixing the UTF-8 encoding of that byte sequence -->
- [X] CHK013 Are the stated determinism properties expressed as requirements from which test cases derive, each carrying the rationale that distinguishes designed invariance from an unnoticed gap? [Able to be validated, data-model §Determinism properties / §VR-008] <!-- Evaluator: Covered by data-model.md §Determinism properties, each bullet a test case for the VR-008 assertion it now names, each carrying its rationale ("The hash tracks roster *content*, not file bytes; reformatting is not drift"; CRLF load-bearing on Windows; `sort_keys=True` for seed independence), reinforced by §State & Lifecycle's "Nothing — hash unchanged **by design**" row, which separates designed invariance from a gap -->

## Content Hash and Hashed Scope

- [X] CHK014 Does a requirement state which fields fall inside the hashed scope and which are excluded, including the derived hash value itself? [Complete, data-model §Entities ProjectVendorRoster] <!-- Evaluator: Covered by data-model.md §Entities ProjectVendorRoster — `CHECK(top-level keys = {projects, vendors})` fixes the hashed payload exactly, and `content_hash` is "DERIVED, never stored", so it cannot fall inside it (a stored one would fail VR-007); §Validation Input Artifacts states the convention and exclusion list are "not covered by its content hash", and CS-1…CS-6 take the whole parsed payload as the hash input -->
- [X] CHK015 Is the hash output format — algorithm prefix, digest encoding, character case — specified precisely enough that values recorded by different implementations are comparable? [Precision, data-model §CS-6] <!-- Evaluator: Covered by data-model.md §CS-6 (`"sha256:" + hexdigest()` in lowercase) and §Entities (`sha256:` + 64 lowercase hex); the §Consumer contract line, which had said only "64 hex", now says lowercase too, and spec.md §TR-027 carries the same format at requirement level -->
- [X] CHK016 Is the sensitivity claim bounded to content inside the canonical scope, rather than asserting that any change to the file produces a different hash? [Correct, data-model §VR-008] <!-- Evaluator: Covered by data-model.md §VR-008, which claims sensitivity to *content* mutation only and lists the formatting mutations that deliberately do not move the hash, and by the determinism property enumerating the sensitive set; the bound is now stated outright ("Sensitivity is bounded to content inside the canonical scope of CS-1…CS-6"), matching the Validation Input Artifacts drift story for files outside it -->
- [X] CHK017 Is there a stated procedure for an algorithm change covering values already recorded under the current prefix, rather than only a claim that such a change would be visible? [Complete, data-model §Determinism properties] <!-- Evaluator: Resolved — the `sha256:` prefix bullet claimed visibility but named no procedure; data-model.md §State & Lifecycle now carries an algorithm-change row: a value under a superseded prefix is not comparable (neither agreement nor drift evidence), is stale by definition, and is handled by regeneration and re-recording — no superseded algorithm is retained and history is never re-hashed to manufacture a match. Mirrors the existing "stale by definition" rule for `Amended(Hₙ)` -->
- [X] CHK018 Is the absence of a stored version field expressed as a bounded assertion naming the prohibited field names, rather than an open-ended prohibition a check cannot close over? [Verifiable, data-model §VR-011 / Spec §Objective 6 VC3] <!-- Evaluator: Covered by data-model.md §VR-011, which names exactly three prohibited fields (`version`, `revision`, `updated`), and §VR-007, whose strict `CHECK(top-level keys = {projects, vendors})` closes the prohibition completely — any additional top-level key fails the read regardless of its name. Left as a bounded assertion by design; not amended toward reintroducing a version field -->

## Producer and Consumer Drift Detection

- [X] CHK019 Does the drift requirement name all three of the recorder, the comparison point, and the mismatch consequence, given that recording alone makes drift recordable rather than detectable? [Complete, Spec §TR-027] <!-- Evaluator: Resolved — TR-027 named only the recorder; it now names all three: E002 and E005 record (recorder), a recorded `roster_hash` is compared against the hash the reader returns for the current fixture (comparison point), and a mismatch makes every artifact carrying the earlier value stale by definition and requiring regeneration or revert (consequence), promoting data-model.md §State & Lifecycle and §Policy -->
- [X] CHK020 Is the obligation to record the hash assigned to named owning epics inside a requirement, rather than appearing only as a consumer-contract note in the design document? [Traceability, data-model §Consumer contract / Spec §IP-003] <!-- Evaluator: Resolved — TR-027 said "consumers" while only data-model.md §Consumer contract named them; TR-027 now names E002 and E005 as the recording consumers with their integration points, and IP-001 gains the recording obligation IP-003 already carried -->
- [X] CHK021 Is the recorded field's name, type, and value format fixed by a requirement, so the two consuming epics cannot diverge on it? [Consistent, data-model §Consumer contract] <!-- Evaluator: Resolved — the field name `roster_hash`, its string type, and its `sha256:` + 64 lowercase hex format are now fixed in spec.md §TR-027 rather than only in data-model.md §Consumer contract, which itself gained the missing "lowercase" and a pointer to the requirement; persistence location stays E003's schema decision -->
- [X] CHK022 Is the failure mode in which a consumer omits the recorded hash — the one case the design records as detected by nothing — given an owner, a mitigation, or an explicit risk acceptance? [Complete, data-model §State & Lifecycle] <!-- Evaluator: Resolved — the row recorded "Detected by: Nothing" with neither owner nor acceptance; it now names E002 and E005 as owners under IP-001 and IP-003 (each owning its own generation output, with the obligation stated in TR-027) and records E001's explicit acceptance: no E001 check can observe a later epic's artifacts, so closing it belongs to the generating epic -->
- [X] CHK023 Does a requirement state what happens on a hash mismatch — refusal, warning, or regeneration — rather than leaving the response to each consuming epic? [Complete, data-model §State & Lifecycle] <!-- Evaluator: Resolved — the response was fixed only in data-model.md (`Amended(Hₙ)` exit condition and §Policy); spec.md §TR-027 now states it at requirement level: regeneration and re-recording, or reverting the roster change — "not a warning and not per-epic discretion" — with detection only and no automatic correction -->
- [X] CHK024 Do the artifacts deliberately excluded from the hashed scope — the naming convention and the real-firm exclusion list — each carry their own stated drift story? [Complete (set), data-model §Validation Input Artifacts] <!-- Evaluator: Resolved — both were marked "not covered by its content hash" with no drift account; data-model.md §Validation Input Artifacts now carries an asymmetric drift story for the pair: a tightening edit fails the next read through VR-005/VR-006, which re-validate all seventeen names against the current artifacts, while a loosening edit (widened pattern, removed exclusion entry) changes what validation permits, moves no hash, and is detected by nothing — recorded as uncovered, with diff review the only control -->
- [X] CHK025 Is the term for the recorded integrity value consistent across requirements, integration points, and edge cases, or do "content hash" and "version" appear interchangeably for the same value? [Consistent, Spec §IP-003 / §Edge Cases / §TR-027] <!-- Evaluator: Resolved — version-marker vocabulary survived the rejection in exactly two places: IP-003 said E005 "records the roster version it read" and the Edge Cases entry said drift is "undetectable without a recorded version". Both now say content hash / `roster_hash`, matching TR-027, OBJ6 VC3, Key Entities, and the Clarifications answer. The remaining uses of "version field" are the deliberate statements of its absence -->
- [X] CHK026 Is the freeze policy covering the consumer generation window stated with an enforcement mechanism, or labelled explicitly as a convention that no check observes? [Feasible, data-model §Policy] <!-- Evaluator: Resolved — the freeze was stated as if binding; data-model.md §Policy now labels it a convention that no check observes, since nothing in this epic watches the fixture during a generation window and the hash mismatch is an after-the-fact detector rather than a preventer. Consistent with the section's existing "only detection... a *detector*, not a repair" -->

## Validation-Rule Completeness and Verifiability

- [X] CHK027 Is every attribute declared on the roster, project, and vendor entities covered by at least one validation rule? [Complete (set), data-model §Entities / §Validation Rules] <!-- Evaluator: Resolved — the mapping holds for every attribute (counts VR-001; identifiers VR-002/VR-003; display names VR-003…VR-007; strict keys VR-007; absent version field VR-011; derived hash VR-008) with one exception that was neither covered nor stated: the identifiers' `immutable, never reused` constraint. data-model.md §Validation Rules now carries an Attribute coverage note recording it as a cross-revision property no single-read rule can observe — a changed identifier surfaces only as a hash mismatch, reuse surfaces as nothing — held by policy and review rather than counted as covered -->
- [X] CHK028 Are validation rules stated singularly, or does a rule bundling determinism, formatting invariance, and content sensitivity combine claims that would pass and fail independently? [Singular, data-model §VR-008] <!-- Evaluator: Resolved — VR-008 bundled three independently-failing claims under one ID; it is now decomposed into named sub-assertions VR-008a (repeated reads), VR-008b (formatting invariance), and VR-008c (content sensitivity), each with its own test case and its own name in failure output, and the §Determinism properties bullets now key to them. The VR-008 ID is preserved, following the spec's own TR-027 sub-ID precedent, so no cross-reference breaks -->
- [X] CHK029 Is the blanket failure-output sentence adequate for every rule, including rules where no offending identifier or display name exists to name? [Complete, data-model §Validation Rules / Spec §TR-019] <!-- Evaluator: Resolved — the sentence demanded "the offending identifier or display name" for every rule, which VR-001, VR-008, VR-010…VR-012 cannot supply; it is now conditioned exactly as TR-019 already words it (record-level, source-location, or neither) and names the observed-versus-required form those rules report instead, e.g. `VR-001: 6 projects, expected exactly 5` -->
- [X] CHK030 Is the normalization applied for display-name uniqueness identical to the normalization applied for exclusion matching, and is that stated rather than left to inference? [Consistent, data-model §VR-004, §VR-006] <!-- Evaluator: Resolved — VR-004 said only "unique under normalization" while the form was defined under the label "Normalization for exclusion matching"; both now name the single committed `NamingConvention.normalization` (NFC, casefold, strip punctuation, strip legal suffixes, collapse whitespace), stated as one form used by both rules so a pair of names cannot be unique under one and colliding under the other. The entity declares one `normalization` object, so no second form was ever intended -->
- [X] CHK031 Is the exclusion-match rule defined precisely enough for two independent implementations to agree, in particular on what constitutes a whole token? [Unambiguous, data-model §Committed convention content / §VR-006] <!-- Evaluator: Resolved — "contains an entry as a whole token" left the token undefined; data-model.md §Committed convention content now derives it from the normalization already committed: punctuation stripped and whitespace collapsed, so name and entry split on a single space into token lists, and containment means the entry's tokens appear as a contiguous subsequence at a token boundary. Sub-token matches (`ironvalefabrication` / `vale`) and non-contiguous scatters are stated as non-violations -->
- [X] CHK032 Does any rule cover an absent, empty, or unparseable roster file, distinguishing a read failure from a validation failure? [Complete, data-model §Validation Rules] <!-- Evaluator: Resolved — VR-012 covered existence and parse for the two validation-input artifacts but nothing covered the roster file itself; added VR-014, which requires the file to exist at its committed path, be non-empty, and parse under CS-1/CS-2, and classifies absence, unreadability, a BOM, malformed JSON, or a duplicate key as a read failure reported distinctly from a validation failure, with no hash returned and VR-001…VR-013 not reached -->
- [X] CHK033 Is the ordering between validation and hashing specified, so it is determined whether a hash is returned for a roster that fails a rule? [Unambiguous, data-model §VR-001, §VR-008] <!-- Evaluator: Resolved — ordering was fixed only for the added-entity case ("The read fails before any hash is returned") and implied by the `Committed(H₁)` state; added VR-015, which generalizes it: all of VR-001…VR-013 are evaluated before any hash is emitted, and a roster failing any rule yields a non-zero exit and no hash, so no consumer can record a value for a roster that never validated. The `Committed` state now reads VR-001…VR-015 -->
- [X] CHK034 Is the single-reader boundary rule given a stated scanned root and detection mechanism comparable to the source-scan requirement it resembles? [Verifiable, data-model §VR-013 / Spec §TR-010] <!-- Evaluator: ASK — see returned question Q1; VR-013 states the rule ("Exactly one module in the repository opens the roster path") but names neither a scanned root nor a detection means, and no artifact decides either. TR-010, the requirement it resembles, obliges only that a root be stated, and that root's extent is itself the open question in testing.md CHK004 -->

## Provenance Disclosure

- [X] CHK035 Do the datasheet's required disclosures cover the recognised provenance categories — motivation, composition, generation process, preprocessing, uses, distribution, maintenance — or is each omitted category justified? [Complete (set), Spec §TR-018 / data-model §RosterDatasheet] <!-- Evaluator: Resolved — added data-model.md §Datasheet Provenance Coverage, mapping each research.md category to the required section that carries it (motivation, composition, generation process, uses) and justifying the three omissions: preprocessing does not exist for a hand-authored fixture, distribution is in-repo only, and maintenance is fixed by the freeze-and-regeneration policy plus the reader-computed hash. The five-section count is unchanged, so TR-018, VR-010, and SC-012 are untouched -->
- [X] CHK036 Is the datasheet-completeness rule stated so that section presence and adequacy are determinable by a check rather than by reader judgement? [Able to be validated, data-model §VR-010] <!-- Evaluator: Resolved — VR-010 listed five disclosures with no way to check them ("with source", "with rationale" are judgements); it now fixes presence as a level-2 heading match on the declared attribute names (`Population Sizes`, `Naming Convention`, `Identifier Scheme`, `Synthetic Status`, `Out of Scope`) and adequacy as stated sub-conditions — both counts and a cited source path, a cited convention file plus the Stated limit, both identifier prefixes, the literal token `SYNTHETIC`, and non-empty out-of-scope content -->
- [X] CHK037 Is the optional status of the hash-method disclosure consistent with the reproducibility claim, which depends on that disclosure being present? [Consistent, data-model §RosterDatasheet] <!-- Evaluator: Resolved — `hash_method` was OPTIONAL while its stated purpose was "so a reader can reproduce the digest", which read as a contradiction. Resolved by scoping the claim rather than by adding a sixth required section (which would have contradicted the five-disclosure count in TR-018, VR-010, SC-012, OBJ6 VC5, and plan.md): CS-1…CS-6 are normative and complete on their own, so the section is an optional non-normative summary whose absence cannot make the digest irreproducible -->
- [X] CHK038 Is the prohibition on a literal digest inside the datasheet stated as a rule with its rationale, so the rejected hand-maintained marker cannot return by way of documentation? [Necessary, data-model §RosterDatasheet] <!-- Evaluator: Resolved — the prohibition and its rationale existed only as an entity-attribute aside; added VR-016, which asserts the absence as a checked rule (no `sha256:` value and no hexadecimal digest in any section) for the same reason VR-011 asserts the absent version field: a committed digest goes stale and reintroduces the hand-maintained marker by way of documentation -->
- [X] CHK039 Is the synthetic declaration required as a stated literal token in a stated location, so the project's data-provenance rule is satisfied by a determinable condition? [Compliance, data-model §RosterDatasheet / §VR-010] <!-- Evaluator: Covered by data-model.md §Entities RosterDatasheet (`synthetic_status: section` REQUIRED — literal token `SYNTHETIC`) and §Physical Artifacts, which fixes the containing file at `data/roster/roster-datasheet.md`; the location became heading-level determinable through the VR-010 amendment made for CHK036 (Synthetic Status section containing the literal token). Satisfies project-instructions.md Data Provenance, whose roster-applicable rule is the datasheet obligation -->
- [X] CHK040 Is the disclosed limit — that pattern conformance and exclusion-list non-membership together do not establish a name as invented — recorded where a consumer of the roster will encounter it? [Credibility, data-model §Stated limit / Spec §Objective 6 Rationale] <!-- Evaluator: Resolved — §Stated limit asserted it was "disclosed in the datasheet" but no required section carried it; the RosterDatasheet `naming_convention` attribute now requires the Stated limit, and VR-010's adequacy sub-conditions check for it, so the limit reaches the document a roster consumer actually reads rather than living only in this design note -->

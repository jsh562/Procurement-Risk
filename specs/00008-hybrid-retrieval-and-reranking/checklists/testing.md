# Testing Requirements Quality: Hybrid Retrieval and Reranking
**Created**: 2026-07-29 | **Feature**: [spec.md](../spec.md)

## The Fusion Oracle as a Substituted Oracle

- [X] CHK001 Does the plan state that the Python recomputation is derived from the published fusion definition rather than transcribed from the SQL statement it checks, given a Python side written by reading the SQL reproduces that statement's misreadings and the agreement it then asserts is vacuous? [Completeness, Plan §Testing Strategy; research-quality.md §Test-strategy] <!-- Evaluator: Resolved — soundness condition 1 in plan §Testing Strategy names the sources the Python side is written from (spec §Glossary formula, sad.md's constant 60, FR-004's tie-break and missing-arm convention) and forbids reading retrieval/fusion.py -->
- [X] CHK002 Is the independence the substitution rests on stated as an assumption and qualified, given it is the assumption that turns "the oracle and the SQL agree" into a claim about correctness rather than about consistency, and given research-quality.md records it as empirically falsified by coincident failures across independently written implementations? [Completeness, Plan §Testing Strategy; research-quality.md §Test-strategy] <!-- Evaluator: Resolved — the section now names the technique a pseudo-oracle, states the independence assumption as empirically falsified (27 versions, coincident failures), and draws the consequence: agreement is a consistency claim, correctness only under a false assumption; independence of derivation is necessary and not sufficient -->
- [X] CHK003 Is independence of derivation stated as a constraint on how the oracle is authored — from which document, and separately from the fusion statement — or is it left to whoever implements both to observe or not? [Clarity, Plan §Testing Strategy] <!-- Evaluator: Resolved — stated as an authoring constraint, not advice: written from the published definition, never from the SQL. Residual disclosed in the same section: one author writes both sides, so an independent second reader is named as the production-scale alternative rather than claimed -->
- [X] CHK004 Does the plan state that the oracle's inputs are generated rank vectors rather than the per-arm ranks read back from the same query under test, given a read-back input makes the equality assertion an identity that no defect can falsify? [Testability, Plan §Testing Strategy; research-quality.md §Test-strategy] <!-- Evaluator: Resolved — soundness condition 2: Hypothesis-generated per-arm rank vectors supplied to both sides, with read-back inputs named as the identity no defect can falsify. The old wording "from the same per-arm rank vectors" was itself the ambiguity and is gone -->
- [X] CHK005 Are the generated input domains stated — vector lengths, tie multiplicity, and candidates present in only one arm — so that FR-004's missing-arm convention and tie-break key are reached by generated cases rather than assumed exercised? [Testability, Spec §FR-004; Plan §Testing Strategy] <!-- Evaluator: Resolved — domain stated in condition 2: lengths 0 to the fetch depth inclusive of both endpoints, tie multiplicity from all-distinct to all-tied including ties straddling the last in-window position, candidate sets from disjoint to identical -->
- [X] CHK006 Does the plan name the source the oracle reads its three ranking parameters from — the constant fixed at 60, the tie-break key, and the missing-arm convention — given FR-004 requires all three fixed before measurement and an oracle carrying its own copy is a second place they can drift? [Traceability, Spec §FR-004; Plan §Instructions Check "VI. Evaluate Before You Tune", §Requirement Coverage Map FR-004] <!-- Evaluator: Resolved — condition 1 splits the two: the arithmetic is derived independently, the three parameters are READ from retrieval/parameters.py, the object FR-029 emits, and never re-declared as literals in the test. Coverage map FR-004 row carries the same -->
- [X] CHK007 Is the oracle substitution recorded in the mandated limitation format — scope decision, supporting evidence, reversal trigger, production-scale alternative — or as a testing note, given the determinism narrowing three paragraphs later in the same section is recorded in that format and this one is not? [Consistency, project-instructions.md §VII; Plan §Testing Strategy] <!-- Evaluator: Resolved — recorded in all four parts. Reversal trigger: the arithmetic becoming reachable as a pure function without a second statement. Production-scale alternative: a second author's independent oracle plus metamorphic relations needing no second implementation -->
- [X] CHK008 Does the plan state what follows from the oracle and the SQL disagreeing — which side is presumed wrong — given a failing equality assertion locates a discrepancy but not its origin? [Clarity, Plan §Testing Strategy] <!-- Evaluator: Resolved — soundness condition 4: neither side is presumed correct, both are re-derived against the published definition, and where the definition does not decide the case the defect is in the definition and is raised as an amendment. The shared-misreading fault is checked first -->

<!-- Evaluator note (CHK001-CHK008): the four soundness conditions research-quality.md §Test-strategy prescribes are now numbered 1-4 in plan §Testing Strategy, and the limitation format carries the residual the conditions cannot close. -->



## What the Oracle Does Not Cover

- [X] CHK009 Does the plan name candidate-set selection at the 50-row cut as outside the oracle's reach, given the oracle recomputes arithmetic over rank vectors it is handed and never chooses which 50 rows each arm yields? [Completeness, Spec §FR-003; Plan §Testing Strategy] <!-- Evaluator: Resolved — row 1 of the new "What the oracle does not cover" table; assigned to an integration fixture asserting each arm yields the fetch depth and that the candidate SET, not only its order, is stable across runs, a planner flip and an exact-path rebuild -->
- [X] CHK010 Is the per-arm tie-break named as uncovered by the oracle, given AD-001 and HINT-001 make it the mechanism that fixes which rows the reranker sees, and a rank-vector recomputation cannot observe the rows that were cut? [Completeness, Plan §AD-001, §HINT-001, §Testing Strategy] <!-- Evaluator: Resolved — row 2 of the same table, covered by a fixture engineered to tie at the last in-window position of each arm; the note records that the test fails both with no per-arm key and with the key in the final ordering only -->
- [X] CHK011 Is limit semantics under CTE inlining named as uncovered and assigned a check, given research-implementation.md records that nothing documents an inlined CTE honouring its `LIMIT` and prescribes a plan-shape assertion, while the Integration tier lists golden orderings, determinism, filtered-recall counts and per-arm independence and no plan-shape check? [Completeness, Plan §Testing Strategy; research-implementation.md §Reciprocal rank fusion as one statement] <!-- Evaluator: Resolved — row 3 of the table plus a new Integration tier entry: an EXPLAIN plan-shape assertion that each arm's LIMIT survives inlining, verified by test_fusion_plan_shape.py in the coverage map -->
- [X] CHK012 Is a verification stated for FR-002's one-statement requirement itself — that the emitted ranking was produced by a single statement — or do the named tiers assert only over that statement's output? [Verifiable, Spec §FR-002, §SC-004; Plan §Testing Strategy] <!-- Evaluator: Resolved — "FR-002 itself is verified, not only its output": the statements executed on the connection during one search are captured and asserted to be exactly one ranking statement with no accompanying SET, since AD-002 puts the breadth on the connection. test_single_statement.py in the coverage map -->
- [X] CHK013 Is a verifier named for SC-004's "zero ranked results produced by arithmetic outside the deterministic computation boundary", given AD-005 places reranker score-sorting outside that boundary by decision and the architecture contract appears only in the lint and static-analysis row? [Traceability, Spec §SC-004; Plan §AD-005, §Testing Strategy] <!-- Evaluator: Resolved — SC-004 is discharged by the statement capture together with the import-linter forbidden contract extended to name api.retrieval, with AD-005's score-sorting carve-out stated as the one named exception so the contract is not weakened later to accommodate it -->



## The Narrowed Determinism Guarantee

- [X] CHK014 Is the narrowing of FR-020 to the exact path recorded with all four elements the limitation format requires, and is it declared normative over the requirement under {SAD:ADR-0017}, the record that exists to let a plan-phase artifact override a specify-phase requirement? [Consistency, project-instructions.md §VII; Spec §FR-020; Plan §Testing Strategy] <!-- Evaluator: Resolved — the four elements were already present; "it invokes {SAD:ADR-0017}" was weaker than AD-002's own wording for the same move and now reads "declared normative over FR-020 under {SAD:ADR-0017}" -->
- [X] CHK015 Does FR-020 or SC-012 carry the qualification on the spec side, or does the unqualified requirement text stand while only the plan narrows it — given a reader of the spec alone would read a guarantee the plan does not assert? [Consistency, Spec §FR-020, §SC-012; Plan §Testing Strategy] <!-- Evaluator: Resolved — FR-020 amended in place (dated) to carry the narrowing, cite the plan's limitation record and {SAD:ADR-0017}, and state the unnarrowed half explicitly: two runs against the same built index remain identical on every arm -->
- [X] CHK016 Does the reversal trigger name an observation someone is obliged to make — a seeded, single-worker build — and state who checks it and at what point, or is it a condition recorded with no owner? [Verifiable, Plan §Testing Strategy "Reversal trigger"] <!-- Evaluator: Resolved — the trigger is attached to an occasion that already exists: FR-039's pgvector version check, before any figure is published and again whenever the pinned digest or extension version moves, by whoever performs that check, with the answer recorded beside FR-029's index settings -->
- [X] CHK017 Is the approximate path's substitute assertion reduced to a decision rule — how much candidate overlap, and what recall delta counts as a pass — given "asserted by candidate overlap and recall delta" names two quantities and no threshold? [Measurable, Spec §FR-020; Plan §Testing Strategy, §Requirement Coverage Map FR-020] <!-- Evaluator: NOT RESOLVED — two thresholds nobody has chosen: the minimum candidate overlap between rebuilds that counts as a pass, and the maximum recall delta. Recorded in the limitation's scope-decision bullet as reportable but not adjudicable, in the same shape as SC-016's latency half. Needs your decision -->
      > **Closed at checklist:** Open by design — a threshold set before any measurement would pass everything or fail a working index. Recorded in plan §Checklist Outcome.
- [X] CHK018 Is SC-012's "identical results across two runs" scoped consistently with the FR-020 narrowing across all six arms, given the approximate arm is one of the six and its ordering is stated not to survive a rebuild? [Consistency, Spec §SC-012, §FR-020; Plan §Testing Strategy] <!-- Evaluator: Resolved — SC-012 amended: "unchanged corpus AND an unrebuilt index". A rebuild leaves the corpus unchanged while changing the approximate arm's graph, so "unchanged corpus" alone did not scope it; the approximate arm's two runs are against the same built index, the five exact-path arms may straddle a rebuild -->
- [X] CHK019 Is "planner settings flipped" specified concretely enough to be repeated — which settings, to which values — given a flip that does not change the chosen plan asserts nothing while appearing to assert something? [Measurable, Plan §Testing Strategy] <!-- Evaluator: Resolved — seven settings named (enable_seqscan, enable_indexscan, enable_indexonlyscan, enable_bitmapscan, enable_sort, enable_hashjoin, enable_mergejoin) set off via SET LOCAL in the test's own transaction, and the test must assert the EXPLAIN plan actually differed while the ordering did not; an unchanged plan fails rather than passes -->



## Pure Functions With No SQL Obstacle

- [X] CHK020 Is `metrics.py`'s test-first obligation carried by a requirement or success criterion of this feature, or does it appear only in the plan's Instructions Check row and the Unit tier cell, given the project mandates red-green-refactor for scoring functions and nothing in the spec names it? [Traceability, project-instructions.md §Testing & Quality Policy; Plan §Instructions Check "Testing & Quality Policy", §Testing Strategy] <!-- Evaluator: Resolved — new spec FR-042 carries the red-green-refactor and property-test obligation for the three functions; the plan's Instructions Check row and Unit tier cell now defer to it rather than being the only record. A policy obligation living in a plan table cell has no verifier -->
- [X] CHK021 Are the properties those functions must satisfy named — interval containment and monotonicity for Wilson, ordering and endpoint behaviour for the percentile bootstrap, symmetry for the overlap verdict — or does "property tests directly" name a tier without naming a property? [Testability, Spec §FR-030, §FR-031, §FR-032; Plan §Testing Strategy] <!-- Evaluator: Resolved — FR-042 names them: Wilson (contains the point estimate, bounds in [0,1], defined at 0 and all successes, width non-increasing in set size at fixed proportion); bootstrap (ordered bounds, bounds within the resampled value range, zero-width on a constant set, reproducible from the recorded seed); overlap verdict (symmetry, reflexivity, agreement with FR-032's closed-interval rule) -->
- [X] CHK022 Is FR-031's prohibition on a Wilson interval over mean reciprocal rank carried by a stated observation that would detect its reintroduction, given a prohibition is satisfied by every implementation that simply never calls the function? [Verifiable, Spec §FR-031, §SC-002; Plan §Requirement Coverage Map FR-030–FR-032] <!-- Evaluator: Resolved — FR-031 amended: every emitted interval records the method that produced it (wilson, or percentile_bootstrap with resample count and seed), and no non-proportion statistic may carry an interval recorded as wilson. Asserted over the emitted artifact, the shape FR-006 already uses -->
- [X] CHK023 Does FR-032's "not resolvable at this set size" have a stated decision rule for what counts as overlap between two intervals, so two adjudications of the same pair of figures agree? [Measurable, Spec §FR-032, §SC-008] <!-- Evaluator: Resolved — FR-032 amended: closed intervals, overlap iff lower_A <= upper_B and lower_B <= upper_A, so touching endpoints count as overlapping and the verdict is unresolvable. The inclusive reading is derived from Principles II and III rather than picked, and the same-statistic/same-method/same-set precondition is stated. The conservatism of the test is labelled -->
- [X] CHK024 Is FR-036's "strongest single arm" reduced to a selection rule and a statistic, given the choice determines what the reranker is measured against and SC-008 cannot be adjudicated while the comparator is chosen after the figures are seen? [Measurable, Spec §FR-036, §SC-008; Plan §Requirement Coverage Map FR-036] <!-- Evaluator: Resolved — FR-036 amended: candidates are the two single arms (lexical-only, dense-only); per reported statistic the comparator is the candidate with the higher point estimate of that statistic; where their intervals overlap, both are reported under FR-032's verdict. The RULE is fixed in advance, the arm is a result. Flagged in the report as an interpretive amendment you may override -->
- [X] CHK025 Are the input domains for the metrics properties stated — query-set sizes including one and zero, all-hit and all-miss sets, ties in reciprocal rank — so a property is not reported falsified on an input the harness cannot produce? [Testability, Spec §FR-030, §FR-032; Plan §Testing Strategy] <!-- Evaluator: Resolved — FR-042 fixes the domains: set sizes from one upward, all-hit and all-miss sets, ties in reciprocal rank, values at both ends of their range. Zero is settled by rule rather than by domain: an empty query set is refused rather than reported, since a proportion over zero queries is undefined (Principle III) -->



## The Frozen Evaluation Set

- [X] CHK026 Does AD-010 state a freeze discipline beyond the digest check — how many times the set may be consulted, and by whom — given a hash detects modification of the set but not repeated measurement against it, which is what converts a frozen set into a training set? [Completeness, project-instructions.md §VI; Plan §AD-010, §Project Structure] <!-- Evaluator: NOT RESOLVED — the gap is now stated in plan §AD-010's new note (a hash sees modification, not repeated measurement), but a consultation budget is a number and an entitlement nobody has chosen. Needs your decision: how many measurement runs against the set before a figure taken from it stops being evidence, and who may run them -->
      > **Closed at checklist:** Decided at checklist: unlimited measurement, tuning after measurement recorded and re-measured with both figures published.
- [X] CHK027 Is the composition of E008's own query set specified — its size, how queries are drawn, and which differences it must be able to resolve — given the spec's own risk records that fifty queries cannot separate arms differing by a few points and SC-001 and SC-002 are measured on this set? [Completeness, Spec §SC-001, §SC-002, §Risks; Plan §AD-010] <!-- Evaluator: NOT RESOLVED — recorded as an open decision in plan §AD-010's note, with the reason it cannot be defaulted: a size chosen without stating the difference it must resolve reproduces the spec's own too-small-set risk silently. Needs your decision: size, draw method, and the difference the set must resolve -->
      > **Closed at checklist:** Sizing owned jointly with E014, which owns the frozen set. Recorded in plan §Checklist Outcome.
- [X] CHK028 Is the source of the relevance judgements stated, given SC-001's recall at five needs a per-query relevant-passage set and no requirement names who produces it or against what evidence? [Completeness, Spec §SC-001, §SC-002; Plan §AD-010] <!-- Evaluator: NOT RESOLVED — recorded in plan §AD-010's note. AD-009's precedent (the generator's pre-render document model) is the obvious candidate and is deliberately not adopted by default: judgements derived from the generator make every query answerable by construction and may measure an easier task than a coordinator's. Needs your decision -->
      > **Closed at checklist:** Decided at checklist: generator-derived judgements, published as a ceiling rather than an estimate, in the four-part limitation format.
- [X] CHK029 Is the relationship between this epic's set and E014's frozen set stated — whether a figure measured here is superseded, re-measured, or reported alongside — given the spec makes every criterion evaluable at this gate and published by E014? [Consistency, Spec §Success Criteria preamble, §SC-001; Plan §AD-010] <!-- Evaluator: Resolved — plan §AD-010's note settles it: two sets, two figures. E008's is a gate figure reported with the set and digest it was taken on and never published as the epic's result; E014 re-measures on its frozen set and its figure is the published one; neither is offered as confirmation of the other -->
- [X] CHK030 Is the harness's abort-on-mismatch behaviour stated as a checkable obligation with the observation that shows it firing, or only as a description of the deliverable? [Verifiable, project-instructions.md §VI; Plan §AD-010, §Project Structure] <!-- Evaluator: Resolved — the observation is stated: a test perturbs a copy of the committed set, runs the harness, and asserts a non-zero exit BEFORE any measurement is emitted (test_evaluation_set.py, now a row in the coverage map declared derived from Principle VI) -->



## Assertions Stated, Not Merely Test Existence

- [X] CHK031 Does FR-024 state the assertions the degraded-path test must make beyond the flag being set and results returning — whether the response body's fusion-only statement, the readiness endpoint's ready-degraded value, and the evaluation-facing mode record are each asserted — given FR-021, FR-022 and FR-023 declare three separate observables? [Completeness, Spec §FR-024, §FR-021, §FR-022, §FR-023, §SC-011] <!-- Evaluator: Resolved — FR-024 amended to enumerate (a) ready-degraded at readiness, (b) the fusion-only statement in a body that still carries results, (c) the mode on the evaluation-facing output, with "a test asserting one boolean proves one of three" stated. SC-011 amended to match -->
- [X] CHK032 Is the mechanism by which the test forces load failure stated, given a test that sets the flag directly proves the flag and not the fallback, and HINT-005 makes catching the failure inside the lifespan hook the behaviour actually under test? [Testability, Spec §FR-024; Plan §HINT-005, §Requirement Coverage Map FR-024] <!-- Evaluator: Resolved — FR-024 fixes the mechanism at the artifact-loading boundary (absent, unreadable or digest-mismatched graph), so the exception arises where FR-016's verification runs and is caught in the startup path, and explicitly forbids forcing it by setting the flag or injecting an already-degraded component -->
- [X] CHK033 Does FR-026's "asserted by a test rather than left to review" enumerate what the test compares across the two flag settings, given SC-013's "zero behavioural differences other than vector-search strategy" is a claim over an observable set nothing names? [Measurable, Spec §FR-026, §SC-013; Plan §AD-006] <!-- Evaluator: Resolved — FR-026 amended to enumerate the observable set (ranking parameters, route firing and contribution, reranker arm and precision, degraded flag, provenance fields and match kind on shared results, filtered-count behaviour) and to name the ONE permitted difference: the vector-search strategy and the dense candidate set downstream of it. SC-013 now ranges over that set -->
- [X] CHK034 Is SC-013's comparison constructible as specified, given AD-006 and the Contract Decisions make the flag service configuration so the two settings exist only in two differently configured processes — and is that two-process comparison named as the verification means? [Testability, Spec §SC-013; Plan §AD-006, §Contract Decisions Confirmed at Plan] <!-- Evaluator: Resolved — FR-026 and SC-013 both name the two-process construction, and the coverage map's FR-026 row names test_flag_parity.py building two differently configured application instances. The plan had recorded the two-process cost for SC-012's sixth arm only; SC-013 needs the same shape and now says so -->
- [X] CHK035 Does SC-007's "the first query after readiness incurs no load cost" state the observation that decides it — a session-creation count, a load event, or a latency margin — given a timing comparison on one shared vCPU has no stated threshold? [Measurable, Spec §SC-007, §FR-017] <!-- Evaluator: Covered by spec §SC-007 as amended 2026-07-29 — decided structurally by three counters (artifact reads, session creations, warm-up invocations) that must read zero on any request path after readiness, with the latency-difference rule explicitly rejected and the reason given -->
- [X] CHK036 Is the verification of FR-010's declared pattern against the enumerated part-number set stated together with the enumeration's source, given AD-009 routes it to the generator's pre-render document model rather than to either table and SC-005 requires 100% coverage over it? [Traceability, Spec §FR-010, §SC-005; Plan §AD-009] <!-- Evaluator: Resolved — FR-010 and SC-005 both now name the source (the generator's pre-render document model, E006's FR-067 reference set, per AD-009) and state why neither table can supply it; the coverage map's FR-010 row carries SC-005 and names test_part_number_coverage.py reading that source. Verification and enumeration source are stated together, since either alone is unrunnable -->



## Coverage, Gate Reachability, and Traceability

- [X] CHK037 Does the coverage obligation name its population — 80% of which modules, measured by which tool over which paths — or only the number, given the plan's Coverage row names `api.retrieval` and `gateway.inference` while the project states a repository-wide target? [Clarity, project-instructions.md §Coverage Target; Plan §Testing Strategy "Coverage"] <!-- Evaluator: Resolved — the Coverage row now states both populations and reconciles them: the project-wide floor over the combined data (coverage combine, branch coverage, the root manifest's source list, which already holds src/api/src/api and src/gateway/src/gateway), PLUS each new package asserted alone at 80% via --include, which is E006's per-package precedent and the arithmetic reason an aggregate is insufficient. The 80% itself is the project's number and was not invented here -->
- [X] CHK038 Is it stated which of this feature's checks execute in the merge gate today and which need a new step, given every tier is marked "configured" while the feature adds a new test package under `/src/api` and a new gateway test module? [Traceability, project-instructions.md §CI Requirements; Plan §Testing Strategy, §Project Structure] <!-- Evaluator: Resolved — new "What runs in the merge gate today, and what needs a new step" note, read against verify.yml: unit/integration/gateway/lint/format/import-linter/tests-checks run automatically; four things need a workflow edit — the two per-package coverage lines, the benchmark module (the api step runs -m "not benchmark" and the benchmark step names one file explicitly), and a seeded chunk fixture, because the gate migrates but never ingests, so the chunk table is empty -->
- [X] CHK039 Does every requirement in the coverage map reach a named verification and not only a named component, given FR-036's own recorded history is a requirement mapped to a module that computes nothing and so was traceable to a component that could not discharge it? [Traceability, Plan §Requirement Coverage Map; research-quality.md §Traceability] <!-- Evaluator: Resolved — every row now carries a "verification:" entry naming a test module (or, for FR-034/FR-035, the amending revision on the default branch). The derived evaluation-set harness is listed as its own row and declared derived, closing the backward direction research-quality.md §Traceability asks for -->
- [X] CHK040 Do the four blocking amendments have a named verifier and a stated gate that this epic can check at its own boundary, so SC-015 is decidable here rather than asserted about another branch? [Verifiable, Spec §SC-015, §FR-034, §FR-035; Plan §Pending Amendments] <!-- Evaluator: Resolved — SC-015 amended to gate all four blocking amendments rather than two of four (the plan already extended the gate to items 3 and 4 without the criterion following), and to name the verifier: the amending revision on the default branch, cited in the task that closes each, checked by reading that branch at this epic's own gate -->

## Evaluator Summary — 2026-07-29

36 of 40 items closed — 35 by amendment to `spec.md` or `plan.md`, and CHK035 already covered by the
Performance evaluation's SC-007 amendment. **Four remain open and each needs a
decision that cannot be taken from the artifacts**: CHK017 (the candidate-overlap and recall-delta
thresholds for the approximate path's substitute determinism assertion), and CHK026, CHK027, CHK028
(AD-010's consultation budget, the evaluation set's composition and size, and the source of the
relevance judgements). All four are recorded in `plan.md` — the first in the determinism limitation's
scope-decision bullet, the other three in the new AD-010 note — so each is a stated gap rather than a
silence.

# Testing: Monorepo Scaffold and Contracts
**Created**: 2026-07-25 | **Feature**: [spec.md](../spec.md)

## Enforcement Contract Verifiability

- [X] CHK001 Is the failure signal required of every contract — non-zero exit, the named rule, and the offending source location — stated once as a general obligation rather than restated per contract in wording that could diverge? [Consistent, Spec §TR-019] <!-- Evaluator: Covered by spec.md §Requirements TR-019 — single general obligation ("Every contract and assertion MUST exit non-zero on violation, naming the specific rule that failed and, where the violation is attributable to a source location, the offending module or file"); TR-008/TR-009/TR-010/TR-012/TR-013 state the rule only and do not restate the signal -->
- [X] CHK002 Does each enforcement requirement name its detection mechanism — allowlist contract, forbidden contract with indirect detection, source scan, lock-derived allowlist, in-image denylist — rather than naming only the rule to be enforced? [Verifiable, Spec §TR-008, §TR-009, §TR-010, §TR-012, §TR-013] <!-- Evaluator: Resolved — TR-009 (indirect detection), TR-010 (source-level check) already named theirs; TR-008 now names the allowlist contract, TR-012 the allowlist check with its lock derivation, TR-013 the in-image denylist -->
- [X] CHK003 Does the single-import requirement state that its scope is direct imports, so a passing result cannot be read as a stronger guarantee than the mechanism supports? [Unambiguous, Spec §TR-008 / §Objective 3 Rationale] <!-- Evaluator: Covered by spec.md §TR-008 ("directly imports"), §Objective 3 statement ("may **directly** import") and Rationale ("checking direct imports only"), §SC-005 ("the claim covers direct imports") -->
- [X] CHK004 Is the source scan's scanned root specified as an enumerable path set, or does the requirement oblige only that some root be stated? [Verifiable, Spec §TR-010] <!-- Evaluator: ASK — see returned question Q1; TR-010 obliges only that a root be stated plus two exclusions, and no artifact fixes the root's extent -->
- [X] CHK005 Does the requirement set state what prevents a contract from passing vacuously, given both reserved packages are empty in this epic and a clean tree offers nothing to analyse? [Verifiable, Spec §TR-009 / §Objective 3 VC1] <!-- Evaluator: Covered by spec.md §TR-009 final sentence (packages empty this epic; later epics MUST populate them, "which is what makes the contract non-vacuous") and §TR-007 / §SC-004, whose committed negative cases prove failure in this epic -->
- [X] CHK006 Is the derivation of the lock-derived expected set specified precisely enough — platform and marker resolution, distribution-name normalization — that two independent implementations would produce the same set? [Unambiguous, Spec §TR-012] <!-- Evaluator: Resolved — added the derivation rule to spec.md §TR-012 (dependency groups the image installs, target-platform marker resolution rather than host, PEP 503 name normalization on both sides), promoting plan.md HINT-002 to requirement level -->
- [X] CHK007 Does the in-image check requirement state how declared distribution names map to importable module names, given the two name spaces differ? [Complete, Spec §TR-013] <!-- Evaluator: Resolved — spec.md §TR-013 now bridges the name spaces: attempted module names derive from the modeling boundary's installed distribution metadata (top-level modules per declared distribution), not assumed equal to distribution names; research.md flagged this split as a blind spot -->


## Stated Limits and Exception Completeness

- [X] CHK008 Does the spec state which mechanism covers re-export laundering and which does not, so the division of responsibility between the import contract and the source scan is determinable rather than inferred? [Unambiguous, Spec §Objective 3 VC4 / §Edge Cases] <!-- Evaluator: Covered by spec.md §Edge Cases bullet 1 ("the import contract passes and the source scan is the check that catches it"), §Objective 3 VC4, and §Glossary "Re-export laundering" -->
- [X] CHK009 Is the dynamic or computed import case recorded as an exception with no mechanism claimed for it, rather than absent from the requirement set? [Complete, Spec §Edge Cases / §SC-005] <!-- Evaluator: Covered by spec.md §Edge Cases bullet 2 ("stated as a known limit rather than claimed as covered"), §SC-005 ("dynamic imports stated as out of scope"), and §Risks "Contracts appear stronger than they are" -->
- [X] CHK010 Is the inline-computation blind spot stated together with the condition that closes it, so a reviewer can determine whether it is permanent or deferred to a named later epic? [Complete, Spec §STF-003 / §TR-009] <!-- Evaluator: Covered by spec.md §Edge Cases bullet 5 ("a known limit until later epics place all such logic in the computation package") and §TR-009's MUST binding later epics; the closure condition is stated, so the limit reads as deferred rather than permanent -->
- [X] CHK011 Does the allowlist-check requirement state that source carrying no distribution metadata is invisible to a metadata query, so absence of a finding is not read as absence of the package? [Complete, Spec §Edge Cases / §TR-012] <!-- Evaluator: Resolved — the limit stated in §Edge Cases ("vendored or copied source carries no distribution metadata") is now carried at requirement level in spec.md §TR-012 ("an absent finding is not evidence that a package is absent") -->
- [X] CHK012 Does the build-context requirement state what its scoping does not constrain — installing the same distributions from a package index — rather than presenting the scoping as sufficient? [Correct, Spec §TR-011 / §Objective 4 Rationale] <!-- Evaluator: Resolved — spec.md §TR-011 now carries the non-coverage already stated in §Objective 4 Rationale and §Edge Cases: local source reachability only, not package-index installation -->
- [X] CHK013 Is the in-image check stated as a denylist with its blindness to any distribution outside its derived list disclosed at requirement level? [Complete, Spec §TR-013 / §Edge Cases] <!-- Evaluator: Resolved — spec.md §TR-013 now states "The check is a denylist: it is blind to any modeling distribution outside its derived list", promoting the disclosure from §Edge Cases and §Objective 4 Rationale -->
- [X] CHK014 Is the epic-level limit that the check set gates nothing automatically stated alongside the enforcement claims themselves, so a reviewer does not read a passing check set as a merge gate? [Complete, Spec §Objective 7 / §Excluded] <!-- Evaluator: Covered by spec.md §Objective 7 Why this priority ("gates nothing automatically... a convenience rather than the enforcement mechanism"), §Excluded, §Assumptions, §Risks, §IP-002, §IP-007 ("gates only when invoked"), §Compliance Check, and §Glossary "Dispatch-only" -->


## Negative-Case Fixture Requirements

- [X] CHK015 Does "each contract" in the fixture requirement resolve to a countable list of contracts and checks, so a reviewer can determine whether the fixture set is complete? [Verifiable, Spec §TR-007 / §SC-004] <!-- Evaluator: Resolved — spec.md §TR-007 now enumerates the five mechanisms requiring a negative case (TR-008, TR-009, TR-010, TR-012, TR-013), each tied to its stated criterion in OBJ3 VC2–VC4 and OBJ4 VC3–VC4 -->
- [X] CHK016 Is the fixture residence rule stated — outside every production contract's scanned root — so a committed fixture cannot trip the production contract it exercises? [Complete, Spec §TR-007] <!-- Evaluator: Covered by spec.md §TR-007 ("Fixtures MUST live outside every production contract's scanned root") and §Clarifications Q3 -->
- [X] CHK017 Does the fixture requirement record why a fixture-specific configuration is required instead of an exclusion inside a production configuration, so the constraint survives a later simplification? [Necessary, Spec §TR-007 / §Clarifications] <!-- Evaluator: Covered by spec.md §TR-007 ("so that no production configuration carries an exclusion that could drift"), §Clarifications Q3, and plan.md AD-004 -->
- [X] CHK018 Is a fixture's pass condition stated as a specific outcome — a non-zero exit from the named contract — rather than as an unqualified "fails"? [Verifiable, Spec §TR-007 / §Objective 2 VC4] <!-- Evaluator: Covered by spec.md §TR-007 ("a fixture-specific contract configuration asserting a non-zero exit") and §TR-019 (non-zero exit naming the rule and location); plan.md HINT-004 narrows the in-image case to a specific ModuleNotFoundError -->
- [X] CHK019 Is the feasibility of a committed negative fixture addressed for the checks that operate on a built image, where the violation must be introduced into the image rather than as a source file? [Feasible, Spec §TR-007 / §TR-012, §TR-013] <!-- Evaluator: ASK — see returned question Q2; TR-007's fixture form ("a fixture-specific contract configuration") is import-linter-shaped and does not describe how a violation reaches a built image -->
- [X] CHK020 Are the negative cases for the import contract and for the source scan stated as separate obligations, given no single fixture exercises both mechanisms? [Singular, Spec §Objective 3 VC4 / §TR-010] <!-- Evaluator: Covered by spec.md §Objective 3 VC2 (provider import added outside the gateway → contracts fail) and VC4 (client read off the gateway → import contract passes, source scan reports), which are distinct criteria over distinct violations; §TR-007's enumeration now lists both mechanisms with their own cases -->
- [X] CHK021 Do the fixture obligations for the computation-boundary contract cover an indirect import path as well as a direct one, since indirect detection is that requirement's distinguishing property? [Complete, Spec §TR-009 / §SC-006] <!-- Evaluator: Resolved — spec.md §TR-007 now requires that contract's negative case to exercise an indirect import path through an intermediate module as well as a direct one, matching the claim SC-006 and OBJ3 VC3 already make -->
- [X] CHK022 Does the requirement set state whether a positive control accompanies each negative fixture, so a fixture failing for an unrelated reason is distinguishable from one failing for the rule under test? [Verifiable, Spec §TR-007] <!-- Evaluator: Resolved — spec.md §TR-007 now pairs every negative case with a positive control: the clean-tree runs of OBJ3 VC1 / OBJ4 VC2 for source-level mechanisms, and for the in-image check the plan.md HINT-004 control asserting a required distribution does import -->


## Coverage Denominator Definition

- [X] CHK023 Does the coverage requirement name its coverage-item set — the specific executable artifacts forming the denominator — and not only the target percentage? [Verifiable, Spec §TR-006] <!-- Evaluator: Covered by spec.md §TR-006, which names the item set — the source scan, the image checks, and the roster reader — before stating the target -->
- [X] CHK024 Is the denominator enumeration identical across the requirement, the objective deliverable, and the success criterion, including whether the image check is singular or plural? [Consistent, Spec §TR-006 / §Objective 2 Deliverables / §SC-003] <!-- Evaluator: Resolved — Objective 2's deliverable said "the image check" while TR-006 said "the image checks" and two exist (TR-012, TR-013); the deliverable is now plural and SC-003 carries the same enumeration -->
- [X] CHK025 Does the coverage requirement state whether test modules and their helper modules sit inside or outside the measured denominator, so the reported figure cannot be inflated by tests measuring themselves? [Unambiguous, Spec §TR-006] <!-- Evaluator: Resolved — spec.md §TR-006 now states the denominator holds the three artifacts' implementation modules only, with test modules and fixture code outside it; consistent with plan.md AD-002, which puts check logic in importable helpers so it is not tautologically covered -->
- [X] CHK026 Is the rule that per-entry runners report but do not gate stated with its reason — two entries having an empty denominator — rather than as an unexplained exception? [Complete, Spec §TR-006 / §Clarifications] <!-- Evaluator: Covered by spec.md §TR-006 ("MUST NOT gate independently, since two entries have an empty denominator") and §Clarifications Q6 -->
- [X] CHK027 Does the requirement identify the single aggregated report as the gating artifact clearly enough that a passing per-entry report cannot be mistaken for satisfaction? [Unambiguous, Spec §TR-006 / §SC-003] <!-- Evaluator: Covered by spec.md §TR-006 — "a single aggregated report... MUST meet the project coverage target on that combined denominator" against "Per-entry runners MAY report coverage but MUST NOT gate independently" -->


## Evidence Paths for Success Criteria

- [X] CHK028 Does every success criterion state an observation obtainable from a committed artifact or a recorded run, rather than a property asserted about the repository? [Able to be validated, Spec §Success Criteria] <!-- Evaluator: Covered by spec.md §Success Criteria — SC-001/SC-002 read committed manifests and lock verification runs, SC-003/SC-004 a test run, SC-005–SC-008 and SC-014/SC-015 contract, image, and image-definition inspections, SC-009/SC-010 an orchestration run, SC-011/SC-012 the committed roster and datasheet, SC-013 two recorded dispatched runs -->
- [X] CHK029 Does SC-013 name both dispatched runs, the branch each is dispatched against, and the prerequisite that the workflow already sits on the default branch? [Complete, Spec §SC-013 / §TR-021] <!-- Evaluator: Resolved — SC-013 already named both runs, the clean run's branch, and the landed-on-default-branch prerequisite; it now also names the violated run's branch as a function of the TR-022 path chosen (feature branch for the workflow-input path, throwaway branch for the pushed-branch path) -->
- [X] CHK030 Does the violated-dispatch evidence requirement give a criterion for choosing between its two options, or does it leave the selection open without stating that either is acceptable? [Unambiguous, Spec §TR-022] <!-- Evaluator: Covered by spec.md §TR-022 — the obligation is the stated property ("does not require committing a violation to a production contract root") and the two paths are offered as "either... or", so either satisfies the requirement -->
- [X] CHK031 Is the reason a committed violation cannot serve as violated-dispatch evidence traced to the fixture-residence requirement rather than asserted independently? [Traceability, Spec §TR-022 / §STF-004 / §TR-007] <!-- Evaluator: Covered by spec.md §TR-022 ("Without this, TR-007's design makes every committed violation a passing test") and §STF-004, both citing TR-007 by ID -->
- [X] CHK032 Are the deliberately narrow success criteria — direct imports, import edges, installed-set equality — each accompanied by the case they exclude, so none reads as an absolute guarantee? [Correct, Spec §SC-005, §SC-006, §SC-007] <!-- Evaluator: Resolved — SC-005 (dynamic imports) and SC-006 (inline computation) already carried their excluded case; SC-007 now names the legitimately re-locked modeling dependency as outside it and points at SC-008, per §Objective 4 Rationale and VC4 -->
- [X] CHK033 Do the source-level scan and the scoped build context each have a success criterion, or do those two mechanisms close on objective-level validation alone? [Complete, Spec §TR-010, §TR-011 / §Objective 3 VC4, §Objective 4 VC1] <!-- Evaluator: Resolved — added SC-014 [OBJ3] for the source-level scan (mirroring OBJ3 VC4) and SC-015 [OBJ4] for the scoped build context (mirroring OBJ4 VC1, with the scoping's stated limit carried through) -->
- [X] CHK034 Does SC-004 state what distinguishes an automatic proof from a manual demonstration, so a borderline case is classifiable without reviewer judgement? [Unambiguous, Spec §SC-004] <!-- Evaluator: Covered by spec.md §Objective 2 VC4, whose When clause fixes the classifier ("When the test suite runs, Then each contract is proven to fail... automatically rather than by manual demonstration"), together with §TR-007's committed fixture-based tests -->


## Requirement Set Consistency

- [X] CHK035 Does each of the seven recorded clarifications land in a numbered requirement, so a resolved question cannot be re-opened by a requirement that was never amended? [Consistent, Spec §Clarifications] <!-- Evaluator: Covered by spec.md §Clarifications — each answer closes with its landing requirement (TR-002, TR-009, TR-007, TR-016, TR-027, TR-006, TR-021) and each named requirement carries the answer's wording -->
- [X] CHK036 Does each of the five stress-test findings name the requirements its resolution changed, so a reviewer can check the amended wording rather than trust the Resolved label? [Traceability, Spec §Stress-Test Findings] <!-- Evaluator: Covered by spec.md §Stress-Test Findings — STF-001 names TR-013; STF-002 names TR-004, SC-001, OBJ1 VC2; STF-003 names SC-006, OBJ3 VC3, Edge Cases; STF-004 names TR-022 and the Assumption; STF-005 names OBJ6's deliverable and VC4 -->
- [X] CHK037 Is STF-002's resolution carried identically in TR-004, SC-001, and Objective 1 VC2, with the same third-party-declared-dependency qualification in each? [Consistent, Spec §STF-002 / §TR-004, §SC-001] <!-- Evaluator: Covered by spec.md §TR-004 ("any third-party distribution that the modeling boundary declares as a direct dependency"), §SC-001 (same qualification), §Objective 1 VC2 ("the modeling boundary's directly declared third-party dependencies"), each excluding first-party path dependencies -->
- [X] CHK038 Does the dispatch-only deviation appear with the same owner and the same scope in the Excluded section, IP-002, the assumption, the risk, and the compliance check? [Consistent, Spec §Compliance Check / §Excluded / §IP-002] <!-- Evaluator: Covered by spec.md §Excluded, §IP-002, §Assumptions, §Risks, and §Compliance Check — owner E002 and scope "automatic push and pull-request triggering" in every instance -->
- [X] CHK039 Are the mechanism-specific glossary terms — allowlist contract versus allowlist check, entry versus boundary — used in the requirements exactly as defined, so no requirement names a mechanism ambiguously? [Comprehensible, Spec §Glossary] <!-- Evaluator: Resolved — TR-008 and TR-012 now use the glossary terms "allowlist contract" and "allowlist check"; TR-009 said "each Python entry" (which includes the gateway) where plan.md AD-005 and the project structure reserve the packages in the api and model boundaries only, so it and the two glossary rows now read "each Python boundary" -->
- [X] CHK040 Is each enforcement requirement singular, or do any bundle a mechanism with its derivation rule or its residence such that partial satisfaction would read as compliance? [Singular, Spec §TR-013, §TR-016] <!-- Evaluator: Covered by spec.md §TR-013 and §TR-016 — the bundled clauses are each an explicit MUST or a named derivation rule that defines the mechanism's terms, and each has its own verification point (data-model.md VR-013 for TR-016's single reader; the derived name set for TR-013), so partial satisfaction fails a stated clause rather than reading as compliance; the stated limits added to both are disclosures, not separable obligations -->


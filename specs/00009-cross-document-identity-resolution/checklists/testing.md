# Checklist: Testing — Requirements Quality

**Feature**: E009 Cross-Document Identity Resolution | **Domain**: Testing | **Depth**: Standard | **Audience**: Reviewer (PR) | **Date**: 2026-07-29

> These items test whether the requirements and success criteria are stated so that a test could be written against them **and could fail**. They do not test implementation behavior. An item passes when the artifact is clear, complete, consistent and unambiguous on the point raised — not when the code works.

## Testability of the success criteria themselves

- [X] CHK001 Is every escape branch in a success criterion given a triggering condition a test can select deterministically, rather than a condition an implementation chooses? [Testability, SC-017, SC-018, SC-019] <!-- Evaluator: Covered — each escape is keyed to a count the test controls through the frozen set and the committed cutoffs: SC-017 on "no merge was made among labeled pairs", SC-018 on "the run auto-merged nothing", SC-019 on the three-way merges-among-labeled-pairs / false-merge counts. None is a condition the implementation elects. data-model.md `ck_resolution_figure__estimate_iff_denominator` makes the undefined branch a stored row rather than a report choice, and VR-014 drives the zero-denominator case end to end -->
- [X] CHK002 Can SC-013 ("all pairs withheld: run completes, zero merges, precision undefined") be distinguished in requirements from a run that generated no candidate pairs at all? If not, a system that blocks nothing passes it. [Unambiguity, SC-013, FR-024] <!-- Evaluator: Resolved — as written it could not. Every clause held vacuously over an empty candidate set, so a resolver that generated nothing passed. FR-024 already required a "populated review queue" and the criterion had dropped that word. SC-013 now requires at least one generated candidate pair, one review item per generated pair, and a populated queue, and points the block-nothing failure at SC-004 and the blocking figures where it belongs -->
- [X] CHK003 Does SC-034 ("a cluster containing several purchase-order lines is emitted without error") state a positive outcome that fails for an implementation producing no clusters? [Testability, SC-034, STF-006] <!-- Evaluator: Covered — SC-034 requires the cluster to be **emitted**, so a run producing no clusters fails it. This is STF-006's finding already closed: the fifth pass reduced SC-034 to the purchase-order clause after the original's first clause "passes any implementation including one producing no clusters at all". VR-019 asserts a three-line cluster is accepted alongside the second-specification refusal, "because a test of only the first would pass an implementation that bounded everything" -->
- [X] CHK004 Is SC-005's failure condition — "a pair present in a cluster with no recorded score is a failure" — stated over the *induced* pair set rather than the scored set, so transitive joining is detectable? [Completeness, SC-005, FR-018] <!-- Evaluator: Covered by SC-005 and FR-018, both written over "every pair induced by a resolved entity's membership", with "Induced pair set" defined in the Glossary as "every pair implied by a cluster's membership, **including pairs whose records were never directly compared**". data-model.md carries the positive half referentially (`fk_resolved_entity_induced_pair__merged_pair` pinning `decision='merge'`) and the completeness half as G-2/VR-010, which is named as "the one path by which FR-018 can be defeated" -->
- [X] CHK005 Does each success criterion name the requirement it grades, so a criterion amended without its requirement (or the reverse) is visible? [Traceability, Success Criteria] <!-- Evaluator: Resolved per owner decision — option A, a **Requirement ↔ Criterion Map** added to spec.md §Success Criteria as a subsection. The `SC-### [US#]` grammar is untouched, because it is fixed in `.github/skills/artifact-conventions/SKILL.md` and is not E009's to alter. The map runs all 50 requirements (FR-001…FR-049 including FR-037a) against the 43 criteria and **leaves the holes visible rather than inventing pairings**: five requirements have no criterion — FR-014 and FR-023 (both open STF-008 residue), FR-045 and FR-048 (structural, graded by VR-020/VR-028/VR-029 and VR-025), FR-049 (report content, graded by report-content tests and G-7) — FR-012 is graded only indirectly, and two criteria grade no requirement, SC-001 and SC-003, both already recorded as unbacked by STF-008 -->
- [X] CHK006 Are the criteria that certify a *refusal* (SC-015, SC-040, SC-043) specified with what must be absent as well as what must be reported, so a partial write cannot pass? [Completeness, SC-015, SC-040, SC-043, FR-035] <!-- Evaluator: Resolved — the absence half was narrow and the report half was missing. SC-015 certified three absences out of nine artifact classes and required no report, so a silent exit passed; it now requires zero rows per relation (as VR-015 counts) and names what the refusal must report. SC-040 and SC-043 said "publishes nothing" and now also name the divergent constants, per plan §Error Handling Strategy's non-zero exit -->
- [X] CHK007 Is SC-041's rejection outcome ("naming the offending record") specified precisely enough to assert on, as opposed to any error? [Unambiguity, SC-041, FR-041] <!-- Evaluator: Covered by data-model.md §`resolved_entity` and `resolved_entity_member` as extended: the rejection is a unique violation of `ix_rem__single_specification_section`, and "the row being inserted **is** the offending record FR-041 requires named — the application reports it from the failed statement rather than reconstructing it". §Conventions fixes the assertion target as the constraint name, "never on message text"; VR-019 and task T045 assert exactly that -->


## The strict test-first mandate

- [X] CHK008 Is the boundary that decides which modules take both the test-first and property-based mandates stated as a **rule** — output is a number that is stored or published — rather than an enumeration, so a module added later can be classified without a ruling? [Clarity, plan AD-001, project-instructions § Testing & Quality Policy] <!-- Evaluator: Covered by plan.md AD-001 ("E006 fixed the classification rule by package placement: a module whose output is *a number that is stored or published* belongs under `model/compute/`… Placing it anywhere else would make the mandate a matter of argument rather than of path") and §Testing Strategy's Property tier, which states the set "is defined by AD-001's placement rule, not enumerated by hand". The Instructions Check row applies the rule to three module classes and reaches the same answer -->
- [X] CHK009 Is the completion condition for a test task specified as an **observed** failure — a collection error for the absent module, never a green suite — and recorded on the task line, so ordering survives a squash merge? [Testability, plan Testing Strategy, tasks T025/T033/T035/T059] <!-- Evaluator: Covered by tasks.md §Dependencies "Mandatory red-green pairs": each test task "names the implementation task it precedes and is complete only when its suite has been run against the absent module … and **observed to fail**, with that observed failure recorded on the task line. A test task marked complete beside a passing suite is the defect this condition exists to catch." Carried on all four task lines ("on an observed collection error"), and neither member of a pair is ever `[P]` -->
- [X] CHK010 For `compute/metrics.py`, which already exists, is the observed-failure condition restated in a form that can actually occur (a collection error naming the absent estimators) rather than one that cannot? [Consistency, tasks T059, FR-027] <!-- Evaluator: Covered — T059 reads "on a collection error naming the absent estimators" rather than the absent-module form the other three pairs use, and §Dependencies carries the same exception explicitly ("or, for `metrics.py`, against the absent estimators"). The condition is reachable: the test imports `precision_interval` from an existing module and fails at collection -->
- [X] CHK011 Does every `model/compute/` module in the plan have a test task that **precedes and names** its implementation task? [Completeness, plan AD-001, tasks T025→T026, T033→T034, T035→T036, T059→T060] <!-- Evaluator: Covered — four `compute` modules, four declared pairs, each edge in §Declared edges: T025→T026 (`calibrate`), T033→T034 (`pair_score`), T035→T036 (`decide`), T059→T060 (`metrics`). The two later implementation tasks on an already-paired module (T061, T062) are covered because §Dependencies scopes T059's relation class to "every published figure including pair completeness, coverage, the withheld share **and the stratum argument**", which is what T061 and T062 add, and both chain behind T060 -->
- [X] CHK012 Is the FR-047 weight calibration covered by the existing `calibrate` test-first pair rather than added as an untested afterthought? [Completeness, FR-047, tasks T025, T026] <!-- Evaluator: Covered — T025 is "Failing tests for determinism and frozen-set dependence, **cutoffs and weights**" and T026 calibrates both, with §Dependencies naming the pair as "`compute/calibrate.py`, cutoffs **and** weights". The consuming edge is declared too — T034 ← T027:`WEIGHTS` — and §Dependencies states why it matters: "a scorer that hardcodes its own weights would satisfy every other task here and leave the scale tunable after observing precision" -->


## Property-based coverage and relation classes

- [X] CHK013 Is a **relation class** declared per module, rather than "property-based tests" named as a bare tool? [Clarity, plan Testing Strategy — Property tier] <!-- Evaluator: Covered by plan.md §Testing Strategy Property tier, which declares one named class per module — `pair_score`: boundedness and monotonicity; `decide`: totality and disjointness; `metrics`: interval containment and estimator selection; `calibrate`: determinism and frozen-set dependence — each with its concrete relation spelled out. tasks.md §Dependencies repeats the four against their task ids, so the class travels with the work rather than only with the plan -->
- [X] CHK014 Is the requirement that `decide` be enumerated **exhaustively** at and around both cutoffs — not sampled — stated as a requirement, given that a sampler will miss exact boundary values? [Completeness, SC-016, SC-035, FR-042] <!-- Evaluator: Covered by plan.md Property tier ("enumerated exhaustively at and around both cutoffs rather than sampled, because the criterion (SC-016, SC-035) is about exact boundary values and a sampler will miss them"), tasks T035 and §Dependencies, and made concrete in data-model.md VR-011 (both cutoffs, both directions — accepting *and* rejecting) and VR-012 (nine enumerated score positions, "disjoint and exhaustive as a property of the expression, not as a sample") -->
- [X] CHK015 Does the `metrics` relation class cover **estimator selection** as well as interval containment, so the three-way branch (rule of three / exact binomial / undefined) is exercised rather than assumed? [Completeness, FR-027, FR-028, SC-019] <!-- Evaluator: Covered by plan.md Property tier, which names both relations and enumerates the branch explicitly: "the published interval contains the point estimate; zero errors selects the rule of three, one or more selects the exact binomial, zero denominator selects undefined". The schema refuses the wrong branch independently — `ck_resolution_figure__rule_of_three_needs_zero_errors`, whose two `coalesce`s are documented as load-bearing -->
- [X] CHK016 Is `pair_score`'s monotonicity relation stated over the component agreements, so a weight sign error is detectable? [Testability, FR-014, plan Property tier] <!-- Evaluator: Covered by plan.md Property tier — "raising one component's agreement never lowers the total" — which is stated over the component agreements and is exactly the relation a negative or sign-flipped weight breaks. Reinforced structurally by data-model.md's `[0,1]` range checks on `weight`, `agreement` and `contribution` and by VR-009's bit-exact summation in declared attribute order -->
- [X] CHK017 Is `calibrate`'s frozen-set dependence stated as a property — a perturbed set yields a detectably different constant pair — rather than left as a test case? [Testability, FR-016, FR-047] <!-- Evaluator: Resolved — it was stated as a property, but over "the same two constants … a detectably different **pair**", which predates FR-047. A perturbation moving only the weights leaves the cutoffs identical, so the relation as written would fail while the implementation was correct, and a weight-only calibration change would go unexercised. plan.md's Property tier now states it over all six committed constants, matching T025's "cutoffs and weights" -->


## Denominators and populations

- [X] CHK018 Is merge precision's denominator stated unambiguously as **merges among labeled pairs** — not the labeled-set size, not the run's total merge count — everywhere precision is named? [Unambiguity, FR-027, FR-019, SC-021] <!-- Evaluator: Covered by FR-027, which states the denominator and both exclusions in one sentence, and repeated at every site that names the figure: SC-019 and SC-020 ("merges among labeled pairs"), SC-017 and FR-019/SC-021 (the estimation stratum's labeled pairs the emitted clusters induce). data-model.md restates it on `resolution_figure.denominator` and stores numerator and denominator on every row, and `ix_resolved_entity_induced_pair__pair` is named as the population FR-019 measures over -->
- [X] CHK019 Is recall's denominator stated as the true pairs in the frozen labeled set, and is every way a true pair can be missed (withheld, rejected, never blocked) enumerated as a miss? [Completeness, FR-030, SC-024] <!-- Evaluator: Covered by FR-030 ("over the true pairs in the frozen labeled set as denominator. A true pair that was withheld, rejected, or never generated by blocking MUST count as a recall miss"), the Glossary entry, and SC-024 for the attribution. FR-037(a) narrows the denominator to the estimation stratum, which is "the sole source for recall and blocking pair completeness"; data-model.md's recall row carries `stratum='estimation'`, and task T068 asserts all three miss paths separately -->
- [X] CHK020 Does every published figure name the **stratum** it draws on, and is computing across the union of the two strata prohibited rather than merely discouraged? [Unambiguity, FR-037, FR-037a, SC-037] <!-- Evaluator: Covered by FR-037a ("Each published figure MUST name the stratum it draws on") and FR-037's flat prohibition ("Publishing a figure over the union of the two strata is prohibited, because the hard-negative stratum is chosen rather than sampled"), with SC-037. Prohibited rather than discouraged in the strongest available sense: `ck_resolution_figure__stratum`'s domain is `('estimation','run_candidate_set')` with no `hard_negative` and no union member, so the value is unwritable; VR-013 asserts the omission by attempting it -->
- [X] CHK021 Is the hard-negative stratum's exclusion from *every* published denominator stated once in a way that binds all figures, rather than repeated per figure with the risk of omission? [Consistency, FR-037, SC-037] <!-- Evaluator: Covered — FR-037(b) states it once and universally ("used only by FR-007's collision guard and SC-003, and excluded from every published denominator"), FR-037a binds each figure to naming its stratum, and the enforcement is a single object rather than six rules: the absent domain member on `resolution_figure.stratum`. Invariant 40 records it as "CHECK, by omission from a domain" -->
- [X] CHK022 Is the census/estimate classification (FR-038) applied to every figure the feature publishes, with none left unclassified? [Completeness, FR-025, FR-038, SC-031] <!-- Evaluator: Resolved — FR-038 classified five of the six. Recall and blocking pair completeness were named as Wilson estimates and coverage, reduction ratio and the withheld share as censuses, but **merge precision** — the one figure with a registered target — was named nowhere in the classification FR-025 routes every figure through, because its estimator is FR-027's rather than Wilson. FR-038 now classifies all six explicitly and says why merge precision does not take Wilson; this matches `ck_resolution_figure__kind_matches_name`, which already classified all six -->
- [X] CHK023 Where a figure's interval is undefined at the realized sample size, is the required output specified as "undefined **with its denominator**" rather than omission or zero? [Completeness, FR-028, SC-013] <!-- Evaluator: Covered three times over — FR-025 ("reported as undefined together with its denominator, never as a bare point estimate"), FR-028 ("with the denominator shown, and MUST NOT be reported as zero or omitted"), FR-037a for the too-small-stratum case, and SC-013/SC-017/SC-019 at the criterion level. Structural in data-model.md: `denominator` is `NOT NULL` on every row and `ck_resolution_figure__estimate_iff_denominator` makes a zero denominator and a NULL estimate mutually entailing -->
- [X] CHK024 Is the n < 30 disclosure obligation tied to the realized denominator rather than to the labeled-set size? [Unambiguity, FR-027, SC-020] <!-- Evaluator: Covered — FR-027 attaches it to "that denominator", the antecedent being "the number of merges made among labeled pairs", and SC-020 restates it as "where the count of merges among labeled pairs is below 30". `ck_resolution_figure__small_sample_disclosure` reads the row's own realized `denominator`, so a run cannot satisfy the disclosure by pointing at the set size -->


## Frozen-set discipline

- [X] CHK025 Is "frozen and hashed **before** calibration" expressed as a checkable ordering with an artifact that evidences it, rather than as an intention? [Testability, FR-016, FR-017, plan HINT-001, tasks T023→T024→T025] <!-- Evaluator: Covered by data-model.md `labeled_pair_set.frozen_at` and `threshold_calibration.calibrated_at` with VR-006 asserting `frozen_at <= calibrated_at` by query, explicitly "because the two facts are on different rows and no `CHECK` can see across them"; the artifact is the committed per-stratum file (task T024 "freezes, hashes and COMMITS"), and plan HINT-001 states why the ordering cannot be reconstructed afterwards. tasks §Dependencies adds that T027→T026→T025 are declared edges "so a scheduler honouring only declared edges cannot invert it" -->
- [X] CHK026 Is the refusal-on-divergence behavior stated **symmetrically** for thresholds and weights, so neither is frozen while the other floats? [Consistency, FR-044, FR-047, SC-040, SC-043] <!-- Evaluator: Covered symmetrically at all three levels — FR-044 and FR-047 use the same freeze / record-in-manifest / bind-to-hash / refuse-on-divergence shape, SC-040 and SC-043 mirror each other (both now naming the divergent constant and the perturb-each-alone method), and one object carries both: the eight-column `uq_threshold_calibration__strata_thresholds` / `fk_resolution_run__calibration` pair, with VR-007 and VR-030 as mirrored assertions. plan AD-005 records the extension to the weights and why the asymmetry was "the appearance of a guarantee" -->
- [X] CHK027 Are the two strata's **separate hashes** distinguishable everywhere a hash is referenced, so verifying one cannot be mistaken for verifying both? [Unambiguity, FR-017, FR-037, SC-037] <!-- Evaluator: Covered by FR-017 and FR-034 as amended earlier in this same evaluation pass (see data-integrity CHK025, which raised the identical point): each stratum is hashed separately, both hashes are verified on every reporting run, both are recorded on the manifest, and the singular phrase "the labeled-set hash" is fixed as meaning both. The schema was already unambiguous — two `labeled_pair_set` rows, two hash columns with pinned kind literals on both the calibration and the run, `ck_threshold_calibration__strata_distinct`, and guard G1 recomputing each stratum's hash -->
- [X] CHK028 Is the canonical serialization each hash is computed over declared, for the labeled set **and** the alias artifact? An unspecified serialization makes the hash unreproducible and the verification untestable. [Completeness, FR-004, FR-017, FR-040] <!-- Evaluator: Covered as of this same evaluation pass (see data-integrity CHK024, the identical point). The alias artifact's serialization was already declared in plan §Project Structure; the labeled set's was named in three places and defined in none, and is now declared in data-model.md §`labeled_pair_set` — one committed file per stratum, UTF-8/LF/no BOM, fixed column order, rows sorted by the canonical pair identity, surrogate key excluded — with VR-005 asserting that declaration rather than inventing one and plan §Project Structure listing both stratum files -->
- [X] CHK029 Does SC-043's verification method (perturb one weight, assert refusal) generalize across all four weights rather than certifying one? [Completeness, SC-043, FR-047] <!-- Evaluator: Resolved — it did not. "Demonstrated by perturbing one weight" certifies one column of an eight-column key, and VR-030 names the precise defect that admits ("perturbing the vector wholesale would pass an implementation that compared only its sum, which is the same defect VR-007 guards against one column over"). SC-043 now requires each of the four weights perturbed alone — four separate refusals, each naming the divergent weight — which is what VR-030 and task T084 already specified -->


## The baseline

- [X] CHK030 Is "same stratum, same estimator" specified precisely enough that the baseline's figure and the resolver's are comparable rather than merely adjacent? [Unambiguity, FR-046, SC-042] <!-- Evaluator: Resolved — the stratum half was precise (FR-019/FR-037a and `resolution_figure.stratum`'s domain), the estimator half was not. Read literally, "the same estimator" forces one interval method onto both arms, which contradicts FR-027 the moment the two arms observe different false-merge counts — a likely case, since AD-012 expects the baseline to be "plausibly winning on precision". FR-046 and SC-042 now say the same estimator **rule** of FR-027 applied independently to each arm, each arm naming its own method and sidedness per FR-026 -->
- [X] CHK031 Is the baseline's normalization specified — shared with the resolver, not private — so both figures are computed over the same values? [Consistency, FR-046, plan AD-012] <!-- Evaluator: Covered by plan.md AD-012, which corrected an earlier draft on exactly this point: "**It does reach `identity.normalize`, and must.** … FR-046 requires agreement on *normalized* values, so a baseline carrying its own private normalization would compare different strings than the resolver and the two figures would not be over the same values. Normalization is not what is on trial." Carried by the import contract (two forbidden modules, not three), plan §Project Structure, and task T063's "over normalize.py's shared output — same strings as the resolver" -->
- [X] CHK032 Is the constraint that `normalize.py` hold **only** syntactic transforms stated as a requirement, so an alias lookup leaking into it would be a violation rather than a design drift the import contract cannot see? [Completeness, plan AD-012, tasks T029] <!-- Evaluator: Covered and stated three times with the hazard named. plan AD-012 ("This requires `normalize.py` to hold **only** syntactic transforms — case folding, whitespace, part-number segmentation — with every alias-dependent step in `alias.py`"), plan §Project Structure on the module line, and tasks §Brownfield Notes, which states precisely why it cannot be left to the contract: "if an alias lookup leaks into `normalize.py`, `lint-imports` stays green and the baseline is contaminated through a module it is allowed to import, so the split is stated on T029 rather than left to the contract to catch" -->
- [X] CHK033 Is the baseline's strength label (strong / weak) required to be *declared*, and declared before the figures exist rather than chosen after? [Testability, FR-046, project-instructions § Principle VIII] <!-- Evaluator: Covered — the label is required to be published (FR-046, SC-042) and is pre-committed as `strong` in plan AD-012 with its justification ("Exact match is the resolver *minus the two things whose complexity is on trial*") and on task T066, so it exists in a reviewable artifact before any run does and cannot move without a plan diff. FR-046 was amended in the same pass as CHK030 to state the ordering requirement explicitly rather than leaving it to the plan to imply -->


## Existing tests this feature falsifies

- [X] CHK034 Are the changes to `test_migration_ranges.py` specified as three distinct edits, including **moving the negative-control probe off `0500`** — which becomes a real revision number and silently stops controlling for anything? [Completeness, plan HINT-005, tasks T001] <!-- Evaluator: Covered in four places, all three edits enumerated and the third called out by name: plan HINT-005, data-model.md §Block-partition assertion ("the third is the one that is easy to miss … Left where it is, it asserts that a now-declared number is undeclared — which is the same off-by-one it exists to catch, pointed at the test instead of at the table"), VR-026 (which asserts the probe is `"0600"` and not `"0500"`), and task T001 with tasks §Dependencies noting that this failure "turns nothing red at all" -->
- [X] CHK035 Are the eight assertions in `test_resolved_entity.py` that `0505` falsifies required to be **restated** against the run-scoped constraints rather than deleted, so the re-scoping does not go unasserted? [Completeness, plan HINT-003, tasks T019] <!-- Evaluator: Covered by plan HINT-003 ("Restate them against the new constraints rather than deleting them — a name-matched assertion that is simply removed leaves the re-scoping unasserted"), data-model.md §The FR-045 Extension cost item 2 and T-2, which itemise all eight (seven name matches plus the exactly-three-foreign-keys count), and task T019, which repeats "restated … never deleted" -->
- [X] CHK036 Is the gap in `test_table_ownership.py` — `E003_OWNED_TABLES` names neither `resolved_entity` nor `resolved_entity_member`, so the FR-065 guard is blind to exactly the alteration FR-045 performs — recorded as a required change rather than left for discovery? [Completeness, plan Propagation Obligations note, tasks T020, T021] <!-- Evaluator: Covered by plan §Propagation Obligations' closing note, which records it as an ordinary test change on this branch rather than an amendment need and states the reason for recording it at all ("a reader who finds the guard green will otherwise conclude no ownership boundary was crossed, when the truth is that the guard never looked"), by data-model.md G-9 and VR-028/VR-029 (a second snapshot window opening at `0404`, with the diff asserted against a hardcoded expected set), and by tasks T020 and T021 -->
- [X] CHK037 Is the gateway migration test required to be **extended with E009's block** rather than re-pinned to a new head, and its positive control over an undamaged revision directory preserved? [Consistency, plan HINT-005, tasks T022] <!-- Evaluator: Covered by plan HINT-005 ("extend it with E009's block and keep its positive control over an *undamaged* revision directory, so a broken fixture cannot make the negative controls pass for the wrong reason"), plan §Tests to extend ("must gain E009's block the same way rather than being re-pinned to a new head"), and task T022 with the same two clauses -->


## Coverage denominator

- [X] CHK038 Is it stated that the coverage `--source` lists **override rather than merge**, so omitting `identity` leaves every line this epic writes outside the denominator while the gate still reports green? [Clarity, plan HINT-002] <!-- Evaluator: Covered by plan HINT-002 ("**enumerations that override rather than merge**. Adding `model.identity` without appending to both leaves every line this epic writes outside the coverage denominator while the gate still reports green. E006 hit this exact trap; it recurs because the lists are enumerations, not globs"), the Coverage tier's "not configured" marking, and tasks §Brownfield Notes -->
- [X] CHK039 Are **both** locations named — `.github/workflows/verify.yml` and the **root** `pyproject.toml` (`[tool.coverage.run] source` and `[tool.coverage.paths]`) — rather than one? [Completeness, plan Project Structure, tasks T003] <!-- Evaluator: Covered — plan §Project Structure names both files and both keys, and is explicit that the root manifest is the right one ("Both live at the root (lines ~65–163), not in `src/model/pyproject.toml`; `src/model`'s own `[tool.coverage.run] source = ["src/model"]` is a directory and needs no append"). Tasks T002 and T003 are one task per file, and §Brownfield Notes states the consequence of doing only one -->
- [X] CHK040 Is a per-package floor on `model.identity` required in addition to the combined floor, so an already-covered package cannot carry a newly added one across the threshold? [Completeness, plan Testing Strategy — Coverage tier] <!-- Evaluator: Covered by plan §Testing Strategy Coverage tier — "Combined 80% floor **and a per-package 80% floor on `model.identity`**, asserted separately" — carried by task T002 (the floor is added with the `--source` edit) and T080, which verifies it "asserted alone" -->

---

**Traceability**: 40 of 40 items carry a reference (100%).
**Not covered by design**: integration-tier database behavior and CI wiring are graded by the Data Integrity checklist and by QC respectively; this checklist stops at whether the requirements admit a failing test.

**Evaluation note** (2026-07-29): 33 passed, 7 resolved, 0 remaining — 40 of 40 checked. Six resolved by the evaluator; **CHK005 resolved by project-owner decision** (option A, a Requirement ↔ Criterion Map in `spec.md` §Success Criteria, criterion grammar untouched). Evaluated after `data-integrity.md`, so CHK027 and CHK028 — which raise the same two points as that checklist's CHK025 and CHK024 — passed against amendments made earlier in the same pass rather than against the artifacts as originally written; both annotations say so. Four of the six resolutions were criteria that had drifted from their own requirements (SC-013 from FR-024, SC-015 from FR-035, SC-043 from VR-030, SC-042/FR-046 from FR-027), which is the same failure mode STF-001, STF-005 and STF-007 each recorded and the reason CHK005's traceability question is being escalated rather than closed.

**One defect found outside the checklist's items** — reported with the ASK batch and **since resolved by owner decision under observability CHK020**: the figure-name domain is now eight, so the baseline's two figures are manifest rows. As found: FR-046 and SC-042 require the baseline's merge precision and recall to be published, and `resolution_figure` had nowhere to put them — `ck_resolution_figure__name`'s domain is the six resolver figures, VR-013 asserts exactly six rows per run, and data-model.md states that "a seventh figure invented at report time has nowhere to live". Task T066 renders them into `resolution-report.md` only, so two published figures do not resolve to a manifest row, against SC-027 and Principle I.

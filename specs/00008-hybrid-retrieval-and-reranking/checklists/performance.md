# Performance Requirements: Hybrid Retrieval and Reranking
**Created**: 2026-07-29 | **Feature**: [spec.md](../spec.md)

## Latency Budget Definition

- [X] CHK001 Is the reranking latency budget stated with a statistic — a mean, a percentile, or a never-exceed — rather than as the bare 150–400 ms range, so that one observation can be adjudicated against it? [Clarity, Plan §Technical Context "Performance Goals"] <!-- Evaluator: NOT RESOLVED — the statistic is owned by specs/sad.md and choosing one sets the pass/fail semantics of an architectural constraint. Raised as plan.md §Pending Amendments item 7; needs your decision -->
      > **Closed at checklist:** Decided at checklist: never-exceed 400 ms, no query in an evaluation run exceeding it. See spec §Decisions Taken at Checklist.
- [X] CHK002 For the latency budget as written, is there an observation that would make it fail, given any value inside a 250 ms-wide band satisfies the sentence? [Testability, Plan §Technical Context; Spec §SC-016] <!-- Evaluator: NOT RESOLVED — same root cause as CHK001. SC-016 now makes an overage a failure rather than a satisfied criterion, so the memory half is falsifiable today; the latency half stays unfalsifiable until item 7 lands -->
      > **Closed at checklist:** Now falsifiable: a single observation above 400 ms fails it.
- [X] CHK003 Is the workload the latency budget applies to fixed in the text — 50 candidates at FR-003's fetch depth, one query at a time under the single-user assumption — rather than left to the reader to assemble from three places? [Completeness, Spec §FR-003, §Assumptions; Plan §Technical Context] <!-- Evaluator: Resolved — FR-033 "Workload" bullet states it in one place; FR-037 makes the 50 a derived constraint -->
- [X] CHK004 Is "one shared vCPU" expressed as a measurable environment — a CPU quota or container limit with a value — so two runs can be compared against the same allocation? [Clarity, Plan §Technical Context; Spec §FR-033] <!-- Evaluator: Resolved — FR-033 "Environment" bullet fixes an enforced CPU quota of one vCPU (1.0), the value sad.md already names, expressed as a container limit -->
- [X] CHK005 Is the measurement point for reranking latency named — the session call, the retrieval handler, or the HTTP response — given each yields a different number for the same request? [Clarity, Spec §FR-033] <!-- Evaluator: Resolved — FR-033 "Measurement point" bullet names the reranker component's scoring call, inclusive of tokenization and batching, exclusive of DB, fusion and the encoder -->
- [X] CHK006 Is the measurement occasion fixed relative to warm-up, so that FR-017's load-and-warm cost is excluded from the latency figure by rule rather than by convention? [Completeness, Spec §FR-017, §FR-033, §SC-007] <!-- Evaluator: Resolved — FR-033 "Measurement occasion" bullet excludes it by rule; SC-007 gains a structural decision rule -->
- [X] CHK007 Is the aggregation window for the latency figure stated — per query, per run, or over the full evaluation set — given SC-016 says "per query" and "over the full evaluation set" in one sentence? [Clarity, Spec §SC-016, §FR-033] <!-- Evaluator: Resolved — SC-016 now fixes the window: one observation per query over every query in the set, one memory reading per run. The combining statistic remains CHK001's open decision -->
- [X] CHK008 Does the latency budget state whether it binds the INT8 arm, the FP32 arm, or both, given FR-025 makes a full-precision reranked arm a required deliverable? [Completeness, Spec §FR-025; Plan §AD-011] <!-- Evaluator: Resolved — FR-033 "Arms" bullet binds the INT8 serving arm and requires the FP32 arm measured against the same budgets with overage published; no separate budget invented -->
- [X] CHK009 Is the query-side embedding step's cost stated as inside or outside the reranking budget, given FR-007 introduces a per-query encoder call that did not previously exist? [Clarity, Spec §FR-007; Plan §Technical Context] <!-- Evaluator: Resolved — FR-033 places the encoder call outside the reranking span and requires it reported as its own figure -->
- [X] CHK010 Is any latency or resource expectation stated for the single fusion statement itself, or does the epic budget only the reranking step of an end-to-end query? [Completeness, Spec §FR-002; Plan §Technical Context] <!-- Evaluator: NOT RESOLVED — FR-033 now requires the fusion statement's wall-clock reported, but no budget exists for a retrieval query as a whole and setting one would invent a value. Needs your decision -->
      > **Closed at checklist:** Open by design — no query-level budget exists to state, and the compute envelope is architectural. Occasion recorded in plan §Checklist Outcome.

## Memory Envelope Definition

- [X] CHK011 Is the ≤ 400 MB envelope stated for the configuration that ships — two model graphs and therefore two sessions — rather than for the single session the constraint and the risk row were both written against? [Consistency, Plan §Constraints, §AD-011, §Risk Mitigation] <!-- Evaluator: Resolved — plan §Technical Context Constraints restated for every session the process holds; spec §Risks restated in the plural; sad.md's matching singular note raised as amendment 8 -->
- [X] CHK012 Does the text state whether both reranker graphs are resident simultaneously or loaded per arm, given FR-017 requires loading once per process and the answer changes the envelope? [Completeness, Spec §FR-017; Plan §AD-011] <!-- Evaluator: Resolved — new AD-013 decides both resident, derived from AD-006 (arms are per-request) plus FR-017; FR-017 amended to load and warm both before readiness -->
- [X] CHK013 Is the counter the memory figure is read from named — process resident set or container working set — so that the reported number and the budget describe the same quantity? [Clarity, Spec §FR-033; Plan §Constraints] <!-- Evaluator: Resolved — FR-033 "Counter" bullet names the serving process's RSS, itemized per session against the one container total -->
- [X] CHK014 Is the measurement occasion for the resident-memory figure fixed — after warm-up, at steady state, or at peak during a maximum-shape batch — rather than carried by the undefined phrase "steady-state"? [Clarity, Plan §Constraints; Spec §FR-033] <!-- Evaluator: Resolved — FR-033 and plan §Constraints define steady state as after readiness and after the run's queries, with the run peak reported beside it, reconciling sad.md's peak-RSS benchmark with its steady-state target -->
- [X] CHK015 Does any requirement apportion the 400 MB between the reranker session or sessions and the rest of the API process, or is "the dominant line item" the only allocation given? [Completeness, Plan §Constraints, §Risk Mitigation] <!-- Evaluator: NOT RESOLVED — FR-033 now requires per-session itemization, which makes the total attributable, but sub-budgets are values nobody has chosen. Needs your decision -->
      > **Closed at checklist:** Deliberately unapportioned; FR-033 itemizes against the one total instead. Recorded in plan §Checklist Outcome.
- [X] CHK016 Is the FP32 graph's memory cost bounded by a stated budget, or is it only required to be reported once observed? [Completeness, Spec §FR-025, §FR-033; Plan §AD-011] <!-- Evaluator: Resolved — AD-013 puts both sessions inside the one serving process, so the FP32 graph is bounded by the 400 MB container total; FR-033 requires it itemized. Not individually sub-budgeted — see CHK015 -->

## Measurement Method and Reporting Obligations

- [X] CHK017 Are the "declared budgets" SC-016 adjudicates against declared with values in an artifact the criterion names, so a reviewer can decide pass or fail from the text alone? [Testability, Spec §SC-016, §FR-033] <!-- Evaluator: Resolved — SC-016 and FR-033 both name specs/sad.md §Technical Context "Performance Goals" and §Quality Attributes "Compute envelope" and restate the 400 MB inline -->
- [X] CHK018 Does SC-016 state a threshold, or is it satisfied by a run that exceeds every budget provided the overage is published? [Testability, Spec §SC-016] <!-- Evaluator: Resolved — SC-016 now requires the figures FALL WITHIN the budgets and states explicitly that publishing an overage discharges FR-033's disclosure duty but does not satisfy the criterion -->
- [X] CHK019 Is the destination of the latency and memory figures named — the diagnostics surface, an evaluation output, or both — so the reporting obligation has a place to land? [Completeness, Spec §FR-033; Plan §API Surface Summary] <!-- Evaluator: Resolved — plan §API Surface Summary "Which figures live where" enumerates both destinations and defers to contracts/openapi.yaml as normative -->
- [X] CHK020 Does the diagnostics payload described in the plan carry every figure FR-033 requires reported, with no figure required by one and absent from the other? [Consistency, Spec §FR-033; Plan §API Surface Summary] <!-- Evaluator: Resolved — the plan's claim that "FR-019 and FR-033's figures live there" contradicted the contract, which puts per-query latency on the search response; corrected. Residual gap recorded in the same section: the contract's single reranker_session object must become a per-session collection under AD-013 -->
- [X] CHK021 Do the performance figures state whether they carry an interval or are declared a census, given every other published figure in this spec is required to carry one? [Consistency, Spec §SC-016, §SC-001, §SC-009] <!-- Evaluator: Resolved — SC-016 declares both figures a census over an enumerated population needing no interval, following SC-009's existing convention -->
- [X] CHK022 Is the corpus size the figures are measured at stated as a qualifier on them, so a number measured at 6,391 chunks is not read as holding across the 5,000–15,000 design band? [Completeness, Plan §Technical Context "Scale/Scope"] <!-- Evaluator: Resolved — FR-033 "Qualifier" bullet, SC-016, and plan §Scale/Scope all require the corpus size carried with every figure -->

## Workload and Configuration Parameters

- [X] CHK023 Is the interaction between FR-003's fixed fetch depth, FR-018's equal reranked count, and FR-027's search breadth stated as one derived constraint, or must a reader infer that these three jointly fix the reranker's workload? [Consistency, Spec §FR-003, §FR-018, §FR-027] <!-- Evaluator: Resolved — new FR-037 states the derived constraint in one sentence and settles a second ambiguity it exposed: the fused set may hold up to 100 candidates, so the reranker scores the top 50 of the fused ordering -->
- [X] CHK024 Is an upper bound or a change procedure stated for the index search breadth, given FR-027 sets only a floor and a breadth far above it trades latency for recall with no budget attached? [Completeness, Spec §FR-027; Plan §AD-002] <!-- Evaluator: Resolved — new FR-040 states the change procedure (a value above the floor is a recorded change carrying a re-measured latency figure, as FR-003 treats fetch depth). No numeric ceiling is invented; FR-033's budget is the bound -->
- [X] CHK025 Are the reranker thread counts stated as a requirement with a derivation rule, rather than appearing only as a `NEW-CONFIG` signal and a mitigation note? [Completeness, Spec §Implementation Signals; Plan §Risk Mitigation] <!-- Evaluator: Resolved — new FR-038 states intra-op = the container's CPU quota in whole vCPUs, inter-op = 1, values reported with the figures, grounded in research-implementation.md -->
- [X] CHK026 Is the maximum batch and sequence shape FR-017's warm-up must use defined numerically, given warm-up at a smaller shape leaves load cost in the first served query? [Clarity, Spec §FR-017, §SC-007] <!-- Evaluator: Resolved — FR-017 fixes batch = the reranked count (50) and sequence = the model's declared maximum sequence length, and requires the shape actually warmed at be reported -->
- [X] CHK027 Is the reranker's sequence limit given a value in the requirements, so FR-019's truncation fraction and the per-candidate share of the latency budget are both anchored to a number? [Completeness, Spec §FR-019; Plan §Technical Context] <!-- Evaluator: Resolved — FR-019 now requires the limit published as a number taken from FR-016's artifact identity. The literal integer is a property of the vendored model, not a choice this spec may make, so it is anchored by declaration rather than by assertion -->
- [X] CHK028 Are candidate truncation and batching — named as the mitigation for the compute-envelope risk — bounded by any stated target, or stated as activities with no threshold? [Testability, Spec §Risks; Plan §Risk Mitigation] <!-- Evaluator: Resolved — spec §Risks and plan §Risk Mitigation both restated: truncation at the declared sequence limit with the fraction published, batching at a fixed shape equal to the reranked count, both held to FR-033's budgets rather than pursued as activities -->
- [X] CHK029 Is the assumption that the corpus stays within the band where an exact vector scan is "fast enough" given a numeric band and a threshold, rather than an unverifiable term? [Clarity, Spec §Assumptions] <!-- Evaluator: NOT RESOLVED — the numeric band is now stated (5,000–15,000 chunks, 6,391 today) and the exact path's latency is reported at the measured size, but the threshold is the same missing query-level budget as CHK010. Needs your decision -->
      > **Closed at checklist:** Same root cause as CHK010; band stated, threshold awaits the first measured figure.
- [X] CHK030 Does FR-018's prohibition on changing the reranked count state the performance consequence of a change alongside the quality one, given the count is the primary driver of the latency budget? [Completeness, Spec §FR-018] <!-- Evaluator: Resolved — FR-018 now requires a re-measured latency figure alongside the re-measured ablation, and names the count as the primary driver of the latency budget -->

## Startup, Readiness and Degraded Paths

- [X] CHK031 Is a bound stated for load-plus-warm-up duration, given readiness is withheld until it completes and a startup probe threshold has to be chosen from it? [Completeness, Spec §FR-017, §SC-007] <!-- Evaluator: NOT RESOLVED — FR-017 now requires the measured load-and-warm duration be reported, so a threshold can be chosen from evidence, but the bound itself is a value nobody has chosen. Needs your decision -->
      > **Closed at checklist:** FR-017 reports the duration so the threshold comes from evidence. Recorded in plan §Checklist Outcome.
- [X] CHK032 Does SC-007's "incurs no load cost" carry a decision rule that separates a first query which paid a load cost from one which did not? [Testability, Spec §SC-007] <!-- Evaluator: Resolved — SC-007 now decides it structurally: zero artifact reads, session creations and warm-up invocations on any request path after readiness, counted and asserted zero. A latency-difference rule is explicitly rejected and why -->
- [X] CHK033 Is any performance expectation stated for the fusion-only degraded path, or is it left unbudgeted on the assumption that removing the reranker can only make a query faster? [Completeness, Spec §FR-021, §FR-022] <!-- Evaluator: Resolved — new FR-041 requires the degraded path's latency reported on FR-033's terms and bounds it by the reranked path's latency over the same query set, a relative bound that invents no value -->

## Consistency, Traceability and Governance

- [X] CHK034 Do spec.md and plan.md name the same budget the same way — "the container's budget", "the declared budgets", and "API container steady-state RSS ≤ 400 MB" — or must a reader equate three phrases to find one number? [Consistency, Spec §FR-033, §SC-016; Plan §Technical Context] <!-- Evaluator: Resolved — FR-033 and SC-016 both now cite specs/sad.md by section and restate ≤ 400 MB inline, and plan §Technical Context names the same owner -->
- [X] CHK035 Is the owning artifact of the 150–400 ms and 400 MB values identified, and is a procedure stated for relaxing either, given the compute envelope is an architectural constraint rather than a feature-local one? [Traceability, Plan §Technical Context, §Pending Amendments] <!-- Evaluator: Resolved — plan §Technical Context "Owner of the numeric envelope" names both sad.md sections and states the relaxation procedure: a superseding decision record, per project-instructions.md §Governance -->
- [X] CHK036 Is the pgvector version check HINT-002 makes a precondition for the iterative-scan design stated as a requirement or a task anywhere, rather than only as an implementation hint? [Traceability, Plan §HINT-002, §AD-003] <!-- Evaluator: Resolved — new FR-039 states it as a requirement with the observed version recorded in FR-029's settings; HINT-002 updated to point at it -->
- [X] CHK037 If the extension predates 0.8.0 and a wider search breadth is the only in-scope remedy, is the resulting latency cost bounded or the fallback's tradeoff stated? [Completeness, Plan §AD-003, §HINT-002; Spec §FR-028] <!-- Evaluator: Resolved — FR-040 requires the fallback state the breadth used, the recall it bought and the latency it cost, and publish any resulting overage -->
- [X] CHK038 Do FR-033 and SC-016 each reach both a named component and a named verification in the Requirement Coverage Map, rather than a component alone? [Traceability, Plan §Requirement Coverage Map] <!-- Evaluator: Resolved — the FR-033 row now carries SC-016 and names a verification: test_performance_report.py plus the container benchmark job sad.md specifies, run under the one-vCPU quota. Rows added for FR-037 through FR-041 -->
- [X] CHK039 Is the performance obligation of the sixth arm — a second differently configured process for the exact/approximate comparison — reflected in any resource statement, given the envelope is written for one serving container? [Consistency, Spec §SC-012; Plan §Contract Decisions Confirmed at Plan, §Constraints] <!-- Evaluator: Resolved — plan §Contract Decisions now states each process is bound by the 400 MB envelope individually, the combined resident set is recorded where they run concurrently, and the envelope is explicitly not restated as 800 MB -->

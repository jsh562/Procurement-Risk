# Observability: Traced Model Gateway
**Created**: 2026-07-25 | **Feature**: [spec.md](../spec.md)

## Field-List Closure and the E013 Read Contract

- [X] CHK001 Is TR-012's field list stated as *closed* — nothing recorded outside it, nothing added to it without amending the requirement — in the way TR-020 states closure for the hashed field list, or is it only enumerated? [Completeness, Spec §TR-012 / §TR-020] <!-- Evaluator: Resolved — added TR-068 to spec.md closing TR-012's list in the sense TR-020 closes the hashed list, plus VR-032 in data-model.md comparing the information-schema column set against the list in both directions -->
- [X] CHK002 Are the three columns the design records as outside TR-012's list — `pricing_timestamp`, `cost_absent_reason`, `invocation_id` — reconciled with TR-012's own claim that the list "MUST be a superset of the fields any other requirement in this spec mandates", including whether they fall inside or outside the set E013 consumes? [Consistency, data-model §Disclosed Divergences D-1…D-3 / Spec §TR-012] <!-- Evaluator: Covered by data-model §Disclosed Divergences ("Status: all three closed") — TR-012 names all three and the E013 Read Contract table lists all three -->
- [X] CHK003 Does any artifact carry the disposition of D-1…D-3, which the data model assigns to the Plan phase, given the plan's Open Items table dispositions OI-1, OI-2, OI-3, OI-5, and PI-1 and not these? [Traceability, plan §Open Items Disposition / data-model §Disclosed Divergences] <!-- Evaluator: Covered by data-model §Disclosed Divergences — each row now carries its own Status ("Closed — named by TR-012, in the read contract"), so the disposition is recorded rather than deferred to the Plan phase -->
- [X] CHK004 Is a change procedure for the read contract stated — what a consumer does when it needs a field the list does not carry, and whether adding a column is a contract change requiring E013's agreement? [Completeness, Spec §IP-005 / data-model §E013 Read Contract] <!-- Evaluator: Resolved — added TR-069 (three-step procedure; E013's agreement required for a removal or rename, not for an addition; unlisted columns carry no compatibility promise) and amended IP-005 and data-model §E013 Read Contract -->
- [X] CHK005 Is the consumed field set fixed at requirement level rather than only in the design document, given IP-005 names the table, the outcome enumeration, and the cost fields but points at no requirement that closes the set? [Traceability, Spec §IP-005 / §TR-012] <!-- Evaluator: Resolved — TR-068 closes the set at requirement level and IP-005 now names TR-068 and TR-069 as the closure and the change procedure -->
- [X] CHK006 Is nullability stated for the recorded fields that OBJ3 VC8 does not cover — `fixture_key`, `cost_usd`, `cost_absent_reason`, `error_type` — so a consumer reading the requirement can tell which fields may legitimately be absent? [Completeness, Spec §TR-012 / §OBJ3 VC8 / data-model §Column Detail] <!-- Evaluator: Covered by spec §TR-044 (per-field nullability contract carried by column constraints) and data-model §VR-021 -->

## Naming Provenance and the Convention Pin

- [X] CHK007 Does any artifact state the pinned OpenTelemetry generative-AI convention *version*, or is the pin named only as a configuration key — leaving OBJ3 VC7's comparison with no fixed referent and the naming requirement unfalsifiable? [Precision, Spec §TR-013 / §OBJ3 VC7 / research §Telemetry-record requirement quality] <!-- Evaluator: Resolved — added TR-070 pinning version 1.36.0, recorded in three places that must agree (config key, COMMENT ON TABLE, the requirement) with a task-level verification duty; data-model §Field Naming Alignment "Pin" carries the same value -->
- [X] CHK008 Is the per-field split between convention-named and gateway-local fields carried at requirement level, together with the rule that a gateway-local column bears no `gen_ai_` prefix, given TR-013 reads as applying to every recorded field while most of the field list is gateway-local? [Completeness, Spec §TR-013 / data-model §Field Naming Alignment / §VR-015] <!-- Evaluator: Resolved — added TR-071 carrying the two-way classification and the no-`gen_ai_`-prefix rule at requirement level; VR-033 enforces both halves -->
- [X] CHK009 Is a stability class recorded per field, distinct from the convention-named/gateway-local split, which records provenance rather than stability? [Completeness, research §Telemetry-record requirement quality / data-model §Field Naming Alignment] <!-- Evaluator: Resolved — added TR-072 (Stable vs Development, explicitly a different axis from provenance) and a Stability class table in data-model §Field Naming Alignment -->
- [X] CHK010 Is the attribute-to-column transform stated precisely enough to be applied in both directions, given that replacing `.` with `_` is not reversible without a rule for attributes whose own segments contain underscores? [Unambiguous, data-model §Field Naming Alignment / Spec §OBJ3 VC7] <!-- Evaluator: Resolved — added TR-073 making the transform forward-only with an injectivity check rather than bidirectional; data-model §Transform rewritten to match -->
- [X] CHK011 Is a procedure stated for a pin bump that renames or reclassifies a field — both `gen_ai_provider_name` and `cache_read_input_tokens` are marked pin-sensitive — for the case where rows already exist, given the design's "cheap edit" holds only until the first row lands? [Completeness, data-model §Field Naming Alignment / Spec §TR-013] <!-- Evaluator: Resolved — added TR-074 (rename in a new higher-numbered migration, three recording points updated together, classification refreshed, TR-069 applied); data-model §Pin bump procedure -->
- [X] CHK012 Do the recorded fields whose provenance is a source other than the generative-AI convention — `error_type` from the general attribute registry, `trace_id` from the trace-context specification — each name and pin their own source? [Traceability, data-model §Field Naming Alignment / Spec §TR-013] <!-- Evaluator: Resolved — TR-071 pins `error.type` to the general attribute registry shipped with the same 1.36.0 release and `trace_id` to W3C Trace Context Level 1; both classification rows updated -->
- [X] CHK013 Is the deliberate divergence for latency — an integer-millisecond column where the convention expresses duration as a metric in seconds — recorded as a stated exception rather than left as a mismatch a reviewer must infer? [Clarity, data-model §Field Naming Alignment / §Column Detail] <!-- Evaluator: Covered by data-model §Field Naming Alignment (`duration_ms` row states the metric-in-seconds divergence and why no attribute spelling exists to match) and spec §TR-056, which fixes integer milliseconds at requirement level -->
- [X] CHK014 Are the high-cardinality values — trace identifier, fixture key, and the schema and template digests — stated as record columns only, with no obligation to appear as metric dimensions? [Precision, Spec §TR-012 / §TR-019 / research §Telemetry-record requirement quality] <!-- Evaluator: Resolved — added TR-075, which states the record is the only telemetry sink and that the two columns and the two digests carry no metric-dimension obligation; data-model §Scope gains a "Not emitted" row -->

## What Is Deliberately Not Captured

- [X] CHK015 Is there an explicit not-captured list — prompt text, completion text, system prompt, credential material, end-user identity — stated as its own obligation, rather than inferable from the absence of those fields in TR-012's list? [Completeness, Spec §TR-012 / §TR-026 / research §Telemetry-record requirement quality] <!-- Evaluator: Resolved — added TR-076, which states the five-item not-captured set as its own closed obligation, including end-user identity, which no other requirement mentioned -->
- [X] CHK016 Is the not-captured obligation stated per sink, given TR-026 addresses log output alone while the invocation record, the spool payload, and the committed fixture store are three further sinks with different content? [Complete (set), Spec §TR-026 / §TR-024 / data-model §VR-020] <!-- Evaluator: Resolved — TR-076 states the obligation per sink across the invocation record, the spool payload, the normalized error, log output, check output, and the fixture store; VR-037 asserts it with seeded content markers -->
- [X] CHK017 Is it stated that the committed fixture store deliberately *does* retain prompt and completion content — the one sink where the not-captured position does not hold — so the position is not read as a global claim about the feature? [Credibility, Spec §TR-024 / §Objective 4 / data-model §Storage Boundaries] <!-- Evaluator: Covered by spec §TR-066 ("Committed fixtures deliberately retain prompt and completion content — that is what a fixture is … a different sink under a different control"), restated per sink by the new TR-076 -->
- [X] CHK018 When content capture is explicitly enabled, is it stated which sinks it may reach and whether the invocation record and the spool are excluded from it? [Unambiguous, Spec §TR-026 / §OBJ5 VC4] <!-- Evaluator: Resolved — TR-066 already scoped capture to log output and excluded the record and the normalized error at any toggle setting; TR-076 adds the spool payload and check output, which were only covered in data-model §VR-020 -->
- [X] CHK019 Does any requirement fix what the gateway's log output *does* carry — the event set and the field that correlates a line to an invocation — or is logging specified only by what it must omit? [Completeness, Spec §TR-026 / §OBJ5 VC4] <!-- Evaluator: Resolved — added TR-077 closing the log event set at five events and requiring the invocation identifier as the correlator on every invocation-scoped line; VR-038 asserts it -->

## Stated Denominators

- [X] CHK020 Do the criteria reading "100% of cases" — SC-004, SC-018, SC-019, SC-020, SC-021's first clause, and SC-016's second clause — each name the population counted, in the way TR-011 names its denominator? [Measurability, Spec §SC-004, §SC-016, §SC-018–§SC-021 / §TR-011] <!-- Evaluator: Resolved — SC-004, SC-016, SC-018, SC-019, SC-020, and SC-021 each amended to name the population each clause counts; no SC ID changed -->
- [X] CHK021 Is SC-005's denominator stated where SC-005 is written, given the row-existence precondition that qualifies it — a row exists only where a provider request was issued or a fixture resolved — appears only in the data model? [Measurability, Spec §SC-005 / data-model §Row-existence precondition] <!-- Evaluator: Resolved — SC-005 amended to restate TR-011's denominator and enumerate the pre-request failures that write nothing -->
- [X] CHK022 Does SC-021 carry its disclosed exclusion in its own denominator, or does it read as unconditional while the design records it as a claim about attempted writes and discloses process death between provider response and spool commit as uncovered? [Credibility, Spec §SC-021 / data-model §State & Lifecycle] <!-- Evaluator: Resolved — SC-021 amended to scope its first denominator to requests whose record write was attempted and to disclose the process-death window as uncovered -->
- [X] CHK023 Are the invocations that fail before any request is constructed — replay miss, credential present in replay, absent mode selection, missing opt-in — excluded from the tracing claim in requirement text as well as in the design document? [Consistency, Spec §TR-011, §TR-021–§TR-023, §TR-027 / data-model §Row-existence precondition] <!-- Evaluator: Resolved — TR-011 amended to carry the excluded set as a closed list of eight, each cross-referenced to the requirement that produces it, rather than leaving it to the data model's row-existence precondition -->
- [X] CHK024 Does any requirement state a denominator for the repaired rate the risk mitigation commits to publishing — all recorded invocations, or only those that reached validation? [Measurability, Spec §Risks / plan §Risk Mitigation] <!-- Evaluator: Resolved — TR-079 fixes the numerator and denominator over stored columns: rows that reached validation (`outcome` in valid/repaired, or failed with `error_type = validation_failed`), transport and deadline failures excluded with a stated reason -->

## Outcome Enumeration and the Repaired Rate

- [X] CHK025 Are TR-009 and TR-042 distinguishable in obligation, or do two requirements state the same invocation-level classification in terms that can drift apart under a later edit to one of them? [Consistency, Spec §TR-009, §TR-042] <!-- Evaluator: Resolved — TR-042 rewritten to stop restating the value set and to fix the division of obligation explicitly: TR-009 owns the values and the transport exclusion, TR-042 the unit, TR-078 the mapping -->
- [X] CHK026 Is the mapping from terminal state and attempt counts to outcome stated exhaustively — including the invocation that both consumed transport retries and repaired successfully — rather than only by the negative rule that a transport failure is never `repaired`? [Completeness, Spec §TR-009 / data-model §VR-003] <!-- Evaluator: Resolved — added TR-078, a total mapping over the three cases that names the retried-then-repaired invocation explicitly; VR-034 asserts it table-driven over every reachable combination -->
- [X] CHK027 Is an extension procedure stated for the outcome enumeration, given IP-005 makes the three values a contract E013 reads and the design deliberately exposes them as a `CHECK` rather than a database type? [Completeness, Spec §IP-005 / plan §AD-006 / data-model §VR-002] <!-- Evaluator: Resolved — TR-069's procedure covers the outcome enumeration and the absent-cost reason set alongside the field list, with the named CHECK replaced in a new higher-numbered migration and E013's agreement required for a removal -->
- [X] CHK028 Does the record carry the context the repaired-rate mitigation depends on — which constraints were enforced by post-decode validators rather than by the submitted schema — given TR-012's field list holds no such field? [Completeness, Spec §Risks / §TR-012 / §TR-005] <!-- Evaluator: Resolved — TR-079 records the decided position: no per-row field, because the split is a property of the caller's schema rather than of the invocation; context is published from the digested schema (TR-038); the limit and its reversal under TR-069 are disclosed rather than left implicit -->
- [X] CHK029 Is the obligation to publish the repaired rate alongside that context assigned to a requirement and an owner, or does it exist only as a risk mitigation with no criterion that can fail? [Traceability, Spec §Risks / plan §Risk Mitigation] <!-- Evaluator: Resolved — TR-079 assigns computability to E004 and publication to E013; SC-023 is the criterion that can fail; IP-005, the Risks entry, and plan §Risk Mitigation all name the owner -->
- [X] CHK030 Is the condition under which the repaired rate becomes a quality *estimate* — and so attracts the interval obligation the compliance check defers to the consuming epic — stated concretely enough for that epic to act on? [Clarity, Spec §Compliance Check Principle II / §Risks / §IP-005] <!-- Evaluator: Resolved — TR-079's estimate condition names three concrete triggers (projection to future invocations, another schema, another model or pin); Compliance Check Principle II amended to cite it -->

## Trace-Identifier Propagation

- [X] CHK031 Is the trace identifier's format and validity rule stated at requirement level, given TR-031 obliges non-null and generation while the 32-lowercase-hex and non-all-zero rules appear only in the design? [Precision, Spec §TR-031 / data-model §VR-004] <!-- Evaluator: Covered by spec §TR-047, which fixes the value domain as 32 lowercase hexadecimal characters excluding all-zero and refuses a non-conforming identifier at the boundary before any provider request -->
- [X] CHK032 Is the propagation mechanism named — an explicit field on the gateway-owned request versus ambient context — given the gateway-owned public surface is the only place a caller can supply one? [Unambiguous, Spec §TR-031 / §TR-002] <!-- Evaluator: Resolved — added TR-080, which names the explicit optional field on the gateway-owned request and rules out thread-locals, context variables, inbound headers, and any propagator -->
- [X] CHK033 Is a caller-propagated identifier distinguishable from a gateway-generated one on the stored row, or does the record lose the fact that an invocation joined no external trace? [Completeness, Spec §TR-031 / §OBJ3 VC5] <!-- Evaluator: Resolved — TR-080 discloses that the row does not distinguish the two, states why (a shape distinction would break TR-047's single value domain; a provenance flag would sit outside TR-012's closed list) and names the reversal under TR-069; data-model `trace_id` Column Detail carries the same limit -->
- [X] CHK034 Is it stated whether the gateway emits telemetry beyond the database record — spans, metrics, or collector-consumable events — and is the absence of a span identifier and parent-span identifier recorded as a scope decision, given the field names otherwise follow a span convention? [Completeness, Spec §Scope Excluded / §TR-013 / §TR-012] <!-- Evaluator: Resolved — added TR-075 (no spans, metrics, exporter, propagator, or SDK dependency; span-id and parent-span-id absence recorded as a scope decision with a reversal) and a matching Scope/Excluded bullet -->

## Cost Recomputability as an Observability Property

- [X] CHK035 Is the quantization rule that makes "reproduces the stored cost exactly" attainable — all four billing-class terms summed at full precision, quantized once, half-even — stated at requirement level rather than only in the data model and an implementation hint? [Precision, Spec §TR-014 / data-model §CD-2 / plan §HINT-004] <!-- Evaluator: Covered by spec §TR-049 ("summing all four billing-class terms at full decimal precision and quantizing exactly once at the end under round-half-even; per-term quantization is a defect, not a variant") -->
- [X] CHK036 Is "exactly" defined as a comparison procedure with a zero tolerance and a stated numeric type, so a recomputation check yields a verdict rather than a judgement? [Able to be validated, Spec §TR-014, §SC-006 / data-model §CD-3] <!-- Evaluator: Covered by spec §TR-049 (exact decimal comparison at the stored scale of 10, no binary-float intermediate on either path) and SC-006, which restates the same procedure -->
- [X] CHK037 Does a requirement state that price-table versions and entries are immutable once published, given recomputability depends on it and the design records immutability as held by convention and review rather than by a database rule? [Completeness, Spec §TR-015 / data-model §State & Lifecycle] <!-- Evaluator: Covered by spec §TR-055, which forbids update and delete on all three tables, makes a rate change a new version, and discloses the enforcement limit rather than presenting it as enforced -->
- [X] CHK038 Is the currency of the recorded cost stated in a requirement, given none names one and the design fixes it in column names alone? [Completeness, Spec §TR-014, §TR-016 / data-model §Column Detail] <!-- Evaluator: Covered by spec §TR-049 ("MUST hold every monetary value in United States dollars … whose replacement or extension requires a visible schema change") -->
- [X] CHK039 Is the `cost_absent_reason` value set enumerated at requirement level, given E013 reads the cost fields and TR-016 requires only "a stated reason"? [Precision, Spec §TR-016 / §IP-005 / data-model §Entities] <!-- Evaluator: Covered by spec §TR-048, which closes the reason set at exactly three and explains why the unresolvable pin is not a fourth; TR-068 now carries that set as part of the closed read contract -->
- [X] CHK040 Is provenance required for the price-table version itself — snapshot date and published source — so a recomputed cost resolves to the rate table it was computed from? [Compliance, data-model §Column Detail — price tables / project-instructions §Principle I] <!-- Evaluator: Resolved — added TR-081 lifting both to requirement level, with the capture-date-versus-effective-from distinction stated and an unsourced version neither seedable nor pinnable; VR-036 asserts it -->

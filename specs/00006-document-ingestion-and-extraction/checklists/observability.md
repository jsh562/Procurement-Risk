# Observability: Document Ingestion and Extraction
**Created**: 2026-07-27 | **Feature**: [spec.md](../spec.md)

## The Ingestion Report as a Published Artifact

- [X] CHK001 Is the ingestion report's required content enumerated as a closed set, given SC-032 names five items while FR-053, FR-061, SC-005, SC-018, SC-041, SC-047, and SC-048 each oblige further content the same artifact must carry? [Completeness, Spec §SC-032 / §FR-053 / §FR-061] <!-- Evaluator: Resolved — added FR-071, whose content list is closed in both directions (20 items, each naming the requirement that obliges it); SC-032 now ranges over the list rather than naming five items -->
- [X] CHK002 Is the report's identity fixed at requirement level — its committed location, its form, and whether one exists per epic or per run — given Scope §Included names only "an ingestion report committed with this epic" and a path appears solely in plan §Project Structure? [Completeness, Spec §Scope Included / plan §Project Structure] <!-- Evaluator: Resolved — FR-071 fixes the committed path, the single-artifact form, and one-per-repository cardinality regenerated in full; Scope §Included restated to match -->
- [X] CHK003 Is it stated which run's figures a published report carries, given FR-043 admits a run that ingests 3 of 51 documents while the other 48 documents' live rows belong to earlier runs? [Unambiguous, Spec §FR-043 / §FR-055] <!-- Evaluator: Resolved — FR-072 requires each figure to declare itself corpus-resident, naming the generation set it ranges over, or run-scoped; a corpus-wide figure credited to one run would claim 48 documents that run never opened -->
- [X] CHK004 Does a requirement oblige each published figure to name the run or generation it was computed over, and the report to name the run record it describes, given data-model records every figure as recomputable by query and none as stored? [Traceability, data-model §Scope "Not a table" / project-instructions §Principle I] <!-- Evaluator: Resolved — FR-072 obliges both, and SC-056 makes it a total check over published figures -->
- [X] CHK005 Is the disposition of a previously committed report stated when a re-run supersedes some documents' generations — replaced, appended to, or left standing beside figures it no longer describes? [Completeness, Spec §FR-043 / §FR-055] <!-- Evaluator: Resolved — FR-071 states regeneration replaces wholesale; the superseded text stays recoverable from version control, so replacement loses nothing while accretion would leave one artifact carrying two accounts of one corpus -->
- [X] CHK006 Is the artifact that records the real-specification extraction exclusion named, given FR-022 and SC-012 require the exclusion to be "recorded" without stating where it is published? [Traceability, Spec §FR-022 / §SC-012] <!-- Evaluator: Resolved — FR-071 item 5 names the ingestion report as that artifact -->
- [X] CHK007 Are the report obligations that exist only as success criteria — chunk counts against the 5,000–15,000 estimate (SC-005) and the separated valid, repaired, and failed counts (SC-018) — carried by a functional requirement, or do they rest on a criterion with no requirement behind it? [Traceability, Spec §SC-005 / §SC-018 / §FR-033] <!-- Evaluator: Resolved — SC-018 is carried by FR-069 and SC-005 by FR-071 item 8; both now rest on a requirement rather than on a criterion alone -->
- [X] CHK008 Is the set of artifacts constituting a run's account closed at the ingestion-run row plus the report, so that no part of "what this run did" depends on output the repository does not retain? [Completeness, Spec §FR-038 / §SC-032] <!-- Evaluator: Resolved — FR-071 closes the account at the ingestion-run record with its associations plus the report, and bars any published figure resting on console or log output -->
- [X] CHK009 Are the report's figures subject to a reproduction obligation with a stated tolerance, given the CI requirements make a reproduction job confirm published metrics before a release tag and FR-045 makes a full run replayable from committed fixtures? [Able to be validated, Spec §FR-045 / project-instructions §CI Requirements] <!-- Evaluator: Resolved — added FR-074 and SC-057: replay reproduction against a committed results manifest, exact for every count, rate, interval, and stored confidence, and within {SAD:ADR-0018}'s encoder parity tolerance for the near-duplicate counts -->


## Run Attribution and the Granularity of Agent Identity

- [X] CHK010 Is the granularity of "agent identity" stated — a person, a software version, an SDD agent role, or a provider model — given FR-038 and SC-022 require the field and data-model constrains it only to non-empty text? [Precision, Spec §FR-038 / §SC-022 / data-model §ingestion_run.agent_id] <!-- Evaluator: Resolved per user — FR-038 fixes it as a composite of the invoking principal and the executing build under a declared parseable grammar, enforced by `ck_ingestion_run__agent_id_format` rather than by convention; the provider model is excluded because it has its own column. SC-022 and data-model VR-003 carry it -->

- [X] CHK011 Is the resolution-mode value set closed at requirement level, given FR-038 names the field while only data-model fixes it to `record` and `replay`? [Completeness, Spec §FR-038 / data-model §ck_ingestion_run__resolution_mode] <!-- Evaluator: Resolved — FR-038 now closes the set at `record` and `replay`, spelled as the traced path spells them -->
- [X] CHK012 Does FR-055's "mark each ingestion run active or superseded" agree with the per-document placement of that state, or does the requirement still assert a run-level property the data model records as unrepresentable under FR-043's skip rule? [Consistency, Spec §FR-055 / §FR-043 / data-model §ingestion_run "No status column"] <!-- Evaluator: Covered by spec.md §FR-055 — the mark is stated per document with the run-level flag rejected for FR-043's reason, and promotion removes the prior generation ({SAD:ADR-0020}) -->
- [X] CHK013 Is run attribution specified for the row kinds FR-039 does not name — contributing-chunk rows and line-item rows — or do they resolve to a run only by inference through a parent value? [Completeness, Spec §FR-039 / §SC-021 / data-model §Entities] <!-- Evaluator: Resolved — FR-039 now specifies attribution for every row kind this epic writes: line-item and parse-signal rows carry run and document directly, held equal to the value's; contributing-chunk rows resolve through their parent and deliberately carry no second copy that could disagree -->
- [X] CHK014 Is the at-least-one half of "exactly one ingestion run" stated as an obligation distinct from at-most-one, given data-model records the existence half as carried by no database mechanism? [Completeness, Spec §SC-021 / data-model §G-1] <!-- Evaluator: Covered by spec.md §FR-039 ("resolve to exactly one" is the obligation) and §Disclosed Limitations "Half of SC-021 is unenforceable", which names the existence half, what covers it, and its production-scale alternative -->
- [X] CHK015 Is the retention bound FR-055 requires to be stated given a value at requirement level, so a reader can tell how far back a run's outputs stay reconstructable, rather than the number appearing only in data-model §Operator Procedures? [Precision, Spec §FR-055 / data-model §Operator Procedures 3] <!-- Evaluator: Covered by spec.md §FR-055 and §SC-043 — the bound is zero retained generations, stated with its reason (ordinals are unique within a document), and reversal is a re-run at the previous chunker version rather than a status change -->


## Failure Taxonomy and What a Partial Run Reports

- [X] CHK016 Are the seven per-field outcomes enumerated in this spec, or referenced only by count and by the three names that happen to appear in US3's acceptance scenarios? [Completeness, Spec §FR-034 / §SC-016 / §US3] <!-- Evaluator: Resolved — FR-034 now enumerates all seven and SC-016 restates them; the list defers to E003 where the two disagree -->
- [X] CHK017 Is the run-level failure set closed at requirement level, given FR-056 names two conditions, data-model fixes five, and FR-005, FR-014, and FR-052 each abort a run without naming the kind their abort is recorded under? [Completeness, Spec §FR-056 / §FR-005 / §FR-014 / §FR-052 / data-model §ck_ingestion_run__failure_kind_domain] <!-- Evaluator: Resolved — FR-056 now enumerates the five kinds and maps FR-005, FR-052, FR-014, and FR-045 onto them; FR-019's encoder-artifact check is named as the one abort outside the set, because it runs before the run record exists -->
- [X] CHK018 Is disjointness stated as a property of the two outcome sets, rather than only as a prohibition on one recording path in FR-056? [Consistency, Spec §FR-056 / §SC-016 / data-model §VR-007] <!-- Evaluator: Resolved — FR-056 states it as a set property checked by intersecting the two declared domains, and SC-016 carries "share zero values" -->
- [X] CHK019 Is it specified what a partially completed run reports about what it did and did not do — which documents committed, which rolled back, and which were never reached — given FR-042 guarantees only that no document is left half-ingested? [Completeness, Spec §FR-042 / §SC-042 / §FR-056] <!-- Evaluator: Resolved — added FR-073 and SC-055: four dispositions (`ingested`, `skipped_unchanged`, `rolled_back`, `not_reached`) partitioning the enumerated corpus, so skipped-by-rule is distinguishable from never-reached -->
- [X] CHK020 Is the required content of a run-level failure's diagnostic detail specified — in particular whether it names the document in flight — given FR-035 fixes five fields for a per-field failure and FR-056 fixes none for a run-level one? [Completeness, Spec §FR-035 / §FR-056 / data-model §run_failure_detail] <!-- Evaluator: Resolved — FR-056 fixes the detail's content per kind and requires the document in flight to be named wherever one exists, which is also what lets FR-073 mark exactly one document `rolled_back` -->
- [X] CHK021 For a field absent from an entire document, is the source chunk and attempted page such a failure record carries specified, given FR-058 records the absence once per document while FR-035 and SC-039 require both fields on every failure? [Unambiguous, Spec §FR-058 / §FR-035 / §SC-039] <!-- Evaluator: Covered by spec.md §FR-058 — the record names the lowest-ordinal chunk the field was attempted on and that chunk's page, deterministic under FR-015 and published as a stated convention rather than a claim about where the field would have been printed -->
- [X] CHK022 Is the standing of an aborted run's already-committed generations stated at requirement level — whether downstream readers may treat them as active, and whether an aborted run is distinguishable from one in flight — given both carry no finish and only a failure kind separates them? [Consistency, Spec §FR-042 / §FR-055 / §SC-044 / data-model §finished_at] <!-- Evaluator: Resolved — FR-042 already fixed the committed generations as active and the corpus as consumable; FR-038 now states the three readable run states and discloses the one it cannot separate, a run whose process died before writing its failure columns -->


## Published Figures and Their Honesty

- [X] CHK023 Is publication of the denominator an obligation on the figure, given FR-060 and SC-047 require Wilson 95% intervals while the printed denominator appears only in plan §AD-011 and research? [Completeness, Spec §FR-060 / §SC-047 / plan §AD-011] <!-- Evaluator: Resolved — FR-060 and SC-047 already carried the obligation; the stale "all three denominators" clause and its F1 sentence, left over from before F1 was withdrawn, are removed, and plan's coverage map and `metrics.py` comment corrected to match -->

- [X] CHK024 Is the Wilson variant fixed — continuity-corrected or not — given research records under-coverage at extreme proportions for very small denominators and requires the variant to be stated and used consistently? [Precision, Spec §FR-060 / research §Wilson score intervals] <!-- Evaluator: Resolved per user — FR-060 fixes the continuity-corrected variant and requires it named with the figures; FR-011's defect bound and SC-047 use the same variant, plan AD-011 updated so the two cannot drift, and the property suite asserts the interval is never narrower than the uncorrected one on the same input -->

- [X] CHK025 Is "extraction quality figure" defined as a named set, so a reader can tell whether the repaired rate, the confidence distribution, and the near-duplicate counts fall inside FR-050's baseline-and-interval obligation or outside it? [Unambiguous, Spec §FR-050 / §SC-029 / §SC-018] <!-- Evaluator: Resolved — FR-050 names the set as FR-060's per-field precision and recall, and states why each of the three named figures is outside it: the baseline issues no invocation, computes no confidence, and produces no chunks, so requiring a baseline figure beside them would require an empty cell -->

- [X] CHK026 Is the criterion for labelling the baseline strong or weak stated, or is the label left to the author's judgement while FR-050 asserts in prose that a template extractor could plausibly win? [Able to be validated, Spec §FR-050 / §SC-029] <!-- Evaluator: Resolved per user — FR-050 states both criteria: a declared label fixed before any figure exists (independent authorship plus template-driven construction) and an observed label read from the published table (beats or ties on at least one per-field figure), with a disagreement published as a finding rather than resolved by revising either label. SC-029 carries all three clauses -->

- [X] CHK027 Is the census-versus-estimate distinction stated for the report as a whole, so a total check such as SC-002 or SC-013 is not published with an interval it does not need and no estimate is published without one? [Clarity, Spec §SC-002 / §SC-013 / §FR-011 / project-instructions §Principle II] <!-- Evaluator: Resolved — FR-072 fixes three kinds (census, sampled estimate, descriptive figure over a designed set) and SC-056 forbids a census with an interval and an estimate without one; FR-060 labels its intervals descriptive rather than inferential, since the 25 transmittals are a seeded set and not a random sample -->
- [X] CHK028 Is the method FR-011 requires for a sampled claim's error bound named, or is "a stated method" satisfiable by a method chosen after the inspection has happened? [Able to be validated, Spec §FR-011 / research §Validating parser page attribution] <!-- Evaluator: Covered by spec.md §FR-011 — rule-of-three 3/n for zero defects, never quoted at n ≤ 30, and the Wilson interval otherwise, fixed rather than left to the report's author so two runs publish comparable bounds -->
- [X] CHK029 Is layer labelling an obligation on every published figure rather than on extraction figures alone, given FR-053 requires a leaf-length distribution measured "across all 51 documents" while the Risks entry requires every extraction figure to be labelled by layer? [Consistency, Spec §FR-053 / §FR-060 / §Risks] <!-- Evaluator: Resolved — FR-072 makes per-layer publication an obligation on every figure whose population spans both layers, and FR-053 applies it to the leaf distribution, sentence-split count, boundary-class counts, and page-terminal list; SC-038 and SC-041 carry it -->
- [X] CHK030 Is the form of the published confidence distribution specified — whether all eight scores FR-057's three signals admit appear with their counts, including scores no stored value took? [Precision, Spec §FR-033 / §FR-057 / §SC-017] <!-- Evaluator: Resolved — FR-033 and SC-017 now require all eight admissible scores with their counts, a score nothing took published as a zero rather than omitted -->

## Disclosed Shortfalls and Their Form

- [X] CHK031 Are the data-model gaps this epic expects a reader to act on — G-1, G-4, and G-7 above all — recorded in the four-part form Principle VII fixes, given each carries a consequence and a reversal but no production-scale alternative? [Compliance, data-model §Disclosed Gaps / project-instructions §Principle VII] <!-- Evaluator: Resolved — data-model §Disclosed Gaps restructured so the reversal trigger and the production-scale alternative are separate columns for all ten gaps; previously the two were merged into one "what would reverse it" sentence, which several rows satisfied by stating only one of them -->
- [X] CHK032 Is the latent append-only enforcement's reporting obligation binding — that SC-024 is published as enforced by design and unenforced in deployment rather than as passing — or does that instruction exist only in plan §Open Items? [Traceability, Spec §Disclosed Limitations / §SC-024 / plan §Open Items G-6] <!-- Evaluator: Covered by spec.md §SC-024 — the criterion itself states it is measured under an explicit role switch and is enforced by design and latent in deployment, with the shortfall published beside an absolute target rather than the target softened; data-model G-6 and the Disclosed Limitations row carry the same -->
- [X] CHK033 Is the HNSW index's absence after an aborted run — correct-but-slow retrieval with no signal — carried as a disclosed limitation of this epic, given the operator procedure that drops it is introduced here? [Completeness, data-model §Operator Procedures 1 / §G-7 / Spec §Disclosed Limitations] <!-- Evaluator: Resolved — added a Disclosed Limitations row in the four-part form; FR-064 and SC-050 already carried the requirement side, and G-7 the enforcement gap -->
- [X] CHK034 Is the uncalibrated-confidence statement and its reversal trigger required beside the published distribution, or only in a limitations table a reader of the figures may never reach? [Consistency, Spec §Disclosed Limitations / §FR-031 / §SC-017] <!-- Evaluator: Resolved — FR-033 and SC-017 now require the statement and its reversal trigger, the frozen hashed labelled sample, printed beside the distribution -->


## Counts Published Rather Than Silent

- [X] CHK035 Is the count of documents skipped on a re-run a required published count, given FR-043 makes skipping the expected outcome and SC-025 measures only that zero rows were added? [Completeness, Spec §FR-043 / §SC-025] <!-- Evaluator: Resolved — FR-073's `skipped_unchanged` is a published count, and the requirement states why SC-025 does not carry it: SC-025 measures that zero rows were added, not how many documents produced none -->
- [X] CHK036 Is the page-terminal fallback published as a list, a per-document count, or both, given FR-053 requires "the list of documents" and US1 scenario 10 requires every such document "with its count"? [Consistency, Spec §FR-053 / §US1 AS10 / §SC-041] <!-- Evaluator: Resolved — FR-053 and SC-041 now require the list with each named document's own count, agreeing with US1 scenario 10 -->
- [X] CHK037 Is the similarity threshold FR-061 counts near-duplicate clusters against given a value, and is it fixed before the run in the way FR-032 fixes the confidence floor? [Precision, Spec §FR-061 / §SC-048 / §FR-032] <!-- Evaluator: Covered by spec.md §FR-061 and §SC-048 — cosine similarity over the chunk embeddings, published at every threshold in the grid 0.80/0.85/0.90/0.95/0.99 declared before the run, so a curve is published and no threshold is privileged by having been selected -->
- [X] CHK038 Is a breakdown of failure counts by outcome a required published count, so a run dominated by `no_value_found` is distinguishable from one dominated by `repair_budget_exhausted`? [Completeness, Spec §FR-034 / §SC-016 / §SC-018] <!-- Evaluator: Resolved — FR-034 and SC-018 require the failure count broken down by each of the seven outcomes, per attempt, with an outcome no failure took published as a zero -->
- [X] CHK039 Is the per-boundary-class chunk count a required published figure, given SC-038 asserts that every boundary falls in one of three named classes without the share taken by each being visible? [Completeness, Spec §FR-012 / §SC-038 / §FR-053] <!-- Evaluator: Resolved — FR-053 and SC-038 require the chunk count in each of the three classes, per layer and pooled, zeros included; the criterion otherwise passes identically on a corpus cut entirely at page breaks -->
- [X] CHK040 Is the count of multi-chunk values a required published count, given SC-020 requires only that at least one exists and the page-split irregularity is the reason the contributing-chunk record exists at all? [Completeness, Spec §SC-020 / §SC-019 / §FR-029] <!-- Evaluator: Resolved — FR-071 item 10 and SC-020 require the count of multi-chunk values and of their contributing-chunk rows, so a run where the representation fired once is distinguishable from one where it fired throughout -->


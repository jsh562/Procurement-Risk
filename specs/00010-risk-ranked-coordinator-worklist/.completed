E010 — Risk-Ranked Coordinator Worklist
Implementation complete: all 52 tasks across 7 phases.

Local verification at completion:
  api tests            179 passed, 1 skipped (model_invocation table is E008's)
  api benchmark        3 passed — p95 50 ms and 44 ms against a 1500 ms budget
  web unit tests       67 passed
  web e2e specs        18 passed against the real page and the real boundary
  root checks          243 passed, 3 pre-existing failures from main
  python coverage      97% (floor 80, verified failing at 99)
  web coverage         100% statements / 98.4% branches (floor 80, verified
                       failing at 100)
  ruff                 clean
  import contracts     3 kept, 0 broken, each with a negative fixture
  api image            builds; 34 image checks pass

Pre-existing failures, outside this epic:
  tests/checks/test_single_import_site.py — 3 tests. The roster filename is
  named in 7 files where 1 is permitted, all in src/model from E005 and E007.
  Fails identically on main.

# Analysis Report: Delivery Forecast Model

**Feature**: E007 · **Branch**: `00007-delivery-forecast-model` · **Date**: 2026-07-27 · **Pass**: 1 (`A-###` series)
**Artifacts**: `spec.md` (40 FR, 35 SC), `plan.md` (11 AD, 21 NC, 8 P), `data-model.md` (41 DV, 19 G, 4 L), `tasks.md` (113 tasks), `research.md`, `checklists/` (115/115)
**Sources**: Spec Validator (read-only), Policy Auditor (read-only), and local cross-artifact analysis
**Verdict**: **FAIL** — 1 CRITICAL, 8 HIGH, 15 MEDIUM, 14 LOW

The CRITICAL must be resolved before `/sddp-implement`. It is not a design defect: the design is sound and the branch is otherwise disciplined about the same rule in eight separate places. It is that the branch performed the one thing those eight places decline to do.

## Findings

| ID | Category | Sev | Location(s) | Summary | Recommendation |
|----|----------|-----|-------------|---------|----------------|
| A-001 | Governance | **CRITICAL** | `specs/prd.md` §Project Context Baseline Updates; `specs/sad.md` same section | **A feature branch performed an amendment to two registered documents.** `project-instructions.md` §Governance: "A feature branch records the need for an amendment and does not perform it," and its rationale names the exact mechanism used — "the amendment procedure re-derives whole managed sections rather than patching lines". Two PRD bullets and five SAD baseline entries were written on this branch (commits `aa02825` Specify, `1ab547b` Plan). Aggravating: `P-4`, `P-6` and `P-7` each decline an amendment citing this rule, so the artifacts assert both that the branch may and may not write registered documents. **Systemic**: `specify-feature` §6.5.4 and `plan-feature` §5.6.3 *instruct* this. Project instructions supersede all other practices, so the skills lose — but the conflict outlives this epic. | Revert all seven lines; record each as a propagation obligation. **Keep** the ADR-0018 catalog row — Governance itself has epics claim record numbers at epic start and makes records append-only, so creating a record is not amending one. Raise the skill conflict separately. |
| A-002 | Testing Policy | HIGH | `plan.md` §What qualifies, `model.py` row | **`model.py`'s clause-3 exclusion is false for what remains in it.** "A malformed graph raises" is true of malformed graphs, not mis-indexed ones: the vendor/category index mapping, the pooling structure and the rework covariate attachment are shape-preserving under a wrong mapping and yield a plausible posterior. The stated mitigation (logp agreement against `likelihood.py`) inherits the mapping it should check, if fed per-row parameters the graph already assembled. Same shape as the closed C-1, surviving C-1's remediation. | Extract the index/design-matrix construction into a pure `design.py`, admit it to the property tier as the **tenth** module with its own RED→GREEN pair, and make the logp oracle build its inputs independently of the graph. |
| A-003 | Contradiction | HIGH | `spec.md` FR-037 vs FR-040, SC-035; `plan.md` NC-21; `data-model.md` DV-041 | **Two requirements contradict on a testable count.** FR-037 requires "one file **per attempt** … never overwritten by a later refusal", justified by the retry-loop case. FR-040 requires "exactly three files", and SC-035/DV-041/NC-21 implement that as an **equality**. A second refusal produces a fourth file and fails DV-041 — in precisely the scenario FR-037 exists to serve. Introduced by two checklist answers taken in one batch and never reconciled. | Restate FR-040 as three **report kinds**; SC-035 and DV-041 become "every emitted file is an instance of a declared kind". Still fails NC-21's planted fourth *kind*. |
| A-004 | Duplication | HIGH | `spec.md` FR-016 first clause, FR-035 | **The duplicate pair.** FR-016 "MUST run at least the published minimum chain count" and FR-035's pre-sampling refusal on the same row are one obligation as duty and as enforcement. Both resolve to DV-035 and NC-14; the Published Constants preamble asserts "one requirement per row class", which is false while both exist; and `spec.md` §Compliance Check credits FR-016 while `data-model.md` §Requirement Traceability credits FR-035. | Fold FR-016's first clause into FR-035; leave FR-016 as the monitor-and-name requirement. Correct the Compliance Check attribution. |
| A-005 | Untestable criterion | HIGH | `spec.md` SC-027; `data-model.md` DV-005 | **The STF-001 replacement can fail a correct implementation — the defect class it was written to fix.** A decile of ~24 open lines is 2.4 lines, the partition rule is unstated, and each operand is a median of ~2 values. The two deciles are *different lines*, so AD-002's monotonicity (per line, fixed θ) does not transfer: a fast-vendor line in the long-elapsed decile inverts the comparison. | Restate as AD-002's **per-line metamorphic** property — for one line, increasing elapsed time does not decrease the drawn remaining median — plus a stored-row semantic check. No between-decile comparison is sound at this n. |
| A-006 | Tautology | HIGH | `spec.md` SC-027 sub-bullet; DV-005 | **The floor is derived from the fit it bounds.** `survival[1]` *is* `1 − F_remaining(1)`, so "a published floor derived from the fitted one-day hazard" bounds a quantity by a derivation of itself — the construction FR-033 and AD-008 reject for SC-008. The two floors live in different criteria, which is why neither review caught it. The derivation rule also appears in no artifact. | Delete the fit-derived floor, or replace it with a bar derived independently of the fit under FR-033's rule. |
| A-007 | Coverage gap | HIGH | `spec.md` FR-035, FR-036, FR-039 | **Three appended requirements have no covering criterion** — zero SC mentions the blocking-precondition class, the `0300` populated-table guard, or the stdout contract. This is STF-015's exact defect recurring on the next generation of appends, and one checklist pass added FR-039 with no criterion while adding SC-035 for FR-040. | Add criteria for all three. |
| A-008 | Negative control | HIGH | `plan.md` §Success Criterion Coverage Map | **Misses by the plan's own admission rule**, which says a claim outside the three exclusion classes with no NC entry "is a miss". Seven rows carry a bare `—` with no class named. Two are serious: **SC-031/DV-032(b)** is an absence check over source text with no planted positive — the exact failure NC-7 exists to exclude for DV-021; and **SC-029/DV-037** cites NC-8, which exercises DV-024's four-part *form*, not DV-037's *presence-by-identity*. | Add negative controls for SC-031 and SC-029; name the exclusion class on the remaining five rows. |
| A-009 | Staleness | HIGH | `plan.md:99, 347, 441`, §Testing Strategy, §SC Coverage Map | **`plan.md` no longer describes the artifacts it audits.** It says 39 rules / 18 gaps / seven propagation obligations; actual are 41 / 19 / eight. The Property-tier caveat list omits DV-040, and SC-013's row omits the very rule added to measure its unmeasured half. `tasks.md` already says 41 and 19, so two artifacts disagree. | Refresh all counts and both omissions. |
| A-010 | Ambiguity | MEDIUM | `spec.md` SC-002 | **The fifth member of the residual-agreement family, live.** SC-002 says the final element "agrees with" the residual tail mass with **no tolerance named**, while SC-003 and US1 scenario 2 both say "within the delivered residual-agreement tolerance" — and SC-003 adds that one phrase for both is how a gate points at the wrong number. Bare "agreeing" is satisfied by an equality implementation E003 ships a test against. | Name the tolerance. |
| A-011 | Inconsistency | MEDIUM | `data-model.md` DV-013 vs `spec.md` FR-017, SC-014 | **The discharging rule is narrower than the criterion.** FR-017 and SC-014 now require five fields per breach for *every* breached blocking diagnostic; DV-013 still says "naming **the** breached metric, its realized value and its threshold" — singular, three fields. NC-1 asserts no field set at all. | Extend DV-013 to the five-field set per breach; add the assertion to NC-1. |
| A-012 | Principle II | MEDIUM | `spec.md` SC-027, FR-038; `plan.md` AD-008 | **Two published figures are plug-ins of estimated quantities with no interval.** M-2 closed this for `vendor_shrinkage` only. SC-027's realized survival floor and AD-008's Kaplan–Meier floor are both published to a reader as bare numbers, and FR-038's new reportable unit standardises a four-part shape with **no interval slot**. | Give FR-038's unit an interval slot where the measure is estimated; require the KM floor to publish its interval. |
| A-013 | Underspecification | MEDIUM | `spec.md` FR-020, FR-038; `data-model.md` L-4 | **The vendor-claim observation count is never stated.** FR-020 requires stating it and FR-038 makes it a decision criterion for a per-vendor verdict, but no number and no derivation rule appears anywhere, and nothing forbids choosing it after the realized shrinkage weights are seen — the move FR-028 prohibits for bands. | Publish a derivation rule in Published Constants and bring it under FR-028's pre-registration duty. |
| A-014 | Staleness | MEDIUM | `spec.md` SC-013 sub-bullet; `data-model.md` DV-040 | **A criterion asserts an absence that no longer holds, and the thing that fills it is an orphan.** SC-013 says the label and anchor FK "are the whole of the evidence today … rather than left for a reader to discover that DV-005 has no counterpart" — DV-040 *is* that counterpart. DV-040 also introduces "the band published before any draw is seen", which no FR requires, no SC covers, and no NC guards. | Correct SC-013; give DV-040 an FR/SC parent and a negative control. |
| A-015 | Test-first | MEDIUM | `tasks.md` T055, T051 | **Two tasks add behaviour to property-tier modules outside any RED→GREEN pair.** T055 adds the total-duration path to `posterior.py`; T051 adds the repeated-seed interval to `ablation.py`. Strict test-first for those modules is a policy MUST, and the pairs list is fixed at nine. | Add two pairs, or fold the behaviour into T026 and T050. |
| A-016 | Phasing | MEDIUM | `tasks.md` §P1 boundary; `spec.md` SC-034 | **The P1-viable claim is false in two places.** SC-034 is tagged `[US1]` (P1) but is asserted only by T109/T110 in Phase 8. SC-035 (P1, T073) asserts an equality over a set including the reproduction report, emitted by T098 in **Phase 7 (P2)**, with no dependency edge recording it. | Re-tag or re-phase; add the missing edge. |
| A-017 | Propagation | MEDIUM | `specs/sad.md:24` | `**Scale/Scope**: … ~120 uncensored delivery events` carries the same figure L-3 falsifies at ~44. `P-4` names only the PRD. | Add a propagation obligation for the SAD. |
| A-018 | Singularity | MEDIUM | `spec.md` FR-014, SC-020 | FR-014 is one sentence carrying ~18 manifest fields, an embedded justification and a second obligation; SC-020 restates the list nearly verbatim, so no independent observable exists. The enumeration is also not authoritative — the covariate list, split hash and shrinkage are manifest facts absent from both. | Split FR-014 into five singular requirements. **APPLIED 2026-07-27.** FR-014 narrowed to the row hash and its convention label; FR-042 (fixture digest), FR-043 (provenance identity), FR-044 (run frame and sampling shape), FR-045 (layer label and datasheet reference, manifest **and** reader-facing) appended. SC-020 narrowed to FR-014 + FR-042; SC-041 and SC-042 appended; T127 covers FR-042. "High churn" was an overstatement — the split appends and renames nothing, so it was permitted outright, and 37 citations were re-pointed across four artifacts. |
| A-019 | Singularity | MEDIUM | `spec.md` FR-017 | FR-017 fuses five obligations, two of which are project-level output policy consumed by four and five other statements. Verification consequence: the **stream contract and the exit-status-class contract have no SC, no DV and no NC**. | Give the stream and exit-status contracts a criterion (folded into A-007). Full split deferred with A-018. |
| A-020 | Spec type | MEDIUM | `spec.md` throughout | `spec_type: product`, but the spec fixes a migration block, names constraint identifiers, mandates a SQL grant set, cites `import-linter` and `model.forecast.config`, and forward-references AD/DV/G identifiers from later-phase artifacts — inverting the phase gate. | **CLOSED 2026-07-27 — and this recommendation was itself wrong.** The retype it proposed is not merely expensive, it is **unavailable**: `specs/project-plan.md:51` registers E007 as `[PRODUCT]`, and Governance makes the project plan canonical — a downstream artifact conflicting with it is corrected, not the reverse — so a retype needs a default-branch amendment this branch may not perform. Independently, `spec-authoring/SKILL.md:119` makes `TR-` mandatory for technical specs, forcing an FR→TR rename across ~748 references that `artifact-conventions/SKILL.md:122` rates CRITICAL with no procedure. **And no MUST-level rule anywhere prohibits implementation detail in a spec** — it is three Spec Validator checkboxes; E003 and E004, both `technical`, cite `docker-compose.yml`, SQL verbs and literal DDL. What was actually actionable is the narrower half: **43 forward-references to Plan-phase identifiers** (`AD-###`, `DV-###`, `G-##`, `L-#`, `NC-##`, `plan.md` sections) in live Requirements, Success Criteria and Scope — the part that genuinely inverts the phase gate. All 43 rephrased to state the fact without the pointer. Every delivered-schema citation kept: they are the premise FR-012's two-table split rests on, not decoration. |
| A-021 | Duplication | MEDIUM | `spec.md` SC-026/SC-035, SC-014/SC-015 | Two near-duplicate criterion pairs. SC-026's check and SC-035's second clause are the same predicate; SC-014's first two conjuncts are SC-015 restricted to one trigger, and both discharge to DV-013 with the same NC-1. | Make each defer rather than restate. |
| A-022 | Coverage gap | MEDIUM | `spec.md` FR-023, SC-019 | FR-023's **provenance-warning** branch — moved fixture digest, unchanged rows, warn and complete — has no criterion. SC-019 covers the refusal half only, so the branch's distinguishing property is asserted nowhere in the criteria. | Add a criterion. |
| A-023 | Coverage gap | MEDIUM | `spec.md` FR-014 sub-bullet | The reader-facing carry-through clause has no criterion; SC-020 asserts the manifest only, which the sub-bullet exists to say is insufficient. It reproduces at criterion level the defect it was added to close. | Extend SC-020 or add a criterion. |
| A-024 | Audit trail | MEDIUM | `plan.md` §Compliance Result, Governance row | **The Governance PASS cites false evidence** — "No registered document edited by this branch — Project Structure touches only `src/model/**`…". Three were. The verdict was reached by reading the plan's own declared file list instead of the repository, which is not a Governance check. A-001 is what it missed. | Correct the row to record what the diff shows. |
| A-025 | Completeness | LOW | `spec.md` §Measurable Outcomes, oracle preamble | The oracle-class taxonomy is presented as exhaustive over three classes, but SC-032 (review, no check), SC-022 (half argued) and SC-011 (declared a proxy) fit none. | Name a fourth class or exempt the three. |
| A-026 | Ambiguity | LOW | `spec.md` §Published Constants | ESS floors are published as fixed constants (400) with a chain-count-dependent basis, while FR-016 permits "at least" the minimum. At eight chains the basis implies 800 and the constant says 400. | State whether the floor is a constant or a function of realized chains. |
| A-027 | Ambiguity | LOW | `spec.md` FR-022, SC-018, SC-005 | "Median and 80th percentile" name no percentile convention, though AD-004's tolerance is derived against one and `compare.py` implements nearest-rank. SC-005's "wider interval" names no credible level. | Fix both conventions. |
| A-028 | Unsourced claim | LOW | `spec.md` FR-031; `data-model.md` G-10 | FR-031 is an unconditional MUST to disclose the horizon extends "well past" the longest observed duration — an unquantified adjective whose premise G-10 records as unsourced, and G-10's remedy is a contingency FR-031 does not admit. | Admit G-10's contingency into FR-031. |
| A-029 | Placeholder | LOW | `spec.md` §Published Constants | The Reproduction tolerance row still says "set in Plan" though AD-004 fixed 5.0 days — a live placeholder in a table whose preamble says a gate against an unpublished threshold is not a gate. The same preamble says "four requirements gate on them"; after STF-010's classification, three do. | Publish the value; correct the count. |
| A-030 | Section rule | LOW | `spec.md:297–298` | **FR-039 and FR-040 sit under `## Success Criteria` → `### Measurable Outcomes`**, not under Functional Requirements. Any reader or tool scanning the requirements section misses two of forty. | Move them. |
| A-031 | Ambiguity | LOW | `spec.md` FR-010, FR-011 | The boundary convention is never stated: DV-003/DV-004 implement strict `>` at day `k` and at the horizon; the spec states neither. FR-011's "independently checkable" is also path-independence, not method-independence — both apply the same formula. | State the convention; soften the independence claim. |
| A-032 | Format | LOW | `data-model.md:186–188, 607–609` | Two table-structure defects: the "Why each weight is a triple" prose block interrupts the `forecast_run` additions table, leaving `open_line_count` and `training_line_count` outside a table that claims fourteen columns; and G-19 is a headerless one-row fragment outside the Disclosed Gaps table. Both caused A-009's miscounts. | Repair both tables. |
| A-033 | Staleness | LOW | `tasks.md:36` | Says "18 modules"; §Project Structure lists 19 (becomes 20 under A-002). | Correct. |
| A-034 | Duplication | LOW | `plan.md:383–384` | `test_migration_chain.py` appears twice in §Project Structure with duplicate AD-007 (f) comments. | De-duplicate. |
| A-035 | Wrong attribution | LOW | `spec.md:152` | Calls `ck_forecast_diagnostic__blocking_rows_passed` "a delivered check"; `forecast_diagnostic` is created by **this** epic's migration `0303`. | Correct to "this epic's check". |
| A-036 | Over-recording | LOW | `plan.md` P-6 | ADR-0013 contains no sentence about the `/tests` directory being split "between two epics" — that wording is the module docstring already handled as AD-007 part (e). ADR-0013 says "later epics claim further blocks at their start", so E007's block falsifies nothing in it. | Restate or retire P-6. |
| A-037 | Ordering | LOW | `plan.md` §Negative Controls | NC-21 sits between NC-13 and NC-12, and the derivation prose covers NC-14–NC-20 without mentioning it. All 21 present. | Reorder. |
| A-038 | Consistency | LOW | `spec.md` §Key Entities | Three of five entities name their delivered table; `SplitAssignment` and `DiagnosticsSummary` do not, though both tables are named elsewhere in the spec. | Name them. |

## Quality Summaries

**Spec Quality** — Spec Validator: **FAIL**, 16/25. Content Quality 2/6, Requirement Completeness 10/13, Feature Readiness 4/6. The failures cluster in three places: implementation detail in a `product` spec (A-020), criteria that restate their requirement rather than adding an observable (A-018), and appended requirements arriving without criteria (A-007).

**Compliance** — Policy Auditor: **FAIL**, 1 CRITICAL / 3 HIGH / 6 MEDIUM / 6 LOW. Eleven of fourteen principle verdicts from the Plan gate still hold. Three were invalidated by growth: **Governance** (A-001), **Testing & Quality Policy** (A-002), **Principle II** (A-012). Principles III and VI survive with disclosed residuals.

Both agents ran read-only. All eight propagation obligations `P-1`–`P-8` were verified **not performed** — each stale sentence still stands in the other epic's file. `specs/project-plan.md` was **not** touched on this branch; its E007 amendment was performed on `main`, as `P-3` states.

Eight was the count when the check ran. This same gate then appended `P-9`–`P-11` from the Governance finding above, so the branch closed with eleven — not performed either, the third of them by construction. `tasks.md` carried the stale eight until 2026-07-28.

## Coverage Summary

| Requirement Key | Has Task? | Task IDs | Notes |
|---|---|---|---|
| FR-001 … FR-040 | **Yes, 40/40** | 113 tasks | No requirement is untasked. Thinnest: FR-039 (T035 only), FR-030 (T044–T045) |
| SC-001 … SC-035 | **Yes, 35/35** | via §SC Coverage Map | Three lack a *criterion-side* parent requirement instead — see A-007 |
| DV-001 … DV-041 | **Yes, 41/41** | — | No orphan rule |
| NC-1 … NC-21 | **Yes, 21/21** | — | Two claims lack a control that should have one — A-008 |

Local checks all passed: no gold-plating, `[COMPLETES]` markers correctly placed on every 3+-task requirement, all five `← T###:Symbol` edges matched by a `→ exports:`, every task path present in §Project Structure, and no planned module left unbuilt.

## Instructions Alignment Issues

**A-001 is the only project-instructions violation**, and it is CRITICAL by `AGENTS.md`. Its remedy is to revert, not to justify — §Instructions Check's "Complexity Tracking — Omitted" remains correct, since nothing here needs justifying.

**The systemic half outlives this epic.** `specify-feature` §6.5.4 and `plan-feature` §5.6.3 both instruct the managed-section rewrite that Governance reserves to the default branch. Reverting fixes E007; the next epic to run either skill will reproduce it. That belongs to whoever owns the skills.

## Unmapped Tasks

None. Every task outside Setup, Foundational and Polish carries a requirement tag.

## Metrics

| Metric | Value |
|---|---|
| Total requirements | 40 FR + 35 SC |
| Total tasks | 113 |
| Requirement coverage | **100%** (40/40 FR tagged; 35/35 SC mapped) |
| Validation-rule coverage | **100%** (41/41 DV, 21/21 NC referenced by a task) |
| Findings | 38 (1 CRITICAL, 8 HIGH, 15 MEDIUM, 14 LOW) |
| Critical issues | **1** — A-001 |
| Findings originating in this session's own work | 12 — A-003, A-006, A-009, A-012, A-014, A-016, A-024, A-029, A-030, A-032, A-035, and A-001's Plan-phase half |

## Remediation (pass A, applied 2026-07-27)

Invoked as `/sddp-analyze apply all`. **36 of 38 findings applied; 2 skipped as requiring user judgement.**

| Finding | Sev | File(s) | Change | Status |
|---|---|---|---|---|
| A-001 | CRITICAL | `specs/prd.md`, `specs/sad.md`, `plan.md` | Reverted 2 PRD bullets and 5 SAD baseline entries to `main`; kept the ADR-0018 catalog row; recorded `P-9`, `P-10`, `P-11` | **Applied** |
| A-002 | HIGH | `plan.md`, `tasks.md` | `design.py` extracted as the **tenth** property module; `model.py`'s row restated; T114/T115 RED→GREEN pair; T116 reworks the logp oracle to build inputs from `design.py` | **Applied** |
| A-003 | HIGH | `spec.md` | FR-040 counts three **report kinds**; SC-035 restated as a closed-kind equality | **Applied** |
| A-004 | HIGH | `spec.md` | FR-016's chain-count clause folded into FR-035; Compliance Check attribution corrected | **Applied** |
| A-005 | HIGH | `spec.md` | SC-027 restated as AD-002's per-line metamorphic property | **Applied** |
| A-006 | HIGH | `spec.md` | The fit-derived survival floor deleted | **Applied** |
| A-007 | HIGH | `spec.md`, `plan.md`, `data-model.md`, `tasks.md` | SC-036–SC-040 added; DV-042, DV-043, NC-24; tasks T122–T124 | **Applied** |
| A-008 | HIGH | `plan.md`, `tasks.md` | NC-22 and NC-23 added; tasks T120, T121 | **Applied** |
| A-009 | HIGH | `plan.md` | Counts refreshed to 41/19/11; DV-040 added to the Property caveat and SC-013's row | **Applied** |
| A-010–A-017 | MED | `spec.md`, `data-model.md`, `tasks.md`, `plan.md` | SC-002 tolerance; DV-013 five-field set; AD-008 interval; FR-020 derivation rule + Published Constants row; SC-013 corrected and FR-041 added as DV-040's parent; T117/T118 RED halves; P-11 | **Applied** |
| A-018 | MED | — | Split FR-014 into five singular requirements | **Skipped — judgement.** High churn against append-only IDs; the coverage half is closed by SC-040 |
| A-019 | MED | `spec.md` | Stream and exit-status contracts given SC-038; full FR-017 split deferred with A-018 | **Partially applied** |
| A-020 | MED | — | Retype the spec `technical`, or move implementation detail downstream | **Skipped — judgement.** Both options are large and change what the spec is for |
| A-021–A-038 | MED/LOW | all four | SC-026/SC-035 de-duplicated; SC-039 for the warning branch; SC-040; Governance row corrected; fourth oracle class named; ESS basis clarified; percentile convention and interval mass fixed; FR-031 given G-10's contingency; tolerance published at 5.0 days; FR-039/FR-040 moved into Functional Requirements; strict-`>` convention stated; both data-model tables repaired; module count 20; duplicate structure line removed; `0303` attribution corrected; P-6 restated; NC-21 reordered; Key Entities name their tables | **Applied** |

### Consequences the remediation itself introduced, found by re-validation and fixed

- Appending T114–T126 moved **seven** `[COMPLETES]` markers and pushed FR-001 and FR-031 past three tasks; all nine relocated. T117 briefly carried two markers, and a RED task is not a completion point — both stripped, with T125 and T126 closing FR-010 and FR-029 over emitted rows.
- Four `after:` edges now run from a lower ID to a higher one (`T027→T115`, `T051→T118`, `T055→T117`, `T073→T098`), because IDs are never reused and Foundational work was discovered after decomposition. The graph was **verified acyclic by traversal**, and `tasks.md`'s validation row was corrected — it had asserted the lower-ID invariant that no longer holds.

### Post-remediation state

41 FR / 40 SC / 43 DV / 24 NC / 19 G / 11 P / 126 tasks. Coverage 100% on every axis; no orphan rule, control, or criterion; no gold-plating; no duplicate completion marker; graph acyclic.

### Pass A closed out, 2026-07-27

**All 38 findings are now closed.** A-018 and A-020 were reported as "skipped, requires user judgement" and then not put to the user — labelling a decision as someone else's and then making it by omission is still making it. Both were put to the user and both are applied.

Investigating them corrected the report itself: **A-020's recommendation was unavailable, not merely expensive**, and the row above now says so. A finding that recommends an action nobody can take is worse than no finding, because it reads as a live option.

Post-close: **45 FR / 42 SC / 43 DV / 24 NC / 19 G / 11 P / 128 tasks**, coverage 100% on every axis, zero live forward-references, and the three historical sections — Stress-Test Findings, Clarifications, Compliance Check — unchanged.

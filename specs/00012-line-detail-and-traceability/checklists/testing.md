# Testing: Line Detail and Traceability

**Created**: 2026-07-30 | **Feature**: [spec.md](../spec.md)

## Requirement Testability and Decidability

- [X] CHK001 Does FR-046 state the invariants the property-based tests must hold over mark allocation, quantile extraction, mass derivation and band alignment, so a property that holds trivially is distinguishable from one that constrains the function? [Testability, Spec §FR-046] <!-- Evaluator: Resolved — added FR-072, which states the invariants per computation, drawn from requirements that already fix them rather than minted: fifty ascending non-negative offsets invariant under line, run and stored draw count; nearest-rank one-based extraction of the 50th and 80th only, ordered, equality admitted; both displayed directions summing to one hundred with the shade boundary equal to the marks before the offset and the miss mass absent rather than zero at the anchor; three contiguous quantile-bounded bands with a non-decreasing delivered-by share whose last is the residual's complement. Each is asserted over every admissible input rather than a fixture, which is what makes it a property. T012 carries them -->
- [X] CHK002 Does FR-046 define "runs in the merge gate" as an observable property of a named workflow, so a check that is present but deselected, skipped or marked expected-failure does not satisfy it? [Clarity, Spec §FR-046] <!-- Evaluator: Resolved — added FR-070, adopting E010 FR-040's rule rather than re-choosing it and fixing the observable form: a step of the `verify` job in `.github/workflows/verify.yml`, read from the **parsed** workflow rather than its text, in the form the delivered `tests/checks/test_worklist_checks_run_in_the_gate.py` establishes. Deselected, marker-filtered, skipped and expected-to-fail all count as not executed and are reported as unrun rather than passed. SC-034 measures it; T043 asserts it -->
- [X] CHK003 Is the test-first obligation expressed as evidence a reviewer can inspect — the ordering of named commits — rather than as an intention, given that the disclosed squash merge removes that evidence once the branch lands? [Testability, Spec §FR-046 / Plan §HINT-001] <!-- Evaluator: Resolved — added FR-071: the obligation is discharged by a recorded red run committed to `evidence/test-first-red-run.md` naming the module, the properties that failed and the commit the run was taken at, alongside the ordering of the named commits on the branch. The record is what survives the squash; the ordering is what does not, and the Recorded Limitation added under CHK005 says so with its reversal trigger. SC-035 measures it; T012 carries it -->
- [X] CHK004 Does any requirement state that the property-based tests must have been observed failing before the derivation module existed, so test-first is evidenced by a red run rather than inferred from file presence? [Completeness, Spec §FR-046, §SC-025] <!-- Evaluator: Resolved — FR-071 requires the tests **observed failing** over the named module before `compute/distribution.py` exists, and requires that run recorded. Stated in the requirement's own terms: a test written before its implementation and one written after are indistinguishable once both are in the tree, which is the whole of what a later reader can see -->
- [X] CHK005 Is the squash-merge limit on ordering evidence recorded with its scope decision, supporting evidence, reversal trigger and production-scale alternative, rather than as an implementation hint? [Completeness, Plan §HINT-001 / Spec §Recorded Limitations] <!-- Evaluator: Resolved — added a third Recorded Limitation to spec.md carrying all four fields: the scope decision (a pre-merge review plus a committed red run), the evidence (squash merge collapses the branch sequence), the reversal trigger (a merge strategy that preserves history, or a disputed test-first claim after a merge), and the production-scale alternative (the gate emits the red run as a retained artifact keyed to the merge commit). HINT-001 now points at it, and the plan's Principle VII row counts three rather than two -->
- [X] CHK006 Does FR-024 name the basis on which its second condition is measured, or does the proxy declaration exist only in SC-010a, AD-010 and the contract's `span_check_basis` — so a check derived from the requirement alone would treat the proxy as the property itself? [Traceability, Spec §FR-024 / Plan §AD-010] <!-- Evaluator: Covered by spec.md §FR-058, added by the API-quality pass: the basis MUST be declared beside the figure on every instance published, and where it is anything other than the cited page the figure MUST be published as a proxy for the property rather than as the property. The declaration is now a requirement rather than a plan decision plus a schema constant -->
- [X] CHK007 Does FR-031 state the comparison basis and tolerance under which a reconstructed covariate value counts as reconciled, so "cannot be reconciled" is decidable rather than left to an implementation's judgement? [Testability, Spec §FR-031] <!-- Evaluator: Resolved — FR-084 fixes the comparison basis as unchanged lifecycle history -->
- [X] CHK008 Do FR-034 and FR-034a give a decidable predicate for the absence of a causal claim, or does compliance rest on a reader's reading of committed copy? [Testability, Spec §FR-034, §FR-034a] <!-- Evaluator: Resolved — added FR-078: the wording is drawn from a **closed set of exactly three forms** (associated with slower delivery, associated with faster delivery, entered into the fit with no direction evidenced), keyed by covariate name in the shape FR-026 commits the state copy, so the absence of a causal claim is decided by set membership rather than by reading. This is FR-048's device for FR-017 and FR-043's for state distinctness, applied to the one panel whose whole obligation is what it does not assert. T042 carries it -->
- [X] CHK009 Does FR-029 state the observation that would falsify it — a write count over named identity tables across an enumerated interaction set — rather than resting on the absence of a write route in the operation table? [Testability, Spec §FR-029] <!-- Evaluator: Resolved — FR-029 now states the falsifying observation: a nonzero count of inserts, updates or deletes against `resolved_entity` and `resolved_entity_member`, counted across the enumerated interaction set — every operation the surface publishes, in each of FR-042's five resolution states, under each admissible parameter value. The requirement records why the operation table is not enough: a lazily materialised row, a connection left in a writable transaction, or a trigger fired by a read each satisfy a write-verb check and fail this one. T038 now counts rather than asserts absence of a route -->


## Success Criterion Measurability

- [X] CHK010 Is each success criterion stated so that a failing run names the requirement that broke, given that SC-005, SC-010 and SC-018 each carry three or more conjuncts under a single identifier? [Testability, Spec §SC-005, §SC-010, §SC-018] <!-- Evaluator: Resolved — added FR-075, which binds the naming to the **checks** rather than splitting the criteria, since splitting would mint identifiers for parts of criteria already referenced externally. Every check names the identifier it discharges, and each conjunct of SC-005's five contents, SC-010's four clauses and SC-018's three is asserted separately under the identifier of the requirement it derives from, so a run failing on the banded summary does not report what a run failing on the reference class reports. SC-038 measures it; T047 asserts it -->
- [X] CHK011 Does SC-003 name the population of lines and the set of figures compared, so "no case in which the two disagree" carries a denominator rather than an unbounded universal? [Measurability, Spec §SC-003] <!-- Evaluator: Resolved — SC-003 now names both. The figure set is cited rather than re-enumerated: the labelled quantile pair FR-004 names and the miss mass in both directions, which FR-052 already fixes as the whole of the set shared with the worklist. The population is every line the active run covers and the worklist ranks, read on both surfaces under one active run and one session adjustment set; a line rendered by neither surface is outside the denominator rather than a silent pass. Note the criterion still has **no owning check** — recorded as unevidenced in the plan's new Criterion Coverage Map -->
- [X] CHK012 Does SC-031 carry FR-015's decidable always-present form — no hover, focus, expansion, click or panel — or the weaker "reachable" phrasing that FR-015 was repaired to replace? [Consistency, Spec §SC-031 vs §FR-015] <!-- Evaluator: Covered by spec.md §SC-031, which carries the decidable form verbatim — "present without any hover, focus, expansion, click or panel required to bring it into view — the same decidable form FR-015 fixes, so the criterion and the requirement cannot be satisfied to different standards". The weaker "reachable" phrasing appears nowhere in either -->
- [X] CHK013 Does SC-020's enumeration reach every location the response publishes a bounded percentage, including each cumulative band's `delivered_by`, or can a percentage outside the five named figures render as a certainty without failing any criterion? [Completeness, Spec §SC-020, §FR-041] <!-- Evaluator: Resolved — SC-020 now names each cumulative band's delivered-by share as a sixth location, with the ground stated: each band's share is either a labelled quantile's share or the complement of the residual mass under FR-038's band note, so naming them adds no figure the view did not already publish. `delivered_by` was the one `PercentFigure` location no criterion reached. Propagated to FR-052 (which said "all five figures SC-020 names"), to T044's bounded-form assertion, and to the contract's `x-figure-classes.band_note` -->
- [X] CHK014 Does SC-026 bound the combinations it quantifies over — the arity as well as the operand set — so the check terminates and a failure names the operands that produced the bare figure? [Testability, Spec §SC-026, §FR-038] <!-- Evaluator: Resolved — SC-026 bounded at arity 2; the residue is a recorded limitation -->
- [X] CHK015 Does SC-016 state the observation that decides "without any element having been added to a worklist row" against E010's three closed content classes? [Measurability, Spec §SC-016, §FR-035] <!-- Evaluator: Resolved — SC-016 now adopts E010 FR-027's own counting procedure, which that requirement states SC-014 is evaluated by: the row's content is counted in its three closed classes — comparison quantities, explanatory context, row status — before and after, the counts are equal in every class, and the only difference is that an element already present carries a link role and the accessible name FR-056 composes from what the row already renders. T024 is the owning check -->
- [X] CHK016 Does SC-029 name which order is observed for "follows the distribution in reading order" — document order, focus order or visual order — so two reviewers reach the same verdict? [Clarity, Spec §SC-029, §FR-007] <!-- Evaluator: Resolved — added a **Reading order** entry to the Glossary fixing the term as document order, the same move the UX pass made for `Region`, and amended both SC-029 and FR-007 to name it. FR-007 additionally requires the visual order to agree, so the two encodings are not met in one order by a screen reader and another by a sighted reader — the drift FR-049 forbids for the equivalent, reached through layout instead of a second copy. Propagated to the contract's `CumulativeView`. The criterion has **no owning check**, recorded as unevidenced in the Criterion Coverage Map -->
- [X] CHK017 Is FR-025's claim that the 100% target was fixed before the first measurement backed by evidence that survives the merge, or does it share the post-merge evidence loss FR-046's ordering claim carries? [Testability, Spec §FR-025] <!-- Evaluator: Covered — it does not share that loss, and the difference is structural rather than procedural. The target lives as `CensusFigure.target_percent: {const: 100}` in the published contract, which the contract's own compatibility rule makes a **breaking change** to alter ("changes a member's admitted values"), so its fixity is checkable at any later date by anyone holding the artifact. FR-046's ordering claim was a property of commit sequence, which the squash destroys — which is why that one needed FR-071 and a Recorded Limitation and this one does not -->
- [X] CHK018 Does any criterion make FR-020a's reversal trigger observable — a document that breaks the latency envelope, or a supported viewer ignoring the page fragment — or is the shortfall recorded with no measurement that would report it widening? [Testability, Spec §FR-020a] <!-- Evaluator: Resolved — FR-085 names the supported viewer set, making both limbs observable -->


## Evidence That the Checks Run

- [X] CHK019 Do the success criteria state which are dischargeable inside the merge gate and which require an acceptance run, so a criterion measured only outside the gate is not counted as gated evidence? [Completeness, Spec §Success Criteria / Plan §Testing Strategy] <!-- Evaluator: Resolved — added a **Criterion Coverage Map** to plan.md § Testing Strategy carrying a Gate? column over all 39 criteria. SC-010a is the one marked "acceptance — not gated"; three others are marked "gate (payload half)" where the rendered half has no check. FR-070 states the rule the column applies — an acceptance-tier result is counted as no part of the gated evidence — and T043 asserts the exclusion rather than leaving it to a reader of the workflow -->
- [X] CHK020 Does any requirement forbid an environmentally skipped check from being reported as evidence, given that the corpus-page measurement runs only where `pdfplumber` is available? [Testability, Plan §AD-010 / Tasks §T034] <!-- Evaluator: Resolved — FR-070 names it explicitly: a check "skipped — including skipped because an environment dependency such as `pdfplumber` is absent" has not executed, MUST be reported as unrun rather than as passed, and MUST fail the gate rather than leave it green. The corpus-page check is the one recorded exception and is recorded as acceptance-tier rather than as a gate check that happened to skip, which is the difference between a disclosed tier and a silent one -->
- [X] CHK021 Does the gate-membership obligation cover every check the requirements mandate, or only the property tests and the detail conformance check the polish phase names? [Coverage, Spec §FR-046 / Tasks §T043] <!-- Evaluator: Resolved — FR-070 states the obligation over **every** check a requirement of this feature mandates rather than over the three T043 happened to name, with one enumerated exception (FR-058's corpus-page re-measurement). T043 is rewritten to assert the general property over the parsed workflow — traversal tests, copy distinctness, the no-write assertion and the identifier-naming check now included — rather than three named files -->
- [X] CHK022 Is every success criterion mapped to a tier or check in the Testing Strategy, so a criterion with no owning check is visible as unevidenced rather than assumed covered? [Coverage, Plan §Testing Strategy / Spec §Success Criteria] <!-- Evaluator: Resolved — the Criterion Coverage Map maps all 39 criteria to a tier, an owning check and a task. Five are recorded **unevidenced** rather than assumed covered, which is what the item asks the table to make visible: SC-003 (nothing compares this view's figures against the worklist's), SC-009, SC-023, SC-029, and the rendered halves of SC-002, SC-006 and SC-030. They are recorded rather than closed here — closing them adds test scope beyond this evaluation -->
- [X] CHK023 Is there a stated convention binding an assertion to the requirement identifier it discharges, so a failing run names a requirement rather than only a file? [Traceability, Spec §Success Criteria / Tasks §Epic Capability Map] <!-- Evaluator: Resolved — FR-075 states it: every check discharging a requirement or criterion names the identifier it discharges. The task tags `{FR-###}` bound tasks to requirements, which is a plan-time trace; this binds the **assertion** to the identifier, which is what a reader of a failing run is standing in front of. T047 asserts the convention over the check set, in `tests/checks/test_detail_checks_name_what_they_discharge.py` -->


## Evidence That the Checks Bite

- [X] CHK024 Do the requirements demand that each structural claim be shown to reject a non-conforming body — a `miss` member present alongside `already_late`, or a central-summary member — rather than only to accept a conforming one? [Testability, Spec §FR-006, §FR-012] <!-- Evaluator: Resolved — added FR-073: every structural claim MUST be evidenced by a **rejection** — a body constructed to violate it and shown to fail — named beside the acceptance case it pairs with, because a check shown only to accept a conforming body evidences that the body conforms and not that the check discriminates. Four cases are named as the minimum: a `miss` alongside `already_late`, a central summary under a name the published list does not anticipate, a sixth property on `StatedFigures`, and a mark series of a length other than fifty. SC-036 measures it; T017 carries it; the contract's `x-prohibited-members` records it -->
- [X] CHK025 Is the conformance validator's coverage of the constructs the contract uses stated as a requirement, so a contract that later adds a keyword the validator does not implement fails rather than reports green? [Completeness, Plan §Testing Strategy / Tasks §T003] <!-- Evaluator: Resolved — added FR-074: the apparatus MUST implement every construct the published contract uses, a construct it does not implement MUST **fail** the check rather than pass unevaluated, and the coverage MUST be asserted against the contract itself rather than a list maintained beside it. Stated as a requirement rather than left as scheduled work for the reason the requirement gives: T003 discharges today's six-keyword gap, while the next keyword arrives after the task closes. SC-037 measures it; T003 now carries the assertion as well as the extension -->
- [X] CHK026 Do the requirements distinguish "the clause was evaluated" from "the response passed", given that `figures`, `encoding`, `identity_state` and `page_state` are reachable only through `allOf` and FR-006's withholding rule only through the root `if`/`then` pairs? [Testability, Spec §FR-011, §FR-012 / Contract §LineDetailResponse] <!-- Evaluator: Resolved — FR-073's second half requires every conditional clause shown to have been **evaluated** rather than merely passed: for each root conditional and each member reachable only through a composition keyword, one body matching the condition and one not. A concrete instance was found and fixed while checking this: **no committed example matches the `beyond_horizon` conditional**, so its `then` branch is currently unexercised by every example in the contract. The conditional's description now says so, T004 seeds the beyond-horizon scenario, and T017 asserts the coverage -->
- [X] CHK027 Is the `x-prohibited-members` name list bound to FR-003's and FR-012's enumerations by a stated obligation, so a prohibition added to a requirement cannot leave the machine-readable list behind? [Consistency, Spec §FR-003, §FR-012] <!-- Evaluator: Covered by spec.md §FR-064, which decides the question by making the binding unnecessary rather than by asserting one: "that closure, and not any list of names, is what keeps the prohibition standing", and "a published list of forbidden member names is an aid to the test that asserts their absence". A prohibition added to a requirement and not to the list leaves a stale aid, never a hole — and the list is already deliberately partial, since FR-003's lone-quantile clause has no member name to list and is enforced by `QuantilePair` and `LabelledQuantile` requiring their members -->
- [X] CHK028 Do the requirements state that a central summary carried under a name absent from that list is still caught, so the prohibition does not depend on anticipating the name a regression would use? [Completeness, Spec §FR-012, §SC-007] <!-- Evaluator: Covered by spec.md §FR-064 in terms: "Every object the successful response admits is closed against members it does not declare; that closure, and not any list of names, is what keeps the prohibition standing when a central summary could arrive under a name no list anticipates." T017 asserts the closure of every object in the 200 tree, and FR-073 now adds the rejection case that shows the closure biting -->
- [X] CHK029 Is the near-tautological character of the `chunk.body_text` check stated as a reason the request-time figure alone cannot discharge FR-024's second condition, in a requirement rather than only in a plan decision row? [Clarity, Spec §FR-024 / Plan §AD-010] <!-- Evaluator: Covered by spec.md §FR-058, added by the API-quality pass, which states it in its own words: "A near-tautological check published as though it were the expensive one is the failure FR-024 exists to catch, made by the artifact that was supposed to catch it" — and requires the same condition re-measured against real corpus documents rather than fixtures on that ground. AD-010 is no longer the only place the argument lives -->
- [X] CHK030 Does any requirement state what a disagreement between the request-time proxy and the corpus-page measurement obliges — whether the published share remains publishable once the agreement that establishes the proxy fails? [Completeness, Spec §SC-010a / Plan §AD-010] <!-- Evaluator: Resolved — FR-058 gains the clause. A disagreement obliges **publication, not withdrawal**: the figure keeps being published, the disagreement is published beside it as a count of the records the two measurements decide differently, and what the disagreement removes is the proxy's *establishment* rather than the figure — the basis stays declared, the proxy stays a proxy, and it MUST NOT be restated as the property it stands for while a disagreement stands. A disagreement of any size is the recorded reversal trigger for AD-010's request-time basis, since two censuses over the same records differing is a defect and not noise. Propagated to AD-010, T034 and the contract's `span_check_basis` -->
- [X] CHK031 Does SC-010a define "published together" by a named artifact, its location and its contents, so the two measurements and their disagreement are discoverable by a reader rather than existing only as a passing check? [Clarity, Spec §SC-010a / Tasks §T034] <!-- Evaluator: Resolved — SC-010a now names all three: the artifact `specs/00012-line-detail-and-traceability/evidence/span-check-agreement.md`, and its contents — both counts with their denominators, the corpus and resolution run each was taken over, the date of the run, and the records the two measurements decide differently. T034 already required "a committed evidence artifact under the feature workspace" without naming one; the path is now the same in the criterion, the task, AD-010 and the plan's Project Structure -->


## Fixture-Evidenced Versus Corpus-Evidenced Claims

- [X] CHK032 Do the requirements distinguish a claim evidenced against frozen fixtures from one evidenced against the corpus, given that `resolved_entity_member` is empty until E009 runs and every traversal check runs against fixtures? [Completeness, Spec §Assumptions / Plan §Integration Points] <!-- Evaluator: Resolved — FR-076's second half requires it: where a claim over linked records is discharged against frozen fixtures rather than against records identity resolution produced, the evidence MUST be **recorded as fixture-evidenced**, on the stated ground that a fixture evidences that the traversal reads what it is given and cannot evidence that the records it will one day read resolve — which is the distinction the census exists to measure. Until now the distinction lived only in the Assumptions, an Integration Points cell and HINT-003, none of which binds a claim. Carried in T009 and the Brownfield Notes -->
- [X] CHK033 Does SC-021 declare itself unjudged at an empty linked-record set, in the shape FR-025 fixes for a zero denominator, rather than passing vacuously in the state the spec calls the common path? [Testability, Spec §SC-021 vs §FR-025] <!-- Evaluator: Resolved — SC-021 now declares itself **unjudged rather than met** where no linked record is shown, in the shape FR-025 fixes for a zero denominator, and records why: until identity resolution first runs there are no linked records at all, which the Assumptions call the common path, and a universal satisfied by an empty list evidences nothing about the generation filter it asserts. The inconsistency the item names — one repaired zero-denominator target beside an unrepaired vacuous universal in the same spec — is closed, and FR-076 generalises the shape so the next such criterion inherits it rather than repeating the defect. Propagated to the contract's `CensusFigure.total_count`, which now cross-links the two treatments -->
- [X] CHK034 Do SC-008 and SC-009 state the precondition that linked records exist, so a run with none reports them unjudged rather than met? [Measurability, Spec §SC-008, §SC-009] <!-- Evaluator: Resolved — both now do. SC-008 is unjudged where no line carries a linked record and is recorded as fixture-evidenced where the records it ran against are frozen fixtures; SC-009 is unjudged where the view shows none, with the reason stated: a universal over an empty list is satisfied by a view that renders nothing at all, which is what every line renders today, so a run reporting it met reports the traversal untested as the traversal correct. T009 carries the reporting obligation for all three criteria -->
- [X] CHK035 Is the obligation to re-verify the traversal after E009's identity-schema change stated as a requirement with an owner and a trigger, rather than as a mitigation cell in a risk table? [Completeness, Spec §Risks / Plan §AD-003] <!-- Evaluator: Resolved — added FR-077 with both fields explicit: *Trigger* is E009's `0500` ALTER reaching the default branch, *Owner* is `risk_read.traceability`, the one module AD-003 confines the traversal to, and the obligation is to regenerate the frozen fixtures against the changed shape and re-run every traversal check against them. Stated as a requirement on the ground the requirement gives: a mitigation cell is discharged by nobody, the risk is recorded at high likelihood, and a traversal verified only against the old shape passes every day until the day it is wrong. T004 records the trigger where the fixtures live -->
- [X] CHK036 Do the requirements state which of US2's acceptance scenarios are dischargeable before identity resolution first runs, so the story's independent test is not reported as passing on evidence the fixtures supplied? [Clarity, Spec §US2 / Tasks §Brownfield Notes] <!-- Evaluator: Resolved — the answer is all four and it is now stated in both places the item names. US2's Independent Test carries "**Fixture-evidenced until identity resolution first runs**: all four scenarios below, and this test with them, require a line carrying at least one linked record, and no line carries one until E009 populates the resolved-entity membership", and the tasks' Brownfield Notes carry the same under FR-076. The story's independent test can therefore be run today, but not reported as this story passing on the corpus -->


## Coverage and Traceability of the Requirement Set

- [X] CHK037 Does the Requirement Coverage Map name, for each requirement, the check that fails when it regresses, rather than only the component and file that implement it? [Traceability, Plan §Requirement Coverage Map] <!-- Evaluator: Resolved — added a companion table, "Requirement Coverage Map — the check that fails, and the criterion that measures", covering every requirement including the nine added here. The existing map is left intact rather than rewritten, since it answers a different question (what implements this) and rewriting sixty rows to add two columns risks more than it repairs. Rows with no owning check are recorded as **unevidenced** rather than filled: FR-018/FR-019/FR-020, FR-020a, FR-030/FR-032, FR-047, FR-050, FR-051, FR-054, and the rendered halves of FR-007/FR-008 and FR-009 -->
- [X] CHK038 Does every one of the 48 requirements reach at least one success criterion, so a requirement carrying task coverage but no criterion is visible as unmeasured? [Coverage, Spec §Requirements vs §Success Criteria] <!-- Evaluator: Resolved — the same table carries a Criterion column, and the requirement set is now 80 rather than the 48 this item was written against (the UX pass added FR-047–FR-057, the API-quality pass FR-058–FR-069, this pass FR-070–FR-078). **Seven reach no criterion** and are recorded as unmeasured rather than assumed measured: FR-020a, FR-047, FR-050, FR-051, FR-054, FR-060 and FR-077 — the last deliberately, since its trigger is a migration on another branch and there is nothing observable today. FR-047's and FR-020a's gaps need the comprehension and display decisions the plan's Open Items already carry, so they are recorded here rather than closed -->
- [X] CHK039 Are the census invariants a polish task asserts — `has_interval` false, the single licensed reason, the run-scoped population, and `total_count` identical across two lines' responses — stated in a requirement, so removing the assertion removes evidence rather than the obligation? [Completeness, Spec §FR-024 / Tasks §T044] <!-- Evaluator: Covered by spec.md §FR-024 for three of the four in terms — the figure "MUST NOT carry an interval", "MUST carry alongside it the declaration that it has no interval and the licensed reason it draws from", and the population is "every linked record the **active resolution run** carries" — and by FR-051, which requires that population rendered in reader-facing words. The fourth is that population clause read at two lines: a census over one run's whole record set, published on every line's response, is the same number on both, which the contract's `total_count` records ("the same on every line's response") rather than invents. Removing T044's assertions therefore removes evidence and leaves all four obligations standing -->
- [X] CHK040 Is SC-007's scope stated as this surface alone, so ADR-0025's absent repository-wide scan is visible as an open obligation rather than implied to be covered here? [Clarity, Spec §SC-007 / Plan §Open Items] <!-- Evaluator: Covered — SC-007 is scoped in its own words to "No response **backing this view**", matching FR-011's scope, and the repository-wide gap is visible in plan.md § Open Items as "ADR-0025 has no repository-wide check … Open — obligation established, not check established", naming E019 as the next posterior-reading surface and the natural vehicle. The API-quality pass left its sibling item (CHK017 there) unchecked on the same ground deliberately: this feature asserts the rule for its own response and does not pre-empt a project-level decision -->

